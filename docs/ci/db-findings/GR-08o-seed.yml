# GR-08 batch 15 (GR-08o) reviewed exact v2 policy seed rows --
# DB_UNAUTHORIZED_MUTATION EXACT_POLICY authorization for
# domain/receipt/lifecycle/ReceiptSideEffectPlanner.kt,
# data/repository/CategoryRepository.kt,
# data/repository/ReceiptItemCategorizationRepository.kt,
# data/repository/SharedExpenseDataPortAdapter.kt,
# domain/bank/BankApiIntegration.kt,
# domain/health/FinancialHealthScoreV2.kt and
# domain/provenance/SourceLinkBackfillWorker.kt.
#
# Consumed ONLY via `scripts/migrate_db_policy_signatures.py --seed-rows`.
# This document is a full v2-shaped policy document: it is loaded through
# `scripts.db_guard.policy_v2_loader.load_policy_v2` (exact field set,
# type/enum/signature validation, canonical path syntax, within-document
# duplicate mutation-key rejection) and merged into the generated candidate
# only after seed/legacy duplicate-key rejection.  Seeds never bypass the
# loader, the evidence verifier, or the promotion gates.
#
# Scope verification against the trusted START report
# (build/guard-debug/gr08o/before.json == trusted GR-08n after.json,
# 63 findings / 20 advisory diagnostics): ReceiptSideEffectPlanner.kt
# carries exactly 4 DB_UNAUTHORIZED_MUTATION findings / 3 UNIQUE
# fingerprints (the two processMatchResult receiptEventDao.insert sites --
# MATCH_SUGGESTED line 382 and MATCH_NOT_FOUND line 408 -- share one
# fingerprint), CategoryRepository.kt carries exactly 3 findings /
# 3 UNIQUE fingerprints, ReceiptItemCategorizationRepository.kt carries
# exactly 3 findings / 3 UNIQUE fingerprints,
# SharedExpenseDataPortAdapter.kt carries exactly 3 findings /
# 3 UNIQUE fingerprints, BankApiIntegration.kt carries exactly 3 findings /
# 3 UNIQUE fingerprints, FinancialHealthScoreV2.kt carries exactly 3
# findings / 3 UNIQUE fingerprints, and SourceLinkBackfillWorker.kt carries
# exactly 3 findings / 3 UNIQUE fingerprints (dedup key: callable identity
# + daoAccessor + daoFqcn + operation).  Combined batch scope is 22
# findings / 21 UNIQUE fingerprints <= the 25-fingerprint batch cap, so NO
# split was required (unlike GR-08c/GR-08e/GR-08i/GR-08j/GR-08k/GR-08l/
# GR-08m/GR-08n); GR-08o1/o2 file groups were NOT created.
#
# Layer legality:
# * ReceiptSideEffectPlanner is a receipt-lifecycle legal writer
#   (domain/receipt/lifecycle/): the post-commit matcher side-effect planner
#   executed by PostCommitActionRunner after receipt save.  Its writes are
#   exactly the P3-P1-03 match-event taxonomy (MATCH_SUGGESTED with the
#   SUGGESTED status update, MATCH_NOT_FOUND durable event, and the
#   writeMatchEvent diagnostics events) -- the machine counterpart of the
#   ReceiptMatchLifecycleService user-facing match path GR-08h authorized.
#   Every mutating site is barrier-checked; the two-site Suggested branch is
#   atomic under database.withTransaction; auto-link delegates to
#   ReceiptLinkService (the LEGAL_PATHS "LINK/UNLINK receipt" authority);
#   CancellationException is rethrown; event metadata carries only
#   IDs/scores -- never raw OCR text.
# * CategoryRepository is the repository-layer legal writer for category
#   seeding/maintenance (legacy MIT-003 rows already cover addCategory/
#   deleteCategory; THIS batch covers the ensureDefaultCategories seeding
#   surface): the atomic seedDefaultsIfEmpty @Transaction convenience, the
#   merchant-dictionary insertAll, the BUG-012 "Uncategorized" backfill
#   insert, and the updateNormalizedCanonicalName normalization backfill.
# * ReceiptItemCategorizationRepository is the repository-layer legal writer
#   for receipt item categorization persistence; all three mutating methods
#   are barrier-checked; status transitions are deliberately NOT managed
#   here (N4: the calling use cases drive them).
# * SharedExpenseDataPortAdapter is the data-layer adapter implementing the
#   domain SharedExpenseDataPort (Hilt-bound in GroupsModule) -- the legal
#   port boundary of the LEGAL_PATHS "Shared Expense Management (Groups --
#   Alternative Facade)" section, which explicitly legalizes
#   SharedExpenseDataPort.removeMember(); archiveGroup/restoreGroup are soft
#   isActive flips via abstract @Query updates; all multi-table/atomic
#   mutations delegate to GroupTransactionCoordinator per LEGAL_PATHS.
# * BankApiIntegration is the stub-mode-gated bank API integration
#   (requireStubMode errors in release AND requires BankApiConfig.isStubMode):
#   completeConnection persists the demo BankConnection; syncTransactions
#   creates low-confidence PendingReview rows only after the dedup check and
#   inside database.withTransaction with a second barrier check, and routes
#   ALL expense mutations through TransactionLifecycleCoordinator.
#   createExpenseStandaloneV2; refreshToken is PRIVATE with the single
#   caller syncTransactions, which barrier-checks before the refresh branch.
# * FinancialHealthScoreV2 is the domain health scoring engine and the SOLE
#   writer of HealthScoreHistoryDao; saveToHistory is its only mutating
#   callable (get-or-update by period + 90-day TTL deleteOlderThan).
# * SourceLinkBackfillWorker is named "Worker" but is NOT a WorkManager
#   worker: a plain @Singleton suspend runner invoked from
#   SourceLinkBackfillViewModel (debug/manual PR8 provenance backfill), so
#   WorkerExecutionGuard does not apply and barrierMode is `helper`, NOT
#   `workerMediated`.  Guard semantics verified in source: barrier checked
#   at runBackfill entry AND per expense; CancellationException RETHROWN;
#   per-expense failures counted; idempotent via EntitySourceLinkDao.insert
#   OnConflictStrategy.IGNORE plus per-expense existence guards; only
#   EntitySourceLink provenance rows are written (notification/receipt raw
#   text is never persisted).
#
# barrierMode is `helper` for every row (the GR-08a..n batch convention; the
# write barrier runs at each findings-carrying callable entrypoint -- for
# the private refreshToken helper the barrier runs at its sole caller
# syncTransactions before the refresh branch -- and no direct-barrier proof
# is claimed per row).
#
# Closure additions (GR-08o blind-spot sweep): ONE closure row for a
# body-carrying @Transaction DAO convenience method the findings scanner
# never reported (the GR-08b/GR-08d/GR-08l1/GR-08n1 pattern):
# * CategoryRepository.ensureDefaultCategories -> categoryDao.seedDefaultsIfEmpty
#   (the B4 atomic count-check + insertAll seeding convenience).
# Every dao-accessor call in ALL batch callable bodies was enumerated, not
# just the findings.  Out-of-scope blind spots (documented, NOT rowed --
# they live in callables that carry NO findings, so they are outside this
# batch's findings-derived scope, are invisible to the scanner for the same
# convenience-method reason, and the coverage section either cannot see
# them at all or lists them as observation-only):
# CategoryRepository.mergeCategories -> categoryDao.mergeCategories
# (body-carrying @Transaction reassign-all + deleteInternal convenience).
# CategoryRepository.addCategory -> categoryDao.getOrInsertByNameNoCase and
# CategoryRepository.deleteCategory -> categoryDao.delete are ALSO
# body-carrying @Transaction conveniences but are ALREADY authorized by the
# active policy's existing addCategory/deleteCategory rows, so they are
# neither findings nor blind spots.  All other DAO-accessor calls in the
# batch callable bodies are read-only lookups (CategoryDao.getAll,
# MerchantCategoryDao.getMappingsMissingCanonicalName, CategoryDao.getById,
# BudgetDao.countBudgetsForCategory, ReceiptItemCategorizationDao.
# getByReceiptId, ExpenseGroupDao.getAllFlow/getActiveFlow/getByIdFlow/
# getById, GroupMemberDao.getAllForGroup/getAllForGroupFlow, GroupExpenseDao.
# getExpensesForGroup/getExpensesForGroupOnce, PendingReviewDao.
# getPendingDuplicateCandidateInRangeTypeAware/getPendingUncapped,
# HealthScoreHistoryDao.getHistoryForPeriod/getMostRecentBefore,
# EntitySourceLinkDao.getForExpense, ReceiptExpenseLinkDao.
# getLinksForExpense, ExpenseDao.getAll, ScannedReceiptDao.getById).
#
# Chain-form census AFTER review: ZERO database-chained
# `appDatabase.xxxDao()` mutation receivers and ZERO method-local DAO
# aliases in ALL SEVEN files.  AppDatabase IS injected in CategoryRepository,
# BankApiIntegration and ReceiptSideEffectPlanner, but only for the
# androidx.room.withTransaction extension -- never as a DAO-accessor chain;
# every DAO call goes through the injected constructor properties.
# NO accessor-normalization source change is needed for this batch.
#
# Residual closure (GR-08o post-promotion rescan, MIT-DB-08O): TWO more
# closure rows.  The gr08o promotion+rescan (build/guard-debug/gr08o/
# after.json, 41 findings / 20 advisory diagnostics) left 2
# DB_UNAUTHORIZED_MUTATION findings popularly labeled "CategoryRepository".
# VERIFICATION AGAINST THE REPORTS AND THE KOTLIN SOURCE shows they are NOT
# CategoryRepository.kt findings: CategoryRepository.kt carries 0 findings
# in after.json (its 3 ensureDefaultCategories findings were authorized by
# this batch's rows) and its class body contains neither residual callable
# (no deleteAll, and its MerchantCategoryDao property is spelled
# merchantCategoryDao, not `dao`).  The two findings were already present
# under their OWN file in the trusted before report (before.json, 63
# findings) and remained because MerchantCategoryRepository.kt was outside
# the promoted batch scope; the "CategoryRepository" label was a
# suffix-substring grouping artifact (MerchantCategoryRepository.kt ends in
# "CategoryRepository.kt" -- the GR-08l1 BusinessExpenseRepository
# precedent).  The rows therefore spell the TRUE path -- a
# CategoryRepository.kt row could never match the v2 fingerprints and the
# rescan would stay at 41:
#   - MerchantCategoryRepository.deleteAll -> dao.deleteAll (line 38;
#     barrier-checked full merchant-dictionary reset -- SQL DELETE FROM
#     merchant_categories, no payloads materialized -- followed by
#     categorization cache invalidation; DB_WRITE_OWNERSHIP.md and
#     docs/architecture/dao-map.md name MerchantCategoryRepository the
#     owner of merchant_categories; currently no production caller, so it
#     is authorized as the exact static site AS WRITTEN);
#   - MerchantCategoryRepository.insert -> dao.insert (line 26;
#     barrier-checked single-mapping insert, OnConflictStrategy.IGNORE --
#     rowId > 0 is Inserted, otherwise Conflict is returned and only the
#     merchantPattern is logged -- with cache invalidation only after
#     actual success; this is the LEGAL_PATHS "LEARN merchant category"
#     write surface routed through CategorizationEngine.learnMerchantCategory,
#     whose FORBIDDEN clause bars CALLERS outside that engine path, not
#     this repository method; the MerchantCategoryDao KDoc itself directs
#     callers to the repository-level insert over the deprecated direct
#     DAO methods).
# Layer legality: repository-layer legal writer (data/repository/), the
# same layer and barrier-at-entrypoint convention as this batch's
# CategoryRepository and ReceiptItemCategorizationRepository rows; the
# `dao` accessor spelling is the injected constructor property (the same
# spelling already authorized for ReceiptItemCategorizationRepository).
# Each of the two findings is its own fingerprint, so these rows also drop
# MerchantCategoryRepository.kt to 0; the follow-up promotion+rescan delta
# expectation is 41 -> 39 with CategoryRepository.kt at 0.
#
# Multi-seed note: the migration CLI accepts a SINGLE --seed-rows value, so
# every generation run consumes the combined document
# docs/ci/db-findings/GR-08-seeds.yml (GR-08a + GR-08b + GR-08c1 + GR-08c2
# + GR-08d + GR-08e1 + GR-08e2 + GR-08f + GR-08g + GR-08h + GR-08i1 +
# GR-08i2 + GR-08i3 + GR-08j1 + GR-08j2 + GR-08k1 + GR-08k2 + GR-08l1 +
# GR-08l2 + GR-08m1 + GR-08m2 + GR-08n1 + GR-08n2 + GR-08o rows verbatim,
# 377 rows after the GR-08o residual closure); passing only GR-08o-seed.yml
# would drop the earlier batches' rows from the rendered candidate.
schemaVersion: 2
entries:
# ── ReceiptSideEffectPlanner.kt (3 findings-derived rows) ────────────────
- path: app/src/main/java/com/yourname/expensetracker/domain/receipt/lifecycle/ReceiptSideEffectPlanner.kt
  ownerFqcn: com.yourname.expensetracker.domain.receipt.lifecycle.ReceiptSideEffectPlanner
  kind: function
  method: processMatchResult
  receiver: null
  parameterTypes:
  - com.yourname.expensetracker.domain.receiptmatching.MatchResult
  - com.yourname.expensetracker.data.database.entity.ScannedReceipt
  daoAccessor: scannedReceiptDao
  daoFqcn: com.yourname.expensetracker.data.database.dao.ScannedReceiptDao
  operation: update
  barrierMode: helper
  reason: "GR-08o EXACT_POLICY: barrier-checked Suggested-branch match-status update -- persists suggestedExpenseId/matchStatus=SUGGESTED/matchConfidence atomically with the MATCH_SUGGESTED ReceiptEvent under database.withTransaction (the machine counterpart of ReceiptMatchLifecycleService.saveMatchSuggestion)"
  owner: '@panospao7'
  linkedIssue: MIT-DB-08O
- path: app/src/main/java/com/yourname/expensetracker/domain/receipt/lifecycle/ReceiptSideEffectPlanner.kt
  ownerFqcn: com.yourname.expensetracker.domain.receipt.lifecycle.ReceiptSideEffectPlanner
  kind: function
  method: processMatchResult
  receiver: null
  parameterTypes:
  - com.yourname.expensetracker.domain.receiptmatching.MatchResult
  - com.yourname.expensetracker.data.database.entity.ScannedReceipt
  daoAccessor: receiptEventDao
  daoFqcn: com.yourname.expensetracker.data.database.dao.ReceiptEventDao
  operation: insert
  barrierMode: helper
  reason: "GR-08o EXACT_POLICY: two durable match-event writes in one callable -- MATCH_SUGGESTED (Suggested branch, atomic with the status update under withTransaction) and MATCH_NOT_FOUND (NoMatch branch, PR4: NoMatch is no longer silent); both barrier-checked, metadata carries only IDs/scores -- never raw OCR text"
  owner: '@panospao7'
  linkedIssue: MIT-DB-08O
- path: app/src/main/java/com/yourname/expensetracker/domain/receipt/lifecycle/ReceiptSideEffectPlanner.kt
  ownerFqcn: com.yourname.expensetracker.domain.receipt.lifecycle.ReceiptSideEffectPlanner
  kind: function
  method: writeMatchEvent
  receiver: null
  parameterTypes:
  - com.yourname.expensetracker.data.database.entity.ScannedReceipt
  - String
  - String
  - Long?
  - Float?
  - String?
  daoAccessor: receiptEventDao
  daoFqcn: com.yourname.expensetracker.data.database.dao.ReceiptEventDao
  operation: insert
  barrierMode: helper
  reason: "GR-08o EXACT_POLICY: barrier-checked P3-P1-03 match-event diagnostics writer (MATCH_ATTEMPTED / MATCH_FAILED / MATCH_SKIPPED_ALREADY_LINKED / AUTO_MATCHED / SIDE_EFFECT_SKIPPED_PRIVACY / RAW_USED_EPHEMERALLY); message.take(500) and controlled errorDetails constants only; CancellationException rethrown"
  owner: '@panospao7'
  linkedIssue: MIT-DB-08O
# ── CategoryRepository.kt (3 findings-derived rows + 1 closure row) ──────
- path: app/src/main/java/com/yourname/expensetracker/data/repository/CategoryRepository.kt
  ownerFqcn: com.yourname.expensetracker.data.repository.CategoryRepository
  kind: function
  method: ensureDefaultCategories
  receiver: null
  parameterTypes: []
  daoAccessor: merchantCategoryDao
  daoFqcn: com.yourname.expensetracker.data.database.dao.MerchantCategoryDao
  operation: insertAll
  barrierMode: helper
  reason: "GR-08o EXACT_POLICY: barrier-checked merchant-dictionary seeding -- bulk OnConflictStrategy.IGNORE insert of the MerchantCategoryProvider blueprint mappings after the default categories exist"
  owner: '@panospao7'
  linkedIssue: MIT-DB-08O
- path: app/src/main/java/com/yourname/expensetracker/data/repository/CategoryRepository.kt
  ownerFqcn: com.yourname.expensetracker.data.repository.CategoryRepository
  kind: function
  method: ensureDefaultCategories
  receiver: null
  parameterTypes: []
  daoAccessor: categoryDao
  daoFqcn: com.yourname.expensetracker.data.database.dao.CategoryDao
  operation: insert
  barrierMode: helper
  reason: "GR-08o EXACT_POLICY: barrier-checked BUG-012 backfill -- ensures the 'Uncategorized' default category exists for pre-existing users (OnConflictStrategy.IGNORE insert, only when a one-shot read shows it missing)"
  owner: '@panospao7'
  linkedIssue: MIT-DB-08O
- path: app/src/main/java/com/yourname/expensetracker/data/repository/CategoryRepository.kt
  ownerFqcn: com.yourname.expensetracker.data.repository.CategoryRepository
  kind: function
  method: ensureDefaultCategories
  receiver: null
  parameterTypes: []
  daoAccessor: merchantCategoryDao
  daoFqcn: com.yourname.expensetracker.data.database.dao.MerchantCategoryDao
  operation: updateNormalizedCanonicalName
  barrierMode: helper
  reason: "GR-08o EXACT_POLICY: barrier-checked canonical-name normalization backfill -- fills normalizedCanonicalName for getMappingsMissingCanonicalName rows using the CategorizationEngine normalizer (targeted SQL update, no payloads materialized)"
  owner: '@panospao7'
  linkedIssue: MIT-DB-08O
- path: app/src/main/java/com/yourname/expensetracker/data/repository/CategoryRepository.kt
  ownerFqcn: com.yourname.expensetracker.data.repository.CategoryRepository
  kind: function
  method: ensureDefaultCategories
  receiver: null
  parameterTypes: []
  daoAccessor: categoryDao
  daoFqcn: com.yourname.expensetracker.data.database.dao.CategoryDao
  operation: seedDefaultsIfEmpty
  barrierMode: helper
  reason: "GR-08o CLOSURE: B4 atomic seeding convenience, evidenced in this repository-layer class with no findings-report row (body-carrying @Transaction CategoryDao method wrapping the count-check + insertAll so concurrent seeding cannot duplicate default categories; GR-08b/GR-08d/GR-08l1/GR-08n1 scanner blind-spot pattern)"
  owner: '@panospao7'
  linkedIssue: MIT-DB-08O
# ── ReceiptItemCategorizationRepository.kt (3 findings-derived rows) ─────
- path: app/src/main/java/com/yourname/expensetracker/data/repository/ReceiptItemCategorizationRepository.kt
  ownerFqcn: com.yourname.expensetracker.data.repository.ReceiptItemCategorizationRepository
  kind: function
  method: deleteByReceiptId
  receiver: null
  parameterTypes:
  - Long
  daoAccessor: dao
  daoFqcn: com.yourname.expensetracker.data.database.dao.ReceiptItemCategorizationDao
  operation: deleteByReceiptId
  barrierMode: helper
  reason: "GR-08o EXACT_POLICY: barrier-checked receipt-scoped cleanup -- SQL delete of all categorization rows for one receipt (no payloads materialized); used when a receipt's categorization is recomputed"
  owner: '@panospao7'
  linkedIssue: MIT-DB-08O
- path: app/src/main/java/com/yourname/expensetracker/data/repository/ReceiptItemCategorizationRepository.kt
  ownerFqcn: com.yourname.expensetracker.data.repository.ReceiptItemCategorizationRepository
  kind: function
  method: saveCategorizationResult
  receiver: null
  parameterTypes:
  - Long
  - com.yourname.expensetracker.domain.ai.model.ReceiptItemCategorizationResult
  - Long
  daoAccessor: dao
  daoFqcn: com.yourname.expensetracker.data.database.dao.ReceiptItemCategorizationDao
  operation: insert
  barrierMode: helper
  reason: "GR-08o EXACT_POLICY: barrier-checked per-item categorization persistence -- inserts one ReceiptItemCategorization row per result item (OnConflictStrategy.ABORT) and returns the successful-insert count; deliberately does NOT side-effect receipt status (N4: status transitions are driven by the calling use cases)"
  owner: '@panospao7'
  linkedIssue: MIT-DB-08O
- path: app/src/main/java/com/yourname/expensetracker/data/repository/ReceiptItemCategorizationRepository.kt
  ownerFqcn: com.yourname.expensetracker.data.repository.ReceiptItemCategorizationRepository
  kind: function
  method: updateUserCorrection
  receiver: null
  parameterTypes:
  - Long
  - Long?
  - String?
  - Long
  daoAccessor: dao
  daoFqcn: com.yourname.expensetracker.data.database.dao.ReceiptItemCategorizationDao
  operation: updateUserCorrection
  barrierMode: helper
  reason: "GR-08o EXACT_POLICY: barrier-checked user-correction recording -- targeted SQL update of userCorrectedCategoryId/Name/At for one item without overriding the receipt's categorization status (N4)"
  owner: '@panospao7'
  linkedIssue: MIT-DB-08O
# ── SharedExpenseDataPortAdapter.kt (3 findings-derived rows) ────────────
- path: app/src/main/java/com/yourname/expensetracker/data/repository/SharedExpenseDataPortAdapter.kt
  ownerFqcn: com.yourname.expensetracker.data.repository.SharedExpenseDataPortAdapter
  kind: function
  method: removeMember
  receiver: null
  parameterTypes:
  - com.yourname.expensetracker.domain.groups.SharedExpenseMember
  daoAccessor: memberDao
  daoFqcn: com.yourname.expensetracker.data.database.dao.GroupMemberDao
  operation: update
  barrierMode: helper
  reason: "GR-08o EXACT_POLICY: barrier-checked soft-left member removal via the SharedExpenseDataPort -- sets leftAt = timeProvider.now() on the member row (history-preserving; LEGAL_PATHS 'Shared Expense Management (Groups -- Alternative Facade)' explicitly legalizes SharedExpenseDataPort.removeMember())"
  owner: '@panospao7'
  linkedIssue: MIT-DB-08O
- path: app/src/main/java/com/yourname/expensetracker/data/repository/SharedExpenseDataPortAdapter.kt
  ownerFqcn: com.yourname.expensetracker.data.repository.SharedExpenseDataPortAdapter
  kind: function
  method: archiveGroup
  receiver: null
  parameterTypes:
  - Long
  daoAccessor: groupDao
  daoFqcn: com.yourname.expensetracker.data.database.dao.ExpenseGroupDao
  operation: archiveGroup
  barrierMode: helper
  reason: "GR-08o EXACT_POLICY: barrier-checked port-level soft archive -- targeted SQL update isActive=0 for one group (the LEGAL_PATHS group FORBIDDEN list prohibits INSERTs outside GroupTransactionCoordinator, not this status flip; multi-table mutations on this port delegate to the coordinator)"
  owner: '@panospao7'
  linkedIssue: MIT-DB-08O
- path: app/src/main/java/com/yourname/expensetracker/data/repository/SharedExpenseDataPortAdapter.kt
  ownerFqcn: com.yourname.expensetracker.data.repository.SharedExpenseDataPortAdapter
  kind: function
  method: restoreGroup
  receiver: null
  parameterTypes:
  - Long
  daoAccessor: groupDao
  daoFqcn: com.yourname.expensetracker.data.database.dao.ExpenseGroupDao
  operation: restoreGroup
  barrierMode: helper
  reason: "GR-08o EXACT_POLICY: barrier-checked port-level soft restore -- targeted SQL update isActive=1 for one group (inverse of archiveGroup; same coordinator-delegation boundary)"
  owner: '@panospao7'
  linkedIssue: MIT-DB-08O
# ── BankApiIntegration.kt (3 findings-derived rows) ──────────────────────
- path: app/src/main/java/com/yourname/expensetracker/domain/bank/BankApiIntegration.kt
  ownerFqcn: com.yourname.expensetracker.domain.bank.BankApiIntegration
  kind: function
  method: completeConnection
  receiver: null
  parameterTypes:
  - String
  - String
  daoAccessor: bankConnectionDao
  daoFqcn: com.yourname.expensetracker.data.database.dao.BankConnectionDao
  operation: insert
  barrierMode: helper
  reason: "GR-08o EXACT_POLICY: stub-mode-gated (requireStubMode errors in release) barrier-checked demo connection persistence -- P10-P1-01: the BankConnection row survives process death; tokens stored only via BankTokenCipher.encryptIfNeeded"
  owner: '@panospao7'
  linkedIssue: MIT-DB-08O
- path: app/src/main/java/com/yourname/expensetracker/domain/bank/BankApiIntegration.kt
  ownerFqcn: com.yourname.expensetracker.domain.bank.BankApiIntegration
  kind: function
  method: syncTransactions
  receiver: null
  parameterTypes:
  - com.yourname.expensetracker.data.database.entity.BankConnection
  - Long?
  daoAccessor: pendingReviewDao
  daoFqcn: com.yourname.expensetracker.data.database.dao.PendingReviewDao
  operation: insert
  barrierMode: helper
  reason: "GR-08o EXACT_POLICY: stub-mode-gated low-confidence bank-transaction review intake -- P10-P1-04 routing below BANK_REVIEW_CONFIDENCE_THRESHOLD; dedup-checked via getPendingDuplicateCandidateInRangeTypeAware first, then inserted inside database.withTransaction with a second barrier check; ALL expense mutations go through TransactionLifecycleCoordinator.createExpenseStandaloneV2; CancellationException rethrown (P10-CURRENT-018)"
  owner: '@panospao7'
  linkedIssue: MIT-DB-08O
- path: app/src/main/java/com/yourname/expensetracker/domain/bank/BankApiIntegration.kt
  ownerFqcn: com.yourname.expensetracker.domain.bank.BankApiIntegration
  kind: function
  method: refreshToken
  receiver: null
  parameterTypes:
  - com.yourname.expensetracker.data.database.entity.BankConnection
  daoAccessor: bankConnectionDao
  daoFqcn: com.yourname.expensetracker.data.database.dao.BankConnectionDao
  operation: updateToken
  barrierMode: helper
  reason: "GR-08o EXACT_POLICY: stub-mode-gated P10-P1-06 token-refresh persistence -- PRIVATE helper whose sole caller syncTransactions barrier-checks before the refresh branch; persists refreshed stub tokens only after BankTokenCipher.decryptWithResult succeeds (KeyInvalidated/Failed never write)"
  owner: '@panospao7'
  linkedIssue: MIT-DB-08O
# ── FinancialHealthScoreV2.kt (3 findings-derived rows) ──────────────────
- path: app/src/main/java/com/yourname/expensetracker/domain/health/FinancialHealthScoreV2.kt
  ownerFqcn: com.yourname.expensetracker.domain.health.FinancialHealthScoreV2
  kind: function
  method: saveToHistory
  receiver: null
  parameterTypes:
  - Int
  - Int
  - Int
  - Int
  - Int
  - Long
  - Long
  - com.yourname.expensetracker.domain.health.HealthTrend
  - String?
  daoAccessor: healthScoreHistoryDao
  daoFqcn: com.yourname.expensetracker.data.database.dao.HealthScoreHistoryDao
  operation: update
  barrierMode: helper
  reason: "GR-08o EXACT_POLICY: barrier-checked get-or-update history write (update branch) -- re-scoring an existing (periodStart, periodEnd) period updates the row in place instead of duplicating it; FinancialHealthScoreV2 is the SOLE HealthScoreHistoryDao writer"
  owner: '@panospao7'
  linkedIssue: MIT-DB-08O
- path: app/src/main/java/com/yourname/expensetracker/domain/health/FinancialHealthScoreV2.kt
  ownerFqcn: com.yourname.expensetracker.domain.health.FinancialHealthScoreV2
  kind: function
  method: saveToHistory
  receiver: null
  parameterTypes:
  - Int
  - Int
  - Int
  - Int
  - Int
  - Long
  - Long
  - com.yourname.expensetracker.domain.health.HealthTrend
  - String?
  daoAccessor: healthScoreHistoryDao
  daoFqcn: com.yourname.expensetracker.data.database.dao.HealthScoreHistoryDao
  operation: insert
  barrierMode: helper
  reason: "GR-08o EXACT_POLICY: barrier-checked get-or-update history write (insert branch) -- first score for a (periodStart, periodEnd) period inserts the HealthScoreHistory row; CancellationException rethrown and persistence failures logged without failing the score computation"
  owner: '@panospao7'
  linkedIssue: MIT-DB-08O
- path: app/src/main/java/com/yourname/expensetracker/domain/health/FinancialHealthScoreV2.kt
  ownerFqcn: com.yourname.expensetracker.domain.health.FinancialHealthScoreV2
  kind: function
  method: saveToHistory
  receiver: null
  parameterTypes:
  - Int
  - Int
  - Int
  - Int
  - Int
  - Long
  - Long
  - com.yourname.expensetracker.domain.health.HealthTrend
  - String?
  daoAccessor: healthScoreHistoryDao
  daoFqcn: com.yourname.expensetracker.data.database.dao.HealthScoreHistoryDao
  operation: deleteOlderThan
  barrierMode: helper
  reason: "GR-08o EXACT_POLICY: barrier-checked 90-day TTL cleanup -- SQL delete of history rows older than the TimeProvider-supplied cutoff (no payloads materialized); keeps the trend table bounded"
  owner: '@panospao7'
  linkedIssue: MIT-DB-08O
# ── SourceLinkBackfillWorker.kt (3 findings-derived rows) ────────────────
- path: app/src/main/java/com/yourname/expensetracker/domain/provenance/SourceLinkBackfillWorker.kt
  ownerFqcn: com.yourname.expensetracker.domain.provenance.SourceLinkBackfillWorker
  kind: function
  method: backfillLegacySource
  receiver: null
  parameterTypes:
  - com.yourname.expensetracker.data.database.entity.Expense
  - List<com.yourname.expensetracker.data.database.entity.EntitySourceLink>
  daoAccessor: sourceLinkDao
  daoFqcn: com.yourname.expensetracker.data.database.dao.EntitySourceLinkDao
  operation: insert
  barrierMode: helper
  reason: "GR-08o EXACT_POLICY: PR8 legacy-source provenance backfill -- inserts the isPrimary LEGACY_BACKFILL EntitySourceLink for expenses with a legacy source string (role/sourceEntityType-guarded, OnConflictStrategy.IGNORE so re-runs are idempotent); metadataJson carries only the legacy source name via SafeProvenanceMetadata"
  owner: '@panospao7'
  linkedIssue: MIT-DB-08O
- path: app/src/main/java/com/yourname/expensetracker/domain/provenance/SourceLinkBackfillWorker.kt
  ownerFqcn: com.yourname.expensetracker.domain.provenance.SourceLinkBackfillWorker
  kind: function
  method: backfillReceiptLinks
  receiver: null
  parameterTypes:
  - com.yourname.expensetracker.data.database.entity.Expense
  - List<com.yourname.expensetracker.data.database.entity.EntitySourceLink>
  daoAccessor: sourceLinkDao
  daoFqcn: com.yourname.expensetracker.data.database.dao.EntitySourceLinkDao
  operation: insert
  barrierMode: helper
  reason: "GR-08o EXACT_POLICY: PR8 receipt-link provenance backfill -- inserts LINKED_PROOF EntitySourceLink rows for receipt-expense links lacking provenance (SCANNED_RECEIPT sourceEntityType-guarded per expense, OnConflictStrategy.IGNORE so re-runs are idempotent; confidence/createdAt carried from the existing link)"
  owner: '@panospao7'
  linkedIssue: MIT-DB-08O
- path: app/src/main/java/com/yourname/expensetracker/domain/provenance/SourceLinkBackfillWorker.kt
  ownerFqcn: com.yourname.expensetracker.domain.provenance.SourceLinkBackfillWorker
  kind: function
  method: backfillNotificationLinks
  receiver: null
  parameterTypes:
  - com.yourname.expensetracker.data.database.entity.Expense
  - List<com.yourname.expensetracker.data.database.entity.EntitySourceLink>
  daoAccessor: sourceLinkDao
  daoFqcn: com.yourname.expensetracker.data.database.dao.EntitySourceLinkDao
  operation: insert
  barrierMode: helper
  reason: "GR-08o EXACT_POLICY: PR8 notification provenance backfill -- inserts the CREATED_FROM EntitySourceLink pointing at the rawNotificationId of approved pending reviews (RAW_NOTIFICATION sourceEntityType-guarded per expense, OnConflictStrategy.IGNORE so re-runs are idempotent); stores only the notification ID -- never raw notification text"
  owner: '@panospao7'
  linkedIssue: MIT-DB-08O
# ── Residual closure rows (GR-08o post-promotion rescan; TRUE path) ───────
# The two residual findings labeled "CategoryRepository" actually live in
# MerchantCategoryRepository.kt (see the header block above for the full
# attribution verification); CategoryRepository.kt stays at 0.
- path: app/src/main/java/com/yourname/expensetracker/data/repository/MerchantCategoryRepository.kt
  ownerFqcn: com.yourname.expensetracker.data.repository.MerchantCategoryRepository
  kind: function
  method: deleteAll
  receiver: null
  parameterTypes: []
  daoAccessor: dao
  daoFqcn: com.yourname.expensetracker.data.database.dao.MerchantCategoryDao
  operation: deleteAll
  barrierMode: helper
  reason: "GR-08o CLOSURE: residual gr08o-rescan site (NOT a CategoryRepository.kt finding -- suffix-substring grouping artifact) -- barrier-checked full merchant-dictionary reset (SQL DELETE FROM merchant_categories, no payloads materialized) followed by categorization cache invalidation; DB_WRITE_OWNERSHIP.md/dao-map.md owner of merchant_categories; currently no production caller -- authorized as the exact static site AS WRITTEN"
  owner: '@panospao7'
  linkedIssue: MIT-DB-08O
- path: app/src/main/java/com/yourname/expensetracker/data/repository/MerchantCategoryRepository.kt
  ownerFqcn: com.yourname.expensetracker.data.repository.MerchantCategoryRepository
  kind: function
  method: insert
  receiver: null
  parameterTypes:
  - com.yourname.expensetracker.data.database.entity.MerchantCategory
  daoAccessor: dao
  daoFqcn: com.yourname.expensetracker.data.database.dao.MerchantCategoryDao
  operation: insert
  barrierMode: helper
  reason: "GR-08o CLOSURE: residual gr08o-rescan site (NOT a CategoryRepository.kt finding) -- barrier-checked single-mapping insert (OnConflictStrategy.IGNORE; rowId > 0 is Inserted, otherwise Conflict with only the merchantPattern logged) and cache invalidation only after actual success; the LEGAL_PATHS 'LEARN merchant category' write surface routed through CategorizationEngine.learnMerchantCategory (its FORBIDDEN clause bars callers outside that engine path, not this repository method)"
  owner: '@panospao7'
  linkedIssue: MIT-DB-08O
