# GR-14 reviewed exact v2 policy seed rows (post-activation candidate truth-sync).
#
# Consumed ONLY via `scripts/migrate_db_policy_signatures.py --seed-rows`
# (indirectly: appended to the COMBINED docs/ci/db-findings/GR-08-seeds.yml
# document that the registered db_artifact_sync guard verifies against).
# Loaded through `scripts.db_guard.policy_v2_loader.load_policy_v2` (exact
# field set, type/enum/signature validation, canonical path syntax,
# within-document duplicate mutation-key rejection).
#
# Scope: the six post-activation exact rows the tracked signatures candidate
# was missing (the candidate trails the active policy -- the 5 GR-14a
# mutator-rule rows plus the GR-14b EXACT_IDENTITY_MOVE target
# writeAssetDeleteFailedEvent), copied verbatim from the approved
# config/guards/db_ownership_policy.yml entries.  This closes the 6-key
# half of the 8-key candidate/active drift recorded in
# docs/ci/db-mediation/GR-14c.yml; the 2 removed-key half is closed by the
# matching legacy-input row removal (same commit).
schemaVersion: 2
entries:
- path: app/src/main/java/com/yourname/expensetracker/data/repository/BudgetRepository.kt
  ownerFqcn: com.yourname.expensetracker.data.repository.BudgetRepository
  kind: function
  method: toggleBudget
  receiver: null
  parameterTypes:
  - Long
  - Boolean
  daoAccessor: budgetDao
  daoFqcn: com.yourname.expensetracker.data.database.dao.BudgetDao
  operation: setActiveAndEnforceScope
  barrierMode: direct
  reason: 'GR-14a EXACT_POLICY: default @Transaction BudgetDao convenience method
    (body calls setActive to enforce the single-active-scope invariant) newly
    indexed by the GR-14a mutator rule; canonical write barrier lexically
    precedes the call in this callable'
  owner: '@panospao7'
  linkedIssue: GR-14a
- path: app/src/main/java/com/yourname/expensetracker/data/repository/BudgetRepository.kt
  ownerFqcn: com.yourname.expensetracker.data.repository.BudgetRepository
  kind: function
  method: updateBudgetOrThrow
  receiver: null
  parameterTypes:
  - com.yourname.expensetracker.data.database.entity.Budget
  daoAccessor: budgetDao
  daoFqcn: com.yourname.expensetracker.data.database.dao.BudgetDao
  operation: updateAndEnforceActiveScope
  barrierMode: direct
  reason: 'GR-14a EXACT_POLICY: default @Transaction BudgetDao convenience method
    newly indexed by the GR-14a mutator rule (the sibling updateBudget row
    already existed); canonical write barrier lexically precedes the call in
    this callable'
  owner: '@panospao7'
  linkedIssue: GR-14a
- path: app/src/main/java/com/yourname/expensetracker/data/repository/CategoryRepository.kt
  ownerFqcn: com.yourname.expensetracker.data.repository.CategoryRepository
  kind: function
  method: mergeCategories
  receiver: null
  parameterTypes:
  - Long
  - Long
  daoAccessor: categoryDao
  daoFqcn: com.yourname.expensetracker.data.database.dao.CategoryDao
  operation: mergeCategories
  barrierMode: direct
  reason: 'GR-14a EXACT_POLICY: default @Transaction CategoryDao multi-table
    merge newly indexed by the GR-14a mutator rule; canonical write barrier
    lexically precedes the withContext(IO) body that performs the call'
  owner: '@panospao7'
  linkedIssue: GR-14a
- path: app/src/main/java/com/yourname/expensetracker/data/repository/MerchantLocationRepository.kt
  ownerFqcn: com.yourname.expensetracker.data.repository.MerchantLocationRepository
  kind: function
  method: saveLocation
  receiver: null
  parameterTypes:
  - String
  - com.yourname.expensetracker.domain.location.LocationResolutionResult.Resolved
  - String
  daoAccessor: dao
  daoFqcn: com.yourname.expensetracker.data.database.dao.MerchantLocationDao
  operation: upsertLocation
  barrierMode: direct
  reason: 'GR-14a EXACT_POLICY: default @Transaction MerchantLocationDao upsert
    newly indexed by the GR-14a mutator rule (the sibling saveCorrection row
    already existed); canonical write barrier lexically precedes the call'
  owner: '@panospao7'
  linkedIssue: GR-14a
- path: app/src/main/java/com/yourname/expensetracker/domain/budget/BudgetForecastingEngine.kt
  ownerFqcn: com.yourname.expensetracker.domain.budget.BudgetForecastingEngine
  kind: function
  method: insertForecast
  receiver: null
  parameterTypes:
  - com.yourname.expensetracker.data.database.entity.BudgetForecast
  daoAccessor: budgetForecastDao
  daoFqcn: com.yourname.expensetracker.data.database.dao.BudgetForecastDao
  operation: insertWithDeactivation
  barrierMode: direct
  reason: 'GR-14a EXACT_POLICY: default @Transaction BudgetForecastDao insert
    newly indexed by the GR-14a mutator rule; write barrier check added to
    insertForecast itself (GR-14a Pattern A -- the only production caller
    generateForecastResult already checks before dispatching, so the added
    check is unreachable no-op in every current path)'
  owner: '@panospao7'
  linkedIssue: GR-14a
- path: app/src/main/java/com/yourname/expensetracker/domain/receipt/lifecycle/ReceiptLifecycleCoordinator.kt
  ownerFqcn: com.yourname.expensetracker.domain.receipt.lifecycle.ReceiptLifecycleCoordinator
  kind: function
  method: writeAssetDeleteFailedEvent
  receiver: null
  parameterTypes:
  - com.yourname.expensetracker.data.database.entity.ScannedReceipt
  - Long
  - String?
  daoAccessor: receiptEventDao
  daoFqcn: com.yourname.expensetracker.data.database.dao.ReceiptEventDao
  operation: insert
  barrierMode: direct
  reason: 'GR-14b EXACT_IDENTITY_MOVE: the ASSET_DELETE_FAILED fallback event
    insert moved out of deleteReceipt''s generic post-commit let callback
    into this private direct owner (GR-14 Pattern A) with its own canonical
    write barrier check; the only caller already checks first, so the added
    check is an unreachable no-op on every current path. Replaces the
    removed deleteReceipt|receiptEventDao|insert row (the mutation no longer
    occurs in deleteReceipt)'
  owner: '@panospao7'
  linkedIssue: GR-14b
