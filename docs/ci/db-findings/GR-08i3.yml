# GR-08 batch 9, part 3 (GR-08i3) tracked batch manifest -- DB_UNAUTHORIZED_MUTATION
# exact-policy authorization for worker/NotificationIntakeWorker.kt.
#
# Batch scope (plan): GR-08i covered THREE files as ONE batch, with a forced
# split if the combined unique fingerprint count exceeded 25.  Scope
# verification against the trusted START report
# (build/guard-debug/gr08i/before.json == trusted GR-08h after.json,
# 291 findings / 20 advisory diagnostics; reference copy SHA-256 recorded
# below): NotificationIntakeWorker.kt carries exactly 15
# DB_UNAUTHORIZED_MUTATION findings collapsing to 7 UNIQUE fingerprints
# (dedup key: callable identity + daoAccessor + daoFqcn + operation; 5
# tuples live on doWork -- 5x markTerminal, 3x markRetryableFailure, 3x
# markFinalFailure, 1x claimForProcessing, 1x
# markPrivacyDeniedAndPurgeAllPayload -- plus 1x
# purgePayloadBestEffort/purgeAllPayload and 1x
# runPrivacyCleanupGuarded/markPrivacyDeniedAndPurgeAllPayload).  Combined
# batch unique count 27 > the 25-fingerprint batch cap, so the batch was
# SPLIT into GR-08i1 / GR-08i2 / GR-08i3 (this manifest) per the GR-08c and
# GR-08e precedent, one part per file; each part is within the cap.  All 15
# file findings are DB_UNAUTHORIZED_MUTATION with disposition EXACT_POLICY:
# this is the worker layer for notification intake processing -- the worker
# owns the intake-row state machine (claim -> process -> terminal) and the
# privacy purge of stored notification payloads.
#
# WORKER-GUARD VERIFICATION (GR-08i task-4 gate, satisfied for EVERY row
# before EXACT_POLICY disposition):
# * doWork -- the ENTIRE mutating body runs inside
#   executionGuard.runGuardedWithContext(WorkerGuardRequest(workerName =
#   WORKER_NAME, requiresDatabaseWrite = true, requiredCapabilities =
#   listOf(PrivacyCapability.NOTIFICATION_CAPTURE), blockedPolicy =
#   BlockedPolicy.RETRY, ...)) { ctx -> ... }: the guard checks the
#   restore/write barrier FIRST, then logs the run (WorkerRunLogger), and
#   every intakeDao mutation below happens inside that guard lambda.
# * runPrivacyCleanupGuarded -- wraps its own body in a SECOND
#   executionGuard.runGuardedWithContext with requiresDatabaseWrite = true
#   and requiredCapabilities = emptyList(): the privacy-cleanup path runs
#   when the guard itself skipped the main run for privacy denial, and it
#   must stay runnable so it can delete raw data (never gated on the
#   raw-retention capability it enforces -- AGENTS.md privacy-cleanup rule).
# * purgePayloadBestEffort -- performs no guard check of its own but is
#   invoked ONLY from inside the doWork guard lambda (8 call sites), rethrows
#   CancellationException, and swallows non-cancellation cleanup failures so
#   post-terminal cleanup can never regress a committed terminal state.
# * Cancellation propagation: doWork catches TimeoutCancellationException
#   FIRST (intentional timeout handling -> retryable/final mark +
#   RetryableWorkerException or terminal failure), then
#   RetryableWorkerException (rethrow), then CancellationException (rethrow),
#   and additionally CancellationSafe.rethrowIfCancellation in the generic
#   handler -- no swallowed cancellation anywhere.
# * Retry contract: retries are signalled ONLY by RetryableWorkerException
#   (WORKER_RETRYABLE_ERROR / WORKER_TIMEOUT / WORKER_UNHANDLED_EXCEPTION
#   reason codes); plain RuntimeExceptions are intentional terminal
#   failures (PERMANENT), never retry signals.
# * Idempotency across WorkManager retries: claimForProcessing is an atomic
#   conditional claim; claimed == 0 returns idempotent NO_WORK success.
# * Privacy posture: payload is loaded ONLY after the mid-run privacy
#   recheck; denial marks privacy-denied AND purges all payload columns in
#   one statement; failureCode fields carry controlled constants only
#   (MAX_ATTEMPTS_EXCEEDED / TIMEOUT / WORKER_EXCEPTION / RETRYABLE_ERROR),
#   never raw exception text or notification payloads.
# * Metrics: ctx.addRowsScanned/addRowsUpdated are incremented only after
#   the corresponding operation actually happened.
#
# Chain-form census: ZERO database-chained `database.xxxDao()` call sites
# in this file (every finding reports identity.call_form=receiver with the
# plain intakeDao constructor property; the worker does not even inject
# AppDatabase).  NO GR-08e accessor-normalization source change is needed
# for this batch; every seed row spells the constructor-property accessor
# exactly as the scanner reports it.
#
# Closure additions (GR-08i3 blind-spot sweep): ZERO closure rows.  EVERY
# dao-accessor call in every mutating-callable body was enumerated, not just
# the findings: the 15 findings == the 15 mutating DAO call sites, and every
# called mutating DAO method (NotificationIntakeDao.claimForProcessing /
# markTerminal / markRetryableFailure / markFinalFailure /
# markPrivacyDeniedAndPurgeAllPayload / purgeAllPayload) is an abstract
# Room-annotated method.  NotificationIntakeDao carries ZERO @Transaction
# methods at all -- a fully abstract interface -- so no convenience-method
# closure row exists for this batch.  The only other DAO-accessor calls in
# the bodies are the read-only lookups intakeDao.getProcessingMetadataById
# (2 sites) and intakeDao.getPayloadForProcessing (1 site).  All pipeline
# writes (raw notification, expense, review rows) happen inside
# NotificationRepository.processAndSave -> NotificationProcessingPipeline,
# whose writers are already authorized by GR-08a/GR-08b.  Constructor-
# property DAO aliases (GR-08a pattern): the single DAO accessor (intakeDao)
# is a direct @AssistedInject constructor property resolved natively by the
# evidence verifier; there are NO alias indirections.
#
# barrierMode is `workerMediated` for every row (the established mode of the
# active policy's worker rows, e.g. DataRetentionWorker.doWork and
# WorkerRunLoggerImpl.start): the write barrier is mediated by
# WorkerExecutionGuard.runGuardedWithContext, and NO direct-barrier proof is
# claimed.  The v2 evidence verifier imposes no local direct-barrier
# requirement on workerMediated rows.
#
# Multi-seed note: the migration CLI accepts a SINGLE --seed-rows value, so
# every generation run consumes the combined document
# docs/ci/db-findings/GR-08-seeds.yml (GR-08a + GR-08b + GR-08c1 + GR-08c2
# + GR-08d + GR-08e1 + GR-08e2 + GR-08f + GR-08g + GR-08h + GR-08i1 +
# GR-08i2 + GR-08i3 rows verbatim); passing only GR-08i3-seed.yml would drop
# the earlier batches' rows from the rendered candidate.
schemaVersion: 1
batchId: GR-08i3
startSha: 64913de1
startReport:
  path: build/guard-debug/gr08i/before.json
  # SHA-256 of the reference copy (trusted GR-08h after report; 291 findings
  # / 20 advisory).  ORCHESTRATOR: fill this in from the reference-copy step
  # before the generate step.  Reference copy created by the batch agent;
  # expected SHA-256:
  # 57c2a6688a96484303b4bc8a17f8886cb7ea06bde9f1f85261e68a2a742a8307
  sha256: 
  trusted: true
  findingCount: 291
activePolicySha256: b2d9d2eaf38d5076076c838ca90ea3b342bb71de5ef708f88b0143a24f6eb4bb
seedFile: docs/ci/db-findings/GR-08-seeds.yml
owner: '@panospao7'
linkedIssue: MIT-DB-08I
counts:
  fileFindings: 15
  batchCallablesFindings: 15
  uniqueFingerprints: 7
selectedFingerprints:
- "v2|db_access|DB_UNAUTHORIZED_MUTATION|path=app%2Fsrc%2Fmain%2Fjava%2Fcom%2Fyourname%2Fexpensetracker%2Fworker%2FNotificationIntakeWorker.kt|symbol.owner=com.yourname.expensetracker.worker.NotificationIntakeWorker|symbol.name=doWork|symbol.receiver=%3Cnone%3E|symbol.parameters=%5B%5D|identity.dao=com.yourname.expensetracker.data.database.dao.NotificationIntakeDao|identity.accessor=intakeDao|identity.operation=claimForProcessing|identity.mutation_kind=ROOM_MUTATING_QUERY|identity.call_form=receiver"
- "v2|db_access|DB_UNAUTHORIZED_MUTATION|path=app%2Fsrc%2Fmain%2Fjava%2Fcom%2Fyourname%2Fexpensetracker%2Fworker%2FNotificationIntakeWorker.kt|symbol.owner=com.yourname.expensetracker.worker.NotificationIntakeWorker|symbol.name=doWork|symbol.receiver=%3Cnone%3E|symbol.parameters=%5B%5D|identity.dao=com.yourname.expensetracker.data.database.dao.NotificationIntakeDao|identity.accessor=intakeDao|identity.operation=markFinalFailure|identity.mutation_kind=ROOM_MUTATING_QUERY|identity.call_form=receiver"
- "v2|db_access|DB_UNAUTHORIZED_MUTATION|path=app%2Fsrc%2Fmain%2Fjava%2Fcom%2Fyourname%2Fexpensetracker%2Fworker%2FNotificationIntakeWorker.kt|symbol.owner=com.yourname.expensetracker.worker.NotificationIntakeWorker|symbol.name=doWork|symbol.receiver=%3Cnone%3E|symbol.parameters=%5B%5D|identity.dao=com.yourname.expensetracker.data.database.dao.NotificationIntakeDao|identity.accessor=intakeDao|identity.operation=markPrivacyDeniedAndPurgeAllPayload|identity.mutation_kind=ROOM_MUTATING_QUERY|identity.call_form=receiver"
- "v2|db_access|DB_UNAUTHORIZED_MUTATION|path=app%2Fsrc%2Fmain%2Fjava%2Fcom%2Fyourname%2Fexpensetracker%2Fworker%2FNotificationIntakeWorker.kt|symbol.owner=com.yourname.expensetracker.worker.NotificationIntakeWorker|symbol.name=doWork|symbol.receiver=%3Cnone%3E|symbol.parameters=%5B%5D|identity.dao=com.yourname.expensetracker.data.database.dao.NotificationIntakeDao|identity.accessor=intakeDao|identity.operation=markRetryableFailure|identity.mutation_kind=ROOM_MUTATING_QUERY|identity.call_form=receiver"
- "v2|db_access|DB_UNAUTHORIZED_MUTATION|path=app%2Fsrc%2Fmain%2Fjava%2Fcom%2Fyourname%2Fexpensetracker%2Fworker%2FNotificationIntakeWorker.kt|symbol.owner=com.yourname.expensetracker.worker.NotificationIntakeWorker|symbol.name=doWork|symbol.receiver=%3Cnone%3E|symbol.parameters=%5B%5D|identity.dao=com.yourname.expensetracker.data.database.dao.NotificationIntakeDao|identity.accessor=intakeDao|identity.operation=markTerminal|identity.mutation_kind=ROOM_MUTATING_QUERY|identity.call_form=receiver"
- "v2|db_access|DB_UNAUTHORIZED_MUTATION|path=app%2Fsrc%2Fmain%2Fjava%2Fcom%2Fyourname%2Fexpensetracker%2Fworker%2FNotificationIntakeWorker.kt|symbol.owner=com.yourname.expensetracker.worker.NotificationIntakeWorker|symbol.name=purgePayloadBestEffort|symbol.receiver=%3Cnone%3E|symbol.parameters=%5B%22Long%22%2C%22Long%22%2C%22com.yourname.expensetracker.domain.workers.WorkerRunContext%3F%22%5D|identity.dao=com.yourname.expensetracker.data.database.dao.NotificationIntakeDao|identity.accessor=intakeDao|identity.operation=purgeAllPayload|identity.mutation_kind=ROOM_MUTATING_QUERY|identity.call_form=receiver"
- "v2|db_access|DB_UNAUTHORIZED_MUTATION|path=app%2Fsrc%2Fmain%2Fjava%2Fcom%2Fyourname%2Fexpensetracker%2Fworker%2FNotificationIntakeWorker.kt|symbol.owner=com.yourname.expensetracker.worker.NotificationIntakeWorker|symbol.name=runPrivacyCleanupGuarded|symbol.receiver=%3Cnone%3E|symbol.parameters=%5B%22Long%22%5D|identity.dao=com.yourname.expensetracker.data.database.dao.NotificationIntakeDao|identity.accessor=intakeDao|identity.operation=markPrivacyDeniedAndPurgeAllPayload|identity.mutation_kind=ROOM_MUTATING_QUERY|identity.call_form=receiver"
findings:
# â”€â”€ doWork intake state machine (5 fingerprints / 13 sites) â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
- fingerprint: "v2|db_access|DB_UNAUTHORIZED_MUTATION|path=app%2Fsrc%2Fmain%2Fjava%2Fcom%2Fyourname%2Fexpensetracker%2Fworker%2FNotificationIntakeWorker.kt|symbol.owner=com.yourname.expensetracker.worker.NotificationIntakeWorker|symbol.name=doWork|symbol.receiver=%3Cnone%3E|symbol.parameters=%5B%5D|identity.dao=com.yourname.expensetracker.data.database.dao.NotificationIntakeDao|identity.accessor=intakeDao|identity.operation=claimForProcessing|identity.mutation_kind=ROOM_MUTATING_QUERY|identity.call_form=receiver"
  ruleId: DB_UNAUTHORIZED_MUTATION
  path: app/src/main/java/com/yourname/expensetracker/worker/NotificationIntakeWorker.kt
  callable:
    ownerFqcn: com.yourname.expensetracker.worker.NotificationIntakeWorker
    method: doWork
    receiver: null
    parameterTypes: []
    kind: function
  daoAccessor: intakeDao
  daoFqcn: com.yourname.expensetracker.data.database.dao.NotificationIntakeDao
  operation: claimForProcessing
  locations: [100]
  disposition: EXACT_POLICY
  rationale: "Atomic per-row claim (conditional UPDATE) inside the WorkerExecutionGuard.runGuardedWithContext lambda -- the load-bearing overlap guard so concurrent WorkManager retries can never double-process an intake row; claimed == 0 returns idempotent NO_WORK success."
  owner: '@panospao7'
  linkedIssue: MIT-DB-08I
  expectedResolution: "Seed row merged into the v2 candidate via --seed-rows (combined GR-08-seeds.yml); finding absent from the after.json rescan."
- fingerprint: "v2|db_access|DB_UNAUTHORIZED_MUTATION|path=app%2Fsrc%2Fmain%2Fjava%2Fcom%2Fyourname%2Fexpensetracker%2Fworker%2FNotificationIntakeWorker.kt|symbol.owner=com.yourname.expensetracker.worker.NotificationIntakeWorker|symbol.name=doWork|symbol.receiver=%3Cnone%3E|symbol.parameters=%5B%5D|identity.dao=com.yourname.expensetracker.data.database.dao.NotificationIntakeDao|identity.accessor=intakeDao|identity.operation=markFinalFailure|identity.mutation_kind=ROOM_MUTATING_QUERY|identity.call_form=receiver"
  ruleId: DB_UNAUTHORIZED_MUTATION
  path: app/src/main/java/com/yourname/expensetracker/worker/NotificationIntakeWorker.kt
  callable:
    ownerFqcn: com.yourname.expensetracker.worker.NotificationIntakeWorker
    method: doWork
    receiver: null
    parameterTypes: []
    kind: function
  daoAccessor: intakeDao
  daoFqcn: com.yourname.expensetracker.data.database.dao.NotificationIntakeDao
  operation: markFinalFailure
  locations: [87, 334, 374]
  disposition: EXACT_POLICY
  rationale: "Terminal failure mark (MAX_ATTEMPTS_EXCEEDED, exhausted TIMEOUT, exhausted WORKER_EXCEPTION) inside the guard lambda; failureCode carries a controlled constant only -- never raw exception text (privacy posture); the 3 call sites share this one fingerprint."
  owner: '@panospao7'
  linkedIssue: MIT-DB-08I
  expectedResolution: "Seed row merged into the v2 candidate via --seed-rows (combined GR-08-seeds.yml); finding absent from the after.json rescan."
- fingerprint: "v2|db_access|DB_UNAUTHORIZED_MUTATION|path=app%2Fsrc%2Fmain%2Fjava%2Fcom%2Fyourname%2Fexpensetracker%2Fworker%2FNotificationIntakeWorker.kt|symbol.owner=com.yourname.expensetracker.worker.NotificationIntakeWorker|symbol.name=doWork|symbol.receiver=%3Cnone%3E|symbol.parameters=%5B%5D|identity.dao=com.yourname.expensetracker.data.database.dao.NotificationIntakeDao|identity.accessor=intakeDao|identity.operation=markPrivacyDeniedAndPurgeAllPayload|identity.mutation_kind=ROOM_MUTATING_QUERY|identity.call_form=receiver"
  ruleId: DB_UNAUTHORIZED_MUTATION
  path: app/src/main/java/com/yourname/expensetracker/worker/NotificationIntakeWorker.kt
  callable:
    ownerFqcn: com.yourname.expensetracker.worker.NotificationIntakeWorker
    method: doWork
    receiver: null
    parameterTypes: []
    kind: function
  daoAccessor: intakeDao
  daoFqcn: com.yourname.expensetracker.data.database.dao.NotificationIntakeDao
  operation: markPrivacyDeniedAndPurgeAllPayload
  locations: [119]
  disposition: EXACT_POLICY
  rationale: "Mid-run privacy recheck (PR12H-2) -- when NOTIFICATION_CAPTURE is denied the row is marked privacy-denied AND all stored payload columns purged in one statement BEFORE any payload is loaded (privacy fail-closed posture; no raw notification text is ever materialized on the denied path)."
  owner: '@panospao7'
  linkedIssue: MIT-DB-08I
  expectedResolution: "Seed row merged into the v2 candidate via --seed-rows (combined GR-08-seeds.yml); finding absent from the after.json rescan."
- fingerprint: "v2|db_access|DB_UNAUTHORIZED_MUTATION|path=app%2Fsrc%2Fmain%2Fjava%2Fcom%2Fyourname%2Fexpensetracker%2Fworker%2FNotificationIntakeWorker.kt|symbol.owner=com.yourname.expensetracker.worker.NotificationIntakeWorker|symbol.name=doWork|symbol.receiver=%3Cnone%3E|symbol.parameters=%5B%5D|identity.dao=com.yourname.expensetracker.data.database.dao.NotificationIntakeDao|identity.accessor=intakeDao|identity.operation=markRetryableFailure|identity.mutation_kind=ROOM_MUTATING_QUERY|identity.call_form=receiver"
  ruleId: DB_UNAUTHORIZED_MUTATION
  path: app/src/main/java/com/yourname/expensetracker/worker/NotificationIntakeWorker.kt
  callable:
    ownerFqcn: com.yourname.expensetracker.worker.NotificationIntakeWorker
    method: doWork
    receiver: null
    parameterTypes: []
    kind: function
  daoAccessor: intakeDao
  daoFqcn: com.yourname.expensetracker.data.database.dao.NotificationIntakeDao
  operation: markRetryableFailure
  locations: [261, 323, 363]
  disposition: EXACT_POLICY
  rationale: "Retryable-failure mark with computed backoff inside the guard lambda for retryable pipeline errors, worker timeouts, and exceptions; every mark is followed by RetryableWorkerException so WorkManager owns the retry (never a plain RuntimeException -- that would burn the attempt budget as PERMANENT); the 3 call sites share this one fingerprint."
  owner: '@panospao7'
  linkedIssue: MIT-DB-08I
  expectedResolution: "Seed row merged into the v2 candidate via --seed-rows (combined GR-08-seeds.yml); finding absent from the after.json rescan."
- fingerprint: "v2|db_access|DB_UNAUTHORIZED_MUTATION|path=app%2Fsrc%2Fmain%2Fjava%2Fcom%2Fyourname%2Fexpensetracker%2Fworker%2FNotificationIntakeWorker.kt|symbol.owner=com.yourname.expensetracker.worker.NotificationIntakeWorker|symbol.name=doWork|symbol.receiver=%3Cnone%3E|symbol.parameters=%5B%5D|identity.dao=com.yourname.expensetracker.data.database.dao.NotificationIntakeDao|identity.accessor=intakeDao|identity.operation=markTerminal|identity.mutation_kind=ROOM_MUTATING_QUERY|identity.call_form=receiver"
  ruleId: DB_UNAUTHORIZED_MUTATION
  path: app/src/main/java/com/yourname/expensetracker/worker/NotificationIntakeWorker.kt
  callable:
    ownerFqcn: com.yourname.expensetracker.worker.NotificationIntakeWorker
    method: doWork
    receiver: null
    parameterTypes: []
    kind: function
  daoAccessor: intakeDao
  daoFqcn: com.yourname.expensetracker.data.database.dao.NotificationIntakeDao
  operation: markTerminal
  locations: [142, 176, 196, 273, 298]
  disposition: EXACT_POLICY
  rationale: "Terminal intake-state write (PAYLOAD_UNAVAILABLE_PRIVACY, FILTER_REJECTED, FAILED_FINAL, PROCESSED, DROPPED_DUPLICATE, DROPPED_POLICY) inside the guard lambda after the pipeline outcome; finalOutcome carries the outcome class simple name only; the 5 call sites share this one fingerprint."
  owner: '@panospao7'
  linkedIssue: MIT-DB-08I
  expectedResolution: "Seed row merged into the v2 candidate via --seed-rows (combined GR-08-seeds.yml); finding absent from the after.json rescan."
# â”€â”€ best-effort payload purge (1 site) â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
- fingerprint: "v2|db_access|DB_UNAUTHORIZED_MUTATION|path=app%2Fsrc%2Fmain%2Fjava%2Fcom%2Fyourname%2Fexpensetracker%2Fworker%2FNotificationIntakeWorker.kt|symbol.owner=com.yourname.expensetracker.worker.NotificationIntakeWorker|symbol.name=purgePayloadBestEffort|symbol.receiver=%3Cnone%3E|symbol.parameters=%5B%22Long%22%2C%22Long%22%2C%22com.yourname.expensetracker.domain.workers.WorkerRunContext%3F%22%5D|identity.dao=com.yourname.expensetracker.data.database.dao.NotificationIntakeDao|identity.accessor=intakeDao|identity.operation=purgeAllPayload|identity.mutation_kind=ROOM_MUTATING_QUERY|identity.call_form=receiver"
  ruleId: DB_UNAUTHORIZED_MUTATION
  path: app/src/main/java/com/yourname/expensetracker/worker/NotificationIntakeWorker.kt
  callable:
    ownerFqcn: com.yourname.expensetracker.worker.NotificationIntakeWorker
    method: purgePayloadBestEffort
    receiver: null
    parameterTypes:
    - Long
    - Long
    - com.yourname.expensetracker.domain.workers.WorkerRunContext?
    kind: function
  daoAccessor: intakeDao
  daoFqcn: com.yourname.expensetracker.data.database.dao.NotificationIntakeDao
  operation: purgeAllPayload
  locations: [421]
  disposition: EXACT_POLICY
  rationale: "Best-effort payload purge after every terminal path; invoked ONLY from inside the doWork guard lambda (WorkerExecutionGuard-mediated, direct barrier not claimed), rethrows CancellationException, and swallows non-cancellation cleanup failures so post-terminal cleanup can never regress a committed terminal state."
  owner: '@panospao7'
  linkedIssue: MIT-DB-08I
  expectedResolution: "Seed row merged into the v2 candidate via --seed-rows (combined GR-08-seeds.yml); finding absent from the after.json rescan."
# â”€â”€ privacy-cleanup path (1 site) â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
- fingerprint: "v2|db_access|DB_UNAUTHORIZED_MUTATION|path=app%2Fsrc%2Fmain%2Fjava%2Fcom%2Fyourname%2Fexpensetracker%2Fworker%2FNotificationIntakeWorker.kt|symbol.owner=com.yourname.expensetracker.worker.NotificationIntakeWorker|symbol.name=runPrivacyCleanupGuarded|symbol.receiver=%3Cnone%3E|symbol.parameters=%5B%22Long%22%5D|identity.dao=com.yourname.expensetracker.data.database.dao.NotificationIntakeDao|identity.accessor=intakeDao|identity.operation=markPrivacyDeniedAndPurgeAllPayload|identity.mutation_kind=ROOM_MUTATING_QUERY|identity.call_form=receiver"
  ruleId: DB_UNAUTHORIZED_MUTATION
  path: app/src/main/java/com/yourname/expensetracker/worker/NotificationIntakeWorker.kt
  callable:
    ownerFqcn: com.yourname.expensetracker.worker.NotificationIntakeWorker
    method: runPrivacyCleanupGuarded
    receiver: null
    parameterTypes:
    - Long
    kind: function
  daoAccessor: intakeDao
  daoFqcn: com.yourname.expensetracker.data.database.dao.NotificationIntakeDao
  operation: markPrivacyDeniedAndPurgeAllPayload
  locations: [411]
  disposition: EXACT_POLICY
  rationale: "Privacy-cleanup path when the guard itself skipped the main run for privacy denial -- runs in its OWN WorkerExecutionGuard.runGuardedWithContext with requiresDatabaseWrite = true and requiredCapabilities = emptyList() so cleanup can always run to delete raw data (never gated on the capability it enforces)."
  owner: '@panospao7'
  linkedIssue: MIT-DB-08I
  expectedResolution: "Seed row merged into the v2 candidate via --seed-rows (combined GR-08-seeds.yml); finding absent from the after.json rescan."
deferred:
  note: "NONE for this file: after the GR-08i3 promotion NotificationIntakeWorker.kt carries 0 DB_UNAUTHORIZED_MUTATION findings (15/15 sites authorized across 7 fingerprints).  Out-of-batch stability: the rescan delta expectation for the WHOLE GR-08i batch is 291 -> 247 (44 = 14 GR-08i1 + 15 GR-08i2 + 15 GR-08i3; the plan prose's 291 -> 246 assumed 15 GTC findings, but the trusted report carries 14 because the 15th site is already authorized by the legacy MIT-003 direct row).  The remaining 247 include the still-unauthorized files deferred to future batches, which must remain byte-stable across this batch's rescan.  Future batches must keep passing the cumulative combined seed document so earlier batch rows never drop from the rendered candidate."
  callables: []
