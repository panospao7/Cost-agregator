# PIPELINE_04 — Fresh Validation (2026-09-04)

- Validated at: d1fa9c688c0a6e5cd1a537ceff9830ad29748057 (branch `atomicity-pr21-enforcement-final`). Static validation only — no compile/test run.
- Source doc: docs/analyses and debug master/PIPELINE_4_CONSOLIDATED_ISSUES.md (last validated 2026-05-31)
- Cross-checked against: PIPELINE_ISSUES_MASTER_TRACKER.md (Pipeline 4 section, lines 144–171) and FIXED_CLAIMS_VALIDATION_AUDIT_v4.md flags (NEW-P4-002 confirmed fixed there; re-verified below).
- All paths below are relative to `app/src/main/java/com/yourname/expensetracker/` unless noted.

## 1. Pipeline theme

Recurring expenses / bill reminders: rule CRUD lifecycle, occurrence generation/materialization, expense→occurrence linking, reminder delivery claim/dispatch, planned-vs-actual reconciliation. The theme of this validation round: all 21 registry items verified fixed or intentionally deferred at HEAD — the recurring pipeline is the strongest atomicity practitioner in the repo (conditional CAS updates, lookup-inside-transaction, typed transition policy). Remaining risks are small: raw-Double sums in the reconciliation report, a residual expense-snapshot read outside the link transaction, and mixed lifecycle-event writer usage that drifts from LEGAL_PATHS law.

## 2. Verdict summary

| Verdict | Count |
|---|---:|
| Verified fixed | 19 |
| Verified open | 0 |
| Verified partial | 1 (P4-P1-05 — key format fixed for new rows, legacy-row migration still absent, DEFERRED stands) |
| Regressed | 0 |
| Tracker drift | 7 (6 stale entries in the consolidated doc + 1 audit-flag confirmation for NEW-P4-002) |
| New issues found | 3 (all MINOR) |
| Not verifiable statically | 0 |

Note: drift again runs in the "consolidated doc is stale" direction — PIPELINE_4_CONSOLIDATED_ISSUES.md still lists NEW-P4-003/005/006/008/009/010 as OPEN while the tracker and the code both say fixed. The master tracker's Pipeline 4 section is accurate at HEAD.

## 3. Issue-by-issue validation

| ID | Title (short) | Old status | Fresh status | Class | Evidence (file:line) | Notes |
|----|---------------|-----------|--------------|-------|----------------------|-------|
| P4-P0-01 | Actual payment does not fulfill planned expense | FIXED | VERIFIED_FIXED | PIPELINE | domain/recurring/lifecycle/RecurringLifecycleCoordinator.kt:300–341 — inside `database.withTransaction`: occurrence match, atomic `occurrenceDao.claimForExpense` CAS (325–334; DAO WHERE `status='PLANNED' AND linkedExpenseId IS NULL`, RecurringOccurrenceDao.kt:57–63), OCCURRENCE_PAID event (336–344), planned fulfilled (347–359) | Full planned→paid path atomic. |
| P4-P0-02 | Paid occurrence does not suppress reminders | FIXED | VERIFIED_FIXED | PIPELINE | RecurringLifecycleCoordinator.kt:369 (`reminderDeliveryDao.suppressOpenDeliveriesForOccurrence`) + REMINDER_SUPPRESSED_PAID event (371–380); materializer suppress path RecurringOccurrenceMaterializer.kt:156, 205 | Suppression on both link and materializer-auto-PAID paths. |
| P4-P1-01 | Reminder dispatch not exactly-once safe | FIXED | VERIFIED_FIXED | PIPELINE | RecurringLifecycleCoordinator.kt:938–940 (`claimReminderDelivery` → DAO claim); RecurringReminderDeliveryDao.kt:69–87 — conditional UPDATE `WHERE (status='SCHEDULED' AND scheduledAt<=:now) OR (status='SNOOZED' AND snoozedUntil<=:now)`; worker revalidates after claim (BillReminderWorker.kt:88–99) | Claim-before-post with post-claim revalidation + stale-claim recovery (`recoverStaleClaimedDeliveries`, coordinator 892–906). |
| P4-P1-02 | Recurring rule CRUD bypasses lifecycle/events | TODO ONLY → FIXED | VERIFIED_FIXED | PIPELINE | domain/recurring/lifecycle/RecurringRuleLifecycleCoordinator.kt:55–88 (deactivate), 93–125 (delete), 131–187 (create), 193–244 (activate), 249–268 (advanceNextDate), 276–359 (update) — each: write barrier + single `database.withTransaction` + RULE_* lifecycle event | Single-writer law holds; regeneration atomic with the mutation. See NEW-P4-2026-003 for event-writer law tension. |
| P4-P1-03 | Bill reminder worker disabled by default | FIXED | VERIFIED_FIXED | PIPELINE | domain/workers/WorkerSpec.kt:68–77 — `bill_reminder_periodic` version 2, `enabled = true` (comment: "bumped: enabled=true (worker was silently disabled)") | WorkerSpecScheduler consumes the spec; worker schedules via spec (BillReminderWorker.kt:289–291). |
| P4-P1-04 | Reminder deliveries only when caller passes windows | TODO ONLY → FIXED | VERIFIED_FIXED | PIPELINE | RecurringLifecycleCoordinator.kt:68 (`DEFAULT_REMINDER_WINDOWS = [3_DAYS_BEFORE, DUE_DAY, OVERDUE]`), 137 (`options.reminderWindows.ifEmpty { DEFAULT_REMINDER_WINDOWS }`); rule CRUD passes it explicitly (RecurringRuleLifecycleCoordinator.kt:160, 220, 327) | Default applied at generation; past-due windows skipped by default (materializer 239–257). |
| P4-P1-05 | `occurrenceKey` can collide across source types | DEFERRED | VERIFIED_PARTIAL (deferred status still accurate) | PIPELINE (schema/identity) | domain/recurring/RecurringOccurrenceExpander.kt:143–155 — key = `"$sourceType|$sourceId|$dayStart|$frequency"`; KDoc states "Source type is included to prevent collisions between different source types with the same numeric sourceId" | New-row collisions across source types are structurally prevented by the key format. However no legacy-row migration/normalization of `occurrenceKey` was found in data/database/DatabaseMigrations.kt (no occurrenceKey rewrite), so the DEFERRED label (design/migration for existing rows) remains justified. Keep open as DEFERRED. |
| P4-P1-06 | Expense→occurrence linking not globally guaranteed | TODO ONLY → FIXED | VERIFIED_FIXED | PIPELINE | domain/transaction/lifecycle/TransactionSideEffectPlanner.kt:318–332 — post-creation RECURRING side effect calls `linkExpenseToOccurrenceDetailed(expenseId, "transaction_created:$source")` | Every `TransactionLifecycleCoordinator` creation path dispatches the recurring link (U-PR8 post-commit side-effect pattern). |
| P4-P1-07 | Existing PAID occurrences downgraded by regeneration | TODO ONLY → FIXED | VERIFIED_FIXED | PIPELINE | RecurringOccurrenceMaterializer.kt:96 — update only when `existing.status !in RecurringOccurrenceStatus.terminalDbValues`; terminal set documented at 360–374 | PAID/CANCELLED/SKIPPED/MISSED/IGNORED never auto-downgraded. |
| P4-P1-08 | Materializer updates status without lifecycle event | TODO ONLY → FIXED | VERIFIED_FIXED | PIPELINE | RecurringOccurrenceMaterializer.kt:104–116 — OCCURRENCE_STATUS_CHANGED event written in the same transaction as the status update | Event carries old/new status metadata. |
| P4-P1-09 | Shared recurring write methods miss restore guard | FIXED | VERIFIED_FIXED | UNIVERSAL (restore/write barrier) | RecurringLifecycleCoordinator.kt barriers at 94, 282, 430, 500, 725, 813, 905, 939, 976, 1011, 1037, 1070, 1105 (+in-transaction re-checks 1073, 1109); RecurringRuleLifecycleCoordinator.kt:56, 94, 132, 194, 250, 277 | The single no-barrier site (projectOccurrences, coordinator 191–192) is documented as read-only-by-design. |
| P4-P1-10 | Legacy `BillReminderManager.markBillPaid()` mixed behavior | TODO ONLY → FIXED | VERIFIED_FIXED | PIPELINE | domain/reminder/BillReminderManager.kt:155–156 — `markBillPaid` returns `Nothing` and throws `error("Legacy markBillPaid removed. Create an actual expense and use linkExpenseToOccurrence(expenseId).")` | Removed per LEGAL_PATHS.md:177; zero production callers. |
| NEW-P4-001 | CancellationException swallowed in bulk reconcile | FIXED (U-PR1) | VERIFIED_FIXED | UNIVERSAL (CE swallowed) | RecurringLifecycleCoordinator.kt:516–519 — per-item `catch (e: Exception) { CancellationSafe.rethrowIfCancellation(e); failed++ }` in `reconcileAllLinkedExpensesAfterBulkUpdate`; helper at domain/util/CancellationSafe.kt:15+ rethrows CE and subclasses | File now has zero CE-swallowing catches (coordinator restructure under PR12b/PR14/PR19 removed the old catch-all). |
| NEW-P4-002 | Variable shadowing — `scheduledAt` computed twice | FIXED (P4-PR1) | VERIFIED_FIXED (audit v4 CONFIRMED_FIXED re-verified) | PIPELINE | RecurringOccurrenceMaterializer.kt:236 — single `val scheduledAt = computeScheduledAt(...)`; removal comment at 261–262 ("P4-NEW-002: … redundant shadowing variable was removed") | Matches audit v4 line-reference (materializer ~207 at that HEAD; current line 236). |
| NEW-P4-003 | Race in `linkExpenseToOccurrence` — lookup outside transaction | OPEN → FIXED (tracker, P4-PR1) | VERIFIED_FIXED (tracker drift) | PIPELINE (race — universal candidate) | RecurringLifecycleCoordinator.kt:300–310 — occurrence lookup moved INSIDE `database.withTransaction` (comment "P4-NEW-003: Occurrence lookup INSIDE the transaction — read + write are atomic"); double-claim prevented by `claimForExpense` CAS (325–334) | Residual minor TOCTOU on the expense snapshot (read outside tx, line 284) tracked as NEW-P4-2026-002. |
| NEW-P4-004 | `BillReminderWorker` uses `System.currentTimeMillis` | FIXED (U-PR7) | VERIFIED_FIXED | UNIVERSAL (System.currentTimeMillis vs TimeProvider) | BillReminderWorker.kt:64 — `val now = timeProvider.now()`; injected `timeProvider` (line 40); zero `System.currentTimeMillis` in file | `isWithinQuietHours(now)` receives injected time. |
| NEW-P4-005 | Notification ID collision risk | OPEN → FIXED (tracker, P4-PR2) | VERIFIED_FIXED (tracker drift) | PIPELINE | BillReminderWorker.kt:247 — `val notificationId = (delivery.id % Int.MAX_VALUE).toInt()` (stable per delivery, not hashCode) | Deterministic across restarts; collisions only for ids ±2^31 apart (unreachable). |
| NEW-P4-006 | PendingIntent request code collision | OPEN → FIXED (tracker, P4-PR2) | VERIFIED_FIXED (tracker drift) | PIPELINE | BillReminderWorker.kt:214–235 — snooze request code `(delivery.id % Int.MAX_VALUE).toInt()` (216), dismiss code `snoozeRequestCode xor 0x40000000` (229); distinct receiver components further disambiguate PendingIntents | Snooze/dismiss can no longer collide with each other or across deliveries. |
| NEW-P4-007 | CancellationException swallowed in `regenerateReminderDeliveries` | FIXED (U-PR1) | VERIFIED_FIXED | UNIVERSAL (CE swallowed) | RecurringLifecycleCoordinator.kt:607–609, 637–639, 677–679 — every per-window catch runs `CancellationSafe.rethrowIfCancellation(e)` | CE propagates; only non-cancellation failures are isolated per window. |
| NEW-P4-008 | `reconcilePlannedVsActual` has write side-effects in query-like method | OPEN → FIXED (tracker, P4-PR1) | VERIFIED_FIXED (tracker drift) | PIPELINE (design) | RecurringLifecycleCoordinator.kt:1157–1172 (`ensureOccurrencesGeneratedForReconciliation` — explicit write), 1180–1247 (`calculatePlannedVsActualReport` — pure read); legacy method @Deprecated(ERROR) with ReplaceWith at 1249–1263 | Split complete; callers must opt into the write. Raw-Double summing inside the report tracked as NEW-P4-2026-001. |
| NEW-P4-009 | JSON injection in lifecycle event metadata | OPEN → FIXED (tracker, P4-PR2) | VERIFIED_FIXED (tracker drift) | UNIVERSAL (injection into durable metadata) | All user-provided strings go through `JSONObject.put()` auto-escaping: RecurringLifecycleCoordinator.kt:352–356 (currency), RecurringRuleLifecycleCoordinator.kt:81–84/117–121/177–182/236–240/261–264/349–355 (merchant), RecurringOccurrenceMaterializer.kt:111–114/132–137/148–152/181–186/197–201/217–221/248–253/286–289 | Remaining hand-built `"""{"…":"$window"}"""` templates interpolate only internal enum-like window constants and Longs (coordinator 598, 623–629, 668–673) — no user input. |
| NEW-P4-010 | `linkExpenseToOccurrenceDetailed` returns Skipped for impossible state | OPEN → FIXED (tracker, P4-PR2) | VERIFIED_FIXED (tracker drift) | PIPELINE | RecurringLifecycleCoordinator.kt:404–408 — impossible state now returns `RecurringExpenseReconcileResult.Error(expenseId, "linked_occurrence_missing_after_successful_claim")` + `Timber.w("Impossible state: …")` | Skipped reserved for legitimately skippable inputs (395–399). |

### 3.1 Tracker/doc drift found this session

1. **Consolidated doc staleness (6 items).** PIPELINE_4_CONSOLIDATED_ISSUES.md still lists NEW-P4-003, NEW-P4-005, NEW-P4-006, NEW-P4-008, NEW-P4-009, NEW-P4-010 as 🔴 OPEN. All six are fixed in code and already ✅ FIXED in PIPELINE_ISSUES_MASTER_TRACKER.md:163–171. The consolidated doc needs a status sweep; the tracker does not.
2. **NEW-P4-002 audit confirmation.** FIXED_CLAIMS_VALIDATION_AUDIT_v4.md confirmed the fix at `RecurringOccurrenceMaterializer.kt:207`; re-verified at HEAD — single `computeScheduledAt` call now at line 236 with the removal comment at 261–262. Audit verdict stands.
3. **P4-P1-05 nuance.** The consolidated doc/tracker say "Needs design/migration". The code shows the design part is done (sourceType in key, Expander 143–155); the migration part (normalizing any legacy keys) is absent — DEFERRED remains the correct status, now scoped to legacy rows only.
4. **Worker-spec version drift is intentional.** `bill_reminder_periodic` version 2 with `enabled=true` (WorkerSpec.kt:70–71) is the documented P4-P1-03 fix, not drift.

## 4. New issues & regressions found this session

| ID | Sev | Evidence | Description | Suspected origin |
|----|-----|----------|-------------|------------------|
| NEW-P4-2026-001 | MINOR | RecurringLifecycleCoordinator.kt:1180–1247 — `calculatePlannedVsActualReport` sums `occ.expectedAmount` and `occ.paidAmount` as raw Doubles (PAID branch ~1214–1223) with no currency grouping; occurrences for one ruleId can carry different `expectedCurrency`/`paidCurrency` after a rule's currency was changed by `updateRule` (old PAID rows keep the old currency) | Reconciliation drift/totalPlanned/totalActual can mix currencies within a single report when a rule's currency changed across its lifetime — wrong numbers, silently. Universal money-math category (raw sums without currency context). Suggest grouping by currency or returning a MoneyAggregate-style partial result. | Pre-existing (report predates currency hardening; not covered by U-PR3/U-MONEY-01 scope) |
| NEW-P4-2026-002 | MINOR | RecurringLifecycleCoordinator.kt:284 — `expenseDao.getById(expenseId)` read outside `database.withTransaction` (opened at 300); the stale snapshot feeds match criteria (292–298) and `claimForExpense` params (amount/currency, 325–330) | Residual TOCTOU: an expense updated between the outside read and the claim can be matched with stale amount/currency, and the claim writes the stale `paidAmount`/`paidCurrency`. The occurrence side is atomic (P4-NEW-003 fix); only the expense snapshot lags. Practical impact low (single-row update window), but it is the same read-outside-transaction pattern the architecture law discourages. | Pre-existing (partial residual of the P4-PR1 fix, which covered only the occurrence lookup) |
| NEW-P4-2026-003 | MINOR (law tension) | RecurringRuleLifecycleCoordinator.kt:74–86, 110–123, 171–184, 230–242, 254–266, 342–357 and RecurringOccurrenceMaterializer.kt:104–116, 125–139, 174–188, 210–223 write lifecycle events via direct `lifecycleEventDao.insert`; vs LEGAL_PATHS.md:391–393 ("❌ Writing lifecycle events directly through DAO insert") and :179 | Mixed lifecycle-event writer usage: expense/occurrence-link provenance events (OCCURRENCE_PAID, PLANNED_FULFILLED, REMINDER_SUPPRESSED_PAID) correctly use `eventWriter.writeCritical` (RecurringLifecycleCoordinator.kt:336–380), but RULE_* events and materializer OCCURRENCE_STATUS_CHANGED / OCCURRENCE_GENERATED / PLANNED_FULFILLED events are direct DAO inserts. Consistency of the audit trail writer is compromised; failure semantics differ (writeCritical fails the operation vs direct insert silently). Either exempt these writers in LEGAL_PATHS or route through `RecurringLifecycleEventWriter`. | Pre-existing (introduced when rule CRUD/materializer events were added; PR19/PR20 did not unify writers) |

No regressions found from the T1–T4C time migration, the PR18–24 cancellation/atomicity rework (bffc4299..65c265fb), or the PII burn-down commits:
- Time migration: T4 commits touched no recurring/recurring-lifecycle/reminder files directly; the shared dependency `TimePeriodUtils` changed `getEndOfDay` from inclusive 23:59:59.999 to exclusive next-midnight (0c68f5c6, following 8e40187f), and the recurring DAO range query was already half-open (`dueDate >= :start AND dueDate < :end`, RecurringOccurrenceDao.kt:26–27) — no semantic break in occurrence matching or reminder windows. `addDays`/`addMonths` remain DST-safe Calendar arithmetic (TimePeriodUtils.kt:1344–1360).
- b00241d7 moved the STALE_DELIVERIES_RECOVERED event inside its transaction (removed a best-effort swallow) — improvement, no edge case lost.
- BillReminderWorker guard structure verified against worker law: `runGuardedWithContext` first (46–56), per-reminder `ctx.checkpoint` + `isStopped` (78–80), CE rethrow at 123/151/164/255, SecurityException caught on post (251–253), `ctx.addNotificationsSent()` only after a real successful post + markReminderSent (106–109), durable skip via guard for permission (requiresNotificationPermission=true is legitimate here — the worker's only purpose is notification posting, so gating does not block unrelated core DB work).

## 5. Universal candidates

| Universal category | Pipeline-4 instances | Covered by U-PR fixes? |
|---|---|---|
| CancellationException swallowed in broad catches | NEW-P4-001, NEW-P4-007 | YES — U-PR1 fully landed here: shared `CancellationSafe.rethrowIfCancellation` (domain/util/CancellationSafe.kt) used in every surviving catch; the rule coordinator no longer has any catch-all. |
| System.currentTimeMillis instead of TimeProvider | NEW-P4-004 | YES — U-PR7 verified: worker uses injected `timeProvider.now()`; reminder math uses TimePeriodUtils DST-safe helpers. |
| TOCTOU read-outside-transaction | NEW-P4-2026-002 (expense snapshot), mirrored on the receipt side (05 doc NEW-P3-2026-001) | PARTIAL — occurrence-side lookup fixed by P4-PR1; no universal rule yet mandates "load inside transaction or CAS" for the counterpart snapshot. Recommend promoting to a universal guard rule. |
| Mixed-currency money math | NEW-P4-2026-001 (reconciliation report raw Double sums) | NO — U-PR3 covered SynthesisEngine; single-entity-per-rule reports were out of scope. Fold into U-MONEY follow-up. |
| Side-effect double dispatch | P4-P1-06 + planner `linkedExpenseIds` pattern (ReceiptSideEffectPlanner.kt:96–100 shows the same guard on receipt side) | YES — post-commit action batches with idempotency keys (e.g., `receipt:$id:saved:warranty_extraction`) and already-linked checks prevent double dispatch. |
| Restore/write barrier mishandling | P4-P1-09 (13 barrier sites) | YES — barrier-before-write ordering verified; the documented read-only exemption (projectOccurrences, coordinator 191–192) is deliberate and safe. |
| Fail-open privacy / metadata injection | NEW-P4-009 | YES — JSONObject.put() escaping verified across all three recurring writers. |
| Permission gating unrelated core work | BillReminderWorker `requiresNotificationPermission = true` | JUSTIFIED — the worker's sole output is notifications; claim/recover work is not blocked for other pipelines. Receipt matching and DB enrichment do NOT gate on notification permission (ReceiptMatchingWorker verified in 05 doc §5). |

## 6. Recommended next actions (ordered, concrete)

1. **Fix NEW-P4-2026-001** — make `calculatePlannedVsActualReport` (RecurringLifecycleCoordinator.kt:1180) currency-aware: group sums per `expectedCurrency`/`paidCurrency` or return a partial/aggregate result; add a test with a rule whose currency changed between occurrences.
2. **Close NEW-P4-2026-002** — move `expenseDao.getById(expenseId)` (RecurringLifecycleCoordinator.kt:284) inside the `database.withTransaction` block (or pass only immutable fields and re-read inside the claim transaction), so the claim uses a transactionally-consistent expense snapshot.
3. **Unify lifecycle-event writing (NEW-P4-2026-003)** — either route RULE_* and materializer events through `RecurringLifecycleEventWriter` (as OCCURRENCE_PAID already does) or amend LEGAL_PATHS.md:391–393 to scope the direct-DAO prohibition explicitly to critical provenance events; document which events are diagnostic-grade.
4. **P4-P1-05 legacy sweep** — when scheduling the deferred migration, add a one-shot data check/normalization for any `recurring_occurrences.occurrenceKey` rows that predate the `sourceType|` key prefix (RecurringOccurrenceExpander.kt:147–155) before relying on key uniqueness for dedup.
5. **Sweep the consolidated doc** — flip NEW-P4-003/005/006/008/009/010 to FIXED in docs/analyses and debug master/PIPELINE_4_CONSOLIDATED_ISSUES.md (tracker at PIPELINE_ISSUES_MASTER_TRACKER.md:163–171 already correct).
6. **Human-run validation** (per repo convention, not run here): `./gradlew :app:testDebugUnitTest --tests "*Recurring*" --tests "*BillReminder*" --tests "*OccurrenceConflictResolver*" --console=plain`.
