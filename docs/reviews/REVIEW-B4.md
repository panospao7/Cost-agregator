## Final Epic Gate

VERDICT: PASS

Issues:
- [ISSUE-B4-1] RESOLVED — Historical `69→70` migration blocker patched in-place; Keystore-backed token work moved out of the DB-open critical path. No version bump. (`MIGRATION_69_70`, Batch 1)
- [ISSUE-B4-2] RESOLVED — Group lifecycle operations (create/add-member/delete) are now transactionally atomic; orphaned system-expense window closed at the coordinator/repository boundary. (`GroupTransactionCoordinator`, `GroupsRepository`, Batch 2)
- [ISSUE-B4-3] RESOLVED — Schema version `70→71` lands one-current-user-per-group uniqueness and non-null system-expense link enforcement in `group_members` and `group_expenses`. (`GroupMember`, `GroupExpense`, migration 71, Batch 3)
- [ISSUE-B4-4] RESOLVED — Schema version `71→72` fixes budget nondeterminism (one active overall/category budget), `ManualRecurringExpense.isSubscription` misclassification, active-only recurring reads, and default-category seeding race. (`Budget`, `ManualRecurringExpense`, `Category`, migration 72, Batch 4)
- [ISSUE-B4-5] RESOLVED — Schema version `72→73` makes merchant lookup deterministic: canonical search-key uniqueness, alias `normalizedKey` uniqueness, merchant-location global-key consistency standardised to `"global"`. Note: `UserCorrection.originalMerchant` DAO-layer tie-breaking fix (REPLACE→ABORT, deterministic secondary sort) landed in Batch 5 Kotlin-only; the missing `Index("originalMerchant")` entity annotation and its corresponding forward migration were deferred and landed in the late closeout fix — see ISSUE-B4-11. (`MerchantCanonical`, `MerchantAlias`, `MerchantLocation`, `UserCorrection` DAO, migration 73, Batch 5)
- [ISSUE-B4-6] RESOLVED — Schema version `73→74` closes bank-disconnect token leak (token fields nulled on disconnect), `BankConnection.defaultCategoryId` FK added, `EmailReceiptDao.insert` changed from destructive `REPLACE` to non-destructive upsert, nullable-field notification dedupe loophole fixed, `AnomalyAlert` FK (`ON DELETE CASCADE`) added. (`BankConnection`, `EmailReceiptSource`, `RawNotification`, `AnomalyAlert`, `EmailReceiptIngestionService`, migration 74, Batch 6). Note: the `AnomalyAlert (category, alertedAt)` composite index was not present in this migration; it was added in the final late-closeout fix via `MIGRATION_77_78` (schema version 78) — see ISSUE-B4-11.
- [ISSUE-B4-7] RESOLVED — Schema version `74→75` enforces one-pending-candidate-per-canonical-merchant uniqueness, wraps `NotificationProcessingPipeline` dedupe+insert in a transaction, transactionally deactivates prior active forecasts before new insert in `BudgetForecastingEngine`, and adds `SubscriptionUsage.usedAt` standalone index. (`SubscriptionCandidate`, `BudgetForecast`, `SubscriptionUsage`, migration 75, Batch 7). Note: the `ExchangeRate` supplementary `toCurrency` index that was planned in Batch 7 was not fully realised until the Batch 29 closeout fix — `Index(["toCurrency"])` entity annotation added to `ExchangeRate.kt` and `MIGRATION_78_79` (`CREATE INDEX IF NOT EXISTS index_exchange_rates_toCurrency ON exchange_rates (toCurrency)`) landed as schema version 79; `MigrationContractTest` extended with `migration_78_to_79_adds_toCurrency_index_on_exchange_rates` and `DatabaseMigrationTest` extended with `migrate_77_to_79_chain_passes_and_has_toCurrency_index` — see ISSUE-B4-11.
- [ISSUE-B4-8] RESOLVED — Schema version `75→76` adds numeric CHECK constraints and impossible-state rejection for `SavingsGoal`, `MileageTracking`, `PendingReview.suggestedType`, and `Expense.splitTemplateId` FK; `HealthScoreHistory.(periodStart, periodEnd)` made unique; `SubscriptionCandidate.convertedSubscriptionId` nullable FK added. (`SavingsGoal`, `MileageTracking`, `PendingReview`, `Expense`, `HealthScoreHistory`, migration 76, Batch 8)
- [ISSUE-B4-9] RESOLVED — Post-schema DAO/repository semantics sweep: atomic savings-goal contribution updates, correct receipt-link state transitions (`UNMATCHED→MATCHED`), business-expense query fixed to use `NOT EXISTS` on `scanned_receipts.expenseId`, `AiChatRepositoryImpl.appendMessage` wrapped in single Room transaction. (`SavingsGoalDao`, `ScannedReceiptDao`, `ExpenseDao`, `AiChatRepositoryImpl`, Batch 9)
- [ISSUE-B4-10] RESOLVED — Integration and presentation closeout: fresh-`Room.databaseBuilder` bypass removed from CSV debug importer, `InvestmentTracker.getInvestmentPerformance` now uses true all-time min/max DAO queries (`getMaxPrice`/`getMinPrice` querying from epoch 0) and uses `recentValues.lastOrNull()` (ASC-ordered window) for correct recent-value selection, `ExpenseWithCategory.formattedAmount` now uses `effectiveAmount` with correct polarity, `ExpenseWithCategory_Extensions` shadowed duplicates removed, `getExpensesPagedDynamic` projection changed to `SELECT e.*`. Note: `InvestmentTracker.getPortfolioValueHistory` still uses per-investment `getValuesBetween()` calls (N+1 pattern); the batched portfolio-history query was not implemented in B.4 and remains an open deferred item. (`CsvExpenseImporter`, `InvestmentTracker`, `ExpenseWithCategory`, `ExpenseWithCategory_Extensions`, `ExpenseRepository`, Batch 10)
  - [ISSUE-B4-11] RESOLVED — Late closeout fixes applied after the main Batch 10 closeout: (1) `ExpenseRepository.getExpensesPagedDynamic` `SELECT e.*` projection confirmed correct after re-verification of the paged query path; (2) `InvestmentTracker.getInvestmentPerformance` recent-value ordering confirmed via `recentValues.lastOrNull()` on the ASC-ordered 30-day window (returns the most-recent sample rather than the oldest); (3) `UserCorrection` entity `Index("originalMerchant")` annotation added and schema version bumped `76→77` via `MIGRATION_76_77` (`CREATE INDEX IF NOT EXISTS index_user_corrections_originalMerchant ON user_corrections (originalMerchant)`); `DatabaseMigrationTest` extended with `migrate_76_to_77_adds_originalMerchant_index` and `migrate_75_to_77_chain_passes_and_has_originalMerchant_index`; (4) `AnomalyAlert` `(category, alertedAt)` composite index added and schema version bumped `77→78` via `MIGRATION_77_78` (`CREATE INDEX IF NOT EXISTS index_anomaly_alerts_category_alertedAt ON anomaly_alerts (category, alertedAt)`); `MigrationContractTest` extended with `migration_77_to_78_adds_category_alertedAt_index_on_anomaly_alerts`; (5) Batch 29 final fix: shadowed formatter API fully resolved — `ExpenseWithCategory_Extensions.kt` extension renamed from `formattedDate` to `formattedTime` (time-only "HH:mm" helper) so it no longer shadows the `ExpenseWithCategory.formattedDate` member property; the stale `formattedDate` extension and the dead `formattedAmount` extension were removed; `TransactionsScreen.kt` import updated from the old `formattedDate` extension to `formattedTime`; (6) Batch 29 final fix: `ExchangeRate` supplementary `toCurrency` index — `Index(["toCurrency"])` entity annotation added to `ExchangeRate.kt` and schema version bumped `78→79` via `MIGRATION_78_79` (`CREATE INDEX IF NOT EXISTS index_exchange_rates_toCurrency ON exchange_rates (toCurrency)`); `MigrationContractTest` extended with `migration_78_to_79_adds_toCurrency_index_on_exchange_rates`; `DatabaseMigrationTest` extended with `migrate_77_to_79_chain_passes_and_has_toCurrency_index`. Note: `InvestmentTracker.getPortfolioValueHistory` N+1 query pattern (one `getValuesBetween()` call per investment) was not addressed in B.4; the batched portfolio-history query remains deferred. (`UserCorrection`, `AnomalyAlert`, `ExchangeRate`, `ExpenseWithCategory_Extensions`, `TransactionsScreen`, `AppDatabase`, `DatabaseMigrationTest`, `MigrationContractTest`, late closeout Batch 29)
- [ISSUE-B4-WAIVER] WAIVER — The following specific test/tool executions were blocked by pre-existing unrelated environment issues and are formally waived:
  - `./gradlew :app:testDebugUnitTest` (full suite): blocked by Gradle daemon conflicts and unrelated dexing failures unrelated to any B.4 changed file.
  - `./gradlew :app:connectedDebugAndroidTest` (full suite): blocked because `adb devices` reported no connected device at the time of broad-suite run attempts (`FAILURE: Build failed with an exception` — `No connected devices!`).
  - Transient `./gradlew :app:compileDebugKotlin` failures during parallel Gradle cleanup: caused by Windows file-lock contention on `.gradle/` cache directories, not by B.4 code changes; resolved on retry.
  B.4 is approved on the basis of: (a) `./gradlew :app:compileDebugKotlin` green across all batches including the late closeout, (b) `./gradlew :app:compileDebugAndroidTestKotlin` green, (c) targeted unit-test selectors green for Batches 2, 4, 7, 9, and 10. Connected androidTest execution is waived (no connected device) per the waiver above; device-dependent test classes including `DatabaseMigrationTest`, `MigrationContractTest`, `GroupMemberDaoTest`, `BudgetDaoTest`, `MerchantNormalizationDaoTest`, and `ScannedReceiptDaoTest` are covered by the waiver. Unrelated full-suite failures are explicitly waived in accordance with B.4 plan Section 4 waiver rule.

Coverage:
- Requirements met: yes — every registry item in `MASTER-ISSUE-REGISTRY.md` lines 260–317 has been resolved or formally dispositioned across B.4 micro-batches 1–10 plus the late closeout (ISSUE-B4-11). Schema migrations 69→70 (patch), 70→71, 71→72, 72→73, 73→74, 74→75, 75→76, 76→77, 77→78, 78→79 land forward-only. Migrations 69→70 through 76→77 are covered by `DatabaseMigrationTest`; migrations 77→78 and 78→79 are covered by `MigrationContractTest` and `DatabaseMigrationTest` chain tests respectively. DAO/repository runtime semantics are aligned with the hardened schema. Presentation and integration touchpoints are closed.
- Testing adequate: yes, with environmental waiver — what passed: `./gradlew :app:compileDebugKotlin` green across all batches including the late closeout; `./gradlew :app:compileDebugAndroidTestKotlin` green (confirms androidTest sources compile without error, but does not execute them on a device); targeted unit-test selectors green for Batches 2, 4, 7, 9, and 10. What was waived: connected execution of `androidTest` migration and DAO classes (`DatabaseMigrationTest`, `MigrationContractTest`, `GroupMemberDaoTest`, `BudgetDaoTest`, `MerchantNormalizationDaoTest`, `ScannedReceiptDaoTest`, and equivalents for the late-closeout migrations v76→v77, v77→v78, and v78→v79) — all blocked by no connected device; waived per ISSUE-B4-WAIVER. Full `:app:testDebugUnitTest` suite also blocked by pre-existing Gradle daemon conflicts unrelated to B.4 changes; likewise waived. B.4 closeout evidence is consistent with the precedent established in A.8, A.9, and A.10.
