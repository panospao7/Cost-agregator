# Deep Analysis — Batch 13: Database - Group & Financial Entities (@debugger)

## Scope
- data/database/entity/ExpenseGroup.kt
- data/database/entity/GroupMember.kt
- data/database/entity/GroupExpense.kt
- data/database/entity/SplitItemAssignment.kt
- data/database/entity/BankConnection.kt
- data/database/entity/ExchangeRate.kt
- data/database/entity/MerchantCanonical.kt
- data/database/entity/MerchantAlias.kt
- data/database/entity/MerchantLocation.kt
- data/database/entity/EmailReceiptSource.kt
- data/database/entity/ManualRecurringExpense.kt
- data/database/entity/Investment.kt

## Issues Found

| # | File:Line | Severity | Type | Description | Reproduction Steps | Suggested Fix |
|---|-----------|----------|------|-------------|-------------------|---------------|
| 1 | Investment.kt:35 | 🔴 Critical | Missing `@ColumnInfo(defaultValue)` | `currentPrice: Double = purchasePrice` uses a Kotlin parameter reference as default. Room cannot express this as a SQL `DEFAULT` value. When Room reads a row from the database where `currentPrice` was not explicitly set (e.g., during a migration that adds the column, or a raw SQL INSERT that omits it), Room cannot reconstruct the object because there's no SQL DEFAULT and no way to evaluate `purchasePrice` at schema level. This also means `@Insert` works at runtime (Kotlin fills it), but raw SQL inserts and migration-added rows will get `0.0` or fail. | 1. Add an Investment via SQL (e.g., migration or raw query) omitting `currentPrice`. 2. Read row — `currentPrice` will be 0.0, not the purchase price. 3. Portfolio gain/loss calculations show incorrect loss. | Add `@ColumnInfo(defaultValue = "0.0")` and document that callers must always explicitly set `currentPrice`. Alternatively, add a validation check in `InvestmentTracker` that falls back to `purchasePrice` when `currentPrice == 0.0`. |
| 2 | GroupExpense.kt:50 / SharedExpenseBudgetOffsetEngine.kt:130 | 🔴 Critical | Divide-by-zero risk | `calculateMyShare()` for `SplitType.EQUAL` divides by `members.size` without a zero-guard: `expense.totalAmount / members.size`. While `SharedExpenseManager.calculateEqualSplit` guards against empty members, this separate code path in `SharedExpenseBudgetOffsetEngine` at line 130 does not. If a group has all members deleted (CASCADE deletes members when group deletes, but `isExpenseFullySettled` at line 175 also calls `expense.totalAmount / members.size`), this produces `Infinity`/`NaN`. | 1. Create a group, add a member, add an expense with `SplitType.EQUAL`. 2. Delete the member (via GroupMember row deletion). 3. Call `calculateEffectiveBudgetSpend()` — divides by 0. | Change line 130 to `if (members.isEmpty()) 0.0 else expense.totalAmount / members.size`. Apply the same guard at line 175. |
| 3 | MerchantCanonical.kt:35-36 | 🟡 Medium | Stale `updatedAt` on `copy()` | Both `createdAt` and `updatedAt` default to `System.currentTimeMillis()`. When an existing entity is loaded from DB and updated via `copy()` (common Kotlin data class pattern), `updatedAt` retains the DB-loaded value instead of being refreshed. The DAO's `incrementOccurrences()` manually sets `updatedAt = :timestamp`, but any code path using `update()` with a `.copy()` will silently keep the old timestamp. | 1. Load a `MerchantCanonical` from DB. 2. Call `dao.update(entity.copy(isVerified = true))`. 3. Observe `updatedAt` unchanged — still shows original value. | Either always explicitly set `updatedAt = System.currentTimeMillis()` in `.copy()` calls, or use `@Update`-time triggers / a wrapper function. |
| 4 | ExchangeRate.kt:15 + ExchangeRateDao.kt:13-14 | 🟡 Medium | `REPLACE` deletes-then-inserts, orphaning FKs | `@Insert(onConflict = OnConflictStrategy.REPLACE)` with a unique index on `(fromCurrency, toCurrency)` works by DELETE + INSERT under the hood. If any future entity adds a FK referencing `exchange_rates.id`, the REPLACE would cascade-delete child rows. Additionally, the auto-generated PK monotonically increases, wasting IDs on every update. Currently safe because nothing FKs to `exchange_rates`, but this is a latent design risk. | 1. Insert rate EUR→USD with id=1. 2. Insert again EUR→USD with new rate — old row deleted, new id=2. 3. Any external reference to id=1 is now dangling. | Consider using `@Query("INSERT OR REPLACE ...")` with an explicit `@Upsert` annotation (Room 2.5+), or use `@Transaction` with a query-then-update pattern. |
| 5 | BankConnection.kt:27-28 | 🟡 Medium | Plaintext token in entity fields / no column-level encryption default | `accessToken` and `refreshToken` are stored as nullable `String?` with no `@ColumnInfo(defaultValue)`. While `BankTokenCipher` encrypts before storage, the entity itself doesn't enforce encryption — any code path calling `dao.insert(BankConnection(..., accessToken = "raw_token"))` would store plaintext credentials in the database. No schema-level guard prevents this. | 1. Create a `BankConnection` with `accessToken = "my_secret_token"` directly (bypassing `BankTokenCipher`). 2. Inspect DB — plaintext token visible. | Add a `require(isEncrypted(accessToken))` check in an `init {}` block or create a factory function that enforces encryption. |
| 6 | SplitItemAssignment.kt:28 | 🟡 Medium | No FK constraint on `receiptItemId` | `receiptItemId: Long? = null` references receipt line items but has no `@ForeignKey` constraint. If a receipt item is deleted, `SplitItemAssignment` rows become orphaned with dangling `receiptItemId` values pointing to nonexistent items. | 1. Create a SplitItemAssignment with `receiptItemId = 5`. 2. Delete the receipt item with id 5. 3. SplitItemAssignment still references id 5 — no CASCADE cleanup. | Add a `ForeignKey` to the receipt items table (likely `ReceiptItemCategorization`) with `onDelete = SET_NULL`, or add application-level cleanup. |
| 7 | Investment.kt:35 | 🟡 Medium | `allTimeHigh`/`allTimeLow` only from 30-day window | In `InvestmentTracker.kt:91-107`, `allTimeHigh` and `allTimeLow` are computed from the last 30 days of `InvestmentValue` history, not all-time. The field names are misleading, and for investments held longer than 30 days, true ATH/ATL values are lost. | 1. Add investment, record price of $200. 2. Wait 31+ days. 3. Query performance — ATH/ATL won't include the $200 data point. | Rename to `thirtyDayHigh`/`thirtyDayLow`, or query without the date filter for true all-time values. |
| 8 | GroupExpense.kt:53 | 🟡 Medium | Fragile JSON format for `customSplitsJson` | Custom splits are stored as a raw String `"memberId:amount,memberId:amount"` (parsed in `SharedExpenseBudgetOffsetEngine.parseCustomSplits`). No schema validation, no standard JSON, and the format silently drops malformed entries. If any other code writes JSON `{"1": 50.0}` instead of `1:50.0`, splits will silently return 0 for all members. | 1. Store `customSplitsJson = "{\"1\": 50.0}"` (valid JSON but wrong format). 2. `parseCustomSplits` returns empty map. 3. All custom split shares become 0.0 — budget calculations silently wrong. | Use a proper JSON format with a Kotlin serializer, or at minimum add validation/logging when parsing fails. |
| 9 | MerchantLocation.kt:34 | 🟢 Low | Nullable `areaKey` with unique index | `areaKey: String? = "global"` is nullable, and the unique index is on `(normalizedMerchantName, areaKey)`. In SQLite, `NULL != NULL` for uniqueness purposes, meaning multiple rows with `areaKey = NULL` and the same `normalizedMerchantName` can coexist, bypassing the intended uniqueness constraint. The code notes legacy NULL rows exist. | 1. Insert two `MerchantLocation` rows with same `normalizedMerchantName` and `areaKey = null`. 2. Both insert successfully — unique constraint doesn't trigger. 3. Queries may return multiple ambiguous results. | Migrate legacy NULL areaKey values to `"global"` and make `areaKey` non-null. |
| 10 | EmailReceiptSource.kt:41-42 | 🟢 Low | `emailMessageId` unique index with nullable column + `defaultValue = "NULL"` | `emailMessageId` has `@ColumnInfo(defaultValue = "NULL")` and a unique index. The string `"NULL"` as a default is the literal text `NULL` in Room, which correctly maps to SQL NULL. However, multiple rows with `emailMessageId = null` won't violate the unique constraint (SQLite NULL uniqueness semantics). This means the unique constraint only protects non-null message IDs. | 1. Insert two `EmailReceiptSource` without `emailMessageId`. 2. Both succeed — no uniqueness violation. 3. Duplicates possible for emails without message IDs. | Rely on the `fingerprint` index for deduplication when `emailMessageId` is null (already implemented). Document that uniqueness is partial. |
| 11 | ManualRecurringExpense.kt:29 | 🟢 Low | `isSubscription` defaults to `true` | `@ColumnInfo(defaultValue = "1") val isSubscription: Boolean = true` means every manually-created recurring expense is marked as a subscription by default. Non-subscription recurring expenses (e.g., rent, utilities) must explicitly set `isSubscription = false`. The comment says "backwards compatibility" but this can silently misclassify entries. | 1. Create a recurring rent expense without setting `isSubscription`. 2. It's classified as a subscription. 3. Subscription analytics inflate subscription counts. | Change default to `false` and explicitly set `true` when creating subscription entries. Or add validation in the ViewModel. |
| 12 | GroupMember.kt:25 | 🟢 Low | Unique index `(groupId, name)` is case-sensitive | The unique constraint on `(groupId, name)` doesn't normalize case. "Alice" and "alice" would be treated as different members in the same group. | 1. Add member "Alice" to group 1. 2. Add member "alice" to same group. 3. Both inserts succeed — conceptual duplicate. | Add `COLLATE NOCASE` to the name column, or normalize names to lowercase before insertion. |
| 13 | BankConnection.kt:46 | 🟢 Low | `consecutiveErrors` not reset on success | `consecutiveErrors: Int = 0` tracks consecutive failures. The entity has no built-in mechanism to reset it on a successful sync. If the DAO only increments, this value grows forever after the first failure, even after subsequent successes. | 1. Bank sync fails 3 times → `consecutiveErrors = 3`. 2. Next sync succeeds but `consecutiveErrors` stays 3. 3. Error-throttling logic incorrectly thinks errors are ongoing. | Ensure the sync success code path resets `consecutiveErrors` to 0 (verify in the sync flow). |

### Cross-Component Pipeline Issues

| # | Pipeline | Severity | Type | Description | Suggested Fix |
|---|----------|----------|------|-------------|---------------|
| 1 | GroupExpense → SharedExpenseBudgetOffsetEngine → Budget | 🔴 Critical | Divide-by-zero propagates NaN into budget calculations | When `members.size == 0` and `SplitType.EQUAL`, `calculateMyShare()` returns `Infinity`. This propagates through `totalSharedSpend` → `effectiveBudgetSpend` → budget comparison screens. `NaN`/`Infinity` in Double arithmetic silently corrupts all downstream sums. | Guard `members.size` with `.coerceAtLeast(1)` or return `0.0` for empty member lists in all division paths. |
| 2 | Investment entity → InvestmentTracker → Portfolio UI | 🟡 Medium | `currentPrice = purchasePrice` default invisible to Room schema | The Kotlin-level default `currentPrice = purchasePrice` works only for Kotlin callers. Room stores/retrieves `currentPrice` as an independent column. After a price update via `InvestmentDao.updatePrice()`, a subsequent `copy()` call on the entity would still use the stored value (correct). But if the DB column has no `DEFAULT` and a raw migration inserts a row omitting `currentPrice`, the value will be `0.0`, making gain/loss = `-purchasePrice * quantity` (100% loss). | Add `@ColumnInfo(defaultValue = "0.0")` and add a null/zero check in `InvestmentTracker.getPortfolioSummary()`. |
| 3 | ExchangeRate → CurrencyConverter → MultiCurrencyRepository | 🟡 Medium | No rate staleness check | `ExchangeRate.lastUpdated` is stored but never checked for staleness during conversion. `CurrencyConverter.convert()` uses whatever rate is in the DB, even if months old. No warning is surfaced to users when rates are stale. | Add a staleness threshold (e.g., 24h) and return a warning or force-refresh when rates are too old. |
| 4 | GroupExpense.customSplitsJson → parseCustomSplits → Budget | 🟡 Medium | No validation between writer and parser format | `GroupTransactionCoordinator` writes `customSplitsJson`, `SharedExpenseBudgetOffsetEngine` reads it. There is no shared serializer/deserializer, so format drift between the writer and reader would silently zero out split amounts in budget calculations. | Extract a shared `CustomSplitsSerializer` used by both writer and reader. |
| 5 | EmailReceiptSource → ScannedReceipt → Expense | 🟢 Low | FK chain lacks atomicity | `EmailReceiptIngestionService` inserts `ScannedReceipt`, then `EmailReceiptSource` in two separate non-transactional DAO calls. If the process crashes between the two inserts, an orphaned `ScannedReceipt` exists without its `EmailReceiptSource` tracking record. | Wrap both inserts in a `@Transaction` annotated method. |

### Summary
- **Total issues: 18** (13 entity-level + 5 cross-component)
- **Files with issues: 10/12** (clean: `MerchantAlias.kt`, `ScannedReceipt.kt` — `MerchantAlias.kt` is well-designed with appropriate FKs, indexes, and defaults)
- **Critical: 3** (divide-by-zero in budget engine, Investment currentPrice schema gap, NaN propagation)
- **Medium: 6** (stale updatedAt, REPLACE semantics risk, plaintext token risk, missing FK, misleading ATH/ATL, fragile JSON format, no staleness check, format drift)
- **Low: 5** (NULL uniqueness semantics ×2, default isSubscription, case-sensitive name uniqueness, error counter not reset, non-atomic FK chain)

**Highest priority fixes:**
1. **SharedExpenseBudgetOffsetEngine lines 130 & 175** — add zero-guard to prevent NaN/Infinity in budget calculations
2. **Investment.kt line 35** — add `@ColumnInfo(defaultValue = "0.0")` and add zero-check in InvestmentTracker
3. **GroupExpense customSplitsJson** — extract shared serializer to prevent format drift between writer and reader
