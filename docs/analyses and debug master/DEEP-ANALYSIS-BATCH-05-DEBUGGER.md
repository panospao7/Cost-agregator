# Deep Analysis — Batch 05: Use Cases - Dashboard, Expense, Forecast (@debugger)

## Scope
- domain/usecase/dashboard/ComputeDashboardWidgetsUseCase.kt
- domain/usecase/dashboard/ComputeMoneyRadarUseCase.kt
- domain/usecase/dashboard/DashboardDataProvider.kt
- domain/usecase/dashboard/DashboardContractsAdapter.kt
- domain/usecase/expense/CategorizeExpenseUseCase.kt
- domain/usecase/expense/DetectDuplicateExpenseUseCase.kt
- domain/usecase/forecast/CalculateFinancialForecastUseCase.kt
- domain/usecase/savings/LifestyleSavingsPromptUseCase.kt
- domain/usecase/savings/MonthlySavingsSweepUseCase.kt
- domain/usecase/warranty/AutoCreateWarrantyFromReceiptUseCase.kt
- domain/usecase/budget/CalculateBudgetStatusUseCase.kt
- domain/usecase/budget/GetMonteCarloBudgetImpactUseCase.kt
- domain/usecase/dashboard/DashboardTextKeys.kt

## Issues Found

| # | File:Line | Severity | Type | Description | Reproduction Steps | Suggested Fix |
|---|-----------|----------|------|-------------|-------------------|---------------|
| 1 | ComputeDashboardWidgetsUseCase.kt:363-364 | 🔴 Critical | Incorrect Formula | **Safe-to-spend uses `forecast.components?.` null-safe operator on a non-nullable field.** `FinancialForecast.components` is declared as non-nullable `val components: ForecastComponents`, but lines 363-364 use `forecast.components?.totalCommitted ?: 0.0`. While not a runtime crash (Kotlin allows `?.` on non-null), this is misleading and masks that the value is always present. More critically: if the `synthesize()` catch-block default ever changed to a different fallback mechanism making `components` nullable, these lines would silently produce 0 instead of propagating the actual error. | N/A — cosmetic but masks intent | Remove `?.` null-safe operators: `val totalCommitted = forecast.components.totalCommitted` and `val totalLikely = forecast.components.totalLikely`. |
| 2 | ComputeDashboardWidgetsUseCase.kt:298-299 | 🟡 Medium | Off-by-one / Semantic Bug | **`daysRemaining = daysInMonth - dayOfMonth` is 0 on the last day of the month**, meaning the `SafeToSpend` widget shows "0 days remaining" on the last day. On the first day, e.g. day 1 of a 31-day month, `daysRemaining = 30`, meaning today is excluded. This is semantically ambiguous — should the current day count as a "remaining" day? More critically, when `daysRemaining = 0`, the runway/daily-budget calculations downstream would divide by zero if they use this value. | Open dashboard on the last day of the month. `SafeToSpend.daysRemaining = 0`. | Clarify semantics. If "remaining" should include today: `daysRemaining = (daysInMonth - dayOfMonth + 1).coerceAtLeast(1)`. Also ensure downstream consumers don't divide by `daysRemaining`. |
| 3 | ComputeDashboardWidgetsUseCase.kt:365 | 🟡 Medium | Division by Zero Risk | **`averageDailyBurn = if (ctx.dayOfMonth > 0) ctx.monthSpent / ctx.dayOfMonth else 0.0`.** On `dayOfMonth == 0` (theoretically impossible but worth defending) or on the 1st of the month (`dayOfMonth == 1`) with `monthSpent == 0`, `averageDailyBurn` = 0.0. Then `runwayDays = 0` even when `totalRemaining > 0`. A user with budget left but zero spending on day 1 gets CRITICAL runway status, which is misleading. | Open dashboard on the 1st of the month with no purchases yet but a positive budget. `averageDailyBurn = 0`, `runwayDays = 0`, status = `CRITICAL`. | When `averageDailyBurn == 0.0 && totalRemaining > 0`, set `runwayDays = daysRemaining` (all budget is available for remaining days), not 0. |
| 4 | ComputeDashboardWidgetsUseCase.kt:302-303 | 🟡 Medium | Data Inconsistency | **`totalSpent = summary.totalSpent` and `monthSpent = summary.totalSpent` are identical**, but `weekSpent` and `todaySpent` are independently computed from `purchases` list filtering. If the `summary.totalSpent` (from analytics repository) and the sum of `purchases.sumOf { it.effectiveAmount }` diverge (e.g., due to timing/caching), `monthSpent` won't match the sum of its sub-periods, leading to UI inconsistencies like `todaySpent + weekSpent > monthSpent`. | Data race where `summary` is stale but expenses list is fresh from a different flow emission. | Derive `monthSpent` from the same `purchases` list: `monthSpent = purchases.filter { it.date >= monthStart }.sumOf { it.effectiveAmount }`, or ensure both come from the same atomic snapshot. |
| 5 | ComputeMoneyRadarUseCase.kt:298 | 🟡 Medium | Suspend in Non-Suspend | **`calculateDueBillsScore` is marked `suspend` but is called from `compute()` sequentially, not in parallel.** The comment at line 138 says "Gather all data in parallel where possible" but `getDueBills`, `getUnresolvedAnomalies`, `getBudgetRisk` are all called sequentially. This is a missed optimization, not a bug, but `calculateDueBillsScore` being `suspend` (due to calling `getMonthlyIncome()`) means it performs a redundant DB query for monthly income that was already available in the parent flow. | N/A — performance issue | Use `async { }` with `coroutineScope { }` to parallelize the three data fetches. Also consider caching monthly income or passing it as a parameter. |
| 6 | ComputeMoneyRadarUseCase.kt:239 | 🟡 Medium | Redundant TimeProvider Call / TOCTOU | **`getBudgetRisk()` calls `timeProvider.now()` independently** at line 239, but `compute()` already captured `now` at line 136. If these calls are separated by even seconds (due to slow `getDueBills` / `getUnresolvedAnomalies` calls), the "now" values diverge, causing inconsistent month boundaries. `getMonthlyIncome()` (line 353) also calls `timeProvider.now()` independently. | Slow `getDueBills()` causes `getBudgetRisk()` to execute seconds later; if near midnight/month boundary, `getMonthRange` returns different months. | Pass `now` from `compute()` as a parameter to all sub-methods for temporal consistency. |
| 7 | ComputeMoneyRadarUseCase.kt:260-262 | 🟢 Low | Redundant Work | **`getBudgetRisk()` calls `recurringExpenseEngine.getPatterns()` again** at line 260, which was already called in `getDueBills()` at line 187. This is a redundant network/DB call for the same data. | Every `compute()` call fetches patterns twice. | Fetch patterns once in `compute()` and pass to both sub-methods. |
| 8 | DetectDuplicateExpenseUseCase.kt:46-55 | 🔴 Critical | Duplicate Detection Misses `isNotMine` Expenses | **`getExpensesBetween` DAO query filters `isNotMine = 0`**, meaning expenses marked "not mine" are excluded from duplicate candidate set. If a user marks an expense as "not mine" and the same transaction is re-imported (e.g., from a different notification), the duplicate check won't find it, resulting in a duplicate entry. | 1. Receive notification for expense. 2. Mark it "isNotMine". 3. Same notification re-processed. 4. No duplicate detected → creates duplicate row. | Use a DAO query for duplicate detection that does NOT filter `isNotMine`, or check the `dedupeKey` index directly. |
| 9 | CalculateFinancialForecastUseCase.kt:114 | 🟡 Medium | Incorrect SpendingPace | **`projectedTotal = monthSpent` is incorrect** — it should be `projectedTotal = (monthSpent / currentDay) * daysInMonth` to represent the projected end-of-month total at current pace. Setting it equal to `monthSpent` means the projected total always equals actual spending-to-date, making the pace analysis useless. | Invoke `CalculateFinancialForecastUseCase` mid-month. `projectedTotal` equals `monthSpent`, so `SpendingPace` never reflects actual pace. | Calculate `projectedTotal` correctly: `projectedTotal = if (currentDay > 0) (monthSpent / currentDay) * daysInMonth else monthSpent`. |
| 10 | CalculateFinancialForecastUseCase.kt:117 | 🟡 Medium | Hardcoded Pace Status | **`paceStatus = PaceStatus.ON_PACE` is hardcoded**, regardless of actual spending relative to budget or history. This means the `SynthesisEngine` always receives `ON_PACE` and cannot produce accurate forecasts or alerts based on real pace. | Any invocation — pace is always `ON_PACE`. | Calculate actual pace status by comparing `projectedTotal` to budget or prior month, similar to `InsightsEngine.getSpendingPaceSuspend()`. |
| 11 | CalculateFinancialForecastUseCase.kt:122 | 🟡 Medium | Empty Past Data | **`pastSumDaily = emptyList()`** — the `SynthesisEngine.synthesize()` receives an empty daily spending history, which means the forecast has no historical spending context for projection. The engine's catch-block fallback still uses `pastSumDaily` for `pastSpendingPoints`, but with empty data the forecast quality degrades significantly. | Any invocation of the forecast use case — `pastSumDaily` is always empty. | Compute daily cumulative sums from `expenses` list (same approach as `ComputeDashboardWidgetsUseCase.computeRunwayAndForecast`). |
| 12 | CalculateFinancialForecastUseCase.kt:106 | 🟢 Low | Inclusive Range Bug | **`expense.date in monthStart..now`** uses inclusive upper bound `now`, which is correct for filtering, but `getExpensesBetween` DAO uses `date < endDate` (exclusive). This means the use case and the DAO have inconsistent range semantics. | Expense with `date == now` is counted in the use case's `monthSpent` but might be missed by other queries using `getExpensesBetween(monthStart, now)`. | Use consistent range conventions. Either always use exclusive upper bound or document the difference clearly. |
| 13 | MonthlySavingsSweepUseCase.kt:265-278 | 🟡 Medium | Goal Allocation Over-Allocation Bug | **Capping individual allocations via `MAX_SINGLE_ALLOCATION_PERCENT` can cause `allocatedTotal < safeSweepAmount` when the last goal also gets capped.** If the last goal has low urgency but the remaining amount (line 272: `safeSweepAmount - allocatedTotal`) is very large (because earlier goals were capped), the last goal receives a disproportionately large allocation that violates the "remaining" cap. The `isLast` branch bypasses the `MAX_SINGLE_ALLOCATION_PERCENT` cap. | 2 goals: Goal A has 90% urgency (capped at 80%), Goal B has 10% urgency but receives the remaining 20%. With $100 sweep: A gets $80, B gets $20 — B gets 2× its proportional share. | Apply `MAX_SINGLE_ALLOCATION_PERCENT` cap to the last goal too, and redistribute any surplus with a second pass, or accept unallocated remainder. |
| 14 | MonthlySavingsSweepUseCase.kt:246-258 | 🟢 Low | Edge Case — Zero Urgency | **`totalUrgency == 0.0` shouldn't happen due to the goal filter on line 127** (`currentAmount < targetAmount` ensures `urgency > 0`). But if floating-point precision causes `1.0 - (targetAmount / targetAmount)` to compute to 0 when `currentAmount` is very close to `targetAmount`, all goals get `urgency = 0.0`. The equal-distribution fallback is correct behavior. | Goal at 99.999999999% progress due to floating point rounding. | Acceptable — fallback handles it. Consider filtering goals where `urgency < epsilon` before allocation. |
| 15 | LifestyleSavingsPromptUseCase.kt:98-99 | 🟡 Medium | Suggested Uplift Can Be Negative or Zero | **`maxCap = currentSavingsRatePercent * MAX_SAVINGS_CAP_PERCENT`** — if `currentSavingsRatePercent` is 0 or negative (user is spending more than earning), `maxCap` ≤ 0, so `suggestedUplift = min(positive_value, non_positive) ≤ 0`. Then line 124 forces `coerceAtLeast(1.0)`, which means the suggestion becomes 1% regardless. But the *confidence* check at line 104 may have already rejected the recommendation. The real issue: a user with a 0% savings rate gets a meaningless "save 1% more" suggestion when they can't save at all. | User has 0% savings rate but lifestyle inflation detected. Gets "save 1% more" prompt with confidence possibly > threshold. | Add guard: if `currentSavingsRatePercent <= 0`, return null (don't suggest savings uplift when user isn't saving). |
| 16 | LifestyleSavingsPromptUseCase.kt:74-79 | 🟢 Low | Ambiguous Savings Rate Normalization | **Savings rate normalization heuristic: `if (abs(currentSavingsRate) <= 1.0)` assumes values ≤ 1 are fractional and > 1 are already percentages.** A savings rate of exactly 1.0 (100%) is treated as fractional and multiplied by 100 → 10000%, which is wrong. Similarly, a rate of 0.8 (legitimate 0.8% in percentage form) would be incorrectly multiplied by 100 → 80%. | User with exactly 1.0 savings rate from detector. Normalized to 100% savings rate, causing inflated `maxCap`. | Use explicit units from `LifestyleInflationDetector` — check if the detector documents whether it returns fractions or percentages. Remove the heuristic. |
| 17 | AutoCreateWarrantyFromReceiptUseCase.kt:82-87 | 🟡 Medium | Draft Created but LowConfidence Returned | **When confidence is between 40-70%**, `createReviewDraftWarranty` is called first. If it succeeds (returns `WarrantyCreationResult.Success`), the code falls through to `else ->` branch at line 86 which returns `LowConfidence(extractionData)`. **The warranty IS persisted to DB but the caller gets `LowConfidence`, not `Success`.** The caller may then try to re-create it, getting `AlreadyExists`. | Receipt with 55% confidence extraction. Draft warranty is persisted. Caller receives `LowConfidence`. Caller tries `createWarrantyForReview()` → gets `AlreadyExists`. | Return `Success` with a `needsReview=true` flag, or don't persist draft until user confirms. Current flow is confusing but technically workable since `AlreadyExists` is handled. Document this intentional behavior clearly. |
| 18 | AutoCreateWarrantyFromReceiptUseCase.kt:45 | 🟢 Low | Non-Singleton Instance Variable | **`warrantyTextExtractor` is instantiated as `WarrantyTextExtractor()` inside a `@Singleton` class**, not injected. This is fine for a stateless extractor, but if `WarrantyTextExtractor` ever acquires state (compiled patterns, caches), there'd be no way to swap it in tests or share state. | N/A — design smell | Inject `WarrantyTextExtractor` via constructor for testability. |
| 19 | ComputeDashboardWidgetsUseCase.kt:206-229 | 🟡 Medium | Thread Safety — Mutable Calendar in ComputeContext | **`ComputeContext.calendar` is a `java.util.Calendar` instance** shared across multiple methods (`calculateStreakData`, `assembleWidgets`). Calendar is mutable and not thread-safe. While the `compute()` function is sequential, if `assembleWidgets` or any sub-method modifies this calendar, subsequent reads are corrupted. `computeSpendingTrend` creates its own Calendar instances, but `calculateStreakData` receives it at line 244. | No immediate repro unless a sub-method mutates `ctx.calendar`. Currently `calculateStreakData` reads `calendar.timeInMillis` and `calendar.get(DAY_OF_MONTH)` — safe if no mutation. | Pass immutable values (`nowMillis`, `dayOfMonth`) instead of a mutable `Calendar` reference, or clone it before passing. |
| 20 | ComputeDashboardWidgetsUseCase.kt:655 | 🟢 Low | Confusing SafeToSpend Fallback | **When no overall budget exists (`ctx.overallBudget == null`), `SafeToSpend.amount = ctx.monthSpent`.** This shows "amount spent" as the "safe to spend" value, which is semantically inverted — the widget says "safe to spend $X" but X is actually how much was *already* spent. | No budget set. Dashboard shows SafeToSpend with `amount = monthSpent`. | When no budget exists, either hide the widget or show a more appropriate value (e.g., 0.0 or "No budget set"). |
| 21 | GetMonteCarloBudgetImpactUseCase.kt:82-101 | 🟡 Medium | Risk Tier Logic Mismatch with Documentation | **Documentation says HIGH requires `overrun < 30% AND P(overrun) < 75%`**, but implementation at lines 93-95 uses `OR` operators: `overrunPercent >= 0.15 || probabilityOfOverrun >= 0.50`. The doc comments (lines 17-21, 82-84) describe AND conditions for LOW and MEDIUM but OR conditions for HIGH and CRITICAL. The code uses OR throughout the cascade, meaning the tier can be elevated by *either* condition alone, not requiring both. The KDoc is misleading. | Budget overrun is 0% but probability is 51% → gets `HIGH` despite zero overrun. | Fix the KDoc comments to match the actual `OR` logic, or change the logic to match the documented `AND` conditions (which would be more permissive). |
| 22 | CalculateFinancialForecastUseCase.kt:32-129 | 🟢 Low | Unbounded Flow — No Debounce | **`combine()` with 5 flows** (expenses, budgets, recurring, planned, goals) means any change to any flow triggers a full recalculation including `synthesisEngine.synthesize()`. With rapid changes (e.g., bulk import), this could cause excessive CPU usage. | Bulk-import 50 expenses. Each insert triggers `getAllExpenses()` flow, causing 50 synthesize calls. | Add `debounce()` or `conflate()` to the combined flow, or use `distinctUntilChanged()`. |
| 23 | MonthlySavingsSweepUseCase.kt:199 | 🟢 Low | Inclusive/Exclusive Range Inconsistency | **`getExpensesBetween(monthStart, now)` uses DAO with exclusive upper bound `date < endDate`**, so expenses with `date == now` are excluded. This is technically correct (current-second expense isn't "spent" yet) but differs from `CalculateFinancialForecastUseCase.kt:106` which uses `date in monthStart..now` (inclusive). | Expense recorded at exact `now` timestamp — counted in forecast UC but not in sweep UC. | Document the convention or unify to one approach. |

### Cross-Component Pipeline Issues

| # | Pipeline | Severity | Type | Description | Suggested Fix |
|---|----------|----------|------|-------------|---------------|
| P1 | `DashboardDataProvider → ComputeDashboardWidgetsUseCase → ComputeMoneyRadarUseCase` | 🟡 Medium | Dual Data Path / Inconsistency | `ComputeDashboardWidgetsUseCase` receives all data via `ProcessedDashboardData` (reactive flows), but `ComputeMoneyRadarUseCase` independently queries `ExpenseRepository`, `BudgetRepository`, and `RecurringExpenseEngine` via direct DB calls (`.first()`). This means the dashboard's Money Radar widget can show data from a different time snapshot than the surrounding widgets. | Pass pre-fetched data from `DashboardDataProvider` into `ComputeMoneyRadarUseCase` instead of having it re-fetch independently, or accept eventual consistency with documentation. |
| P2 | `DetectDuplicateExpenseUseCase → ExpenseRepository → ExpenseDao` | 🔴 Critical | Silent Duplicate Miss for `isNotMine` | The DAO's `getExpensesBetween` filters out `isNotMine = 0`, so the duplicate detector never sees expenses marked as "not mine." If the same transaction arrives from a different source, it won't be caught as a duplicate. The `dedupeKey` unique index on the DB is a secondary protection layer, but it depends on identical key generation — cross-source deduplication by merchant/amount/time is bypassed for these rows. | Use a separate DAO query for duplicate detection that doesn't filter `isNotMine`. |
| P3 | `CalculateFinancialForecastUseCase → SynthesisEngine` | 🟡 Medium | Degraded Forecast Quality | The standalone `CalculateFinancialForecastUseCase` passes `emptyList()` for `pastSumDaily` and hardcoded `ON_PACE` for pace status, while the dashboard's `computeRunwayAndForecast` computes real values. Any consumer using the standalone forecast use case (outside dashboard) gets materially worse forecasts. | Unify the daily-spending computation and pace calculation into a shared helper used by both code paths. |
| P4 | `MonthlySavingsSweepUseCase → MonteCarloSimulator` | 🟢 Low | Known Upcoming = 0 | Sweep UC passes `knownUpcoming = 0.0` to Monte Carlo (line 111), while the dashboard and Money Radar paths include upcoming recurring expenses. This makes the sweep's risk buffer underestimate uncertainty, potentially over-recommending sweep amounts in months with upcoming bills. | Pass the sum of upcoming recurring/planned expenses from the budget data already fetched. |
| P5 | `ComputeDashboardWidgetsUseCase (Singleton) + Mutable Calendar` | 🟢 Low | Thread Safety | `ComputeDashboardWidgetsUseCase` is `@Singleton` and `compute()` is `suspend`. If called concurrently (unlikely but possible during rapid config change), the `ComputeContext` with its mutable `Calendar` could be safely used since each call creates its own `ComputeContext`. However, the `@Singleton` `ComputeMoneyRadarUseCase` shares no state, so this is safe. | No immediate fix needed — document that `compute()` should not be called concurrently, or add a `Mutex`. |

### Summary
- **Total issues: 23** (issues table) + **5 pipeline issues**
- **Files with issues: 10/13**
  - `ComputeDashboardWidgetsUseCase.kt` — 7 issues (#1, #2, #3, #4, #19, #20, plus pipeline)
  - `ComputeMoneyRadarUseCase.kt` — 3 issues (#5, #6, #7)
  - `DetectDuplicateExpenseUseCase.kt` — 1 issue (#8)
  - `CalculateFinancialForecastUseCase.kt` — 4 issues (#9, #10, #11, #12)
  - `MonthlySavingsSweepUseCase.kt` — 3 issues (#13, #14, #23)
  - `LifestyleSavingsPromptUseCase.kt` — 2 issues (#15, #16)
  - `AutoCreateWarrantyFromReceiptUseCase.kt` — 2 issues (#17, #18)
  - `CalculateBudgetStatusUseCase.kt` — 0 issues (clean)
  - `GetMonteCarloBudgetImpactUseCase.kt` — 1 issue (#21)
  - `CalculateFinancialForecastUseCase.kt` — 1 issue (#22)
  - `DashboardDataProvider.kt` — 0 issues (clean)
  - `DashboardRepositoryContracts.kt` — 0 issues (clean)
  - `DashboardTextKeys.kt` — 0 issues (clean)

- **Critical (🔴):** 2 — Duplicate detection misses `isNotMine` expenses (#8/P2); unnecessary null-safety on non-nullable field (#1 — low runtime risk but misleading)
- **Medium (🟡):** 13 — Incorrect formulas, hardcoded values, data inconsistencies, doc mismatches
- **Low (🟢):** 8 — Edge cases, design smells, performance opportunities

### Top Priority Fixes

1. **Issue #8 / P2 (DetectDuplicateExpenseUseCase)**: The DAO query filters out `isNotMine = 0`, so the duplicate detector never sees expenses marked as "not mine." Use a separate DAO query for duplicate detection that doesn't filter `isNotMine`.

2. **Issue #3 (ComputeDashboardWidgetsUseCase:365)**: When `averageDailyBurn == 0.0 && totalRemaining > 0`, set `runwayDays = daysRemaining` (all budget is available for remaining days), not 0.

3. **Issue #9 (CalculateFinancialForecastUseCase:114)**: Calculate `projectedTotal` correctly: `projectedTotal = if (currentDay > 0) (monthSpent / currentDay) * daysInMonth else monthSpent`.
