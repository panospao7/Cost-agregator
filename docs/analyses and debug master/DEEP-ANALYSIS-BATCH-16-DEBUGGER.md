# Deep Analysis — Batch 16: UI Components (@debugger)

## Scope
- domain/health/FinancialHealthScoreV2Widget.kt
- domain/dashboard/MoneyRadarWidget.kt
- domain/analytics/PersonalityProfileCard.kt
- ui/components/ForecastTimeline.kt
- ui/components/SpendingTrendChart.kt
- ui/components/BentoCard.kt
- ui/components/CategoryDonutChart.kt
- ui/components/SpendingPaceGauge.kt

Here is the complete bug analysis of all 8 UI component files:
> Note: 3 files (FinancialHealthScoreV2Widget.kt, MoneyRadarWidget.kt, PersonalityProfileCard.kt) were found in subdirectories health/, dashboard/, and analytics/ respectively.
Bug Report
File:Line	Severity	Type	Description	Suggested Fix
ForecastTimeline.kt:90-92	🔴 Critical	Edge-case crash	When pastPoints is non-empty but projectedPoints is empty (or vice-versa), projectionEntries/pastEntries becomes an empty list. entryModelOf() receives an empty series list, which causes an IllegalArgumentException crash in Vico.	Guard before building the model: if a series list is empty, either exclude it from entryModelOf or insert a single zero-value sentinel entry. Also adjust lineSpecs count to match.
ForecastTimeline.kt:96 vs 90	🔴 Critical	State / data mismatch	lineSpecs is keyed only on hasValidBudget (2 or 3 specs), but chartEntryModel may contain fewer actual series when one data list is empty. Vico crashes with IndexOutOfBoundsException when line specs count ≠ data series count.	Compute lineSpecs keyed on the same conditions that determine how many entry lists are passed to entryModelOf (e.g., remember(hasValidBudget, pastPoints.isNotEmpty(), projectedPoints.isNotEmpty())).
SpendingTrendChart.kt:124-129	🟠 High	Edge-case crash	If any SpendingTrendSeries has data = emptyList(), the entryModelOf vararg receives an empty entry list for that series, which crashes Vico internally. No guard exists for empty-data series.	Filter out series with empty data before building the model: allSeries.filter { it.data.isNotEmpty() }.map { ... }. Also filter lineSpecs correspondingly.
SpendingTrendChart.kt:39-48	🟡 Medium	Recomposition waste	currentSeries, historicalSeries, allSeries, and subtitle are recomputed on every recomposition. These perform list filtering, concatenation, and string building without remember.	Wrap each in remember(series) { ... } to avoid redundant allocations on recomposition when the input series list hasn't changed.
SpendingTrendChart.kt:132-135	🟡 Medium	Recomposition / object allocation	startAxisFormatter is a new lambda/object allocated on every recomposition (not wrapped in remember). Vico uses referential equality, so this can trigger unnecessary chart re-layout.	Wrap in remember { AxisValueFormatter<...> { value, _ -> ... } }.
SpendingTrendChart.kt:137-141	🟡 Medium	Recomposition waste	currentValues, allValues, minValue, maxValue, currentValue are recomputed on every recomposition without remember, duplicating flatMap work that chartEntryModel's remember block already guards.	Wrap in remember(allSeries) { ... } or move inside the existing remember block.
BentoCard.kt:86	🟡 Medium	Visual bug (gradient clipping)	HeroBentoCard applies .background(heroGradient, RoundedCornerShape(28.dp)) on the outer modifier of the Card. The gradient is painted outside the Card's clipping surface. If the Card has any elevation shadow or the caller's modifier changes the layout, the gradient can visually leak outside the rounded corners or be occluded by the Card's own transparent container.	Move the gradient inside the Card (e.g., use a Box(Modifier.background(heroGradient)) as the first child), or apply .clip(RoundedCornerShape(28.dp)) before .background(...) in the modifier chain.
BentoCard.kt:138	🟡 Medium	Locale-dependent formatting	String.format("%.2f", amount) uses the device's default Locale. In locales like German (de_DE), this outputs 1234,50 (comma separator) instead of 1234.50, breaking the currency display (e.g., €1.234,50 vs €1234.50). Same issue in CategoryDonutChart.kt:101,159, SpendingTrendChart.kt:133-134,143, ForecastTimeline.kt:125,127, MoneyRadarWidget.kt:401.	Use String.format(Locale.US, "%.2f", amount) or the app's CurrencyFormatter utility for all monetary formatting.
CategoryDonutChart.kt:137	🟡 Medium	Visual edge case	Donut arc sweep calculation (item.percentage / 100f) * 360f assumes all percentages sum to exactly 100%. Floating-point rounding can cause the total sweep to be slightly < or > 360°, leaving a visible gap or overlap at the seam.	Compute the last arc's sweep as 360f * animationProgress.value - accumulatedSweep to guarantee the arcs cover exactly 360°.
FinancialHealthScoreV2Widget.kt:127	🟡 Medium	Deprecated API	Divider(color = ...) is deprecated in Material3 ≥ 1.2.0. It still compiles but will be removed in a future version.	Replace with HorizontalDivider(color = SemanticColors.GlassBorder).
FinancialHealthScoreV2Widget.kt:241-242	🟡 Medium	Edge case (clamping)	LinearProgressIndicator(progress = { contribution.score / 100f }) — score is Int and not clamped. If a calculation bug produces score > 100 or score < 0, the progress indicator will either overflow or show negative, causing visual glitches.	Clamp: (contribution.score / 100f).coerceIn(0f, 1f).
SpendingPaceGauge.kt:42	🟡 Medium	Edge case (negative pace)	pace.pacePercentage is Float and could theoretically be negative (e.g., refunds). coerceIn(0f, 1f) handles this, but the center label "${pace.pacePercentage.toInt()}%" (line 103) would display a negative percentage like "-15%", which is confusing for users.	Show max(0, pace.pacePercentage.toInt()) in the label, or display a specific "N/A" state for negative values.
MoneyRadarWidget.kt:54-56	🟢 Low	Recomposition	animateFloatAsState targets data.urgencyScore / 100f. Since urgencyScore is Int, this is integer division if both operands were Int — but 100f makes it Float division, so it's correct. However, no clamping: if urgencyScore > 100, the CircularProgressIndicator on line 151 will receive progress > 1f, causing overdraw.	Clamp: (data.urgencyScore / 100f).coerceIn(0f, 1f).
MoneyRadarWidget.kt:88-89	🟢 Low	Redundant padding	The outer BentoCard already applies contentPadding = PaddingValues(16.dp) by default, and the inner Column adds another .padding(16.dp), resulting in 32dp total padding — likely unintentional double-padding.	Remove the inner .padding(16.dp) from the Column at line 89, since BentoCard already provides padding.
MoneyRadarWidget.kt:407-409	🟢 Low	Dead code	formatAmount() and formatDate() private helper functions are defined but never called anywhere in the widget.	Remove the unused functions or use them where amounts/dates need formatting.
BentoCard.kt:15	🟢 Low	Unused import	import java.util.Currency is used only in the AmountText default parameter. While technically used, Currency.getInstance("EUR") is evaluated at every call site, creating a new Currency object each time.	Extract to a top-level private val EUR_SYMBOL = Currency.getInstance("EUR").symbol constant.
CategoryDonutChart.kt:86-91	🟢 Low	Unstable remember key	categoryColorByKey uses a composite key (item.category.id to item.category.name) which is over-specified (id alone is unique). More importantly, the remember key includes categoryColors (a List) — list identity changes on every recomposition even if contents are the same, defeating the remember.	Use categories alone as the key (it already captures the information), and compute categoryColorByKey inside the same remember(categories) block as categoryColors.
PersonalityProfileCard.kt:261-265	🟢 Low	Non-deterministic default	formatTimestamp(0L) returns "just now" which is misleading if lastUpdated = 0 means "never computed". Also DateFormat.getDateTimeInstance() is locale-dependent and creates a new formatter on every call.	Use a remember-cached formatter, and return "Not yet computed" for timestamp <= 0.
Summary
- 2 Critical bugs in ForecastTimeline.kt — empty series lists crash Vico, and lineSpecs/data series count mismatch
- 1 High bug in SpendingTrendChart.kt — empty data series crash
- 6 Medium issues across recomposition waste, locale-dependent formatting, visual clipping, deprecated API, and unclamped values
- 5 Low issues including dead code, double padding, unused imports, and unstable remember keys
Highest Priority Fixes
1. ForecastTimeline.kt — Add empty-list guards before entryModelOf() and synchronize lineSpecs count with actual series count
2. SpendingTrendChart.kt — Filter out series with empty data before building the chart model
3. All files using String.format — Pass Locale.US explicitly to avoid comma-separated decimal formatting in European locales


UI Components Debug Analysis
File:Line	Severity	Type	Description	Suggested Fix
TransactionsScreen.kt:119-126	🟡 Medium	State Bug	Multiple remember { mutableStateOf<Expense?>(null) } dialog states hold references to Expense objects. If the underlying data updates (e.g., via refresh()), these stale Expense references may cause the ViewModel to operate on outdated entities (wrong id, old categoryId, etc.), leading to silent data corruption when confirming dialogs.	Use expense.id as the remembered key, and re-fetch the latest Expense from the ViewModel before performing mutations. Or, store only the expense.id in the dialog state and resolve it at confirm time.
TransactionsScreen.kt:521-528	🔴 High	Null Safety / Race Condition	expenseToDelete!! is read twice — once in the DeleteConfirmationDialog composable and once inside onConfirm. Between the two reads, a recomposition could theoretically set expenseToDelete = null if another state change triggers it (e.g., a background refresh()), causing a NullPointerException on the !! in onConfirm. The same pattern repeats at lines 537, 551, 602, 617, 645, 655.	Capture the value in a local val: val expense = expenseToDelete ?: return at the top of the if block, and use that local throughout. Or use ?.let { expense -> ... } pattern consistently.
TransactionsScreen.kt:865-871	🟡 Medium	Recomposition / Edge Case	transaction.categoryColor.toInt() — if categoryColor is a String (e.g., hex color like "#FF5733"), calling .toInt() will throw NumberFormatException. The catch block handles it, but this means every recomposition of every transaction item that has a hex-format color silently throws and catches an exception, impacting scroll performance.	Use android.graphics.Color.parseColor(transaction.categoryColor) instead of .toInt(), or ensure categoryColor is always stored as an integer-compatible string.
TransactionsScreen.kt:460	🟡 Medium	Recomposition	groupedTransactions.forEach inside LazyColumn rebuilds the entire item list on every recomposition. Since stickyHeader requires inline LazyListScope calls, there's no key on the forEach loop itself. If dates are added/removed, Compose may not efficiently diff the stickyHeader entries.	This is somewhat mitigated by key = { item.expense.id } on the items, but consider using a flat list with a sealed type (Header/Item) to give Compose full keying control.
TransactionsScreen.kt:159-175	🟡 Medium	Race Condition	shouldLoadMore is a derivedStateOf that reads isLoadingMore, but the LaunchedEffect(shouldLoadMore) fires based on the derived boolean changing. If loadMore() completes very fast and isLoadingMore returns to false before the next composition, shouldLoadMore can bounce true → false → true, triggering duplicate loadMore() calls. The guard in loadMore() helps, but the loadMoreJob?.cancel() on line 332 could cancel a nearly-complete request, losing data.	Add a hasMoreData flag (set to false when a page returns fewer than PAGE_SIZE items) and include it in the derivedStateOf guard.
TransactionsScreen.kt:767	🟢 Low	Compose Performance	DateHeader has Modifier.background(MaterialTheme.colorScheme.surface) on the outer Box to make sticky headers opaque, but the inner Surface also sets color = Color.Transparent with a glass background. This double-background layering means overdraw on every date header.	Consolidate to a single Surface with the desired color.
TransactionsScreen.kt:1118	🟡 Medium	Unnecessary Recomposition	remember(expense.isNotMine, expense.isSharedExpense, expense.transactionType, ownershipNotMine, ownershipShared, ownershipTransfer, ownershipSettings) — the string keys (ownershipNotMine, etc.) are loaded from stringResource and are stable across recompositions in the same locale. Including them in remember keys is harmless but clutters the key set. More importantly, this remember is inside a DropdownMenu which is only composed when showActionMenu = true, so the remember is destroyed and recreated every time the menu opens/closes.	Move the ownership icon resolution outside the DropdownMenu, or use derivedStateOf at a higher scope.
TransactionFilterSheet.kt:256-257	🟡 Medium	Time Zone Bug	calendar.timeInMillis = System.currentTimeMillis() is used instead of the ViewModel's timeProvider.now(). This creates an inconsistency: the filter sheet uses real system time while the ViewModel uses the injected TimeProvider. In tests with fake time providers, filters will compute wrong date ranges. Also, calendar.set(SECOND, 59) but MILLISECOND is never set, meaning the end of day/year is off by up to 999ms.	Pass TimeProvider to the sheet or use a callback, and also set calendar.set(Calendar.MILLISECOND, 999) for the end range. **[RESOLVED BY A.5]**
TransactionFilterSheet.kt:38-42	🟡 Medium	State Initialization Bug	selectedYear and selectedMonth are always initialized to null, ignoring any existing currentFilter?.dateRange. If a user opens the filter sheet while a date-range filter is already active, the year/month chips won't reflect the current filter state — it will appear as "All Years" even though a specific date range is active.	Parse currentFilter?.dateRange back into year/month values during initialization. **[RESOLVED BY A.5]**
TransactionFilter.kt:13	🟡 Medium	Unnecessary Recomposition / Equality Bug	correlationId: Long = System.currentTimeMillis() means every new TransactionFilter() instance has a unique correlationId, so data class equality always returns false for independently constructed instances with the same logical filter. This can cause unnecessary re-filtering in flatMapLatest (line 134 in ViewModel) even when the actual filter criteria haven't changed.	Remove correlationId from the data class or exclude it from equals/hashCode by moving it outside the primary constructor.
AddExpenseSheet.kt:100-104	🟡 Medium	State Bug	LaunchedEffect(Unit) runs only once when the composable enters. But AddExpenseSheet uses hiltViewModel() which creates a ViewModel scoped to the navigation graph. If the sheet is dismissed and re-opened, the ViewModel may already have stale state from the previous session. The setInitialValues call happens in LaunchedEffect(Unit) which won't re-run if the composable is recomposed from the same nav entry.	Key the LaunchedEffect on initialAmount, initialMerchant instead of Unit, or call viewModel.reset() before setInitialValues.
AddExpenseSheet.kt:89-97	🟡 Medium	State Bug / Timing	LaunchedEffect(state.saveResult) triggers onDismiss() when SaveResult.Success is observed. However, viewModel.reset() is called before onDismiss(). If reset() changes state.saveResult back to null, it triggers recomposition, which may re-enter this LaunchedEffect with a null value before onDismiss() completes. In practice, the when block's else arm handles this, but the ordering is fragile.	Call onDismiss() first and then reset(), or use a Channel-based one-shot event instead of observing state for navigation side effects.
AddExpenseSheet.kt:657	🟢 Low	Edge Case / Crash	if (suggestion != suggestions.last()) uses referential equality. If MerchantSuggestion doesn't properly implement equals() or if the same suggestion object appears multiple times in the list, this could draw the divider incorrectly (cosmetic) or always/never draw it.	Use index-based comparison: suggestions.forEachIndexed { index, suggestion -> ... if (index < suggestions.lastIndex) HorizontalDivider(...) }
AddExpenseViewModel.kt:121	🟢 Low	Locale Bug	String.format("%.2f", suggestion.avgAmount) uses the system default locale, which may use , as decimal separator (e.g., 12,50 in German locale). This value is then set as state.amount and later parsed by AmountUtils.parseAmount(). If AmountUtils doesn't handle commas, the pre-filled amount from suggestion selection may fail validation on save.	Use Locale.US explicitly: String.format(Locale.US, "%.2f", ...), or ensure AmountUtils.parseAmount handles both separators.
AddExpenseViewModel.kt:274-279	🟢 Low	UX / Validation	When isSharedExpense = true, both share percentage and share amount being set is rejected (hasSharePercentage == hasShareAmount). But this condition also rejects the case where neither is set (false == false → true), forcing the user to always provide one. This might be intentional but prevents saving a shared expense where the split isn't yet known.	Consider allowing empty share fields and defaulting to 50/50, or make the error message clearer.
LoadingSkeleton.kt:50-51	🟢 Low	Theme Inconsistency	SkeletonBox uses hardcoded SemanticColors.SurfaceLight and SemanticColors.TextSecondary for colors, while sibling components (EnhancedEmptyState, ErrorState) use MaterialTheme.colorScheme.*. This means skeleton colors won't respond to theme changes (dark/light) if SemanticColors are static.	Use MaterialTheme.colorScheme.surfaceVariant and MaterialTheme.colorScheme.onSurfaceVariant or ensure SemanticColors are @Composable getters that respect the theme.
LoadingSkeleton.kt:238	🟢 Low	Visual Bug	val heightFraction = (index + 1) / bars.toFloat() — with bars = 7, the fractions are 1/7, 2/7, ..., 7/7, always ascending left-to-right. This looks like a progress bar rather than a realistic chart skeleton.	Use a pseudo-random but deterministic height pattern, e.g., listOf(0.4f, 0.7f, 0.3f, 0.9f, 0.5f, 0.8f, 0.6f).
EmptyState.kt:122-124	🟢 Low	Theme Inconsistency	SemanticColors.TextSecondary used for icon tint, while EnhancedEmptyState.kt:118 uses MaterialTheme.colorScheme.onSurfaceVariant for the same purpose. Inconsistent theming between the two empty state variants.	Standardize on one approach (preferably MaterialTheme.colorScheme).
ContextualActionRegistry.kt:14-15	🟡 Medium	Thread Safety	actions and completedActionsMap are plain mutableMapOf() with no synchronization. The registry is a @Singleton (provided in EmptyStatePresentationModule). If registerActions(), getActions(), or markCompleted() are called from different coroutines/threads concurrently, this can cause ConcurrentModificationException or lost updates.	Use ConcurrentHashMap or wrap mutations in a Mutex/synchronized block.
TransactionsViewModel.kt:317-318	🟢 Low	Integer Overflow (Theoretical)	_refreshTrigger.value += 1 — after ~2 billion refreshes, this will overflow. Extremely unlikely in practice, but not ideal.	Use _refreshTrigger.value = _refreshTrigger.value + 1 or update { it + 1 }, or use Long, or just use System.currentTimeMillis().
TransactionsViewModel.kt:93	🟢 Low	Race Condition	loadInitialAllRequestId is a plain Long (not @Volatile or AtomicLong). It's only read/written from loadInitialAll() which is called from UI thread methods, so this is safe in practice, but if any call site changes to a background dispatcher, it could race.	Mark as @Volatile or use AtomicLong for future-proofing.
AddExpenseSheet.kt:282-286	🟢 Low	Hardcoded Strings	contentDescription = if (state.showTransactionType) "Transaction type expanded" else "Transaction type collapsed" and similar on line 328 — these are hardcoded English strings instead of using stringResource().	Extract to strings.xml and use stringResource().
Summary
Severity	Count
🔴 High	1
🟡 Medium	10
🟢 Low	10
Top 3 Critical Fixes
1. !! force-unwrap race condition (TransactionsScreen.kt, multiple dialog sites) — Can cause NullPointerException crashes in production. Fix by capturing into a local val before the if block.
2. TransactionFilter.correlationId breaks equality — Every new filter instance is "different" even with the same criteria, causing unnecessary database re-queries and UI re-renders on every filter application.
3. ContextualActionRegistry thread safety — A @Singleton registry with unsynchronized HashMap can crash under concurrent access from coroutines. Use ConcurrentHashMap or synchronized.

