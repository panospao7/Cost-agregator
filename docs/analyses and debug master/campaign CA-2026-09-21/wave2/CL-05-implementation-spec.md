# CL-05 Implementation Spec — Currency labeling and conversion contract
Provenance: 2026-09-25; pin 37601232 (anchor) + current HEAD cdf501d4 verified; astra high Stage-3 session; source cluster-map.md.

## GATE

**UPDATE 2026-09-25 (post-spec): GATE LIFTED.** All member findings verified by the independent adversarial pass `../verification-2026-09-25-wave2-s2.md` (10/10 CONFIRMED, 0 refuted; E-01-003 and E-04-004 recalibrated — read the artifact's per-finding notes before implementing, they change framing/severity). Implementation may proceed.
- Implementation of this spec is blocked until ALL member findings are revalidated by the independent adversarial verifier. All Wave-2 findings are currently UNVERIFIED. Do not treat the audit evidence below as verification approval.
- Members: CA-P-01-006, CA-P-04-006, CA-P-06-001, CA-E-01-002, CA-E-02-002, CA-E-02-005 (P2); CA-E-04-004 (P3, dormant API). Nine work items, including a shared-contract prerequisite and binding A6. No member is promoted to VERIFIED here.
- **Placement is PRE-APPROVED:** CurrencyConverter/MultiCurrencyRepository own the shared money contract. The approval requires **(A) core-contract PR**, then **(B) consumer-sweep PR**. Do not combine them. A must receive strict review, required guardian approval and human-recorded validation before B starts; B must rebase/read A's actual API rather than assume these proposed additions already exist.
- CL-27 must land first; retain its publication-date, ASCII-key and source/count-conservation fixes. Repo gate was rerun successfully: source status for app/config/scripts empty, anchor is an ancestor, HEAD cdf501d4. No builds, tests or source changes were performed in this spec-writing session.

## Context for the coder
- Users choose a display currency, but saved expenses, recurring rules and budgets retain their own source currencies. Reminders, recommendations and analytics must either convert those amounts or display their actual source labels. Tax category totals target the configured filing currency, which can differ from home currency; unknown currency must remain unavailable, not become a plausible default amount.
- Root cause from cluster-map.md: consumers infer home/default currency for ambiguous or foreign amounts, and refresh/selection catalogs diverge from the converter whitelist. Establish one catalog and an explicit source-to-display contract, then migrate only the listed consumers.
- Read inputs: cluster-map.md header/revision/wave map, CL-05 74–88, placement decision and A5/A6/A8; member ledger rows 34/61/75/136/146/149/161; the corresponding sections of cell-P-01, cell-P-04, cell-P-06, cell-E-01, cell-E-02 and cell-E-04 audit reports; relevant LEGAL_PATHS sections; TRIAGE-2026-09-test-recovery.md. A8 belongs to the preceding CL-27 spec.
- Path prefixes: `M/` = `app/src/main/java/com/yourname/expensetracker/`; `T/` = `app/src/test/java/com/yourname/expensetracker/`. Source anchors were reopened. Unless marked otherwise they are **@pin 37601232**, checked against unchanged files at HEAD. **CURRENT HEAD cdf501d4** anchors apply to CurrencyConverter.kt, InsightsEngine.kt, SharedExpenseBudgetOffsetEngine.kt and SharedExpenseBudgetOffsetEngineTest.kt because merged Wave-1/CL-17 changed them. Preserve that drift. Actual converter path is `M/domain/currency/CurrencyConverter.kt`, not the cluster-map shorthand under domain/core/money.
- Important A6 drift finding: at CURRENT HEAD all three returned BudgetSpendBreakdown warning producers already append the constant MISSING_RATE, and two explicit regression tests exist. A6 remains an explicit revalidation/preservation work item, not an instruction to reintroduce payload strings or manufacture an unnecessary production patch.

## Pre-implementation checks (coder MUST run, in order)
1. Run the exact allowed-file drift check below and read every hunk; function names/signatures are authoritative, line numbers advisory. The allowlist is the union of A/B; edit only the current PR's subset. Newly proposed tests have no pin-side content.
   ```powershell
   $m='app/src/main/java/com/yourname/expensetracker'
   $t='app/src/test/java/com/yourname/expensetracker'
   $allowed=@(
     "$m/domain/currency/CurrencyConverter.kt", "$m/data/repository/CurrencyRatesRepositoryImpl.kt",
     "$m/data/repository/MultiCurrencyRepository.kt", "$m/domain/core/money/MoneyAggregateResult.kt",
     "$m/ui/screens/currency/CurrencyManagementViewModel.kt",
     "$m/domain/notification/money/NotificationMoneySignalDetector.kt", "$m/data/repository/NotificationProcessingPipeline.kt",
     "$m/domain/reminder/BillReminderManager.kt", "$m/ui/screens/reminder/BillRemindersViewModel.kt", "$m/ui/screens/reminder/BillRemindersScreen.kt",
     "$m/domain/budget/BudgetAutopilotEngine.kt", "$m/ui/screens/budget/BudgetViewModel.kt", "$m/ui/screens/budget/BudgetScreen.kt",
     "$m/domain/analytics/AdvancedAnalyticsEngine.kt", "$m/domain/analytics/InsightsEngine.kt",
     "$m/domain/tax/TaxEstimator.kt", "$m/data/repository/BusinessExpenseRepository.kt", "$m/domain/groups/SharedExpenseBudgetOffsetEngine.kt",
     "$t/data/repository/CurrencyRatesRepositoryImplTest.kt", "$t/ui/screens/currency/CurrencyManagementViewModelTest.kt",
     "$t/domain/currency/ConversionSemanticsHardeningTest.kt", "$t/data/repository/MultiCurrencyRepositoryTest.kt",
     "$t/domain/notification/money/NotificationMoneySignalDetectorTest.kt",
     "$t/data/repository/NotificationProcessingPipelineOversizedAmountTest.kt", "$t/data/repository/NotificationProcessingPipelineReliabilityTest.kt", "$t/data/repository/ReviewQueueRepositoryTest.kt",
     "$t/domain/reminder/BillReminderManagerTest.kt", "$t/ui/screens/reminder/BillRemindersViewModelTest.kt",
     "$t/domain/budget/BudgetAutopilotEngineTest.kt", "$t/ui/screens/budget/BudgetViewModelAutopilotQualityTest.kt", "$t/ui/screens/budget/BudgetAutopilotUiContractTest.kt",
     "$t/domain/analytics/AdvancedAnalyticsEngineNormalizedTest.kt", "$t/domain/analytics/InsightsEngineValidationTest.kt",
     "$t/domain/tax/TaxEstimatorTest.kt", "$t/data/repository/BusinessExpenseRepositoryTest.kt", "$t/scenarios/ExpenseDaoAggregateFilterTest.kt",
     "$t/domain/groups/SharedExpenseBudgetOffsetEngineTest.kt"
   )
   git diff 37601232..HEAD -- $allowed
   ```
2. Re-read each target function at current HEAD before editing. Read CL-27's merged implementation and A8 disposition, the independent verifier's seven member decisions, and A6 current-source evidence. Record the lane HEAD in each PR.
3. For PR B, inspect A's reviewed diff and validation result, then use its actual signatures. Proposed helper names below are explicitly **new design**, not claims of existing APIs. Stop on an unsafe/stale proposal, schema surprise, failed review/test, unresolved privacy question or needed out-of-fence change; ask the campaign owner rather than weaken a guard or expand scope silently.

## Legal path constraints
- LEGAL_PATHS.md **Money / Currency** (522–539): MoneyAggregate/MoneyAggregateBuilder and MultiCurrencyRepository are the safe aggregate path; AnalyticsCurrencyNormalizer owns historical row normalization. Never sum foreign amounts then attach a display currency; retain warnings/partiality through adapters.
- **Notification Capture** (838–857) and **Expense Mutations** (11–37): preserve NotificationIntakeCoordinator capture, the pipeline's existing write-barrier/transaction/source-link boundary, and approval through TransactionLifecycleCoordinator to ExpenseDao.insertAtomic. Detector/UI/review adapters must NOT write directly to ExpenseDao or NotificationIntakeDao. This lane only changes fallback currency transport and currency-dependent duplicate guards, not capture identity or mutation ownership.
- **Recurring Rule Mutations** (281–315): reads may use the existing RecurringExpenseRepository; any rule writes remain with RecurringRuleLifecycleCoordinator. Bill/insight fixes must NOT write to ManualRecurringExpenseDao or migrate reminder scheduling/payment paths.
- **Analytics** (1033–1079): preserve normalized-input entry points and AnalyticsInputAssembler ownership; do not self-fetch expenses in an empty factory or normalize already-normalized snapshots a second time.
- **Business Reports / Tax** (1001–1028): keep purchase-only, effective-share and filing-currency semantics. Use the existing per-category/per-currency DAO projection through a repository; TaxEstimator/UI must NOT add direct DAO writes or bypass lifecycle ownership. No tax rate, bracket, jurisdiction, export or fiscal-calendar redesign.
- Budget apply remains BudgetViewModel → BudgetRepository.updateBudget/updateBudgetOrThrow → write barrier → BudgetDao.updateAndEnforceActiveScope (repository anchors 689–710/740–748). No direct BudgetDao writes from ViewModel/engine; preserve Apply All transaction rollback.
- Rate storage remains refresh → CurrencyConverter.storeRates → ExchangeRateStoreAdapter/write barrier → ExchangeRateDao. No schema change, mass currency relabeling, or rewriting stored amounts on home-currency change.

## Work items
**Group A — core-contract PR. Only WI-1/WI-2 and their tests. Currency settings UI changes here are catalog/contract plumbing, not the consumer sweep. Stop after this PR's review/validation gate.**

### WI-1 — Unify supported currencies and typed validation (CA-E-01-002, P2, UNVERIFIED)
- Location: `M/domain/currency/CurrencyConverter.kt` — `enum class SupportedCurrency(...)` and `fromCode(code: String)` 20–43, `suspend fun convertOutcome(amount: Double, fromCurrency: String, toCurrency: String, rateBasis: RateBasis, atMillis: Long? = null, stalePolicy: StaleRatePolicy = StaleRatePolicy.Default): ConversionOutcome` 306–357; nullable `convert(...)` 131–190 and `convertAsOf(...)` 232–244 — **CURRENT HEAD cdf501d4**. `M/data/repository/CurrencyRatesRepositoryImpl.kt` — PRIORITY_CURRENCIES 37–40 and `refresh(homeCurrency: String): Int` 82–104. `M/ui/screens/currency/CurrencyManagementViewModel.kt` — priority list 63–67, loadCurrencyData 79–107, setHomeCurrency 166–180 and convert 186–208.
- Defect: the refresh/selector list offers currencies rejected by convertOutcome while dropping other converter-supported provider rates. Identity conversion happens before currency validation, allowing an unknown same-code amount to escape the typed failure boundary.
- Change:
  1. Use the existing SupportedCurrency owner as the single catalog. Preserve the union of currently accepted codes and currently offered priority codes; do not silently remove CNY/NZD/INR/etc. or PLN/DKK/CZK/HUF/RON/ISK to make tests pass. Existing HRK inactive status remains; preserve historical/manual recognition separately from active selection. Catalog support is not a promise that this provider has a current quote. Do not expand to arbitrary provider/ISO codes beyond that union without a product decision.
  2. Replace duplicated membership lists in refresh/selector with this catalog. Ordering may remain a centralized presentation preference, never a different whitelist. Refresh generates cross-pairs for catalog codes actually present in the dated provider publication, including EUR identity base, without priority-list exclusions. Missing provider coverage produces explicit missing-rate/unavailable behavior, never synthetic rates. Retain CL-27's publication-date parameter, XML security and privacy gate.
  3. Normalize code matching locale-independently. Validate source/target against the catalog **before identity conversion or constructing CurrencyCode**. Unknown/malformed/blank codes return ConversionOutcome.Failed with INVALID_SOURCE_CURRENCY/INVALID_TARGET_CURRENCY, not EUR/XXX/default/identity success. Keep valid identity semantics and historical missing-date rules after validation. Do not globally tighten CurrencyCode's syntactic constructor or remove the XXX failed-source representation used by CL-27.
  4. Nullable legacy convert/convertAsOf share catalog acceptance and return null for rejected codes; do not change their existing staleness/look-up policies while doing so. In particular, legacy latest uses 24h against lastUpdated, whereas typed latest callers use an explicit named policy; do not replace one with the other incidentally.
  5. Selector membership and setHomeCurrency validation use the active catalog; reject an unknown choice without settings mutation/cache invalidation. Conversion UI calls the typed converter and maps a success to its existing ConversionResult state; on failure clear any stale result and expose a controlled unavailable error, not raw exception text. Keep home-currency changes from rewriting existing expense/budget/rule source units.
- Acceptance criteria: a publication containing PLN supplies usable PLN/EUR conversions after refresh; selectable CNY works through typed and legacy entry points with the same stored pair; unknown source/target and unknown same-code identity never yield success; a missing quote is distinguished from an unsupported code.

### WI-2 — Provide one source-to-display boundary for the sweep (CA-E-01-002 / CA-P-04-006 / CA-P-06-001, P2, UNVERIFIED)
- Location: `M/data/repository/MultiCurrencyRepository.kt` — constructor 70–78, cache invalidation 89–111, `private suspend fun resolveHomeCurrencyForMoneyMath()` 415–429 and `requireHomeCurrencyForMoneyMath(): CurrencyCode` 441–449; existing MoneyAggregateResult producer `getHomeCurrencyPurchaseTotalHistoricalResult(startDate: Long, endDate: Long)` 610–631 is the result-shape reference. `M/domain/core/money/MoneyAggregateResult.kt` 9–19; `M/domain/core/money/ConversionOutcome.kt` 9–31 (read-only type reference). CurrencyConverter's explicit outcome API is at WI-1's CURRENT HEAD anchor.
- Defect: new display consumers otherwise recreate local fallback/conversion rules or lose source/display identity when returning scalars. This prerequisite supplies the approved shared contract; it is not an additional finding.
- Change:
  1. Add a narrow explicit-target display aggregation entry point **in MultiCurrencyRepository**, delegating to the existing typed MoneyAggregateBuilder path, not DAO mutations. Proposed signature (not present at inspected HEAD): `suspend fun aggregateDisplayAmounts(amounts: List<Pair<Double, String>>, transactionCounts: List<Int>, targetCurrency: String): MoneyAggregateResult`. Its scope is the sweep's current-rate display amounts, with LATEST_AVAILABLE and BucketDatePolicy.Latest; it is not a replacement for historical APIs.
  2. Validate raw source/target codes through WI-1 before creating typed buckets; reject unknown currency with a controlled typed Unavailable result, never parseOr(EUR). Require one supplied nonnegative count per amount; nonfinite amounts or inconsistent counts are typed unavailable, not fabricated zero/counts. A legitimate empty input with a known target is available zero. Each known-currency input becomes one MoneyBucketInput with its supplied count; preserve CL-27's complete source/failure provenance.
  3. Mixed successful/failed conversions retain MoneyAggregate partiality and failure counts. For these **new consumer adapters**, nonempty input with no successful conversion returns MoneyAggregateResult.Unavailable; do not infer this from displayAmount==0 (valid zero/canceling amounts exist). Use actual conversion outcomes/failure-to-input correspondence, not transactionCount==0. Do not change the general builder's conversionQuality classifier: CA-E-01-004 remains CL-15's separately verified work.
  4. Validate resolved/cached home currency at the repository money boundary against the same catalog. Preserve the explicit FirstRunDefault distinction; failed settings resolution is HOME_CURRENCY_UNAVAILABLE, never a new default. Keep reactive cache invalidation and cancellation. Do not pass arbitrary settings failure messages to warning text. Legacy return types that cannot represent unavailable may use a **new, controlled MoneyDisplayUnavailableException(reasonCode)** beside MoneyAggregateResult; this is a proposed typed compatibility bridge, not an existing class or permission to catch failures as zero.
  5. Budget round-tripping must not recalculate FX in a ViewModel. Add a narrowly scoped **proposed** CurrencyConverter helper `fun reverseDisplayQuote(amountInTarget: Double, forward: ConversionOutcome.Converted): ConversionOutcome`. It converts the final home-denominated recommendation back using the same immutable positive finite forward.rateUsed (inverse direction), with source/target swapped and quote provenance retained; no new provider lookup, formatting or rounding. Reject invalid/nonfinite quote/input/output with a controlled Failed result. This avoids drift between the cap comparison and the amount eventually saved; it is not a general FX-cache redesign.
- Acceptance criteria: source/home/filing target are explicit; unknowns are typed unavailable; partials carry source/count metadata; all-failed is not successful zero in the new adapters. A USD100→EUR90 quote reversed after home-side recommendation math yields USD100, even if rates refresh before Apply. Existing historical basis/TTL/manual-rate behavior remains intact.

**Group B — consumer-sweep PR. WI-3 through WI-9, only after A's accepted API is available. No global money refactor and no unrelated lifecycle/worker/diagnostics edits.**

### WI-3 — Preserve ambiguous notification currency through review (CA-P-01-006, P2, UNVERIFIED)
- Location: `M/domain/notification/money/NotificationMoneySignalDetector.kt` — CurrencyDef 149–154, currency definitions 18–35, `suspend fun bestTransactionAmount(text: String, homeCurrency: String? = null): MoneySignal?` 37–130. `M/data/repository/NotificationProcessingPipeline.kt` — `private suspend fun resolveCurrency(fullText: String, homeCurrency: String?): String` 877–880; processInternal parser-null consumers 293–302/329–388/424–490; OversizedAmountCandidate/TransactionSignalCandidate 826–835; `detectOversizedAmountCandidate(title: String?, text: String?, bigText: String?, defaultCurrency: String = "EUR")` 913–944 and `detectTransactionSignalCandidate(title: String?, text: String?, bigText: String?, defaultCurrency: String = "EUR")` 947–1003. Existing MoneySignal/CurrencyResolution: `M/domain/currency/CurrencyResolution.kt` 7–35. Approval null guard: `M/data/repository/ReviewQueueRepository.kt` 146–153, read-only.
- Defect: CurrencyDef.isoCodes includes symbols, so kr is falsely explicit SEK before the home-candidate branch. Even a corrected unresolved signal would currently be collapsed by the pipeline's Elvis chain to home/EUR.
- Change: make the explicit-ISO pass use true code tokens only; route shared kr to the existing SEK/NOK/DKK candidate branch. Normalize the provided home code through the shared catalog and resolve only if it belongs to that candidate set. Retain AMBIGUOUS_SYMBOL_RESOLVED_BY_HOME versus AMBIGUOUS_UNRESOLVED and existing confidence semantics. Separate qualified/unambiguous symbol aliases from ambiguous bare symbols, so US$/C$/A$/€ etc. do not regress when removed from the ISO list; do not redesign numeric parsing or add candidate currencies beyond this finding.
- Change: carry the typed resolution and nullable currency through the pipeline's parser-null fallback, both normal and oversized candidates. Remove the helper's fabricated EUR default and the home/EUR collapse when a signal is unresolved/absent. A small private result wrapper in this file may pair nullable code with the existing CurrencyResolution enum; it is new design, not an existing API. An unresolved monetary candidate still creates a review with suggestedCurrency=null, not a dropped notification or guessed amount/currency. Skip the expense/pending currency-equality duplicate queries when currency is unknown, while preserving raw-notification identity dedupe, barriers, transactions, source links and sanitization. Known-currency duplicate behavior stays unchanged. Approval already requires a user currency override when the suggestion is null; no ReviewQueueRepository production edit is needed.
- Acceptance criteria: “42 kr” + NOK home suggests NOK with home-resolution provenance; DKK/SEK work likewise. EUR/unknown/no home leaves kr unresolved and review cannot create an expense without explicit currency. True “42 SEK” wins over NOK home. Neither branch falls through to EUR nor logs/persists new raw payloads.

### WI-4 — Make reminder rows and monthly totals currency-honest (CA-P-04-006, P2, UNVERIFIED)
- Location: `M/domain/reminder/BillReminderManager.kt` — constructor 50–53, `getUpcomingReminders(daysAhead: Int = 14): List<BillReminder>` 70–104 and `suspend fun getMonthlyBillsTotal(): Double` 162–173. `M/ui/screens/reminder/BillRemindersViewModel.kt` — monthlyTotal state 27–28, calculateMonthlyTotal 46–54, refresh 57–60. `M/ui/screens/reminder/BillRemindersScreen.kt` — screen bindings 41–47, MonthlyBillsCard 91–113, BillReminderCard amount binding 190–193.
- Defect: original rule amounts are labeled home currency, and monthly equivalents in different currencies are summed before conversion.
- Change: keep each row's amount paired with **reminder.currency** (source-label option, no unnecessary per-row FX). An invalid/unknown source is an unavailable amount, not a home-labeled number. For the monthly summary preserve RecurrenceCalculator.toMonthlyAmount frequency semantics, active-rule filtering and one count per contributing rule; send those source amount/currency pairs to A's shared display aggregate with an explicitly resolved home target. Change the manager's scalar return/VM state to MoneyAggregateResult and migrate its only production caller, the VM. Add the necessary injected shared repository/settings dependencies to the manager, not a screen-side conversion algorithm.
- Change: UI formats Available.displayAmount with Available.displayCurrency and retains partial warning/counts. Unknown home, total FX failure or fetch failure is Unavailable (display no numeric total, e.g. an em dash plus controlled warning), never catch-to-0.0. Recompute on refresh/home-currency change without relabeling a previously computed total under the new setting. Keep cancellation, due dates, urgency, mark-paid prohibition and reminder scheduling unchanged; no worker edits.
- Acceptance criteria: USD100 row remains USD100; with EUR100 and USD/EUR0.9 the monthly summary is EUR190, not EUR200. A partial conversion is visibly partial; all failed is unavailable; an actually empty list is known-currency zero.

### WI-5 — Separate recommendation display units from persisted budget units (CA-P-06-001, P2, UNVERIFIED)
- Location: `M/domain/budget/BudgetAutopilotEngine.kt` — `suspend fun generateRecommendations(): BudgetAutopilotRecommendations` 79–275, model declarations 473–520; home-history producer `M/data/repository/MultiCurrencyRepository.kt` getHistoricalCategoryMonthlySpend 783–847 (read-only consumer dependency after A). `M/ui/screens/budget/BudgetViewModel.kt` — generateAutopilotRecommendations 285–307, applyAutopilotRecommendation 310–361, applyAllAutopilotRecommendations 363–409, dismissAllAutopilotRecommendations 414–422. `M/ui/screens/budget/BudgetScreen.kt` — `AutopilotRecommendationItem(recommendation: CategoryBudgetRecommendation, onApply: () -> Unit, homeCurrency: String = "")` 1173–1253.
- Defect: home-currency history is compared/capped against budget.amount in source currency, then the home number overwrites the source amount without changing its currency.
- Change:
  1. Capture an explicit resolved home currency for the generation and ensure every history aggregate uses it; if settings/history currency changes during the load, fail unavailable/regenerate rather than mix units. For each budget obtain a typed budget.currency→home quote before comparisons, including identity. Treat missing/invalid FX as a typed money-unavailable generation result/compatibility exception that the VM maps to its controlled unavailable error; no actionable recommendation or stale prior list survives that failure.
  2. Perform historical recommendation, period normalization, ±15% cap, delta, hierarchy comparisons and summary totals **all in home units**, using the converted current budget instead of raw budget.amount. Preserve existing coefficients, low-history eligibility, partial-history handling, period factors, summary-scope selection and hierarchy algorithm. CL-16 owns the separate post-hierarchy clamp issue; do not fix or weaken that here.
  3. After the existing hierarchy adjustment, reverse-project each final home recommendation through A's captured quote to a MoneyAmount in the original budget currency. Extend the in-memory recommendation with explicit displayCurrency and a separately named sourceAmountToApply; keep currentBudget/recommendedBudget/delta consistently home-denominated for display. Give the summary an explicit display currency too. These fields are proposed additions, not current APIs; no persisted schema changes.
  4. Apply Single/All save only sourceAmountToApply.amount, preserving budget.currency. Re-read the target budget as now; require its current currency to match the captured source currency before any write. A changed currency requires regeneration, never applying the home number or redoing ad hoc FX in the VM. Retain repository result handling and Apply All transaction rollback. Do not implement unrelated amount/date CAS (CL-09). Low-history/non-actionable entries still cannot write.
  5. Bind BudgetScreen's recommendation amounts to their captured displayCurrency, not an unrelated live setting. Preserve that context in VM clear/dismiss constructors (copy an existing context or clear the optional result; no invented EUR context). Update only the two constructor-fixture test files listed below; no UI redesign.
- Acceptance criteria: USD100 budget, EUR90 stable history and USD/EUR0.9 display EUR90 current/recommended and Apply saves USD100, not USD90. Mixed-budget hierarchy/totals compare like units. Rate refresh before Apply does not change source units. Missing quote or changed budget currency produces no write.

### WI-6 — Thread resolved currency into empty analytics factories (CA-E-02-002, P2, UNVERIFIED)
- Location: `M/domain/analytics/AdvancedAnalyticsEngine.kt` — `computeSpendingPatternsCore(purchases: List<ExpenseSnapshot>, period: AnalyticsPeriodRange, displayCurrency: String, allWarnings: List<AnalyticsConversionWarning>)` 540–547 → createEmptyPatternAnalysis 610–625; `computeStatisticalInsightsCore(...)` 677–684 → createEmptyStatisticalInsights 744–754; normalized entry points supply input.homeCurrency at 537/674; unrelated defaultDisplayCurrency 1002–1004 is a read-only contrast.
- Defect: empty factories discard the supplied display currency and consult device locale, potentially throwing for a language-only locale.
- Change: add a required displayCurrency parameter to both private empty factories and pass the already-resolved core parameter at each call. Use it for every nested currency-bearing zero model, including WeekendWeekdayComparison and TransactionPercentiles. Preserve incoming warnings exactly, including all-excluded input. No settings/locale lookup or new expense fetch in either factory; leave unrelated deprecated overloads/fallback machinery alone.
- Acceptance criteria: with home=JPY, empty results carry JPY under US, en-only or root locales without throwing. Zero counts remain zero and input warnings survive; empty does not become unknown/default currency.

### WI-7 — Preserve manual recurring source labels in insight adapters (CA-E-02-005, P2, UNVERIFIED)
- Location: `M/domain/analytics/InsightsEngine.kt` — `private suspend fun findRecurringExpenses(allExpenses: List<ExpenseSnapshot>, displayCurrency: String): List<RecurringExpense>` 752–780; `getLegacyInsights(snapshot: InsightsSnapshot, homeCurrency: String = "EUR")` recurring branch 496–511; recurring async branch/assembly 318–334/379–387 — **CURRENT HEAD cdf501d4**. Read-only producer: `M/domain/logic/RecurringExpenseEngine.kt` — `getPatternsFromSnapshots(allExpenses: List<ExpenseSnapshot>): List<RecurringPattern>` 44–75. Existing model fields: `M/domain/analytics/AnalyticsModels.kt` RecurringExpense 118–127, AnalyticsConversionWarning 155–166, InsightsSnapshot 169–185.
- Defect: normalized expense input does not normalize the separate manual-rule repository read; the adapter replaces pattern.currency with home while leaving pattern.averageAmount unchanged.
- Change: choose the **source-label option** explicitly allowed by the finding: retain each pattern's validated source currency in RecurringExpense.displayCurrency and its unchanged averageAmount. Format recurring descriptions using that per-item currency, not getLegacyInsights' homeCurrency parameter. Detected patterns from normalized input already carry home currency and must not be converted twice. No FX is required to honestly show USD100 as USD100, even when a quote is absent.
- Change: validate source identity using the shared catalog. Unknown source must not construct a falsely labeled money item: exclude that numeric recurring item and carry an existing typed INVALID_TRANSACTION_CURRENCY AnalyticsConversionWarning with a controlled message/count into InsightsSnapshot.conversionWarnings, preserving the input warnings. A narrow private result pairing recurring items with warnings in this same engine is allowed; do not change the public analytics model or invent a financial amount. Retain manual precedence, cadence and confidence sorting, cancellation propagation, and CURRENT HEAD code/class-only branch logging. No RecurringExpenseEngine production change or rule mutation is needed.
- Acceptance criteria: a USD100 manual rule plus EUR normalized analytics renders USD100, never EUR100; already-home detected patterns retain their correct units. Unknown source produces an explicit typed warning, not home/default money. Missing FX alone does not suppress a legitimately source-labeled obligation.

### WI-8 — Restore filing-currency category buckets including uncategorized (CA-E-04-004, P3, UNVERIFIED)
- Location: `M/domain/tax/TaxEstimator.kt` — `suspend fun getTaxYearSummary(year: Int, taxConfig: TaxConfiguration = TaxConfigurationFactory.getConfiguration(taxSettings.getTaxCountry())): TaxYearSummary` 319–395, especially 353–375. `M/data/repository/BusinessExpenseRepository.kt` — `getExpensesByCategory(startDate: Long, endDate: Long): List<BusinessCategoryTotal>` 57–59. Read-only correct projection already exists: `M/data/database/dao/ExpenseDao.kt` — `getBusinessCategoryCurrencyTotals(startDate: Long, endDate: Long): List<BusinessCategoryCurrencyTotal>` 2512–2530; projection fields businessCategory/currency/total/txCount at 2672–2676. Deprecated raw path is 2478–2493.
- Defect: raw category-only sums are wrapped as successful filing-currency aggregates; the typed map drops the null-category remainder. This yearly method is dormant in the audited production call graph; do not wire it into a screen or inflate severity.
- Change: add a narrow BusinessExpenseRepository read-through to the **existing** currency/category query, then group rows by businessCategory and aggregate their source currencies/counts through A's explicit-target shared helper, targeting **taxSettings.getFilingCurrency()**, never home. Keep the existing latest-rate basis used by this tax path; do not silently change historical tax/rounding policy. The SQL already coalesces null to “Uncategorized” and groups by category/currency; merge those source buckets with explicitly named Uncategorized before conversion. Do not invent a remainder by subtracting mixed/partial totals.
- Change: derive the deprecated numeric category map from the converted typed map rather than an independent raw query. Remove this yearly caller's dependency on the suppression-backed raw projection; do not broadly migrate unrelated business reports/project APIs. Include category partiality and controlled failure warnings in summary quality. Unknown filing/source currency or a required wholly-unavailable category cannot become singleCurrency(0): propagate A's typed unavailable compatibility exception where the existing TaxYearSummary return type cannot represent it. No new DAO query, entity, schema, UI, tax-rate or mileage mutation is needed.
- Acceptance criteria: EUR100+USD100 in one category produces EUR190 at USD/EUR0.9 for EUR filing (even with a different home currency). Null and explicit Uncategorized both survive in the typed and derived legacy map with their real counts. Partial FX is visible; no unlike-currency raw sum is called complete.

### WI-9 — Revalidate and preserve caller-visible warning redaction (addendum A6, CA-E-04-001 wider surface; audit P0, CURRENT HEAD revalidation required)
- Location: `M/domain/groups/SharedExpenseBudgetOffsetEngine.kt` — `suspend fun calculateEffectiveBudgetSpend(periodStart: Long, periodEnd: Long, categoryId: Long? = null): BudgetSpendBreakdown` 72–214; personal/shared/reimbursement failure branches 126–129/157–160/173–176; BudgetSpendBreakdown.conversionWarnings 257–269 — **CURRENT HEAD cdf501d4**. Existing tests in `T/domain/groups/SharedExpenseBudgetOffsetEngineTest.kt` 83–114 and 117–168 are also **CURRENT HEAD** anchors.
- Defect context: A6 requires the consumer sweep to address unredacted merchant/amount/currency/date in returned warnings, not just log sinks. The pin had those strings; the current diff replaces all three with MISSING_RATE and removes raw logs. **Do not claim the old producer is still present.** Source inspection is not independent verification or executed-test evidence.
- Change: independently revalidate the returned object for personal, shared-share and reimbursement failures and preserve the bounded code/count contract. Existing methods `failed personal shared and reimbursement conversions expose codes and counts only` and `real historical converter missing rates logs no currency amount merchant or time` already assert the exact three-code list and count=3. Reuse them, extend only missing boundary coverage, and record the human execution result. If coverage/behavior is sufficient, make **no production change**. If verifier shows a current gap, repair only warning construction/return in this file; use controlled constants/counts, not regex masking of a constructed financial sentence. Do not log exceptions or turn payload fields into reason codes.
- Acceptance criteria: all caller-visible warning elements contain only approved controlled text, no merchant/amount/source or target currency/date/raw exception payload; failedConversionCount/isPartial and numeric semantics remain correct. Log safety alone is insufficient evidence. No changes to split, settlement, archived-group, fallback or aggregate-provenance behavior are authorized by A6.

## Tests
All paths below are explicit. CurrencyRatesRepositoryImplTest is the new class specified by prerequisite CL-27; NotificationMoneySignalDetectorTest is **new/proposed here**. Other classes exist and were reopened. Nineteen classes are named; tests were **NOT RUN** in this session.
- **WI-1:** `T/data/repository/CurrencyRatesRepositoryImplTest.kt`, `T/ui/screens/currency/CurrencyManagementViewModelTest.kt`, `T/domain/currency/ConversionSemanticsHardeningTest.kt`. Cases: (1) PLN/DKK/CZK/HUF/RON/ISK supplied by provider are not dropped, (2) CNY/NZD/INR and remaining offered codes are accepted by typed conversion, (3) selector catalog/refresh/catalog acceptance parity, (4) provider omits a supported code → missing quote, no synthetic rate, (5) unknown/blank/malformed source and target including same-code identity, (6) lower-case codes under Turkish locale, (7) inactive historical code remains recognizable but not newly selectable, (8) selection rejection causes no settings write, (9) failed conversion clears stale UI result, (10) latest legacy 24h versus explicit typed policy and historical TTL-exempt behavior remain pinned, (11) CL-27 publication date persists after catalog change.
- **WI-2:** `T/data/repository/MultiCurrencyRepositoryTest.kt` and ConversionSemanticsHardeningTest. Cases: (1) known-target empty input → available zero, (2) EUR100+USD100→EUR190, (3) partial one missing-rate bucket with all sources/counts retained, (4) all fail → new adapter Unavailable, (5) valid zero/net-zero/zero-count success is not mistaken for failure, (6) unknown source/target including XXX is unavailable without EUR fallback, (7) invalid amount/count-size/count-sign cannot fabricate success, (8) resolved/first-run/failed home distinctions and cache invalidation, (9) cancellation propagates, (10) captured USD→EUR quote reverses the final recommendation without another rate read, (11) invalid inverse quote and nonfinite output fail, (12) filing target is honored independently of home.
- **WI-3:** new `T/domain/notification/money/NotificationMoneySignalDetectorTest.kt`; `T/data/repository/NotificationProcessingPipelineOversizedAmountTest.kt`; `T/data/repository/NotificationProcessingPipelineReliabilityTest.kt`; `T/data/repository/ReviewQueueRepositoryTest.kt`. Cases: (1) 42 kr with each SEK/NOK/DKK home and expected provenance/confidence, (2) EUR/no/unknown home → typed unresolved, (3) explicit SEK/NOK/DKK prefix/suffix wins over home, (4) case and whitespace, (5) qualified dollar aliases/unambiguous symbols retain behavior, (6) normal and oversized unresolved candidates create currency-null reviews and never query currency-specific duplicates with a guessed code, (7) known candidates still use currency-aware duplicate checks, (8) null suggestion cannot approve without explicit override, (9) retained raw-id dedupe/source links/privacy sanitization and cancellation. Existing helper tests that relied on the default EUR must pass an explicit known resolution; retain their amount/PAN scoring assertions.
- **WI-4:** `T/domain/reminder/BillReminderManagerTest.kt`; `T/ui/screens/reminder/BillRemindersViewModelTest.kt`. Cases: (1) EUR100+USD100 at 0.9→EUR190, (2) row retains USD100 source label, (3) annual/semiannual/irregular frequency oracles unchanged, (4) inactive rules excluded, (5) partial versus total missing FX, (6) no home/unknown code → unavailable, (7) no rules → true zero, (8) home change recomputes rather than relabels, (9) load failure never becomes numeric zero and cancellation propagates. Check the exact row/card binding during review; do not resurrect/remove markBillPaid behavior to fix unrelated old tests.
- **WI-5:** `T/domain/budget/BudgetAutopilotEngineTest.kt`; `T/ui/screens/budget/BudgetViewModelAutopilotQualityTest.kt`; `T/ui/screens/budget/BudgetAutopilotUiContractTest.kt`. Cases: (1) USD100/EUR90/0.9 stable history round trip, both Apply paths, (2) mixed-currency overall/category hierarchy compares home units, (3) ±15% cap measured against converted budget and weekly/daily/yearly factors preserved, (4) missing/invalid quote → no actionable list/write, (5) changed target budget currency at Apply → no write, (6) rate refresh after generation keeps captured source result, (7) low-history remains non-actionable; partial history labels remain, (8) failed Apply All rolls back, (9) snapshot display currency survives a later home-setting change and clear/dismiss, (10) existing same-currency results remain unchanged. Extend constructor fixtures with explicit currencies/sourceAmountToApply; do not weaken quality assertions or repair CL-16's separate clamp here.
- **WI-6:** `T/domain/analytics/AdvancedAnalyticsEngineNormalizedTest.kt`. Cases: (1) both empty factories with home=JPY/GBP but US locale, (2) language-only en and Locale.ROOT do not throw, (3) nested percentile/weekend comparison currency equals input.homeCurrency, (4) empty because all rows excluded preserves conversion warnings, (5) nonempty behavior unchanged. Restore default locale in finally; oracle uses explicit currency, not the device default.
- **WI-7:** `T/domain/analytics/InsightsEngineValidationTest.kt`. Replace the default empty recurring mock for these cases with a **real RecurringExpenseEngine** plus fake/stubbed RecurringExpenseRepository: (1) manual USD100 + EUR normalized input stays USD100 through snapshot and legacy rendered text, (2) manual foreign item with no FX quote still honestly source-labeled, (3) already-home detected pattern not converted twice, (4) unknown/manual malformed currency yields typed warning and no default-labeled number, (5) existing input warnings preserved, (6) cadence/manual precedence/first-three ordering unchanged, (7) cancellation and bounded CURRENT HEAD logging preserved. Do not rely solely on a mocked already-correct RecurringExpense list.
- **WI-8:** `T/domain/tax/TaxEstimatorTest.kt`; `T/data/repository/BusinessExpenseRepositoryTest.kt`; `T/scenarios/ExpenseDaoAggregateFilterTest.kt` (real Room/Robolectric). Cases: (1) mixed EUR/USD same category converted to filing EUR, (2) home differs from filing, (3) null plus explicit Uncategorized coalesce without overwriting and counts conserved, (4) source/failure buckets and partial summary warnings survive missing rate, (5) all failed/unknown filing or source → typed unavailable, (6) empty fiscal window, (7) purchase-only/effective-share and half-open date boundaries unchanged, (8) no call to deprecated raw category query. Preserve the meaningful existing yearly and 75+150=225 Uncategorized numeric oracles by stubbing actual currency buckets; do not invent a residual or relax assertions to accommodate stale fixtures.
- **WI-9:** `T/domain/groups/SharedExpenseBudgetOffsetEngineTest.kt`, existing two named methods plus only missing cases: (1) each failure branch individually and all three together, (2) mixed success/failure, (3) no failures → empty warnings/count0, (4) distinctive merchant, amount, currencies, date and exception text absent from returned warnings and logs, (5) list/count/partiality consistency, (6) real converter/cancellation behavior unchanged. Existing equality to MISSING_RATE proves returned content, not merely logs; record revalidation rather than adding duplicate tests.

## Validation
- **NOT RUN by the spec writer.** Strict review/guardian gates precede validation for **each PR separately**. Only the validation-runner owner or HUMAN executes. The coder never invokes Gradle directly. Human execution evidence records exact command, run ID, exit code, worktree fingerprint, result.json and stdout/stderr paths; no completion without the required evidence.
- Check active runs first; one live validation process. Templates (serial, one class filter per invocation):
  ```powershell
  powershell -NoProfile -ExecutionPolicy Bypass -File scripts/validation-runner.ps1 -Action List
  powershell -NoProfile -ExecutionPolicy Bypass -File scripts/validation-runner.ps1 -Action Start -Profile targeted-unit-test -TestFilter '*CurrencyRatesRepositoryImplTest'
  powershell -NoProfile -ExecutionPolicy Bypass -File scripts/validation-runner.ps1 -Action Wait -RunId '<returned-run-id>'
  ```
- **PR A filters:** *CurrencyRatesRepositoryImplTest, *CurrencyManagementViewModelTest, *ConversionSemanticsHardeningTest, *MultiCurrencyRepositoryTest. Run each via targeted-unit-test/Wait, then `powershell -NoProfile -ExecutionPolicy Bypass -File scripts/validation-runner.ps1 -Action Start -Profile compile` and Wait. Do not start B while a gate is unresolved.
- **PR B filters:** *NotificationMoneySignalDetectorTest, *NotificationProcessingPipelineOversizedAmountTest, *NotificationProcessingPipelineReliabilityTest, *ReviewQueueRepositoryTest, *BillReminderManagerTest, *BillRemindersViewModelTest, *BudgetAutopilotEngineTest, *BudgetViewModelAutopilotQualityTest, *BudgetAutopilotUiContractTest, *AdvancedAnalyticsEngineNormalizedTest, *InsightsEngineValidationTest, *TaxEstimatorTest, *BusinessExpenseRepositoryTest, *ExpenseDaoAggregateFilterTest, *SharedExpenseBudgetOffsetEngineTest. One serial targeted-unit-test run per filter; then compile. Re-run A's affected contract classes if B changes a core adapter.
- RUNNING must be polled, not restarted. Missing/skipped/unknown/timed-out/stale/infra-error is never PASS. Ask before expensive broader suites; any required guard suite must also use validation-runner, with FINAL_CI_GUARD_ACCEPTANCE_GATE.md referenced by FG-ID (FG-17 for money/time; FG-03/06/07/23 for fail-closed/no-weakening/self-protection), not pasted or bypassed. No schema change is authorized; a schema surprise requires rescoping and migration-test planning.

## Blast-radius fence
- **ALLOWED A production (exact files/functions in WI-1/2):** `M/domain/currency/CurrencyConverter.kt`; `M/data/repository/CurrencyRatesRepositoryImpl.kt` (catalog only, preserve CL-27 date parser); `M/data/repository/MultiCurrencyRepository.kt` (money target validation and additive display adapter only); `M/domain/core/money/MoneyAggregateResult.kt` (proposed typed compatibility exception, no global result redesign); `M/ui/screens/currency/CurrencyManagementViewModel.kt` (catalog and conversion-state plumbing).
- **ALLOWED B production:** `M/domain/notification/money/NotificationMoneySignalDetector.kt`; `M/data/repository/NotificationProcessingPipeline.kt` (specified fallback helpers/branches only); `M/domain/reminder/BillReminderManager.kt`; `M/ui/screens/reminder/BillRemindersViewModel.kt`; `M/ui/screens/reminder/BillRemindersScreen.kt`; `M/domain/budget/BudgetAutopilotEngine.kt`; `M/ui/screens/budget/BudgetViewModel.kt` (autopilot block only); `M/ui/screens/budget/BudgetScreen.kt` (recommendation amount bindings only); `M/domain/analytics/AdvancedAnalyticsEngine.kt` (two empty factories/call sites only); `M/domain/analytics/InsightsEngine.kt` (recurring adapter/rendering/warning transport only); `M/domain/tax/TaxEstimator.kt` (yearly category assembly only); `M/data/repository/BusinessExpenseRepository.kt` (currency-category read-through only). `M/domain/groups/SharedExpenseBudgetOffsetEngine.kt` is **conditional warning-only** permission under WI-9; inspected HEAD requires no production redaction change.
- **ALLOWED tests:** exactly the nineteen classes/paths enumerated under Tests/pre-checks, partitioned by PR. No ignored/deleted tests or unrelated assertion repairs. This session writes only this spec, the CL-27 spec and one journal line; the allowlists govern the later coder lanes.
- **READ ONLY dependencies, not implicit edit permission:** `M/domain/core/money/CurrencyCode.kt`, ConversionOutcome.kt, MoneyBucketInput.kt, MoneyAggregate.kt, MoneyAggregateBuilder.kt, MoneyNormalizationEngine.kt; `M/domain/currency/CurrencySettingsRepository.kt` and CurrencyResolution.kt; `M/domain/logic/RecurringExpenseEngine.kt`; `M/domain/analytics/AnalyticsModels.kt`; `M/data/repository/ReviewQueueRepository.kt` and BudgetRepository.kt; `M/data/database/dao/ExpenseDao.kt`; rate store/DAO/entity. Existing APIs supply the required reads; do not add direct DAO writes or new SQL.
- **FORBIDDEN neighboring ownership:** MoneyAggregateBuilder's universal all-failed availability classifier — CL-15/CA-E-01-004; publication-date/key/source-count changes — CL-27; budget clamp policy — CL-16; dashboard time windows — CL-14; other analytics aggregation/forecast-input fixes — CL-28; notification retry/worker/capture-identity work — CL-01/02/03; recurring reconciliation/delivery — CL-12/13; expense mutation/CAS — CL-08/09; group split/settlement — merged CL-29. Preserve CURRENT HEAD CL-17 logging in the three drifted money/analytics/group files.
- **ABSOLUTE money isolation:** no worker, privacy gate, cloud service, backup/restore, Room entity/schema/migration, DI-module, guard/config/baseline/allowlist, tax-rate, parser-registry, receipt, export, navigation or unrelated UI edits. If an unexpected caller cannot compile without an out-of-fence edit, stop for a named scope amendment rather than refactor the shared graph.

## Review gate
- **architecture-guardian + reviewer-strict for both PRs**, with **privacy-security-guardian** for nullable notification review flow, controlled errors and A6 caller-visible warnings. room-migration-guardian must confirm the reuse of the existing tax projection/no schema change; any schema need blocks the lane.
- A reviewer verifies one catalog, unknown-before-identity handling, legacy/typed basis compatibility, explicit source/target/count/quality contract, no stale default-labeled result, and safe captured-quote reversal. B reviewer traces each amount to its label and persisted currency, both Apply paths, unknown review approval guard, per-item recurring currency, empty analytics nested fields, filing-versus-home tax target and Uncategorized rows.
- Review the actual status/diff/surrounding sources/tests; verify no CL-27 regression or CL-15/16/28 scope creep, no loss of CL-17 logging controls, no nullable-currency fingerprint rewrite or DAO ownership bypass. For A6, inspect returned strings and existing tests at CURRENT HEAD, not just pin-era evidence/log sinks. A failed review or target validation blocks completion. Proposed test/API changes must not be mistaken for existing implementation or independent approval.

## Money constraints (this cluster)
- Money/currency changes preserve rounding semantics; boundary cases tested; display formatting never mixed with domain calculation. Keep existing finite Double representation, RecurrenceCalculator frequency factors, amount signs, effective-share rules, transaction filters and fiscal windows. No new intermediate rounding or BigDecimal migration.
- Historical expense analytics retain transaction-date FX; configuration/display obligations use the explicit latest policy; tax category conversion retains the existing tax-path basis and filing target. A view's formatter cannot establish currency identity. Unknown never means EUR/home, except the separately typed legitimate FirstRunDefault resolution—not a substitute for a failed read or ambiguous notification.
- Source-label display is intentionally selected for individual bill rows and recurring insight items. Aggregated summaries and autopilot comparisons must use real conversions; final budget writes remain in the original stored currency.

## Out of scope
- **A5 placement (binding): candidate CA-W1-003 phone-heuristic over-redaction belongs to the independent adversarial verifier (ZCode, different model/session), then a separately verified CL-17-adjacent redaction owner; it is fenced out because regex/phone classification is not this conversion contract—no redactor or Keystore assertion edits in CL-05.** Its verifier disposition is required before treating the candidate as resolved; no verification is asserted here.
- A6 is addressed explicitly by WI-9, including the fact that CURRENT HEAD already has code-only returned warnings. CA-E-04-001's original log-sink remediation remains merged CL-17; do not reopen it as a new finding.
- A8/CA-E-01-005 May assertion landmine: CL-27 WI-4 and human decision register. CA-E-01-001/003/005 are prerequisite CL-27; CA-E-01-004 is CL-15. Other test-recovery candidates and assertion-contract disputes remain with their named addendum owners.
- CA-P-11 parser currency/total-row defects: CL-24, not a wholesale notification/email parser rewrite. Adjacent budget, analytics, recurring and lifecycle findings remain with the fence's cluster owners; tax yearly API stays dormant and tax calculation policy is unchanged.
- No new findings, source patches, independent-verification claims or implementation milestones are created by this spec. Partial/unavailable conditions are never relabeled DONE/GREEN to satisfy a test.
