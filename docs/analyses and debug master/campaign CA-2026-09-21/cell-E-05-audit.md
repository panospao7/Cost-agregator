# Cell E-05 Audit

> **Latest continuation: PARTIAL — 1 newly added discovery, 4 total (001, 002, 005, 008).** Independent Phase 2 verification is pending. The original three-finding checkpoint below is preserved; the appended continuation contains the current coverage, corrections, and final provenance.

- Date: 2026-09-22
- Pinned commit: `37601232b9778170c57a656a245b199ab6d7d965`
- Session: **direct astra session**
- Cell: E-05 - Warranty, subscriptions, offers
- Mode: AUDIT, static only
- Source verification: `git rev-parse --short HEAD` returned `37601232`; `git diff --stat 37601232..HEAD -- app config scripts` was empty. Final working-tree source checks for app/config/scripts were also empty.
- Status: **PARTIAL cell coverage**. Named primary implementations were examined, but the entire transitive entry-to-DAO/event/side-effect graph and guard coverage were not exhaustively audited. See limits below.
- Final discovery count: **3 new findings: 1 P2, 2 P3**. These are Phase 1 findings; independent Phase 2 verification is pending. Earlier draft IDs 003, 004, 006, and 007 are withdrawn/excluded below.

## Governing intent and scope

Read the governing prompt sections 2 and 4, including known-debt exclusions, intended behavior, all 15 classes, severity calibration to actual production impact, and the production-caller rule.

Architecture extracts read:
- `docs/architecture/COVERAGE_MATRIX.md:41`: E-05; warranty/subscription/offers, reminders, recurring, receipt, and notification ownership; WarrantyTextExtractor, NotificationSubscriptionDetector, SubscriptionManagerEngine, BillReminderWorker, WorkerExecutionGuard rows.
- `docs/architecture/CODEBASE_SEGMENTS.md:663-680`: segment 34 and representative owners.
- `docs/architecture/LEGAL_PATHS.md:281-365,471-520,714-751,838-884,921-969`: recurring mutations, workers, subscription mutations, notification capture, negotiation, warranty auto-create.
- Named engine rows were identified from the matrix; the engine map itself was not audited wholesale. No complete backend/import map was loaded.

Consulted known-debt sources: REVALIDATED_ISSUE_REGISTRY_2026-09-07.md, remediation README/RP-04/RP-16 and recorded decisions/status excerpts, `docs/development/FUTURE-WORK.md` warranty/subscription/worker rows, and `config/baselines/cancellation.json`. Scoped cross-cell P-03/P-09 report excerpts were used as signposts, with source/known-debt evidence checked directly.

## Findings

### CA-E-05-001 | Subscription candidate legal-path documentation contradicts its registered production writer | defect class 1 | severity P3

- **Evidence at pin:** `app/src/main/java/com/yourname/expensetracker/data/repository/NotificationProcessingPipeline.kt`, `launchSubscriptionDetection` / `detectAndSaveSubscriptionCandidate`, lines 1730-1735,1758-1813: detection writes through `writeBarrier.runWrite` to `subscriptionCandidateDao.insert` at 1805-1813. `config/guards/db_ownership_policy.yml:1423-1438` explicitly registers this method as the post-commit candidate persistence writer. In contrast, `docs/architecture/LEGAL_PATHS.md:745-750` prohibits candidate insert/delete outside `SubscriptionManagerEngine.validateAndCreate/acceptCandidate`.
- **Impact path:** Maintainers following the stated single legal path are instructed to treat a policy-registered writer as forbidden. This is source/policy-versus-documentation divergence, not evidence that pending-candidate creation bypasses accepted-subscription validation or produces ghost reminders.
- **Caller trace:** Pipeline post-commit handling at 1671-1673 -> `launchSubscriptionDetection` -> `detectAndSaveSubscriptionCandidate` -> detector/entity conversion -> barrier -> candidate DAO insert.
- **Existing tests/guards:** Exact ownership registration above is positive evidence of an authorized writer. Pipeline atomicity/reliability/source-link tests were located/search-inspected; no complete candidate-specific test or guard execution was performed. The initial draft incorrectly claimed no guard coverage.
- **Cross-cell impact:** Notification intake/P-01 and E-05 architecture documentation. No actual recurring-rule/reminder mutation is claimed for a pending candidate insert.
- **Old-ID cross-refs:** none for the documentation contradiction. MIT-DB-08A is the ownership-policy provenance, not a new barrier finding. Candidate uniqueness debt DB-2/I8 and old P4 ghost-reminder issues are excluded.

### CA-E-05-002 | Documented engine-only usage path conflicts with the registered repository writer | defect class 1 | severity P3

- **Evidence at pin:** `app/src/main/java/com/yourname/expensetracker/ui/screens/subscription/SubscriptionManagementViewModel.kt`, `recordUsage`, lines 234-241 -> `app/src/main/java/com/yourname/expensetracker/data/repository/SubscriptionManagementRepository.kt`, `insertUsage`, lines 52-55 -> barrier precheck and `usageDao.insert`. `config/guards/db_ownership_policy.yml:3085-3099` registers this exact method and explicitly describes it as the repository-layer RECORD-usage path. `docs/architecture/LEGAL_PATHS.md:730-732` instead requires `SubscriptionManagerEngine.recordUsage`; that separate implementation is at `domain/subscription/SubscriptionManagerEngine.kt:311-324` under the same main source root.
- **Impact path:** Documentation and enforceable ownership policy disagree about the user-facing usage writer. Both inspected implementations insert usage with a timestamp and barrier check; no present-day lost validation, missing critical event, divergent analysis, or ghost notification was established. P3 reflects that limitation.
- **Caller trace:** Subscription ViewModel `recordUsage` -> repository `insertUsage` -> `SubscriptionUsageDao.insert`. No production invocation of the engine's `recordUsage` was found in the scoped call-site search.
- **Existing tests/guards:** Exact ownership policy explicitly permits the repository writer. Repository/ViewModel tests were search-inspected; no engine-routing usage assertion was found. No test or guard was executed.
- **Cross-cell impact:** E-05 maintainability and P-04 subscription legal-path documentation; rule update/delete/toggle still use the recurring coordinator.
- **Old-ID cross-refs:** none for this documentation mismatch. MIT-DB-08L is contextual policy provenance. FRESH-P4-001/-002 delete/toggle bypasses are not restated.

### CA-E-05-005 | Warranty extraction failure is acknowledged as a completed receipt side effect | defect class 12 | severity P2

- **Evidence at pin:** `app/src/main/java/com/yourname/expensetracker/domain/usecase/warranty/AutoCreateWarrantyFromReceiptUseCase.kt`, `execute`, lines 76-102 and 142-145: exceptions from receipt/warranty access or creation are converted to `WarrantyCreationResult.Failure`. `app/src/main/java/com/yourname/expensetracker/domain/receipt/lifecycle/ReceiptSideEffectPlanner.kt`, `makeWarrantyExtractionAction`, lines 201-220: the returned result is ignored at 211 and `SideEffectOutcome.Completed` is returned unconditionally at 212; its `FailedRetryable` catch at 215-220 only handles thrown exceptions. `app/src/main/java/com/yourname/expensetracker/domain/sideeffect/PostCommitActionRunnerImpl.kt:75-99` then emits a completed event/increments completed, rather than taking its failed-retryable branch.
- **Impact path:** A transient warranty DAO/read/write exception -> use-case Failure value -> planner Completed -> completed side-effect diagnostic despite no warranty being created. Failure classification and evidence needed for recovery are lost. No automatic retry scheduler or exactly-once delivery guarantee is inferred.
- **Caller trace:** Camera/batch receipt save through `ReceiptLifecycleCoordinator`: lines 651-676 construct the plan with ephemeral OCR input; lines 682-690 dispatch it after commit -> warranty action -> use case -> repository/DAO access -> returned Failure -> false Completed. The saved receipt correctly remains successful; it is the warranty side-effect outcome that is wrong.
- **Additional same-path evidence:** `AutoCreateWarrantyFromReceiptUseCase.createWarranty:232-250` persists the warranty before `persistReturnWindow:362-367`, whose `runCatching` logs and suppresses a return-window failure. `execute:94-98` exits AlreadyExists on a subsequent invocation, so merely rerunning execute does not repair that missing return window. This supports the misleading-outcome/recovery concern; it is not presented as a required all-or-nothing warranty/return-window transaction.
- **Existing tests/guards:** Search-inspected `AutoCreateWarrantyFromReceiptUseCaseTest` asserts Failure for empty/malformed/missing-field inputs. `ReceiptSideEffectPlannerClaimTest`, `ReceiptSideEffectPlannerStructuredDataTest`, and `ReceiptSideEffectPlannerEphemeralItemsTest` exercise other actions and mock this use case; no assertion mapping a returned warranty Failure to a failed/skipped action was found. Tests were not run; this is not an exhaustive test-absence claim.
- **Cross-cell impact:** P-03 receipt side-effect outcome classification and shared side-effect diagnostics. The post-commit runner's persistence/replay internals remain outside this bounded audit.
- **Old-ID cross-refs:** none for discarded warranty result. FRESH-P3-001 tracked absent receipt dispatch; current dispatch is present and is not reported again. Known cancellation catches in this use case are separately excluded.

## Withdrawn draft claims and known-debt exclusions

These entries are not new findings and are not included in the final count.

- **CA-E-05-003 withdrawn:** `WarrantyTrackerRepository.reconcileExpiredItems:366-389` does issue separate expiry updates. No requirement was established that warranties and return windows with independent deadlines must transition atomically. No premature expiry or corrupt paired state was proved. A failed second update does not cause the batch event to report unapplied work: control exits before that event. The draft overclaimed this.
- **CA-E-05-004 withdrawn:** Warranty events are best-effort in `WarrantyTrackerRepository:114-167,211-303,371-384`. `WarrantyTrackerRepositoryTest:788-828` deliberately verifies event-failure resilience. The inspected warranty legal-path section does not classify these events as critical audit events that must roll back the mutation. Missing critical provenance was therefore not established.
- **CA-E-05-006 known debt, excluded:** `WarrantyExpirationWorker.doWork:72-88` gates reconciliation on posting permission. The actual context overload checks permission at `WorkerExecutionGuard.kt:358-362` (the draft cited the other overload). `docs/development/FUTURE-WORK.md:385` already tracks **WRK-16**, mixed warranty reconciliation/posting. Also, active warranty reads filter by end date (`WarrantyDao:16-17,67-68`); stale status alone does not prove expired warranties remain visible as active in every UI.
- **CA-E-05-007 withdrawn as stated:** `execute:142-145` returns arbitrary exception text, but the traced live planner discards that result. A user-visible failure-message leak was not proved. Raw throwable/product/merchant logging remains a privacy follow-up; logging-tree, retention and export sinks were not fully traced, so no release/remote exposure is claimed.
- Cancellation swallowing exists, contrary to the initial draft's clean assessment: `NotificationSubscriptionDetector:95-101` and `AutoCreateWarrantyFromReceiptUseCase:142-145,362-367` catch broadly. These files are already listed in `config/baselines/cancellation.json:31,36,62,68`; not new findings.
- `SubscriptionManagementRepository:64-85` routes update/delete/activate/deactivate through `RecurringRuleLifecycleCoordinator`; the old **FRESH-P4-001/-002** ghost-notification writers are not reproduced as new issues.
- Other exclusions: FRESH-P9-006/-007/-008 worker debt; DB-2/I8 candidate constraint debt; WRN-7 return-window uniqueness; WRN-28 market metadata, WRN-29 billing normalization, WRN-32 history-count tenure; REC-19 raw savings limitations; manual-placeholder behavior/WRN-4. No blanket closure or regression claim is made for those IDs.

## Coverage ledger

Paths below are relative to `app/src/main/java/com/yourname/expensetracker/` unless explicitly prefixed.

| Source | Coverage and checks |
| --- | --- |
| `data/repository/WarrantyTrackerRepository.kt` | Read sections 1-210,211-429,430-671: barrier prechecks, insert transactions, event catches, reject/claim, currency aggregation, expiry SQL callers, return upsert, cloud routing/confidence conversion, default return policy, calendar deadlines, placeholder path. |
| `domain/subscription/SubscriptionManagerEngine.kt` | Read 1-380 and 378-670: validation, creation/acceptance transactions, price update, usage, history, recommendation/health math, grouped aggregate, deprecated savings. Outer transaction around coordinator-routed writes was identified; transitive coordinator dispatch was not fully audited here. |
| `domain/negotiation/SmartBillNegotiationEngine.kt` | Read analysis/provider/classification/monthly conversion sections and 300-793: opportunity construction, scripts/offers, scoring, outcome transaction, validation, history/data contracts. The outcome reads a snapshot before the transaction; concurrency behavior requires coordinator review before promotion. |
| `domain/negotiation/MarketRateProvider.kt` | Entire interface/quote/result enums read. No mutation/IO implementation in the interface. |
| `data/negotiation/StaticMarketRateProvider.kt` | Seed provider read: explicit static source, zero last-updated timestamp, EUR seeds, currency/region handling. No live market research or network call made. |
| `domain/receipt/WarrantyTextExtractor.kt:1-607` | Full file read at the correct path: every regex/default/confidence branch, date parsing/reasonable-date bound, duration arithmetic, product/merchant heuristics, phone/email/return extraction and exclusive end calculation. Earlier guessed `domain/warranty/` path did not exist and is not counted as coverage. |
| `domain/subscription/NotificationSubscriptionDetector.kt:1-307` | Full file read: normalizer fallback, merchant/currency key, effective amount, interval bins/tolerances, recency, confidence, entity conversion and annualization. |
| `data/repository/NotificationProcessingPipeline.kt:1671-1820` | Selected post-commit launch/detection/read/dedupe/write path; full pipeline not read. |
| `data/repository/SubscriptionManagementRepository.kt:1-90`; `ui/screens/subscription/SubscriptionManagementViewModel.kt:226-250` and accept/create caller searches | Usage and rule mutation ownership, UI entry, acceptance wiring. Full UI not audited. |
| `service/warranty/WarrantyExpirationWorker.kt:1-140`; `domain/workers/WorkerExecutionGuard.kt:150-190,349-370` | Request/gate ordering, expiry entry, stale recovery call, notification counters; not a full shared-worker audit. |
| `domain/usecase/warranty/AutoCreateWarrantyFromReceiptUseCase.kt:76-146,232-368` | Failure conversion, confidence gating, dedup, warranty/return persistence and promotion excerpts. |
| `domain/receipt/lifecycle/ReceiptSideEffectPlanner.kt:175-223`; `ReceiptLifecycleCoordinator.kt:640-700`; `domain/sideeffect/PostCommitActionRunnerImpl.kt:75-99` | Real receipt entry to action and failure/completion handling. |
| `data/database/dao/SubscriptionCandidateDao.kt` | Entire DAO read, including unconditional conversion update and pending queries. |
| `data/database/dao/WarrantyDao.kt:10-28,64-68,93-114`; `ReturnWindowDao.kt:66-70` | Selected active/expiry/aggregate SQL. Full warranty/return/delivery/event DAO and schema audit remains open. |
| `config/guards/db_ownership_policy.yml:1423-1438,3085-3099` | Exact registered candidate/usage writers; ownership evidence, not an executed guard result. |
| Tests listed in findings plus warranty repository/worker, guard, engine, repository/ViewModel tests | Test-name/assertion excerpts and targeted searches only. No test suite was read end-to-end or executed. |

## All 15 defect classes assessed

| Class | Assessment and limits |
| --- | --- |
| 1 Legal path | 001/002 are documentation contradictions with registered writers, calibrated P3. Rule mutations route through the coordinator. No new ghost-reminder bypass established. |
| 2 Barriers | Most inspected writes have a precheck/runWrite; worker is guarded. Not a global restore exclusion proof. Cloud low-confidence diagnostic insert and all transitive owners were not fully classified. |
| 3 Atomicity/TOCTOU | Reviewed warranty transactions and negotiation/subscription price snapshots. Separate independent expiry updates alone did not establish a defect; 003 withdrawn. Full concurrency/DAO tests not examined. |
| 4 Idempotency | Status-gated expiry, receipt AlreadyExists/conflict behavior, candidate pending queries and worker durable claim calls examined. Missing-return retry limitation belongs to 005; candidate uniqueness is already DB-2/I8. |
| 5 Cancellation | Broad catches exist and are in the cancellation baseline. No claim that all catches are safe. |
| 6 Side effects | Receipt action dispatch is after save; warranty completion classification is 005. Best-effort warranty events are not assumed critical. Nested recurring dispatch needs separate full traversal. |
| 7 Money | EUR-only negotiation eligibility, finite-positive inputs, monthly conversion/inverse, currency-bucket totals and deprecated raw savings read. Snapshot/conversion consumers not exhaustively traced; no global money correctness claim. |
| 8 Time | Extractor formatters/regexes, injected now, system-zone date conversion, calendar additions, DAO comparison boundaries and detector interval bins examined. No new boundary finding promoted; timezone/locale and invalid-date adversarial coverage remains incomplete. |
| 9 Privacy | Ephemeral input route and privacy/cloud gates observed; arbitrary message/raw logging sites identified. Traced returned failure has no UI sink. Logging-tree/export/retention reachability incomplete. |
| 10 Worker hygiene | Permission-before-maintenance is known WRK-16; counters follow successful reminder returns in the inspected worker body. Worker internals/cancellation test coverage not comprehensively re-audited. |
| 11 Data integrity | Receipt conflict/lookup, event best effort, candidate conversion and expiry SQL read. Full FK/index/migration/delivery DAO traversal remains incomplete. |
| 12 Errors | 005 proves returned Failure becoming Completed. Primary receipt save remaining successful after an optional side-effect failure is intentional. |
| 13 Wiring/dead code | Production receipt/pipeline/ViewModel entry traces found; engine recordUsage has no found live caller and repository alternative is policy-registered. No speculative runtime issue from uncalled code promoted. |
| 14 Tests | Targeted assertions expose blind spots; tests explicitly accept best-effort warranty events. No tests/guards executed, no test pass or exhaustive absence claim. |
| 15 Regression | Old P4 writer fixes are present; known worker/cancellation/constraints/deferred behavior excluded. Full historical fix-diff audit not completed. |

## Limits and handoff

This is a bounded partial audit, not a completed end-to-end cell certification. Interrupted/compacted context and truncated combined outputs required targeted recovery; only visible/fresh evidence is cited. The six named primary implementations were reviewed, but full dependency closure was not completed: recurring coordinator and event dispatch internals; all warranty/return/delivery/event DAOs and schemas; cloud provider/privacy/logging sinks; complete UI callers; and full tests/guard implementation.

No additional full-file sweep is claimed. No builds, tests, lint, or guards ran. No production/config/script files were changed. The initial seven-finding draft and premature completion wording were corrected in this same report; the journal's E-05 count is corrected to three.

## Provenance

- Auditor: astra-cell-auditor (direct session).
- Agents invoked: `/root/e05_scope_scout`, read-only reconnaissance; final findings written by the direct auditor. No independent verifier invoked.
- Pinned commit: `37601232b9778170c57a656a245b199ab6d7d965`.
- Timestamp: 2026-09-22 (final local timestamp is recorded in JOURNAL.md).
- Result: PARTIAL, 3 new discovery findings, Phase 2 verification pending.


## Continuation — 2026-09-22T10:25:31+03:00

- Mode: AUDIT, static only; direct session. Existing discovery findings 001, 002, 005 retained; withdrawn IDs remain excluded. Phase 2 verification remains pending.
- Pin: 37601232b9778170c57a656a245b199ab6d7d965. HEAD short: 37601232. Production/config/script committed diff and working-tree status: empty before source work.
- Read governing prompt sections 2/4, previous report in full, E-05 coverage row, referenced legal-path sections and segment 34. Source reads below refer to this verified checkout at the pin.
- Initial status: PARTIAL. Continuation checklist: A subscription/negotiation caller-to-recurring persistence/dispatch; B warranty/return/event/delivery persistence and recovery; C receipt outcome persistence/replay; D cloud/privacy/logging sinks; E tests and exact guard registrations.
- Full-file read budget: maximum 20 this session; ledger below records source coverage and any remaining gaps. No execution of builds, tests, lint, or guards is authorized.

### Continuation coverage ledger (append-only)

- A checkpoint: read SubscriptionManagerEngine 110–261,330–375; SmartBillNegotiationEngine 578–649; RecurringExpenseRepository 100–129; subscription ViewModel 310–348,378–422 and negotiation ViewModel 46–99. recordPriceChange production call-site search found declaration only (latent behavior cannot establish live runtime impact). Candidate acceptance uses a saved object and unconditional conversion update; UI has per-ViewModel mutatingIds protection. Candidate acceptance/rejection replay remains an unpromoted lead pending baseline/test checks.
- A coordinator checkpoint: RecurringRuleLifecycleCoordinator 45–269,334–530, covering CRUD transactions, active-state preservation, reconciliation and critical update event. No notification/post-commit dispatch occurs in these coordinator bodies; rows are materialized for later worker consumption. Nested Room transaction alone is not a premature-notification finding. P-04-003 owns inactive-rule regeneration; P-04-005 owns activation/delete race. No duplicate findings here.
- B full source reads 1–9: WarrantyDao (1–116), ReturnWindowDao (1–71), WarrantyLifecycleEventDao (1–15), WarrantyReminderDeliveryDao (1–158), SubscriptionCandidateDao (1–148), Warranty entity (1–96), ReturnWindow entity (1–100), WarrantyLifecycleEvent entity (1–36), WarrantyReminderDelivery entity (1–65). Checked predicates, conflict policies, unique keys, FKs, deletion, recovery and timestamp boundaries. Warranty receipt index is NOT unique; return receipt/expense indexes are unique. Receipt/expense deletion SET_NULL preserves warranty/return data; warranty deletion CASCADE removes delivery rows; event table intentionally has no warranty FK (deletion/batch events can survive). No critical-event requirement inferred.
- B repository reread: WarrantyTrackerRepository 70–310, manual insert/event behavior, conflict-ignore, AI receipt event, update/delete, transactional rejection and claim. No missing-transaction claim based only on best-effort warranty events. Relevant debt rows WRN-4/6/7/20, DB-2/I8, WRK-16 consulted; actual source takes precedence over stale resolved/deferred descriptions.
- B/C checkpoint: read WarrantyExpirationWorker 59–235 end-to-end execution path; AutoCreateWarrantyFromReceiptUseCase 60–368; WarrantyTrackerRepository 366–470,578–671; WarrantyTrackerViewModel 125–235. The live manual UI writes receiptId=null: createManualPlaceholderReceipt is a legacy helper with no production caller found. The live review UI calls updateWarranty directly, not createWarrantyForReview/promoteReviewDraft (no production caller found). Repository expiry uses strict <, while active counts use >; exact equality leaves status ACTIVE but excludes active counts. Not promoted as a material new defect from that millisecond boundary alone.
- C full source reads 10–15: PostCommitActionRunnerImpl (1–157), DiagnosticSideEffectEventWriter (1–143), CompositeSideEffectEventWriter (1–75), CompositeDiagnosticEventWriter (1–60), DefaultCloudPayloadPolicy (1–139), DiagnosticEventWriter including RoomDiagnosticEventWriter (1–69). Also read PostCommitSideEffectEvidenceService 28–156, TransactionSideEffectFailureEventWriter 19–68, planner 73–115,175–223, ReceiptLifecycleCoordinator 630–702 and DiagnosticsModule binding search. FailedRetryable is an emitted result, not an automatic retry request in the runner. The camera/batch path calls runBestEffortAfterCommit directly, not the separate evidence service.
- D checkpoint: CloudWarrantyExtractionService 1–205 (key, gate, payload policy, prepared HTTP request, retry/catches, prompt inputs); WarrantyTrackerRepository 437–465 passes persisted receipt.rawOcrText, whereas the local planner passes ephemeral OCR. CloudAiPrivacyGate 20–73; EffectiveCloudAiPolicy 119–192; startup configureDebugTools 625–653. Only found production Timber planting is DebugTree behind BuildConfig.DEBUG. Raw extraction product/merchant and thrown exceptions can reach debug logcat; this does NOT establish release/remote logging. Existing cancellation baseline/dead-sanitizer debt remain excluded.

### CA-E-05-008 | Warranty reminder claim does not revalidate a warranty claimed by the user | defect class 3 | severity P2

- **Evidence at pin:** `app/src/main/java/com/yourname/expensetracker/service/warranty/WarrantyExpirationWorker.kt`, `doWork`, 98–114, and `deliverReminder`, 163–223, retain the selected Warranty snapshot and seed/claim/send without rereading its current status. `data/database/dao/WarrantyReminderDeliveryDao.kt`, `claim`, 55–68, only predicates on the delivery key and delivery status, not the parent warranty. `data/database/dao/WarrantyDao.kt`, `getWarrantiesExpiringSoon`, 19–23, establishes ACTIVE as eligibility, while `updateWarrantyStatus`, 56–62, can independently set CLAIMED.
- **Impact path / concrete invariant:** An expiration reminder should be dispatched for an eligible ACTIVE warranty. Worker selects an ACTIVE warranty into its list; user completes Mark claimed, committing CLAIMED; worker resumes before delivery seed/claim, both of which still succeed because the warranty exists; it posts the old expiration reminder and marks the delivery SENT. This is a bounded stale notification race, not duplicate expenses, lost warranty data, or a requirement for an impossible atomic DB/OS-notify transaction. Revalidation at claim would prevent this demonstrated sequence where the user mutation commits before claim.
- **Caller trace:** WorkerRegistry warranty entry -> scheduled WarrantyExpirationWorker.doWork -> repository.getWarrantiesExpiringSoon -> DAO ACTIVE snapshot -> deliverReminder -> NotificationService.sendBudgetAlert. Concurrent production UI: WarrantyTrackerScreen onMarkClaimed (301) -> WarrantyTrackerViewModel.markAsClaimed (211–215) -> WarrantyTrackerRepository.markWarrantyAsClaimed (277–303) -> WarrantyDao.updateWarrantyStatus. AndroidNotificationService.sendBudgetAlert (65–85) posts the supplied message if notifications are enabled.
- **Existing tests/guards:** WarrantyExpirationWorkerTest 64–105 mocks the repository while using a real delivery DAO; 158–232 checks SENT, NOT_DELIVERED, across-run dedup and an already-SENT delivery. WarrantyReminderDeliveryDaoTest 94–159 checks unique-key dedup and claim transitions; these inspected assertions do not interleave a parent warranty CLAIMED update between selection and claim. `config/guards/db_ownership_policy.yml:7028–7108` explicitly registers claim/seed/failed/sent worker operations (MIT-DB-08M, workerMediated). This is a logic gap within the authorized writer, not a bypass. No tests or guards run.
- **Cross-cell impact:** E-05 warranty UI and P-09 background notification delivery. No independent verification yet.
- **Old-ID cross-refs:** none for stale parent-status eligibility. WRK-16 concerns permission gating maintenance; FRESH-P9-006 concerns an absent delivery after claim, not this existing-parent status race; FRESH-P9-007/-008 concern metrics/spec metadata; durable delivery-key dedup from P9-P1-09 remains intact for this sequence.

### Evidence-backed continuation of CA-E-05-005 (existing discovery, not a new count)

- **What Completed persists:** the camera/batch coordinator at ReceiptLifecycleCoordinator 682–697 ignores the batch result and returns the saved receipt. PostCommitActionRunnerExtensions 13–31 invokes the runner once. PostCommitActionRunnerImpl 75–99 emits completion and increments its completed count. DiagnosticsModule binds CompositeSideEffectEventWriter; its completed method (24–32) invokes DiagnosticSideEffectEventWriter.completed (31–38): pipeline RECEIPT, stage SIDE_EFFECT, outcome SIDE_EFFECT_COMPLETED, DEBUG severity, terminal=true. emitEvent (100–125) supplies receipt ID/correlation/source and sanitized action metadata. RoomDiagnosticEventWriter in domain/diagnostics/DiagnosticEventWriter.kt 35–68 inserts a PipelineDiagnosticEvent via PipelineDiagnosticEventDao.insert (12–13). The metadata factory (6–35) records sideEffectName/category/trigger/target and a hashed idempotency key. This is diagnostic evidence, not a durable executable action queue. Persistence itself is best-effort: CompositeDiagnosticEventWriter 29–58 falls back to the maintenance sink on blocked/failed Room writes; sink failure can also be swallowed.
- **Failure consumer:** FailedRetryable follows the same diagnostic writer with FAILED_RETRYABLE/WARNING (DiagnosticSideEffectEventWriter 56–81). The runner does not enqueue a retry; the result is discarded on this receipt path. TransactionSideEffectFailureEventWriter 64–67 mirrors only TRANSACTION/Expense actions, so a RECEIPT warranty action is not mirrored into transaction events. PipelineDiagnosticEventDao 27–33 feeds recent-failure diagnostics; a false Completed is outside that failure filter. DiagnosticsRepositoryImpl 81–94 reads that filter. No retry/replay consumer was found in the traced runner, receipt path, DAO, or registered warranty worker. This bounded traversal is NOT a repository-wide proof that no future/manual tool can replay an action.
- **Recovery limitation:** AutoCreateWarrantyFromReceiptUseCase.execute 94–98 returns AlreadyExists before return-window work; createWarranty 232–250 and createReviewDraftWarranty 303–320 swallow a failed return-window upsert via persistReturnWindow 362–367. The only production call to upsertReturnWindowForReceipt found under app/src/main is that helper. createWarrantyForReview/promoteReviewDraft could upsert during promotion, but have no found production caller. WarrantyTrackerViewModel.confirmWarranty 180–198 instead calls updateWarranty and loadStats, so confirmation does not repair a missing return window. Expiry reconciliation only updates existing rows; delivery recovery only resets existing reminder claims. The inspected path therefore has no independent missing-return-window repair. Do not equate receipt rescan or a diagnostic FAILED_RETRYABLE record with guaranteed recovery.
- **Correction to previous limits:** outcome persistence and the live confirmation consumer are now covered. Existing 005 remains P2/class 12 and discovery-only. Its scope is incorrect outcome classification/diagnostic visibility plus the demonstrated repair limitation, not loss of an established automatic retry guarantee. Manual warranties no longer create placeholders in the live UI. Local extraction itself uses ephemeral text; cloud return-policy extraction separately reads the persisted receipt representation.

### Further exact coverage and exclusions

- A: RecurringOccurrenceMaterializer.materializeInCurrentTransaction 79–300, RecurringPlanProjectionService.projectFromOccurrencesInCurrentTransaction 57–94, RoomRecurringLifecycleEventWriter.writeCritical 29–59. Occurrence/materialization events and reminder rows stay inside the outer subscription/negotiation Room transaction; critical event insert failures propagate. No OS notification call is made there. Reminder dispatch is a later worker operation. ManualRecurringExpenseDao mutation annotations/query excerpts 60–88; RecurringOccurrenceDao 7–75; RecurringReminderDeliveryDao 8–15,150–179,209–221; PlannedExpenseDao 21–26,75–151. Delete removes all derived rows for the rule; deactivation removes PLANNED occurrences, their deliveries and PLANNED rows. Previously posted OS notifications are not equivalent to standing DB delivery rows; no claim of cancelling already-displayed OS notifications is made.
- A: full source read 16, SubscriptionPriceHistoryDao 1–35; SubscriptionPriceHistory entity 10–42 (FK CASCADE to rule, index includes recordedAt). History and successful negotiation rule reconciliation share the outer transaction. The negotiation snapshot is read before that transaction; coordinator refreshes isActive but copies other supplied fields. Lost-update/repeated-outcome leads remain unpromoted without completing the competing live writer sequence. recordPriceChange has no production caller found, so its pre-read/threshold issues are latent here. Candidate acceptance uses an unconditional markAsConverted returning Unit; missing/rejected/converted candidates are not reread. Tests at SubscriptionManagerEngineTest 340–403 assert one-call insert/history/convert routing using mocks, not persisted-state replay exclusion. UI mutatingIds reduces same-ViewModel double submission. Candidate state/replay concerns remain a baseline-reconciliation lead under DB-2/I8 rather than a newly counted constraint finding; multi-entry/retry reachability is not certified.
- B: WorkerExecutionGuard 329–425 runs the worker block without an encompassing Room transaction and handles cancellation/retry; it is a maintenance lease, not parent-warranty eligibility serialization. WarrantyTrackerScreen 293–308,605–621 provides the live claim action for 008. AndroidNotificationService 65–85 checks notification enablement and posts; no local SecurityException catch is present in that method. This follow-up is not promoted without reconciling the wider notification/permission debt. Warranty delivery FAILED rows can be reclaimed while the parent still appears in the selected expiry window; stale CLAIMED recovery uses an inclusive one-hour threshold, invoked at the next worker run, not an hourly recovery job. A crash after OS notify but before SENT can replay; no exactly-once guarantee is asserted.
- D: DefaultCloudPayloadRedactor 41–95 delegates WARRANTY_EXTRACTION text to the shared CloudPiiSanitizer; amounts/dates are intentionally preserved. CompositePrivacyGate 19–60 catches gate failures as bounded fail-closed decisions. NetworkModule 35–44 provides the cloud client without a logging interceptor; AiModule 227–234 supplies the real privacy gate/payload policy/audit logger. The complete shared PII regex engine, consent-change races, privacy-store load semantics and retention/export implementation remain outside this pass; no privacy-completeness claim is made.
- D: DiagnosticEventWriter 35–68; EventMetadataSanitizer 159–207; DataStoreMaintenanceSafeDiagnosticSink 90–140; DiagnosticsRepositoryImpl 47–105. Explicit diagnostic calls persist sanitized metadata and exception messages, not the Timber log stream. The warranty returned Failure string is discarded by the actual receipt planner; its exception is logged to the debug tree but does not become that action's persisted exception. A thrown planner exception can reach diagnostic persistence through the shared writer. Shared sanitizer limitations/diagnostic privacy are cross-cell follow-up, not resurrection of withdrawn 007. Full export/retention sink closure remains open.
- E final checkpoint: read WarrantyReminderDeliveryDaoTest 207–258 recovery/cascade assertions; WorkerGuardArchitectureGuardTest 20–92 (guard invocation recognition and empty allowlist); scripts/verify_worker_boundaries.py guard-invocation/scan signposts; scripts/ci/guard_registry.py db_access registration (script scripts/verify_db_access_boundaries.py, ownership and structural exception policies). Exact worker writer registrations were read at db_ownership_policy.yml 7028–7108 and candidate registration 6065–6102. These establish registered ownership/guard structure, not runtime eligibility checks or passing guards. CI contract references: FINAL_CI_GUARD_ACCEPTANCE_GATE.md, FG-12/FG-14. No guard execution.
- E: AutoCreateWarrantyFromReceiptUseCaseTest 98–155 asserts AlreadyExists/Failure; its mocked receipt-unique-conflict test forces -1 even though the current Warranty entity receipt index is non-unique. This mock does not prove race-safe receipt dedup. WRN-6 schema/compatibility history remains a reconciliation limit rather than a new duplicate finding. Planner test searches show the use case mocked and action-presence checks, not a returned Failure-to-outcome assertion. Tests are inspected evidence only.
- C: ReceiptLifecycleCoordinator 699–718 confirms the insert-race duplicate returns inserted=false and postCommitBatch=null, so that duplicate exit itself does not replay warranty extraction.

### Updated all-15-class assessment (continuation paths)

| Class | Continuation disposition |
| --- | --- |
| 1 Legal path | Create/accept/negotiation rule writes enter RecurringExpenseRepository -> RecurringRuleLifecycleCoordinator. Registered candidate/usage/worker writers are authorized; existing 001/002 remain P3 doc drift. Manual UI writes nullable receiptId. No new bypass claimed. |
| 2 Barriers | Read coordinator runWrite and repository prechecks, materializer scope, critical writer check, guarded warranty block and maintenance-safe diagnostic routing. Ordinary warranty UI writes are not serialized against the worker's parent snapshot. This is 008 eligibility logic, not a newly demonstrated restore bypass. Global barrier/lease proof remains outside E-05. |
| 3 Atomicity / TOCTOU | Outer subscription/negotiation transactions encompass history/rule/derived writes; no notification dispatch inside those methods. 008 demonstrates selection -> committed user claim -> delivery claim/post. Negotiation stale non-active fields remain an unpromoted concurrency lead. Independent warranty/return expiry transactions and best-effort events do not revive 003/004. |
| 4 Idempotency / duplicates | Delivery UNIQUE key + conditional claim/SENT + stale recovery read with real-DB test assertions. Candidate acceptance lacks persisted-state CAS; DB-2/I8 overlap and multi-entry/replay reachability remain unresolved. Warranty receipt index is non-unique; mocked IGNORE conflict test does not prove dedup. AlreadyExists and duplicate receipt exits do not repair a missing return window (005). |
| 5 Cancellation | Worker and negotiation explicitly rethrow cancellation; receipt planner rethrows thrown cancellation, but use-case broad catches/runCatching remain known baseline debt. Event-writer catch paths are not certified globally. No new cancellation finding. |
| 6 Side-effect timing | Rule flows persist future deliveries inside Room and rely on later worker dispatch. Receipt runner executes once after save transaction; completion is diagnostic evidence, not an outbox. Crash-after-notify/before-SENT may replay; no exactly-once promise inferred. |
| 7 Money / currency | Subscription validation and baseline/history currency preserved; negotiation converts monthly proposal to saved billing frequency before paired history/rule write. History FK cascades on delete. No new money finding; concurrent price/outcome semantics and mixed-frequency history displays not fully closed. |
| 8 Time correctness | Warranty and return DAOs: active > now, expiring-soon >= currentStart and < futureExclusive, reconciliation < now; exact equality is a status/count mismatch not promoted on its own. Calendar deadline helpers and daily worker cadence distinguished from one-hour stale threshold. Full timezone/DST/UI date review remains open. |
| 9 Privacy | Local planner uses ephemeral raw only; cloud return-window path uses persisted receipt representation through cloud gate/payload policy. DebugTree is debug-only; no release Timber/remote stream sink established. Explicit diagnostic Room/DataStore sinks and sanitizer inspected; full shared sanitizer/retention/export closure remains open. Withdrawn 007 stays withdrawn. |
| 10 Worker hygiene | Guard, checkpoints, cancellation, actual post-before-metric, FAILED reclaims, stale recovery, prune and CASCADE inspected. New 008 is stale parent eligibility; WRK-16 and FRESH-P9-006/-007/-008 are excluded. NotificationService permission-race handling requires wider debt reconciliation. |
| 11 Data integrity | Warranty/return/event/delivery entity indices/FKs and DAOs read fully. SET_NULL preserves receipt/expense dependents; CASCADE deletes warranty deliveries; no FK on best-effort deletion/batch events. Missing return repair narrows 005. Migration SQL/schema snapshots not independently compared. |
| 12 Errors | Existing 005 now traced through persisted SIDE_EFFECT_COMPLETED and recent-failure filtering. Returned FailedRetryable is recorded, not automatically scheduled, on this receipt consumer. Optional side-effect failure must not invalidate saved receipt. No new user-visible warranty Failure leak claimed. |
| 13 Wiring / dead code | Live UI/create/accept/negotiation/worker paths traced. recordPriceChange, createWarrantyForReview and createManualPlaceholderReceipt have no found production callers under app/src/main. Their hypothetical effects are not represented as live defects. No missing replay capability invented from names/comments. |
| 14 Tests | Inspected assertion bodies for subscription routing, warranty use-case results, worker sent/failure/dedup and DAO recovery/cascade. Parent-status interleaving is absent from the inspected tests. Source guards recognize ownership/guard use, not the 008 invariant. All execution NOT RUN. Mocked receipt uniqueness assumption is a test limitation, not independently verified schema coverage. |
| 15 Fix regression | RP-04 contract and current repaired delete/toggle path reconciled; P-04-003/-005 cross-referenced. Debt records and decisions D1–D8 read. No wave-wide fix-diff review or claim that 008 was introduced by a particular commit. |

### Continuation disposition, counts, and precise remaining limits

- **Existing discoveries retained: 3** — CA-E-05-001 (P3), CA-E-05-002 (P3), CA-E-05-005 (P2). 005 gains persistence/consumer/recovery evidence; this does not add a finding.
- **New discoveries in this continuation: 1** — CA-E-05-008 (P2, class 3).
- **Total discoveries: 4** — 2 P2 and 2 P3. Independent Phase 2 verification: **pending for all four**. Withdrawn/excluded IDs 003/004/006/007 remain excluded; none renumbered or resurrected. Next genuinely new ID is 009.
- **Status: PARTIAL.** A–E received additional static coverage, but this is not a complete cell certification. No builds, tests, lint or guards were executed. No production/test/config/script edits or commits were made.
- Remaining A paths: resolve candidate acceptance replay/deleted/rejected state concerns against detailed DB-2/I8 historical scope and a demonstrated multi-entry UI/retry sequence; close the negotiation pre-transaction snapshot against competing live non-active-field writers (e.g. category/nextDate changes), repeated submissions and all outcome consumers. Shared recurring invariant helpers/expander/resolver/full dispatch internals remain owned by P-04 and were not read wholesale here.
- Remaining B paths: compare warranty/return/delivery migration SQL and schema snapshot to declarations; close concurrent review promotion/rejection, return update/refund-field preservation, multiple-warranties-per-receipt semantics, and exact-boundary/DST UI behavior. Current DAO/entity coverage is complete for the four requested warranty persistence types, but these historical/concurrent consumers are not.
- Remaining C paths: no automatic retry found on the inspected live receipt chain; wider debug/manual replay entry points and all alternate receipt ingestion paths were not exhaustively audited. A future pass must not infer replay support from FailedRetryable alone.
- Remaining D paths: full shared CloudPiiSanitizer, privacy-settings load/consent-change behavior, raw-retention cleanup, lifecycle diagnostic retention and every export/support sink; actual UI rendering of other ViewModel error strings (distinct from discarded use-case Failure). No release/remote leak conclusion is warranted from the debug logs inspected.
- Remaining E paths: complete guard implementation and broader assertion bodies for the above residual leads. Static assertion inspection cannot substitute for independent Phase 2 verification. No execution is requested or authorized by this audit.
- Reading discipline: **16 full production-source files** this continuation (enumerated above), plus the original report in full; large files were read by named-method slices. No claim of coverage for missing guessed paths, truncated search output, or unprinted source. Initial combined UI output was recovered with separate ViewModel reads; broad debt search truncation was replaced by bounded exact debt searches. Source line citations were read in this session against the verified pinned checkout.

### Final continuation provenance

- Timestamp: 2026-09-22T10:39:01+03:00.
- Auditor: astra-cell-auditor (direct session); campaign CA-2026-09-21, E-05, AUDIT/static only.
- Pin: 37601232b9778170c57a656a245b199ab6d7d965. Final HEAD check: 37601232; committed diff from pin and working-tree status for app/config/scripts both empty.
- Agents invoked in this continuation: none. The earlier report's scout belongs to the earlier session; no new agent or independent verifier was invoked.
- Files written: this report and JOURNAL.md inside the campaign folder only.
- Result: 1 new discovery; 4 total; PARTIAL; Phase 2 pending.
