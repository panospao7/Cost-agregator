# Deep Analysis — Batch 08: AI Use Cases - Navigation, Review, Sync (@debugger)

## Scope
- domain/ai/usecase/JudgePendingReviewDuplicateUseCase.kt
- domain/ai/usecase/MapFinancialQueryToNavigationUseCase.kt
- domain/ai/usecase/PrioritizeReviewItemsUseCase.kt
- domain/ai/usecase/SuggestCategoryFallbackUseCase.kt
- domain/ai/usecase/SuggestReceiptExtractionUseCase.kt
- domain/ai/usecase/SyncProactiveBriefingWorkUseCase.kt
- domain/ai/usecase/AiUseCaseModels.kt (not found in codebase)
- domain/ai/service/CloudCategorizationAssistService.kt
- domain/ai/service/CloudDashboardBriefingService.kt
- domain/ai/service/CloudDedupeJudgeService.kt
- domain/ai/service/CloudQueryInterpretationService.kt
- domain/ai/service/CloudReceiptAssistService.kt

## Issues Found

| # | File:Line | Severity | Type | Description | Reproduction Steps | Suggested Fix |
|---|-----------|----------|------|-------------|-------------------|---------------|
| 1 | SuggestReceiptExtractionUseCase.kt:69-87 | HIGH | Cache bypass ignored | The `force` parameter is accepted but **never checked** in the cache lookup. When `force=true`, the cache should be bypassed, but the code checks `existing != null && existing.status == READY && ...` without a `!force &&` guard. Users calling `invoke(receiptId, force=true)` will get stale cached results instead of forcing a re-generation. | Call `invoke(receiptId=1, force=true)` on a receipt that already has a cached artifact. Expected: AI service is called. Actual: Cached result returned silently. | Add `!force &&` before `existing != null` on line 70, mirroring the pattern in the other two use cases. |
| 2 | SuggestCategoryFallbackUseCase.kt:36 | MEDIUM | Thread-unsafe mutable state | `uncategorizedCategoryIdsCache` is a mutable `var` with no synchronization. This class is injected via Hilt (likely `@Singleton` or scoped), and multiple coroutines could race: coroutine A reads `null`, coroutine B reads `null`, both compute the cache, and one overwrites the other mid-write. On Kotlin/JVM, reference writes are atomic, so this won't crash, but the cache may be computed redundantly, and there's no TTL — categories added later named "Uncategorized" will never be seen. | Add a second "Uncategorized" category after first use. The cache is never invalidated. | Use `@Volatile` or an `AtomicReference`, and consider a TTL or invalidation mechanism. Alternatively, compute it every time (it's cheap). |
| 3 | ReviewPriorityModels.kt:63 | MEDIUM | Testability / System.currentTimeMillis | `calculateTimeSensitivity` hardcodes `System.currentTimeMillis()` instead of using the app's `TimeProvider` abstraction. This makes unit tests non-deterministic and inconsistent with the rest of the AI layer which consistently uses `TimeProvider`. In an emulator with an incorrect clock, scores will be wrong. | Write a test that creates a review 25h ago, but with system clock mocked — the age calculation uses real clock, not mock. | Pass `currentTimeMs: Long` as a parameter to `calculateTimeSensitivity` and `fromReview`, sourced from `TimeProvider`. |
| 4 | PrioritizeReviewItemsUseCase.kt:44 | LOW | Incorrect priority formula / sorting anomaly | The sorting comparator applies `score * 0.8f` for items with score ≤ 0.7, creating a discontinuity: an item with score 0.70 gets effective score `0.70 * 0.8 = 0.56`, but an item with score 0.71 gets `0.71`. This means items near the 0.7 threshold jump dramatically in priority with a tiny score change. A review scored 0.69 (effective 0.552) is ranked far below one scored 0.71 (effective 0.71), a gap of 0.158 for a real delta of 0.02. | Score two reviews at 0.69 and 0.71. The 0.71 item will rank dramatically higher despite near-identical underlying scores. | Use a smooth function, e.g., `if (score > 0.7f) score + 0.3f else score`, or remove the discontinuous boost entirely. |
| 5 | MapFinancialQueryToNavigationUseCase.kt:13 | MEDIUM | Missing QueryMetric.MIN handling | Line 13 checks for `LIST`, `TOTAL`, `COUNT`, `AVERAGE`, `MAX` but **excludes `QueryMetric.MIN`**. The `QueryMetric` enum includes `MIN` (FinancialQueryModels.kt:13). If a user asks "What was my smallest purchase?", the AI interprets it as `MIN`, but this use case returns `null`, causing navigation to fail silently. | Ask "what was my cheapest transaction this month?" — AI returns metric=MIN, navigation returns null, no drill-down. | Add `QueryMetric.MIN` to the check on line 13, or invert the logic to only exclude truly unsupported metrics. |
| 6 | MapFinancialQueryToNavigationUseCase.kt:20 | LOW | Data loss with multi-filter queries | `categoryIds.singleOrNull()` and `merchants.singleOrNull()` silently drop all filters when more than one category or merchant is specified. A query like "show me food and transport expenses" yields `categoryId = null`, losing both filters. | Query "food and transport spending" → AI returns categoryIds = {1, 2} → both are discarded, all categories shown. | Accept a `List<Long>` for category/merchant in `DomainTransactionFilter`, or pick the first one and log a warning, rather than silently nullifying. |
| 7 | CloudCategorizationAssistService.kt:180 | MEDIUM | PII leakage in AI prompt | The `buildPrompt()` method sends raw `input.merchant`, `input.amount`, `input.supportingText` (which includes notification text and OCR text) to the cloud API. However, the `redactBeforeCloud` check happens in the `CategorizationAssistInputBuilder`, and the builder correctly sanitizes when `shouldRedact=true`. But `input.supportingText` includes `review.notificationTitle` and `review.notificationText` — these may contain personal info (e.g., "John sent you €50"). The notification text is sent in the prompt template on line 191 without PII scrubbing beyond the builder's `shouldRedact` flag. If `shouldRedact` is false (user hasn't enabled it), raw notification text goes to cloud. | Set `redactBeforeCloud=false` in settings, receive a notification "John Doe sent you €100 to GR1234567890123456" → the full text with name and IBAN goes to Gemini. | Apply `CloudPiiSanitizer.sanitizeText()` unconditionally to `supportingText` in the prompt, or at minimum scrub IBANs/card numbers regardless of the redact flag. |
| 8 | JudgePendingReviewDuplicateUseCase.kt:103 | LOW | Nullable `.take()` on null receiver | Line 103: `suggestion.rationale?.take(...)` returns `String?`, then `.withRouteDiagnostics(routeDecision)` is called on the result. The extension function `String?.withRouteDiagnostics` (line 173) accepts `String?` receiver, so it doesn't NPE. However, the same pattern in `SuggestCategoryFallbackUseCase.kt:199` and `SuggestReceiptExtractionUseCase.kt:131` calls `.withRouteDiagnostics()` on a potentially null value but uses `String?` return. This is actually correct. | N/A — no crash, but reviewer note: the inconsistent return types between these extension functions (`String` in JudgePendingReview vs `String?` in SuggestReceipt) could cause issues if callers expect non-null. | Standardize the return type. `JudgePendingReviewDuplicateUseCase`'s `withRouteDiagnostics` returns `String` (non-null), while `SuggestReceiptExtractionUseCase`'s version returns `String?`. Make them consistent. |
| 9 | CloudDashboardBriefingService.kt:48 | MEDIUM | Eager instantiation bypasses DI | `private val promptHelper = OnDeviceDashboardBriefingService()` creates an `OnDeviceDashboardBriefingService` instance directly (calling the no-arg constructor) instead of injecting it. Similarly, `CloudQueryInterpretationService.kt:42` does the same with `OnDeviceQueryInterpretationService()`. This bypasses any DI lifecycle management and could lead to duplicate `GenerativeModel` instances being created (one via DI, one via this direct instantiation), wasting memory. | Both on-device and cloud services are created → two `GenerativeModel` singletons coexist, each holding ML model resources. | Inject the on-device service as a constructor parameter, or extract the prompt-building logic into a shared utility class that doesn't extend the service interface. |
| 10 | OnDeviceDashboardBriefingService.kt:101-106 | MEDIUM | Naive JSON extraction — grabs too much | `extractFirstJsonObject` uses `indexOf('{')` and `lastIndexOf('}')`, which means if the AI returns text like `"Here's your briefing: {"title":"x"} more text {"title":"y"}`, it captures everything between the first `{` and last `}`, including garbage in between. The Cloud services have a proper depth-tracking parser. | AI returns `{"title":"a","text":"b"} extra text {"other":"c"}` → captured string is the entire blob between first `{` and last `}`, which is invalid JSON → `JSONObject(jsonText)` throws → returns null → `ParseError`. | Replace with the same balanced-braces parser used in `CloudDedupeJudgeService.extractFirstJsonObject()` or use the shared `CloudJsonParser`. |
| 11 | DedupeJudgeInputBuilder.kt:113 | MEDIUM | Wrong threshold — skips single-candidate dedup | `if (candidates.size < 2)` returns `NotNeeded`, meaning when there is exactly 1 candidate, the AI dedupe judge is never consulted. But a single strong duplicate candidate is the most common and most important case for AI judgment. The check should be `< 1` (empty), not `< 2`. | A pending review with exactly 1 near-duplicate expense → AI returns "NotNeeded" instead of actually judging the pair. | Change `< 2` to `< 1`, or better `isEmpty()`, so single-candidate comparisons go through to the AI judge. |
| 12 | CloudDedupeJudgeService.kt:234 | LOW | IllegalArgumentException from valueOf on invalid AI response | `DuplicateVerdict.valueOf(suggestion.optString("verdict"))` will throw `IllegalArgumentException` if the AI returns an unexpected verdict string. This exception propagates to the `JSONException` catch on line 137, which wraps it as a `ParseError`. While not a crash (it's caught), it loses the original response and returns a generic error. | AI returns `{"verdict":"POSSIBLY_DUPLICATE"}` → `valueOf` throws `IllegalArgumentException` → caught as generic error → user sees opaque failure. | Use `runCatching { DuplicateVerdict.valueOf(...) }.getOrNull()` and return `UNCERTAIN` as fallback, or return `null` to signal parse failure explicitly. |
| 13 | SuggestCategoryFallbackUseCase.kt:180 | LOW | Service returns null treated as error | `categorizationAssistService.suggest(input)` returns `CategoryAssistSuggestion?`. When it returns `null` (e.g., API key missing in `CloudCategorizationAssistService`), the code enters the `validated == null` branch and writes `failureMessage(routeDecision.reason, routeDecision)` as the error — but `routeDecision.reason` is the routing reason (e.g., "Cloud available"), not an error message. The user sees something like "Cloud available Route: CLOUD, provider: google-ai-studio…" which is confusing. | Trigger with missing API key → `suggest()` returns null → error message shows route diagnostic instead of meaningful error. | Use a dedicated error message like `"AI category assist returned no result"` instead of `routeDecision.reason`. |
| 14 | CloudReceiptAssistService.kt:310-311 | LOW | Large image file read into memory | `file.readBytes()` reads the entire receipt image into a byte array in memory before Base64 encoding. With `MAX_INLINE_IMAGE_BYTES = 2MB`, the peak memory usage is ~2MB (raw) + ~2.67MB (Base64 string) ≈ 4.7MB. On memory-constrained devices, this could trigger GC pauses or OOM alongside other AI operations. | Scan a 1.9MB receipt image → ~4.7MB transient heap allocation. | Use streaming Base64 encoding or reduce `MAX_INLINE_IMAGE_BYTES`, or add a try-catch for `OutOfMemoryError`. |
| 15 | SuggestReceiptExtractionUseCase.kt:169-170 | LOW | NPE risk on rawOcrText.trim() | Line 170 calls `receipt.rawOcrText.trim()`. `rawOcrText` is declared as `String` (non-null) in the entity, but Room doesn't enforce this — a NULL value in SQLite maps to a platform null, which would NPE here. The `ScannedReceipt` entity has `val rawOcrText: String` with no default — if a migration error or manual DB edit leaves it null, this crashes. | Insert a row with NULL rawOcrText in the database (e.g., via adb) → `hasUsableOcrText()` crashes with NPE. | Add a null check: `val rawText = receipt.rawOcrText?.trim() ?: ""` or ensure Room migration sets a default. |
| 16 | CloudCategorizationAssistService.kt:60 | LOW | Request object reuse across retries | The `request` object is built once (line 49-53) and reused across all retry attempts. OkHttp `Request` objects are immutable and safe to reuse, so this isn't a bug. However, the `requestBody` string is also built once — if the input includes a timestamp (like `currentTimeMs`), the same stale timestamp is sent on retries. | Retry after 5s delay sends the same `currentTimeMs` from the original attempt. | Not critical — just a note that retry semantics are "replay", not "rebuild". |
| 17 | JudgePendingReviewDuplicateUseCase.kt:56 | LOW | Weak source hash using hashCode | `input.hashCode().toString()` is used as `sourceHash` for cache invalidation. `hashCode()` for data classes is based on all fields, but collisions are common in 32-bit space. Two different inputs with the same hashCode would return a stale cached result. Same issue in `SuggestReceiptExtractionUseCase.kt:67` and `SuggestCategoryFallbackUseCase.kt:142`. | Two different inputs with same hashCode → stale cache hit. Probability is low but non-zero (~1/2³² per pair). | Use a content-based hash like SHA-256 of the serialized input, or at minimum `.toString().hashCode()` combined with a second discriminator. |

---

### Cross-Component Pipeline Issues

| # | Pipeline | Severity | Type | Description | Suggested Fix |
|---|----------|----------|------|-------------|---------------|
| CP-1 | `SuggestReceiptExtractionUseCase` → `CloudReceiptAssistService` → cache | HIGH | force flag ignored in receipt assist | `SuggestReceiptExtractionUseCase.invoke(receiptId, force=true)` is called when users tap "Retry AI" — but the missing `!force` guard in the cache check (Issue #1) means retries return stale cached data, making the retry button useless. The other two use cases (`JudgePendingReview`, `SuggestCategoryFallback`) correctly bypass cache on force. | Add `!force &&` to the cache check, matching the pattern in sibling use cases. |
| CP-2 | `DedupeJudgeInputBuilder` → `JudgePendingReviewDuplicateUseCase` → `CloudDedupeJudgeService` | MEDIUM | Single-candidate dedup silently skipped | The `DedupeJudgeInputBuilder` returns `NotNeeded` when exactly 1 candidate exists (Issue #11). The `JudgePendingReviewDuplicateUseCase` receives this as `DedupeJudgeBuildResult.NotNeeded` and returns `DedupeJudgeGenerationResult.NotNeeded` to the UI. The user sees "no duplicate check needed" even when there's an obvious candidate. The entire AI dedupe pipeline is short-circuited for the most common case. | Change threshold to `< 1` in `DedupeJudgeInputBuilder`. |
| CP-3 | `MapFinancialQueryToNavigationUseCase` → `InterpretFinancialQueryUseCase` → UI navigation | MEDIUM | MIN metric breaks drill-down | AI interpretation returns `QueryMetric.MIN`, `MapFinancialQueryToNavigationUseCase` returns `null` (Issue #5), the UI receives no navigation filter, and the "View transactions" button either does nothing or shows all transactions unfiltered. | Add `MIN` to the supported metrics list. |
| CP-4 | `CloudDashboardBriefingService` → `OnDeviceDashboardBriefingService` (prompt helper) | MEDIUM | Dual model instantiation | `CloudDashboardBriefingService` directly instantiates `OnDeviceDashboardBriefingService()` for prompt-building (Issue #9). If both cloud and on-device services are created by DI, two separate `GenerativeModel` instances exist (one in each `OnDeviceDashboardBriefingService`). The `cachedModel` in the DI-managed instance is separate from the one in the Cloud service's helper, doubling ML model memory usage. | Extract prompt building into a shared utility, or inject the on-device service. |
| CP-5 | `SuggestCategoryFallbackUseCase` → `CloudCategorizationAssistService` → artifact | LOW | Confusing error message on null service result | When `CloudCategorizationAssistService.suggest()` returns null (e.g., missing API key), the use case writes `routeDecision.reason` as the failure error message instead of a meaningful error (Issue #13). The artifact stored in DB has confusing diagnostics as its error, and the UI displays routing info to the user. | Use a descriptive error string for the null-return case. |
| CP-6 | `ReviewPriorityModels.fromReview()` → `PrioritizeReviewItemsUseCase` | LOW | Non-deterministic priority in tests | `calculateTimeSensitivity` uses `System.currentTimeMillis()` (Issue #3) while the use case ecosystem uses `TimeProvider`. Tests that mock `TimeProvider` for artifact caching get inconsistent time values in priority scoring, leading to flaky priority ordering tests. | Thread `currentTimeMs` through the scoring pipeline. |

### Summary
- **Total issues: 23** (17 file-level + 6 cross-component)
- **Files with issues: 11/12** (only `AiWorkScheduler.kt` interface has no issues)
- **Critical/High: 2** (Issues #1, CP-1 — force flag ignored in receipt assist cache)
- **Medium: 9** (Issues #2, #3, #5, #7, #9, #10, #11; CP-2, CP-3, CP-4)
- **Low: 12** (Issues #4, #6, #8, #12, #13, #14, #15, #16, #17; CP-5, CP-6)

### Top 3 Priorities for Immediate Fix

1. **Issue #1 / CP-1**: `SuggestReceiptExtractionUseCase` missing `!force &&` — the retry button is broken. One-line fix.
2. **Issue #11 / CP-2**: `DedupeJudgeInputBuilder` threshold `< 2` should be `< 1` — the AI dedup judge is never consulted for single-candidate (most common) scenarios. One-line fix.
3. **Issue #5 / CP-3**: `MapFinancialQueryToNavigationUseCase` missing `QueryMetric.MIN` — user queries for minimum values break navigation. One-line fix.
