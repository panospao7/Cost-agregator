# Revalidated Issue Registry — post adversarial re-evaluation

> **Date:** 2026-09-07 · HEAD `d1fa9c68` + uncommitted working-tree changes (incl. the one-line test fix described in §6).
> **Method:** 13 adversarial re-validation agents attempted to **disprove** every one of the 124 fresh findings and re-verify the tracked open/partial items. Each check re-read the cited code with full context, traced production reachability (callers, DI wiring, UI flows), hunted counter-evidence (DB unique indexes, manifest entries, ProGuard rules, tests pinning behavior, guard exemptions), verified platform-semantics claims (Kotlin coroutines, Room `withTransaction`, Android scoped storage, notification identity), and calibrated severity against architecture invariants.
> **Evidence hierarchy (agreed with stakeholder):** verdicts rest on **static code evidence**. The Gradle unit-test run (compile: PASS; suite: ~178 failures across ~70 classes) is treated as a **weak signal only** — the failures mix outdated expectations, environment/locale-sensitive time-DST tests, and possible real regressions, and cannot attribute cause. Where a test result happens to agree with a statically-proven claim it is noted as corroboration, never as proof. See §7.

**Outcome: 112 of 124 fresh findings CONFIRMED (many with calibrated adjustments) · 3 REFUTED outright · 9 sub-claims refuted or corrected · all tracked open/partial items re-verified (2 reframed).** Net effect: the issue set shrinks slightly and — more importantly — severity and reachability are now trustworthy.

---

## 1. REFUTED — false positives removed

| ID | Original claim | Why it is NOT a bug (counter-proof) |
|---|---|---|
| **FRESH-U-005** | Source-link `exists`→`insert` TOCTOU allows duplicate provenance rows; `AlreadyExists` branch dead | `EntitySourceLink.kt:22-25` **does** declare `Index(["targetEntityType","targetEntityId","sourceIdentityKey"], unique = true)` (the original audit cited only the non-unique indices at :15-20). `EntitySourceLinkDao.insert` is `@Insert(OnConflictStrategy.IGNORE)` → returns `-1` on unique conflict, so the `insertedId > 0 else AlreadyExists` branch at `SourceLinkWriterImpl.kt:86-90` is live and correct. Duplicates are impossible at the SQLite level; the racy `exists()` fast-path resolves to a harmless `-1`. |
| **FRESH-P1-007** | `captureForRetry` REPLACE policy cancels a pending deferred worker, orphaning its row | The scenario is unreachable: a second deferred capture for the same notification key produces the identical `DEFERRED_<keyHash>` fingerprint; **no code path ever deletes intake rows** (grep: zero `DELETE FROM notification_intake`; retention only nulls payload columns), so `insertOrIgnore` returns `-1` and the function returns at `NotificationIntakeCoordinator.kt:215-218` — **before** `enqueueUniqueWork` (:232). Different keys use different unique work names, so REPLACE never touches another key's worker. Trivia: a deferred row can have two candidate workers, but `claimForProcessing`'s atomic status-guarded UPDATE makes the loser a no-op. The REPLACE policy is dead code, not a hazard. |
| **P3-P1-07 (tracked partial)** | `ProcessReceiptUseCase.kt:26` `?: "EUR"` fallback mislabels receipt currency | The class has **zero production injection or call sites** (only its own definition + one unit test instantiate it). It is dead code; the EUR line can never run in production. Resolution: **delete the use case** (or mark `@Deprecated(ERROR)` like its siblings) — no fix needed. This closes a long-tracked item. |

**Sub-claim refutations (parent findings confirmed, these parts removed):**
- **FRESH-U-001**: "cancelled caller still performs `withTransaction { upsert }`" is **overstated** — Room's `withTransaction` is `withContext`-based and throws `CancellationException` on entry when the job is already cancelled, so the write block does **not** run. Real impact: swallowed CE → wasted conversion/dedup work, wrong results (`DuplicateSkipped`/`InsertConflict`) returned to dead callers, event writes at :652/:691 silently skipped while the method continues. Severity HIGH → **MED-HIGH**.
- **FRESH-U-004**: the claim that `DirectEventDaoInsertGuardTest.kt:171-175` allowlists `RecurringOccurrenceMaterializer` in the **write-barrier** guard is wrong — that allowlist belongs to a different guard and has no effect. The core claim (barrier guard blind to `intakeDao`/`sourceLinkDao`/`auditDao` receivers; materializer visible and unexempted) is **confirmed**, and the observed `WriteBarrierArchitectureGuardTest` FAILURE in the (weak-signal) test run is consistent with the static prediction.
- **FRESH-P9-001**: the snooze/dismiss **PendingIntent request-code** collision sub-concern is refuted — dismiss = snooze `xor 0x40000000` and the two Intents target different receiver components, so `filterEquals()` is false regardless of codes. The main notification-ID claim stands (receipt alerts genuinely post into 20000–29999 via `NotificationIdGenerator.forReceipt` with a null tag; Android notification identity is (tag, id), channel is irrelevant).
- **FRESH-P4-002**: the "price/category edits → stale amounts" sub-claim is refuted — the subscription screen has no amount editor, `recordPriceChange` routes through `updateRule`, and the only other raw update writes a display-only label. The **toggle-off bypass** stands (notifications continue after deactivating).
- **FRESH-P5-005**: imprecise month count — the 6 keys are M-5..M-0, so **M-2..M-5 (four months)** are zero-filled, not five.
- **FRESH-P5-008**: `FinancialHealthScoreV2.kt:93` does not fall back to raw sums — it converts the swallowed failure into an `IllegalStateException` that lands in `catch(Exception)` and returns a **fabricated default all-50 score** (worse in a different way). The :99/:383 raw-fallback part stands.
- **FRESH-P11-004**: the fingerprint re-split trigger is a **device timezone change / tzdata update**, not DST within one zone — `LocalDate.atStartOfDay(zone)` is deterministic for a fixed past date. Additionally, `senderDomain` in the fingerprint splits dedup for forwarded mail independently.

---

## 2. CONFIRMED findings — final calibrated registry

Severity shown is post-calibration. "Reach" = production reachability class: **LIVE** (reachable today), **RELEASE-GATED** (live in release only), **DEBUG-ONLY**, **LATENT** (real defect, path currently unwired/dead), **CODE-REAL** (defect real but no production effect until wired).

### 2.1 Universal / cross-cutting (6)

| ID | Sev | Reach | Why it is a problem (root cause → failure) | Files / systems |
|---|---|---|---|---|
| FRESH-U-001 | **MED-HIGH** | LIVE | Raw `runCatching` wraps **suspending** `currencyConverter.convertAsOf`; `CancellationException` is swallowed into `null` → the cancelled create/update continues silently (wasted network/dedup work, misleading results, event writes at :652/:691 skipped). Siblings (:456, :274) rethrow — inconsistency proves intent. The whole file is excluded from the cancellation guard via `KNOWN_VIOLATIONS` (:135), so no CI signal | `TransactionLifecycleCoordinator.kt:476,874,652,691`; `CancellationSafe` helper; cancellation arch-guard KNOWN_VIOLATIONS |
| FRESH-U-002 | **MED** | LIVE (enforcement gap) | The U-PR1 architecture guard skips any suspend fun with a **default parameter** (`=` in the parameter list breaks the body scan) and accepts a mere *mention* of `CancellationException` in a catch body as "guard evidence" → the rule is vacuous for a large fraction of suspend functions, which is exactly why U-001 survives | `CancellationSafetyArchitectureGuardTest.kt:664-669,210`; all production suspend code |
| FRESH-U-003 | **MED** | Meta | `RAW_RUN_CATCHING_ALLOWLIST` entries carry `expires = 2026-10-01` but expiry is enforced only for `KNOWN_VIOLATIONS` — 25 days from now the exemptions silently become permanent | `CancellationSafetyArchitectureGuardTest.kt:184-197,350-358,405-413` |
| FRESH-U-004 | **HIGH** | LIVE (CI red) + LATENT (writes) | The write-barrier guard's caller regex matches only receivers named like the lowercased DAO interface → three unbarriered DAO writers are **invisible**: `NotificationIntakeCoordinator` (`intakeDao.insertOrIgnore` :125/:214), `SourceLinkWriterImpl` (`sourceLinkDao.insert` :85), `DataRetentionWorker` (local-var `auditDao.insert` :196/:206 — defeats both regex shapes). `RecurringOccurrenceMaterializer` (`plannedExpenseDao.fulfillByOccurrenceKey` :123, no barrier, not in EXEMPT_CLASSES) is visible → the guard assertion "all DAO write callers are barrier-protected or exempt" should be **red today** | `WriteBarrierArchitectureGuardTest.kt:239-256`; the 4 writer classes; `WorkerExecutionGuard` checkpoint |
| FRESH-U-006 | LOW | LIVE (harmless) | Raw `runCatching` around suspending startup recovery; the wrapping lifecycleScope is only cancelled at process death, so no observable consequence — pattern debt only | `AppStartupCoordinator.kt:353,369-371` |

### 2.2 Pipeline 1 — Notification capture (8)

| ID | Sev | Reach | Why it is a problem | Files / systems |
|---|---|---|---|---|
| FRESH-P1-001 | **HIGH** | LIVE | Deferred-intake rows fingerprint as `DEFERRED_<keyHash>` (content-blind) while the live path fingerprints content+postTime. Android routinely reuses `pkg|tag|id` keys → (a) re-post during a later gate-unavailable window hits the unique index, `insertOrIgnore` returns -1 → **silent permanent drop** (only `Timber.d`, no terminal diagnostic, no enqueue — contrast the live Duplicate branch which emits one); (b) same content re-captured live after warm-up → second raw row outside the 5-min duplicate window. Stale `DEFERRED_*` rows are never deleted | `NotificationIntakeCoordinator.kt:197,214-218`; `NotificationIntakeEntity` unique index; pipeline secondary dedupe `:317-351` |
| FRESH-P1-002 | **MED-HIGH** | LIVE | `captureForRetry` hardcodes `"STORE_METADATA_ONLY"`; the worker builds storage payloads **exclusively** from the stored mode and never re-reads settings → a STORE_RAW user permanently loses title/text/bigText for anything captured during the gate warm-up window. Bonus inconsistency: no DO_NOT_STORE early-return on the deferred path (unlike `capture()` :75) | `NotificationIntakeCoordinator.kt:207`; `NotificationIntakeWorker.kt:227-236,454-487` |
| FRESH-P1-003 | MED | LIVE | Enqueue `Operation` never awaited → process death in the insert→persist window orphans RECEIVED rows. Bounded by the `onListenerConnected` sweep, **but** the RecoveryScheduler KDoc claims app-start/restore sweeps that don't exist — if the listener never rebinds, rows sit indefinitely | `NotificationIntakeCoordinator.kt:142-146`; `NotificationCaptureService.kt:291-298`; `NotificationIntakeRecoveryScheduler.kt:26` |
| FRESH-P1-004 | MED | LIVE | Steps 1–4 (RECEIVED emit → gate decide (up to ~600-900ms self-heal) → dedupe → filter) are cancellable with no try/finally terminal: `onDestroy` cancels in-flight work leaving dangling RECEIVED events / silent drops. `SHUTDOWN_DRAIN_TIMEOUT_MS` (:188) is declared and never used — a drain was planned then dropped | `NotificationCaptureService.kt:412-511,837,855,188` |
| FRESH-P1-005 | LOW | STORE_RAW-only | Missing sensitive keys (`android.messages` = OTP text, `textLines`, `remoteInputHistory`, `conversationTitle`) in extras JSON — marginal because extrasJson persists only under STORE_RAW and the same text already lands in `bigText` there. Defense-in-depth | `NotificationCaptureService.kt:192-206,811-832,554-564` |
| FRESH-P1-006 | LOW | LIVE (narrow) | Deduper TTL on wall clock — backward jump suppresses identical re-posts; forward jump re-admits duplicates. Keyed on content fingerprint so blast radius is same-key+same-content only. `SystemMonotonicTimeProvider` exists, unused here | `NotificationCaptureDeduper.kt:28`; TimeModule binding |
| FRESH-P1-008 | LOW | LIVE | NUL-delimited field join in transient payload crypto — `\u0000` in any notification-controlled string shifts all subsequent fields and drops extrasJson beyond index 4 | `NotificationTransientPayloadCrypto.kt:45-56,70-77` |
| NEW-P1-011 (tracked) | accepted-debt | — | Duplicate SHA-256 in `RawNotificationFingerprint` is **pinned** by MIGRATION_104_105 (byte-identical concatenation + unique index across eras) — changing it would silently break cross-era dedupe. Accept + document, don't "fix" | `RawNotificationFingerprint.kt:26`; `AppDatabase.kt:6454-6474,6489-6493` |

### 2.3 Pipeline 2 — Transaction lifecycle (10)

| ID | Sev | Reach | Why it is a problem | Files / systems |
|---|---|---|---|---|
| FRESH-P2-001 | **HIGH** | LIVE | Entity-overload `deleteExpense`: `getById ?: return@withTransaction` exits only the lambda; execution **falls through** to unconditional `planDeleted` + `runBestEffortAfterCommit` and returns `Result.success` for a missing row (id-overload returns failure). Stale-entity double-tap → UI says "deleted", no DELETED event, budget/anomaly/recurring-unlink side effects fire against a nonexistent id | `TransactionLifecycleCoordinator.kt:2113-2145` vs `2044-2067`; `TransactionsViewModel.kt:473` |
| FRESH-P2-002 | MED | LIVE (narrow) | `insertAtomic` (IGNORE) returns -1 for **either** unique index (`dedupeKey` or `rawNotificationId`), but the conflict resolver never checks `rawNotificationId` → an edited-then-reprocessed notification misattributes to fuzzy match or reports a bogus `CREATE_INSERT_CONFLICT`; at `NotificationProcessingPipeline.kt:1352` the assert then throws ISE and the raw notification is never marked processed (retry loop) | `TransactionLifecycleCoordinator.kt:155-186,587,646-688`; `Expense.kt:36,44` |
| FRESH-P2-003 | LOW-MED | LIVE | `bulkUpdateMerchant` has no per-row collision guard → renaming into an existing merchant with same amount/day/currency/type hits the dedupeKey unique index; the **whole rename rolls back** with a raw SQLiteConstraintException (no corruption — atomic; sibling single-row paths throw clean `DuplicateUpdateException`) | `TransactionLifecycleCoordinator.kt:1964-1968` vs `1334-1347,1414-1427` |
| FRESH-P2-004 | LOW | LIVE | `skipDeduplication=true` doesn't skip dedupe (precheck only; key + unique index + STANDARD resolver still apply). Impact capped: caller handles `DuplicateSkipped` gracefully and the 5-min-bucket key means collisions imply the caller's own check would too — contract-vs-behavior mismatch + narrow race only | `TransactionLifecycleCoordinator.kt:507-584,648`; `CreateExpenseRequest.kt:65`; pipeline `:1267,1340-1352` |
| FRESH-P2-005 | MED | LIVE (part latent) | Six update paths (`updateCategory/Location/Merchant/Type/TransferDetails/TypeAndTransferDetails`) silently no-op on missing rows yet four still dispatch planner side effects after the txn (audit gap + wasted dispatch), while `updateBusinessExpensePatch`/`updateOwnershipDbOnlyV2` correctly return NotFound. `updateTypeAndTransferDetails` additionally has **zero production callers** (its worst no-op check absence is latent) | `TransactionLifecycleCoordinator.kt:1043/1069,1325/1375,1405/1454,1485/1532,1558/1629` vs `1197-1199,1732-1739` |
| FRESH-P2-006 | LOW | LATENT | Type-unchanged path re-persists the recomputed canonical dedupeKey without collision check → legacy stale-key rows can crash the update (unique index). Zero production callers | `TransactionLifecycleCoordinator.kt:1561-1581,1608` |
| FRESH-P2-007 | LOW | LIVE | `updateTransferDetails` / `updateTypeAndTransferDetails` drop `correlationId` → UPDATED events with null corrId, side effects get a **fresh unrelated** corrId → transfer-edit audit trail broken (live via `TransactionsViewModel:567`) | `TransactionLifecycleCoordinator.kt:1473-1479,1526-1532,1611-1629`; planner `:56` |
| FRESH-P2-008 | LOW | LIVE | Blocking dedupe doesn't exclude `isNotMine` rows; the candidate-list query does → a not-mine row can block creation yet never appears in review lists (likely intentional prevention-vs-suggestion split, but undocumented and inconsistent) | `ExpenseDao.kt:546-650,780,852` vs `969` |
| FRESH-P2-009 | LOW | LIVE | 5 best-effort event writes use bare `runCatching` (CE swallowed; `:652` additionally fabricates `eventLogged=false`) while 4 siblings rethrow — inconsistent cancellation hygiene | `TransactionLifecycleCoordinator.kt:321,343,515,652,691` vs `274,392,1184,2244` |
| FRESH-P2-010 | LOW | LIVE | `bulkUpdateMerchant` dispatches bulk side effects even when `affectedCount = 0` (siblings gate on `> 0`) — wasted plan/run, log noise | `TransactionLifecycleCoordinator.kt:~1991` vs `1844,1924` |

### 2.4 Pipeline 3 — Receipts/OCR/statement (10)

| ID | Sev | Reach | Why it is a problem | Files / systems |
|---|---|---|---|---|
| FRESH-P3-001 | **HIGH** (regression) | LIVE | `processReceiptInput` ends at insert+review with **no planner/dispatch**: `planAfterReceiptSaved` runs only on the email path (:1035) and `ReceiptSideEffectDispatcher.dispatchAfterSave` has zero callers → camera/**batch** receipts never get warranty auto-extraction or price-protection checks (interactive scan separately covers item categorization; the matching worker covers matching — batch import gets **neither**). Test debt: `ReceiptLifecycleCoordinatorTest:234` stubs the runner and pins dispatch, so the suite encodes the removed contract | `ReceiptLifecycleCoordinator.kt:244-565`; `ReceiptSideEffectPlanner.kt:208`; `ReceiptSideEffectDispatcher.kt:42`; `ReviewViewModel.kt:967` |
| FRESH-P3-002 | **HIGH** | LIVE | Statement AI merge pairs `validatedTransactions[i]` with `parsedTransactions[i]` but the AI parser can **skip** entries (blank merchant/amount≤0/bad date) with no size recheck → after a skip, `type`/`date` come from the **wrong** parser row (a DEPOSIT stamped PURCHASE defeats type-aware dedupe) and tail rows are silently dropped | `BankStatementLifecycleProcessor.kt:245-266`; `ValidateBankStatementTransactionsUseCase.kt:228-235,264` |
| FRESH-P3-003 | **HIGH** | LIVE | `TimeoutCancellationException` (a `CancellationException` subclass) from the 15s OCR `withTimeout` is caught by `catch (CancellationException) { throw }` **before** the retry logic → OCR timeouts never retry, and in `processBatch`'s `coroutineScope{async{}-awaitAll}` one slow page **cancels the whole batch** | `ReceiptOcrService.kt:587-603,771-794`; `ReceiptRepository.kt:595-631,612-613` |
| FRESH-P3-004 | MED (privacy) | LIVE | Link/unlink/suggest load the **full receipt entity before the txn** and write it back with full-row Room `@Update` inside → a concurrent retention purge (`rawOcrText=''`, column-scoped) landing in the gap is **resurrected** (purged OCR text restored, `rawOcrTextPurgedAt` nulled → re-picked next cycle), and concurrent column updates are reverted | `ReceiptLinkService.kt:168,252,382,411`; planner `:283,:374`; `ScannedReceiptDao.kt:23-24,155-164` |
| FRESH-P3-005 | MED | LIVE | Cancelled statement import is **unretryable**: the statement receipt (with `imageHash`) commits at step 4; cancellation finalizes the run CANCELLED but nothing clears the hash; re-import hard-fails at the pre-OCR `EXACT_HASH` check (`getByImageHash` has no status filter) before the graceful duplicate path | `BankStatementLifecycleProcessor.kt:162-176,838-867`; `ScannedReceiptDao.kt:111-112` |
| FRESH-P3-006 | MED | LIVE | Run status counts **skips as failures**: any `STATUS_SKIPPED` item → run FAILED + `PROCESSING_FAILED` + receipt stuck PARSED + `Result.failure`, even when 20 valid PendingReviews committed (duplicate-skips correctly yield COMPLETED_WITH_SKIPS) | `BankStatementLifecycleProcessor.kt:725-729,762-768,785-821` |
| FRESH-P3-007 | MED (privacy) | LIVE (default) | Camera path persists `parsedItems` (product names/quantities JSON) **unredacted in every storage mode** — policy itself treats parsedItems as raw content (`ReceiptPersistencePayload` nulls it under DO_NOT_STORE on other paths), default mode is STORE_RAW, and it survives debug export + DB backups (ExportAnonymizer nulls only `rawOcrText`) | `ReceiptRepository.kt:284-285` vs `ReceiptLifecycleCoordinator.kt:832-836`; `ReceiptPersistencePayload.kt:27-29`; `ExportAnonymizer.kt:107-117` |
| FRESH-P3-008 | LOW-MED | LIVE | Image saved before OCR; recognition failure orphans the file (no delete on any path) and the repository fallback writes a **second** copy — unreferenced JPEGs accumulate | `ReceiptOcrService.kt:230-238,277-280`; `ReceiptRepository.kt:216,289-298` |
| FRESH-P3-009 | LOW-MED | LATENT | Post-save auto-match calls `linkReceiptToExpense` without `requireUnmatchedClaim=true` → could stomp REJECTED→AUTO_MATCHED. Currently unreachable (that planner branch is email-only and email receipts never carry a match; worker path passes the claim) — latent dead-path hazard | `ReceiptSideEffectPlanner.kt:339-347`; `ReceiptLinkService.kt:157,252-261` |
| FRESH-P3-010 | LOW-MED | LIVE | AI blank currency → hardcoded `"EUR"` adopted wholesale when confidence>0.5 → wrong `PendingReview.suggestedCurrency` and currency-scoped dedupe that never matches (duplicate reviews/expenses) | `ValidateBankStatementTransactionsUseCase.kt:223`; processor `:247-251,625,530-546` |

### 2.5 Pipeline 4 — Recurring rules (6)

| ID | Sev | Reach | Why it is a problem | Files / systems |
|---|---|---|---|---|
| FRESH-P4-001 | **HIGH** | LIVE | Subscription delete bypasses the rule coordinator (raw `subscriptionDao.deleteById`): `RecurringOccurrence` has **no FK to the rule**, `PlannedExpense` only to Category, delivery CASCADE fires only on occurrence delete → up to ~12 months of pre-materialized PLANNED occurrences + SCHEDULED deliveries survive; the pending-deliveries query matches on occurrence status only (no rule join / isActive check) → **ghost bill notifications keep firing** after deleting a subscription. No repair sweep exists (BackupVerifier only *flags* orphans). Notifications are finite per materialized occurrence — no *future* ones generate — but nothing advances or cancels the standing ones either | `SubscriptionManagementRepository.kt:49-52`; `RecurringOccurrence.kt:12-21`; `RecurringReminderDeliveryDao.kt:53-60`; `SubscriptionManagementViewModel.kt:276-287` |
| FRESH-P4-002 | **HIGH** | LIVE | Subscription **toggle** (same screen) bypasses `deactivateRule`/`activateRule`: deactivating leaves SCHEDULED deliveries + PLANNED occurrences, and neither dispatch query checks rule `isActive` → **notifications continue after deactivation**, while the identical toggle on the manual-recurring screen behaves correctly. The architecture guard misses it (`RecurringArchitectureGuardTest` matches the variable name `manualRecurringExpenseDao.` — the `subscriptionDao` alias evades it) | `SubscriptionManagementRepository.kt:44-47,31-37`; `SubscriptionManagementViewModel.kt:256-271`; `RecurringLifecycleCoordinator.kt:960-966,105`; guard `:48-55` |
| FRESH-P4-003 | **HIGH** | LIVE | `updateRule` deletes **all** open PLANNED occurrences (no date filter) but regenerates only from `maxOf(nextDate, today)` → a past-due **unpaid** occurrence (rent due Aug 30, rule typo-fixed Sep 2) is silently destroyed, never re-created, its OVERDUE/DUE_DAY reminders gone. The only past-window regeneration path has **zero production callers** | `RecurringRuleLifecycleCoordinator.kt:286-303`; `RecurringOccurrenceDao.kt:44-45,66-67`; `RecurringLifecycleCoordinator.kt:1159-1177` |
| FRESH-P4-005 | MED | LIVE | Month-end anchor drift: chained `Calendar.add(MONTH)` clamps Jan-31 → Feb-28 → **Mar-28 permanently** (no re-anchor anywhere; `RecurrenceCalculator` drifts `rule.nextDate` identically). Occurrence dates, reminders and planned rows silently mis-date; keys stay unique so nothing errors | `RecurringOccurrenceExpander.kt:161-171`; `TimePeriodUtils.kt:1344-1348`; `RecurrenceCalculator.kt:107` |
| FRESH-P4-004 | LOW | DEAD CODE | `projectFromRule` passes raw `now` (not start-of-day) as generation start → due-today occurrence skipped; code contradicts its own comment. **Zero callers repo-wide** — latent | `RecurringPlanProjectionService.kt:62-80`; `RecurringLifecycleCoordinator.kt:111` |
| FRESH-P4-006 | LOW | LATENT | `FAILED_PERMISSION` is a dead-end delivery status (never reopened; not in TERMINAL_STATUSES) — but effectively **unwritable** in production (worker diverts permission-denials to cancelClaimed; pinned by test). Latent inconsistency | `RecurringReminderDeliveryDao.kt:163-181`; `RecurringLifecycleCoordinator.kt:71,1040`; `BillReminderWorker.kt:133-137` |

### 2.6 Pipeline 5 — Dashboard/synthesis (14) — all CONFIRMED

| ID | Sev | Reach | Why it is a problem | Files / systems |
|---|---|---|---|---|
| FRESH-P5-001 | **HIGH** | LIVE | Dashboard aggregates span the **2-month fetch window** but are consumed as current-month: TopCategories = prev+current combined; `monthlyIncome` includes last month's deposits (inflating `totalRemaining`/runway); MonteCarlo `spentToDate` includes all of last month. Only `monthAggregate` is clipped | `ComputeDashboardWidgetsUseCase.kt:368-397,605,611-612,737,771-772`; `DashboardContractsAdapter.kt:55-63` |
| FRESH-P5-002 | **HIGH** | LIVE | Pace pipeline dead: `paceStatus` hardcoded NO_BASELINE ("Will be refined by synthesis" is false — nothing writes it on the dashboard path; `buildSpendingPace` only feeds the legacy `assemble()`), gate at :1081 never passes → SpendingPaceWidget never rendered, OVER_PACE risk/insight branches unreachable | `ComputeDashboardWidgetsUseCase.kt:577,586,597,599,1081-1082`; `ForecastInputAssembler.kt:367-382,731`; `SynthesisEngine.kt:788,824` |
| FRESH-P5-003 | MED-HIGH | LIVE | Current MTD passed as `averageMonthlyTotal` (semantically historical) → early-month predicted discretionary collapses toward 0; the null-baseline confidence penalty becomes unreachable after the month's first purchase | `ComputeDashboardWidgetsUseCase.kt:575`; `SynthesisEngine.kt:288-292,407` |
| FRESH-P5-004 | **HIGH** | LIVE | `isSharedExpense` dropped at the adapter boundary (`DashboardExpense` lacks the field; DAO query has no shared predicate) → the documented NEW-P5-003 exclusion is tautologically true → shared-deposit repayments counted as personal income | `DashboardContractsAdapter.kt:179-191`; `DashboardPrimitives.kt:12-26`; `ExpenseDao.kt:178` vs `1315` |
| FRESH-P5-005 | MED | LIVE | 6-month trend from the 2-month fetch → M-2..M-5 always zero-filled | `ComputeDashboardWidgetsUseCase.kt:830-835` |
| FRESH-P5-006 | MED | LIVE | Home-currency change invalidates the repo cache but no dashboard recompute source depends on it (reload = midnight/retry/data-change only; `updateExpenseCurrency` is a stub) → widgets keep old-currency values all session | `MultiCurrencyRepository.kt:86-95,935-943`; `HomeViewModel.kt:216,222,228-248`; `DashboardDataProvider.kt:46-106` |
| FRESH-P5-007 | MED | LIVE | `runBlocking` per conversion inside Default-dispatcher dashboard compute (thread starvation under many patterns) + LATEST-basis conversions mixed with TRANSACTION_DATE spend in `discretionaryBudget` | `SynthesisEngine.kt:83-88,384-387`; `CurrencyConverter.kt:98-112,121` |
| FRESH-P5-008 | MED | LIVE | `runCatching` swallows CE in health scoring → raw mixed-currency fallback (:99/:383) or a **fabricated default all-50 score** (:93→ISE→catch(Exception)) that is persisted | `FinancialHealthScoreV2.kt:93,99-103,187,383-385` |
| FRESH-P5-009 | MED | LIVE | Four different home-currency failure behaviors: throw→empty charts, typed Unavailable→0.0 rows, catch→emptyList, average→0.0 — same failure, four user-visible outcomes | `MultiCurrencyRepository.kt:412-415,438-447,607-619`; `TotalsAggregationEngine.kt:633-646` |
| FRESH-P5-010 | LOW-MED | LIVE | Day-1 zero burn → runway 0 days CRITICAL despite full budget; `.toInt()` truncation flips CAUTION/HEALTHY; committed/likely expenses displayed but not deducted | `ComputeDashboardWidgetsUseCase.kt:603,617-628`; `TimePeriodUtils.kt:1306-1308` |
| FRESH-P5-011 | LOW | LIVE | Block-party history probed with `DAY_IN_MILLIS` keys against zone-aware buckets → post-DST days read as 0/neighbor-day (display-only; raw sums take precedence) | `ComputeDashboardWidgetsUseCase.kt:671-673`; `TimePeriodUtils.kt:145,452-459` |
| FRESH-P5-012 | LOW | LIVE | Uncategorized aggregate dropped from TopCategories but kept in the denominator → percentages under-sum, largest unknown bucket invisible | `ComputeDashboardWidgetsUseCase.kt:771-776` |
| FRESH-P5-013 | LOW | LIVE | MoM insight degenerate at `previousMonthTotal == 0` and systematically premature "spent less" early-month (MTD vs full-month) | `ComputeDashboardWidgetsUseCase.kt:1109-1136,448` |
| FRESH-P5-014 | LOW | LIVE | Monthly status average includes the partial current month → systematic UNDER_AVERAGE (excludeCurrent exists, used for YEAR only) | `TotalsAggregationEngine.kt:91,257,373-387` |

Tracked P5 items all re-confirmed: **P5-CURRENT-009** (block-party actuals drive UNDER/OVER_BUDGET status from raw sums — the "display-only" comment is wrong), NEW-P5-009/012/013 verbatim.

### 2.7 Pipeline 6 — Budget/forecast/cashflow (10) — all CONFIRMED

| ID | Sev | Reach | Why it is a problem | Files / systems |
|---|---|---|---|---|
| FRESH-P6-001 | **HIGH** | LIVE | Failed-rule fallback **double-counts**: materialized read keeps failed rule ids AND the fallback re-expands the same patterns with no dedup (CashFlowCalculator's identical fallback is saved by content dedup; the stress engine has none) → inflated `recurringObligations` → false HIGH/CRITICAL crunch risk. Constructible: projection is in-memory; materialized rows persist from earlier runs | `FinancialStressForecastEngine.kt:307-394`; contrast `CashFlowCalculator.kt:236-244,334-363` |
| FRESH-P6-002 | **HIGH** (release-only) | RELEASE | Autopilot reflects on private `expenseDao` field; **no keep rule exists** (proguard-rules.pro read in full; Dagger/Hilt consumer rules don't preserve injected field names; `-keepattributes *Annotation*` ≠ member names) → R8 renames → `NoSuchFieldException` → swallowed → empty history → `recommendedBudget` coerced to **−15% for every category budget**; apply-all commits it in one transaction. Debug builds and unit tests unaffected (test even pins 85% as "intended" empty-history behavior) | `BudgetAutopilotEngine.kt:238-245,290,102-125`; `app/build.gradle.kts:55`; `BudgetViewModel.kt:328-346`; `BudgetAutopilotEngineTest.kt:253-262` |
| FRESH-P6-003 | MED | RELEASE (same path) | The reflected query is `@Deprecated(ERROR)` raw mixed-currency SUM (`€300+$100=400`) → wrong trend/volatility even when reflection works; overall-budget path migrated, category path never did | `BudgetAutopilotEngine.kt:242-243`; `ExpenseDao.kt:1516-1530` |
| FRESH-P6-004 | MED | LIVE | Partial current month counted as a full history bucket (both callers pass `now`) → early-month averages collapse → −15% ratchet + understated predictions | `BudgetHistorySeriesBuilder.kt:48-51,76`; callers `:231,267,386,437` |
| FRESH-P6-005 | MED | LIVE | BudgetMonitor ignores `isPartial` in alert decisions and notification text → partial spend vs full limit reads as safe (and full limit-failure sets `percent=0f` which suppresses alerts entirely) | `BudgetMonitor.kt:243-244,301,318-352,400-435`; `BudgetRepository.kt:309-314` |
| FRESH-P6-006 | MED | LIVE | Rollover skipped whenever the limit aggregate is partial — **including the benign latest-rate fallback** → surplus silently vanishes; same flag also zeroes percent/remaining and forces UNKNOWN health | `BudgetRepository.kt:258,309-326,500-512` |
| FRESH-P6-007 | MED | LIVE | Forecast engine still mixes FX bases (limit PERIOD_END vs spend TRANSACTION_DATE) — the exact class fixed in BudgetRepository (P6-CURRENT-001) but never mirrored here → rate moves flip risk bands and overspend probability | `BudgetForecastingEngine.kt:122-129,185-186,206,603-639` |
| FRESH-P6-008 | LOW | LIVE | Overdue detected patterns contribute zero (while-gate) making the 90-day stale allowance dead in this path (CashFlowCalculator's catch-up loop is correct) | `FinancialStressForecastEngine.kt:418-433`; `CashFlowCalculator.kt:603-631` |
| FRESH-P6-009 | LOW | LIVE | Currency-blind fallbacks: fabricated `daysAhead*20.0` spend; 500/100/0 risk bands (¥ vs € ~150×) | `FinancialStressForecastEngine.kt:512-516`; `CashFlowCalculator.kt:426-431` |
| FRESH-P6-010 | LOW | LIVE | "Earliest crunch date" returns the horizon **end**; `/30.0*30.0` no-op | `FinancialStressForecastEngine.kt:622-630,647-648` |

Tracked: P6-P1-13 confirmed **half-done** (`StressForecastMode` exists but zero UI consumers — the honest labeling is never rendered); NEW-P6-010/013/015 confirmed verbatim.

### 2.8 Pipeline 7 — Backup/export/restore (11) — all CONFIRMED

| ID | Sev | Reach | Why it is a problem | Files / systems |
|---|---|---|---|---|
| FRESH-P7-003 | **HIGH** | LIVE (all devices) | `.costbackup` written via `java.io.File` into public Documents; manifest has no `MANAGE_EXTERNAL_STORAGE`/`requestLegacyExternalStorage`, targetSdk 35. Platform verdict: on Android 10+ scoped storage forbids it (mkdirs silently false, FileOutputStream throws); on API 26-28 `WRITE_EXTERNAL_STORAGE` (maxSdk 28) is a runtime permission **never requested**. → **The only user-facing backup path fails on every supported device**, after snapshot+verify succeeded, with a generic "Backup failed" | `DatabaseBackupRepositoryImpl.kt:657-665`; `CostbackupBundle.kt:292-299`; `AndroidManifest.xml:23-26`; `BackupRestoreViewModel.kt:89` |
| FRESH-P7-002 | **HIGH** | LIVE | ASSETS_RESTORING journal preserved forever; startup re-enters RESTART_REQUIRED and returns **before** the auto-reset block; nothing consumes `assetTasks` to resume → every launch shows the full-screen lock whose only action is `Runtime.exit(0)`. Reachable **without a crash**: cancelling asset restore rethrows CE before `exit()` | `AppStartupCoordinator.kt:71-78,209-217`; `DatabaseBackupRepositoryImpl.kt:1017-1021,1106-1108`; `MainActivity.kt:399-407`; `BackupRestoreViewModel.kt:181` |
| FRESH-P7-001 | MED-HIGH | DEBUG-caller | `resetDatabase` deletes the live DB while the journal says PREPARING; crash in the window → PREPARING classified **non-destructive** → journal deleted → app opens EMPTY with no CRITICAL lock; the safety backup exists but no recovery path references it. Sole caller is the debug screen | `DatabaseBackupRepositoryImpl.kt:2356-2454`; `RestoreJournal.kt:705-711`; `DebugViewModel.kt:511` |
| FRESH-P7-004 | MED | LIVE | `.pre_restore` (intact pre-swap DB) is never consulted as a recovery source and never cleaned up on failed/critical paths → missed recovery + DB-sized leak in `databases/` | `AppStartupCoordinator.kt:85-190`; `DatabaseBackupRepositoryImpl.kt:947-959,1029` |
| FRESH-P7-005 | MED | LIVE (power-loss window) | Safety copy not fsynced while the journal is → power loss can durably record SAFETY_BACKUP_CREATED with a partial safety file. Consequence is fail-closed (verify → CRITICAL), not silent corruption | `DatabaseBackupRepositoryImpl.kt:2342-2348`; `RestoreJournal.kt:531-540` |
| FRESH-P7-006 | MED | LIVE | Verification-failure rollback leaves staged DB+`-wal`/`-shm`; journal already renamed → startup `cleanStagingFiles` never fires → permanent plaintext staging files | `DatabaseBackupRepositoryImpl.kt:975,1096-1104` |
| FRESH-P7-007 | MED | Meta (future-brick) | PBKDF2 iterations (600k) compile-time, not in the bundle header → any future bump bricks every shipped backup, indistinguishable from a wrong password | `BackupEncryptionService.kt:46,117-134,159-164`; `CostbackupBundle.kt:38-43,198-259` |
| FRESH-P7-008 | MED | LIVE (narrow) | Outer catch after the file swap exits to NORMAL (`forceRestartRequired=false`) while the journal says SWAPPING → session writes against a DB that the next restart will roll back | `DatabaseBackupRepositoryImpl.kt:983,1105-1112` |
| FRESH-P7-009 | LOW | LIVE | Asset restore commits `imagePath` **before** the rename; failure deletes both copies → dangling DB pointer, image lost | `DatabaseBackupRepositoryImpl.kt:1248-1268,1033` |
| FRESH-P7-010 | LOW | LIVE | `commitJournal` overwrites the per-task asset ledger with the stale outer entry — success-journal diagnostics lost | `DatabaseBackupRepositoryImpl.kt:1024,1039-1040,1218-1325` |
| FRESH-P7-011 | LOW | LIVE | `ai_chat_sessions.title` (user-derived) not purged by ExportAnonymizer → redacted backups leak session titles | `ExportAnonymizer.kt:216-224`; `AiChatSessionEntity.kt:16` |

Tracked: **P7-P1-05 confirmed documented-intentional** (BackupVerifier.kt:20-33 spells out the planned Tier-0 SEMANTIC pass).

### 2.9 Pipeline 8 — Privacy/retention/cloud-AI (8)

| ID | Sev | Reach | Why it is a problem | Files / systems |
|---|---|---|---|---|
| FRESH-P8-001 | **HIGH** (privacy at rest) | LIVE | `transaction_events.beforeSnapshot/afterSnapshot` serialize the **full expense incl. merchant and notes** (`expenseToSnapshot` :2163-2184) and deliberately survive expense deletion; `deleteOlderThan` has **zero callers** and no retention target exists → deleted-expense PII persists indefinitely in the DB and ships in every `.costbackup` (TIER_1_EXACT) and the debug raw-DB copy | `TransactionEvent.kt:79-80`; `TransactionLifecycleCoordinator.kt:2046-2063,2115-2134,2163-2184`; `TransactionEventDao.kt:35-36`; `RetentionModule.kt:33-228`; `BackupVerifier.kt:94` |
| FRESH-P8-002 | MED-HIGH (privacy) | LIVE (default) | OCR purge nulls `rawOcrText` only; `parsedItems`/`parsedMerchant` keep receipt content forever — and policy itself classifies parsedItems as raw content (nulled under DO_NOT_STORE elsewhere); **default mode is STORE_RAW** so this is live, not latent | `ScannedReceiptDao.kt:155-164`; `ScannedReceipt.kt:54,71`; `ReceiptPersistencePayload.kt:27-29`; `PrivacySettings.kt:16` |
| FRESH-P8-003 | MED | LIVE | `operation_runs`/`operation_run_events`/`receipt_events`: zero delete queries, zero retention targets → unbounded growth (content sanitized at write, so drift not leak — but inconsistent with background_job_runs' 30-day redaction) | `OperationRunDao`, `OperationRunEventDao`, `ReceiptEventDao`; `RetentionModule.kt` |
| FRESH-P8-004 | MED (latent) | LATENT | `expiresAt = NULL` artifacts are purge-immune; all 7 current writers set TTL via the single `upsert` gateway, so it stays latent until a new writer forgets | `AiArtifactDao.kt:70-71`; `AiArtifactEntity.kt:62`; 7 use cases |
| FRESH-P8-005 | LOW-MED | DEAD CODE | Duplicate sanitizer copies in the warranty service predate the NEW-P8-004 PII patterns — a trap for the next caller (currently uncalled) | `CloudWarrantyExtractionService.kt:297-318,381+` |
| FRESH-P8-006 | LOW | BY DESIGN | `requireAllowed` handles 4 of 25 capabilities, throws on the rest — intentional fail-closed dev guard, but it means the "register capabilities" migration never happened and the next wiring crashes | `EffectiveCloudAiPolicy.kt:20-40`; sole caller `ReceiptLifecycleCoordinator.kt:296` |
| FRESH-P8-007 | LOW | LIVE | `privacy_audit_events` never purged (insert+getRecent only) → unbounded audit growth in DB and backups (content safe) | `PrivacyAuditDao`; `DataRetentionWorker.kt:117-126,196-214` |
| FRESH-P8-008 | LOW | LIVE | Phone regex floor is 8 chars (separator-bearing 7-digit numbers do match; bare 7-digit locals pass); merchant hash = unsalted truncated SHA-256 (dictionary-reversible in outbound payloads) | `CloudPiiSanitizer.kt:9,56-63`; `DefaultCloudPayloadRedactor.kt:44-46,64` |

Tracked: **P8-P1-12 adjusted** — a shared `PrivacyBlockedCard` component and a typed blocked state exist; the gap is inconsistent adoption (BackupRestore/ExportOptions/SpendingMap/Review ViewModels string-match "denied" or stringify results). **NEW-P8-006 confirmed** (permanent purge failures → `Result.success`; codified by `DataRetentionWorkerTest:206`; mitigated by the daily periodic re-run, missing only immediate-retry semantics). **RawPersistencePolicyResolver confirmed production-dead** (definition + doc comment only; 5 test files reference it). Test-debt found: `RawStoragePolicyAuditTest.retention_registry_covers_all_sensitive_targets` is tautological (checks its own 4-target set, never the registry) — see REVAL-3.

### 2.10 Pipeline 9 — Workers (9)

| ID | Sev | Reach | Why it is a problem | Files / systems |
|---|---|---|---|---|
| FRESH-P9-001 | **HIGH** | LIVE | Bill reminders post with raw `(delivery.id % Int.MAX_VALUE)` (tag=null); receipt-match alerts post `20000 + floorMod(receiptId,9999)` (tag=null). Android notification identity is (tag,id) — channel irrelevant → `delivery.id=20450` **replaces** the receipt alert for id 20450 (and vice versa); delivery ids 1-9999/10000-19999 similarly invade budget/warranty ranges. `NotificationIdGenerator.forBill` (30000+) exists with zero callers | `BillReminderWorker.kt:247`; `ReceiptMatchingWorker.kt:104`; `NotificationIdGenerator.kt:51-53`; `AndroidNotificationService.kt:84` |
| FRESH-P9-002 | MED | LIVE | Only the success terminal forwards ctx counters; `retry/failure/cancelled` signatures accept none (`WorkerRunLogger.kt:46-48`) → partial work systematically recorded as 0/0/0 on every non-success terminal (observability) | `WorkerExecutionGuard.kt:363-370,416-427`; `WorkerRunLogger.kt:44-48,173-186` |
| FRESH-P9-003 | MED | LIVE | `startRunSafely`'s generic catch returns **Retry in both branches** → a permanent run-row insert failure (e.g. SQLiteFull) retries forever with no run row and no FAILED terminal (per-attempt diagnostics do fire) | `WorkerExecutionGuard.kt:574-580,153,335` |
| FRESH-P9-004 | MED | LIVE | Retention: permanent target failures → run SUCCESS (codified by `DataRetentionWorkerTest:206`); crash-resume **skips audit events entirely** for pre-crash purges (`if (count>0)` guard against absent results); `_failed` checkpoint keys never read and wiped by `clearCheckpoint` | `DataRetentionWorker.kt:195-205,216-235,266,281,298` |
| FRESH-P9-005 | MED | LIVE | Intake `claimForProcessing` lacks a `nextAttemptAt` predicate → WorkManager's 30s exponential retry beats the in-row backoff (30s→1h) from the 2nd retry; attempts burn at WM pace → premature MAX_ATTEMPTS_EXCEEDED (recovery-scheduler path respects it; WM path doesn't) | `NotificationIntakeDao.kt:88-99`; `NotificationIntakeCoordinator.kt:136-142`; worker `:441-447` |
| FRESH-P9-006 | LOW | LIVE (near-unreachable trigger) | Null `getByKey` after successful claim strands the row CLAIMED; recovery sweep cadence is the worker's **24h** spec interval (not the 1h STALE_CLAIM_MS). Trigger requires an interleaved cascade delete or transient read failure | `WarrantyExpirationWorker.kt:185-193,219`; `WorkerSpec.kt:101-108` |
| FRESH-P9-007 | LOW | LIVE | NO_WORK predicate ignores `rowsSkipped`/`errors`; BillReminder never calls `addRowsScanned` → quiet-hour skips and all-claims-lost runs log SUCCESS/NO_WORK; ReceiptMatching counts notificationsSent without checking `DeliveryResult` (Warranty checks it) | `WorkerExecutionGuard.kt:361-371`; `BillReminderWorker`; `ReceiptMatchingWorker.kt:103-108` |
| FRESH-P9-008 | LOW | LIVE | 4 workers (Warranty/LocationBackfill/MerchantKeyBackfill/ReceiptMatching) omit `specVersion` from guard requests → NULL in `background_job_runs`, breaking spec-vs-run correlation | those 4 workers vs the 3 passers + intake's documented null |
| FRESH-P9-009 | LOW | META | Scheduler logs/comments cite a non-existent `ExistingWorkPolicy.UPDATE`; code (and tests) use REPLACE — doc/log drift, no functional defect | `WorkerSpecScheduler.kt:127-141,242-256`; `WorkerSpec.kt:26` |

### 2.11 Pipeline 10 — Bank sync (12) — **entire pipeline is DEBUG-stub-gated**

**Meta-adjustment (applies to all rows):** `BankApiConfig.isStubMode` is immutable `BuildConfig.DEBUG` and `syncTransactions` opens with `requireStubMode()` → in **release** builds the sync throws `IllegalStateException` immediately; the coordinator maps that to `RetryableFailure` and the ViewModel drops it silently. Every finding below is therefore real **code** but reachable only in debug builds today; release relevance is limited to the silent-swell error path (REVAL-10) and to whenever a real provider is wired.

| ID | Sev | Reach | Why it is a problem | Files / systems |
|---|---|---|---|---|
| FRESH-P10-001 | MED | DEBUG | Coordinator discards `SyncResult`; reauth/barrier/per-tx failures all return `ConnectionSyncResult.Success`; the VM maps every branch to `{}` — zero user feedback anywhere | `BankConnectionLifecycleCoordinator.kt:43-45`; `BankApiIntegration.kt:166,203,212,383-389`; VM `:45-49` |
| FRESH-P10-002 | MED (latent) | DEBUG | TRANSFER imports impossible: redacted default nulls `transferAccountName`; validator requires direction **and** non-blank name → guaranteed ValidationFailed. Latent: the mock generator emits only negative PURCHASEs, so no TRANSFER occurs today | `BankApiIntegration.kt:496-500,518,579-591`; `TransactionValidator.kt:141-156`; `PrivacySettings.kt:18` |
| FRESH-P10-003 | MED | DEBUG | `updateSyncStatus` zero callers → `lastSync`/`lastSyncStatus` never written → status icon shows "syncing" forever, "Last synced" never renders (`shouldSync` also has no production caller) | `BankConnectionDao.kt:42-43`; `BankConnectionsScreen.kt:230,265-287,461-474` |
| FRESH-P10-004 | LOW (dead code) | DEAD | `refresh()` collects an immortal Room flow with unreachable finally → stuck isLoading + full-screen spinner — but `refresh()` itself has **zero callers** (dead; fires only if wired) | `BankConnectionsViewModel.kt:75-87`; screen `:68-74` |
| FRESH-P10-005 | MED (latent) | DEBUG | `abs(amount)` sign loss → refunds become positive DEPOSIT-typed expense rows; `amount==0→UNKNOWN` always fails validation. Mock emits only negatives → latent | `BankApiIntegration.kt:270,512,543,548`; `TransactionValidator.kt:79` |
| FRESH-P10-006 | LOW-MED | DEBUG | No sync mutex/in-flight guard; check-then-insert PendingReview without unique index → concurrent syncs can duplicate reviews. Realistic window small: the full-screen spinner removes the button while loading | `BankConnectionsScreen.kt:68,243-248`; VM `:41-56`; `BankApiIntegration.kt:244-287` |
| FRESH-P10-007 | MED | DEBUG (constructible now) | STRICT dedupe key omits `bankAccountIdHash`; mock provider ids are **bank-scoped** → two connections to the same bank compute identical hashes → the second connection silently imports nothing | `TransactionLifecycleCoordinator.kt:107-116,507-508`; `Expense.kt:44` |
| FRESH-P10-008 | LOW-MED | DEBUG (thin) | `updateToken` has no optimistic lock; refresh writes from the stale start-of-sync snapshot (harmful variant needs the disconnect interleave) | `BankConnectionDao.kt:45-52`; `BankApiIntegration.kt:184,428-434` |
| FRESH-P10-009 | MED | DEBUG | Disconnect only nulls token columns; **no cleanup exists** for bank-source PendingReviews (`packageName = "bank.sync.<id>"`) → approvable reviews survive disconnect; token re-write onto a disconnected row is possible mid-sync | `BankConnectionDao.kt:39-40,45-52`; `BankApiIntegration.kt:280` |
| FRESH-P10-010 | LOW | DEBUG (not constructible) | Keystore taxonomy collapses `UserNotAuthenticated`/`KeyExpired` into permanent `Failed` — but the key spec sets neither auth-gating nor validity, so those exceptions can't occur for this key; diagnostics-only | `BankTokenCipher.kt:77-81,96-114`; `BankApiIntegration.kt:207-215` |
| FRESH-P10-011 | LOW | DEBUG | Low-confidence dedupe matches pending reviews of **all** sources (no packageName predicate) → a notification-sourced review suppresses a distinct bank transaction, unlinked | `PendingReviewDao.kt:530-640`; `BankApiIntegration.kt:244-262` |
| FRESH-P10-012 | MED | DEBUG | Stale-run recovery never wired: `recoverStaleRunningOperationRuns` + `getStaleRunningRuns/markStaleFailed` zero production callers → process death leaves BANK_SYNC/import runs RUNNING forever | `OperationRunRecorder.kt:145-197`; `BankStatementImportRunDao.kt:44,53`; `AppStartupCoordinator.kt:349-352` |

Tracked P10 partials (P10-P1-03/07/08/09) all re-confirmed as stated.

### 2.12 Pipeline 11 — Email receipts (10) — **pipeline production-unwired (confirmed exhaustively)**

**Meta-adjustment:** `EmailReceiptIngestionService` is injected/called **nowhere** in main sources (Hilt module provides parsers only; `saveEmailReceiptTyped` zero callers in main *and* tests). Yet `ReceiptRepository.kt:374` marks the delegation row "✅ Done" and ARCHITECTURE.md lists F14 as shipped — the docs overstate wiring. Every finding below is CODE-REAL but has zero production reach until an entry point exists; fix the parsers anyway (they're test-verified real), then wire.

| ID | Sev (when wired) | Reach | Why it is a problem | Files / systems |
|---|---|---|---|---|
| FRESH-P11-001 | HIGH | CODE-REAL | `Total\s*([^\n]{1,32})` has no boundary/anchor → leftmost `find()` matches the `Total` **inside "Subtotal"** (which precedes Total in every invoice) → expense = pre-tax subtotal, tax/shipping silently dropped; the "Total Amount" pattern is dead in both directions | `AppleReceiptParser.kt:25-32` |
| FRESH-P11-002 | HIGH | CODE-REAL | Currency indicators include bare `US`/`IT`/`ES`/`GR`/`DE`/`FR` matched as bounded tokens on the uppercased body, checked USD→EUR→GBP first-hit → the English words "us"/"it" flip a £ receipt to USD/EUR (Uber has no symbol fallback but defaults EUR) | `AppleReceiptParser.kt:66-70,207,210,239`; `UberReceiptParser.kt:81-85,242,251`; `EmailReceiptParser.kt:204-209` |
| FRESH-P11-003 | HIGH (wiring/docs) | DEAD | Pipeline has no production entry point while docs claim shipped → every defect in this pipeline is invisible to users and untested in production conditions | `EmailReceiptIngestionService.kt:52`; `di/EmailIngestionModule.kt:14-38`; `ReceiptRepository.kt:374`; ARCHITECTURE.md:1929 |
| FRESH-P11-004 | LOW-MED | CODE-REAL | Fingerprint date bucket = local-midnight parse ÷ UTC epoch hours → device **timezone change** (travel/tzdata) re-splits the bucket → forwarded mail re-ingests as new (senderDomain already splits too) | `EmailReceiptParser.kt:175`; `EmailReceiptIngestionService.kt:402,409` |
| FRESH-P11-005 | MED | CODE-REAL | HMAC key is a derivable constant (doc admits Keystore deferred); `hmacSha256Prefix("")` hashes empty → all blank-messageId emails share one hash → second email always `Duplicate` | `DefaultSensitiveHashingService.kt:22-27`; `PrivacyModule.kt:46-48`; coordinator `:762-768` |
| FRESH-P11-006 | MED | CODE-REAL | Total capture grabs VAT annotations → amount parse fails → falls through to the :30 catch-all which can match any line ending in a currency symbol → wrong amount booked or receipt dropped | `AppleReceiptParser.kt:25-32,156-171`; `AmountUtils.kt:139-143` |
| FRESH-P11-007 | LOW-MED | CODE-REAL | Amazon item extraction lacks Apple's total/tax skip list (mitigated: ITEM_PATTERN usually requires a quantity integer on those lines) | `AmazonReceiptParser.kt:174-197` vs `AppleReceiptParser.kt:266-269` |
| FRESH-P11-008 | LOW | CODE-REAL | STORE_RAW branch stores the HMAC **hash** as `emailMessageId` while the comment and sanitizer intend plaintext — `getByMessageId(rawId)` can never match | `ReceiptLifecycleCoordinator.kt:864-868`; `RawContentSanitizer.kt:70-79` |
| FRESH-P11-009 | LOW | CODE-REAL | Refunds: positive "refund" emails book a second PURCHASE; negative amounts rejected as unparseable (no review trail) | `AppleReceiptParser.kt:120`; `EmailReceiptIngestionService.kt:367-371`; coordinator `:960,1059` |
| FRESH-P11-010 | LOW | CODE-REAL | NeedsReview is a dead end: Duplicate short-circuit never re-evaluates a low-confidence receipt; the email path never deletes/reconciles its PendingReview rows | `ReceiptLifecycleCoordinator.kt:762-777,1051-1069`; `PendingReviewDao.deleteByScannedReceiptId` is photo-path-only (:368) |

Uncommitted-change assessment: the Apple regex un-escape (NEW-P11-2026-001) and sender-gated `canParse` (NEW-P11-2026-002) are **correct** and introduce no regression.

### 2.13 Pipeline 12 — Export/import/accounting (10)

| ID | Sev | Reach | Why it is a problem | Files / systems |
|---|---|---|---|---|
| FRESH-P12-001 | **HIGH** (contract) / blast-radius DEBUG | LIVE (export) / DEBUG (import) | Exporter quotes multi-line fields per RFC-4180 (`CsvCellSanitizer:55-59`; notes is a multi-line field by UI design) but the importer pre-splits `csvContent.lines()` before its own quote-aware parser → multi-line notes fail ("unclosed quote") and continuation fragments can parse as **spurious expense rows**. No roundtrip test feeds multi-line data back through the importer | `CsvExpenseImporter.kt:55,196-229`; `CsvCellSanitizer.kt:55-59`; `AddExpenseSheet.kt:351-356`; golden test `:48-52` (never re-imports) |
| FRESH-P12-002 | MED | DEBUG | UTF-8 BOM (Excel "CSV UTF-8" re-save) survives `trim()` → the `#` metadata line becomes the header → 100% import failure; `detectFormat` strips BOM for detection only and passes the original through | `CsvExpenseImporter.kt:60-68`; `ImportCoordinator.kt:28,39-44`; `ExportOptionsViewModel.kt:766` |
| FRESH-P12-003 | MED | DEBUG (+ interop) | `ofPattern("yyyy-MM-dd")` unpinned → non-ASCII-digit FORMAT locales fail every row; exporter date formatters equally unpinned → same-device roundtrips self-consistent, cross-device files break | `CsvExpenseImporter.kt:40,137-138`; `ExportOptionsViewModel.kt:396,429,506,746` |
| FRESH-P12-004 | MED | DEBUG | CSV import reads only `amount` + hardcodes PURCHASE despite exported `TransactionType`/`EffectiveAmount` columns → shared (25.50/22.50) and refund rows re-import as full-amount PURCHASE. JSON importer reads both; the roundtrip test pins the lossy behavior and its fixtures contain no shared/refund rows | `CsvExpenseImporter.kt:131,172`; `JsonExpenseImporter.kt:56,61`; `ExportImportRoundtripTest.kt:112-117,139` |
| FRESH-P12-005 | MED | LIVE | Xero/FreshBooks format the conversion **rate** with `%.2f` → rates < 0.005 export as "0.00" (audit destroyed) while generic CSV carries full precision for the same field | `AccountingExporters.kt:113-119,165-171`; `CurrencyFormatter.kt:79`; `ExportOptionsViewModel.kt:573-578,846` |
| FRESH-P12-006 | LOW | LIVE | `cancel()` without join + same-ms filename → a cancelled export keeps writing; double-launch can interleave on one temp file. Button `enabled=!isLoading` narrows the window (isLoading set inside the coroutine) | `ExportOptionsViewModel.kt:188-191,250,294,708-725`; `ExportDataRepository.kt:91-96` |
| FRESH-P12-007 | LOW | LIVE | `allowsEmptyDataset()` true for all 5 formats → the zero-rows guard is dead; header-only file = success (UI button disabled at count 0; programmatic path unprotected; repo helper's PDF=false is inverted-unused) | `ExportOptionsViewModel.kt:258,892-895`; `AccountingExportRepository.kt:222-227` |
| FRESH-P12-008 | LOW | Meta (future-brick) | `.enc` = salt+IV+ciphertext with compile-time 600k iterations, no version/KDF header — also used for DB backups, so a future param change bricks exports AND backups | `BackupEncryptionService.kt:26-35,46,80-99,117-134` |
| FRESH-P12-009 | LOW | LIVE | `filesDir/exports` never pruned; process death orphans plaintext `.tmp_*` financial files forever; finals accumulate unbounded | `ExportDataRepository.kt:94`; `ExportOptionsViewModel.kt:345-350`; no sweep exists |
| FRESH-P12-010 | LOW | DEAD | `ImportCoordinator` discards `perRowResults` — but has zero consumers (DebugScreen uses the importer directly and shows counts only) | `ImportCoordinator.kt:29`; `DebugScreen.kt:1446` |

Tracked P12 partials re-confirmed; **P12-P1-06 adjusted**: no Tag/Attachment entities exist anywhere in the codebase, so "tags/attachments absent from CSV" is vacuous — the real omission is receipt links (explicit TODO, `ExportTransaction:56-59`). **P12-P0-01 confirmed**: release builds ship exporters with **no import path at all** (debug-gated at `MainActivity:844-853`).

---

## 3. Tracked (old) issues — final re-verified status

| Item | Final status |
|---|---|
| U-MONEY-01 | Still partial, **narrowed**: TODO P2-20 verbatim, but SynthesisEngine converts per-item; the residual is raw `?:` fallbacks **on conversion failure** (:333,:345,:460,:478,:538,:554) mixing currencies into sums |
| U-BARRIER-02 | Still partial, **LOW outside stub mode**: `refreshToken`'s unbarriered `updateToken` is `@StubForDemo`-gated; all real entry points barrier-checked; call-site (non-interceptor) enforcement remains the design debt |
| U-PRIVACY-03 | Still partial, documented-intentional (= P7-P1-05) |
| NEW-P1-011 | Accepted debt (migration-pinned hash format) — do not "fix" |
| NEW-P2-005 | Still partial: atomic/audited but bypasses planner side effects; single caller `ReceiptLinkService:296` |
| NEW-P2-006 / NEW-P2-008 | Still partial → reframe as **dead dangerous API removal** (both `@Deprecated(ERROR)`/`@Restricted` with zero compilable production callers; tests pin non-use) |
| NEW-P2-016 | Still partial: 2 hot-path sites (create :470 / update :868) call raw `homeCurrency().first()` with no timeout while the timeout-protected `resolveHomeCurrency()` exists unused; ~49 raw sites repo-wide |
| P3-P1-05 | Downgraded: call site exists but the whole chain is production-dead (latent debt) |
| P3-P1-07 | **REFUTED as live bug** — dead code; delete the use case (closes the item) |
| P4-P1-05 | Confirmed accurate (occurrence key embeds sourceType\|sourceId; structurally collision-free) |
| P5-CURRENT-009 | Confirmed; **not display-only** (drives UNDER/OVER_BUDGET status) |
| P6-P1-13 | Confirmed **half-done**: provider + mode enum exist, zero UI consumers |
| P7-P1-05 | Confirmed documented-intentional |
| P8-P1-12 | Adjusted: shared `PrivacyBlockedCard` exists; adoption inconsistent across 4 ViewModels |
| NEW-P8-006 | Confirmed (codified by test); daily periodic re-run mitigates |
| P10-P1-03/07/08/09 | All confirmed as stated |
| P12-P1-02/03/05/07/08 | Confirmed; P12-P1-06 adjusted (vacuous tags/attachments; real gap = receipt links) |

---

## 4. New issues discovered during re-validation (REVAL series)

| ID | Sev | Finding | Evidence |
|---|---|---|---|
| REVAL-1 | MED (workflow) | The **uncommitted** P11 test file did not compile (`seventhArg` is not a MockK API) — the working tree cannot run tests as-is. Coordinator applied a 1-line fix (`args[6] as String`); disclosed here and part of the uncommitted set | `EmailReceiptIngestionServiceTest.kt:913` (pre-fix) |
| REVAL-2 | MED (test debt) | `ReceiptLifecycleCoordinatorTest` pins the **removed** camera-path side-effect dispatch (:234 stubs the post-commit runner) — test↔code mismatch; consistent with the ~10 failures observed in that class in the (weak-signal) run | `ReceiptLifecycleCoordinatorTest.kt:234` |
| REVAL-3 | MED (test debt) | `RawStoragePolicyAuditTest.retention_registry_covers_all_sensitive_targets` is **tautological** — checks its own 4-target set, never queries the 10-target registry; would catch none of the missing purge targets (P8-001/002/003/007) | `RawStoragePolicyAuditTest.kt:211-226` |
| REVAL-4 | LOW | `NotificationIntakeRecoveryScheduler` KDoc claims app-start/restore-complete sweeps that don't exist (only `onListenerConnected`) | `NotificationIntakeRecoveryScheduler.kt:26` |
| REVAL-5 | MED (docs) | Email ingestion documented as shipped ("✅ Done" `ReceiptRepository.kt:374:386`; ARCHITECTURE.md F14) while production wiring is absent — planning docs unreliable for F14 | see FRESH-P11-003 |
| REVAL-6 | MED (test debt) | `BudgetAutopilotEngineTest` codifies the −15% empty-history recommendation as intended behavior — the destructive default that P6-002's R8 failure triggers is enshrined | `BudgetAutopilotEngineTest.kt:253-262` |
| REVAL-7 | LOW (test debt) | `DataRetentionWorkerTest` codifies permanent-failure → `Result.success` | `DataRetentionWorkerTest.kt:206` |
| REVAL-8 | LOW | `MultiCurrencyRepository.updateExpenseCurrency` is a stub (writes nothing) — part of why currency changes never propagate (P5-006) | `MultiCurrencyRepository.kt:935-943` |
| REVAL-9 | META | Unit-test suite health: ~178 failures across ~70 classes at HEAD (mix of environment/locale-sensitive time-DST tests, outdated expectations per REVAL-2/6/7, and possible real regressions). Attribution not attempted — per stakeholder guidance the suite is **not** used as issue evidence until triaged. Compile (`:app:compileDebugKotlin`): **PASS** | `build/reval/test2.log` |
| REVAL-10 | LOW | Release-mode bank sync: `requireStubMode()` throws → coordinator maps to `RetryableFailure` → ViewModel drops it silently — the release error path is a silent swallow (design decision needed: hide the feature or surface the error) | `BankApiIntegration.kt:597-599`; coordinator `:43-45`; VM `:45-49` |

---

## 5. Architecture-invariant mapping (why these matter beyond the symptoms)

- **Lifecycle legal paths** ("rule mutations go through the coordinator"): violated by P4-001/002 (subscription repository is a parallel write path), NEW-P2-005, P3-009 (latent), and missed by `RecurringArchitectureGuardTest`'s name-based matching — the guard needs type/alias-based detection like the write-barrier guard's blind spot (U-004).
- **Worker rules** (idempotency, terminal integrity, reason codes): P9-002/003/004/005/007, P7-009, P10-012 — ledger trustworthiness and retry budgets.
- **Privacy fail-closed / retention**: P8-001/002/003/007, P3-004 (purge resurrection), P3-007, P1-002/005, P7-011 — the retention perimeter predates several data classes (snapshots, parsedItems, audit/diagnostic tables).
- **Money/currency rules** (no mixed-currency math): U-MONEY-01 residual, P5-001/004/008, P6-003/007/009, P12-005, P3-010.
- **Recovery/state machines**: P7-001/002/004, P10-012 — recovery paths exist but no consumer, or classify wrongly.
- **Documentation accuracy**: REVAL-5, P9-009, NEW-P1-003 KDoc — docs claim behavior that doesn't exist.

## 6. Working-tree note

The only coordinator-made change this session: `EmailReceiptIngestionServiceTest.kt:913` `seventhArg()` → `args[6] as String` (MockK has no `seventhArg`; required to compile the suite). It is part of the already-uncommitted NEW-P11 set and should be committed together with it.

## 7. Test-run note (weak signal, per stakeholder guidance)

`./gradlew :app:compileDebugKotlin` → PASS. `:app:testDebugUnitTest` first could not compile (REVAL-1, fixed); the subsequent run produced ~178 failures across ~70 classes before being stopped. Consistent with the stakeholder's assessment, these results were **not** used to confirm or refute any finding: many classes are time/DST/environment-sensitive (e.g. `TimePeriodUtilsT4C*`, `BudgetCalculatorTimeBoundaryTest`, `SpendingPersonalityClassifierTest` DST), several encode outdated expectations (REVAL-2/6/7), and attribution (machine vs code) was not established. Two overlaps worth noting as corroboration only: `WriteBarrierArchitectureGuardTest` was among the failures (consistent with U-004's static prediction) and `CancellationSafetyArchitectureGuardTest`/`DirectEventDaoInsertGuardTest` failures are consistent with the allowlist-hygiene findings (U-003). A dedicated triage of the red suite is recommended as its own work item before it can gate anything.

## 8. Updated remediation order

1. **Backup/restore integrity:** P7-003 (backup saving broken on all supported APIs — move to MediaStore/SAF), P7-002 (cancel → infinite restart lock), P7-001, P7-005.
2. **Ghost notifications / recurring loss:** P4-001, P4-002, P4-003 (subscription repo → coordinator; date-bounded deletion).
3. **Silent wrong numbers:** P5-001/002/003/004 (dashboard window/pace/baseline/shared-deposit cluster — one PR), P6-001/002/004 (stress double-count, R8 reflection, partial-month bias), P3-002, P11-001/002 (fix parsers now; wiring decision separately).
4. **Privacy at rest:** P8-001, P8-002, P3-004, P3-007, P7-011 (+ REVAL-3 test fix).
5. **Cancellation/guard hygiene:** U-001, U-002, U-003, U-004 (+ REVAL-2), P1-004.
6. **Notification identity & workers:** P9-001 (use `forBill`), P9-002/003/005, P1-001/002/003.
7. **Bank-sync decision:** REVAL-10 first (hide vs surface in release), then the P10 debug-cluster in one PR including P10-012.
8. **Import/export correctness:** P12-001…005 (+ decide import's product status: debug-only is currently implicit).
9. **Decisions needed:** P11-003 (wire email ingestion or re-document as staged), P12-P0-01 (import UX), NEW-P2-006/008 + P3-P1-07-remnant (delete dead dangerous APIs), P8-006 (finish capability registration).
