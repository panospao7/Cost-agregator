# RP-16 — Worker observability and notification identity

> **Mode:** strict (workers). **Status:** PARTIAL — code and tests authored for 16-A/16-B/16-C/16-D; two validation runs triaged (4 worker-test fixes + 2 migration-test fixes applied, re-run pending; remaining failures pre-existing or expected policy findings — see Failure triage).

## Implementation status (2026-09-20, lane RP-16 specialist-coder)

- **16-A — Typed notification identity: DONE (code + tests authored, validation NOT RUN).**
  `NotificationId` (private constructor, generator-only factories) and `NotificationKey` added in
  `domain/util/NotificationIdGenerator.kt`. BillReminderWorker now posts with
  `NotificationId.forBill(delivery.id)` (was raw `(delivery.id % Int.MAX_VALUE)`); PendingIntent
  request codes derive from the typed ID. Kind-specific mediated posting helpers (`postBudgetAlert`,
  `postAiBriefingReady(+WithResult)`, `postAnomalyAlert`) added to `NotificationService` accepting
  only `NotificationId` (distinct names avoid overload ambiguity with legacy Int methods).
  AnomalyAlertOrchestrator switched from raw `100000 + id%100000` to `NotificationId.forGeneral` (D6).
- **16-B — Terminal ledger counters: DONE (code + tests authored, validation NOT RUN).**
  D9 applied: immutable `WorkerRunCounters(rowsScanned, rowsUpdated, notificationsSent, rowsSkipped,
  errors, partialFailureCount, failedTargetCount)` + `WorkerRunContext.snapshot()`. All terminal APIs
  gained an optional `snapshotProvider` invoked under the terminal mutex immediately before
  persistence; scalar-only mapping to `BackgroundJobRunDao.completeTerminal` (no data object /
  COALESCE). `rowsSkipped`/`errors` are DURABLE columns (`INTEGER NOT NULL DEFAULT 0`).
  Room version **148 → 149** with additive `MIGRATION_148_149`, hand-authored `149.json` snapshot
  (identityHash is regenerated by KSP on next build), migration tests, and chain/registration tests.
  Post-run correction: `rowsSkipped`/`errors` moved to the END of the `BackgroundJobRun` entity (and
  `149.json` field/createSql order updated to match) so the fresh-install CREATE TABLE column order
  equals the `ALTER TABLE ADD COLUMN` append order produced by `MIGRATION_148_149` (fresh-vs-migrated
  schema parity; see Failure triage).
- **16-C — Retention checkpoint state machine: DONE (code + tests authored, validation NOT RUN).**
  `RetentionCheckpointStore` (v2 records: PENDING / COMPLETED(rowsPurged, auditEmitted) /
  FAILED(controlledErrorCode, transient, attempts)) with legacy boolean compatibility (booleans read
  as booleans only; never reinterpreted as counts). Exactly-once audit identity via
  `PrivacyAuditDao.countAuditEventsByKey` over "[retention-audit:<target>:<cutoffDay>]" appended to
  the audit reason. Per-record cleanup only after COMPLETED+audit durable; FAILED records persist for
  the next scheduled run (permanent-failure policy); transient failure aborts the run as RETRY and
  records persist for resume. NO blanket preferences clear. Barrier denial during audit emission still
  propagates (RP-02 U-004). Partial/failed target counts fed into ctx counters (D9).
- **16-D — Bounded repairs: DONE (code + tests authored, validation NOT RUN).**
  `claimForProcessing` gained the `nextAttemptAt` backoff predicate; warranty absent-after-claim is
  returned to retryable FAILED via new `WarrantyReminderDeliveryDao.markFailedByKey`; required
  specVersions added (receipt_matching, location_backfill, merchant_key_backfill, warranty_expiration_check);
  NO_WORK now includes skipped/errors; BillReminder counts scanned work; ReceiptMatching counts only
  DELIVERED results and records `RECEIPT_MATCH_NOTIFICATION_SUPPRESSED_NOT_DELIVERED` suppression.

## Invariants

Preserve WorkerExecutionGuard, barriers, idempotency, cancellation propagation, sanitized diagnostics, and metrics only after actual delivery. Retention cleanup is never gated by the retention capability it enforces.

## 16-A — Typed notification identity

Use `NotificationIdGenerator.forBill(...)` in BillReminderWorker. Establish a typed allocation boundary (`NotificationId`/`NotificationKey`) accepted by AndroidNotificationService, or notification-kind-specific service methods whose IDs can only originate from the generator. A source scan is supplementary only.

Tests: all reserved ranges; cross-kind collision separation; negative/large IDs and floor-mod wrap; helper-mediated posting cannot pass raw IDs; bill delivery metric increments only after actual delivery.

## 16-B — Terminal ledger counters

Add immutable `WorkerRunCounters(rowsScanned, rowsUpdated, notificationsSent, rowsSkipped, errors, partialFailureCount, failedTargetCount)` and `WorkerRunContext.snapshot()`. Capture snapshot immediately before success, retry, failure, cancellation, stale abort, and blocked/skipped terminal persistence under the terminal mutex.

Extend terminal APIs with optional snapshot/defaults for old callers. Map only scalar arguments to DAO columns—never `COALESCE(:counters, 0)` or a Room data object.

`rowsSkipped` and `errors` are not durable columns today. Decide explicitly before code: add columns with Room version, migration, snapshots and migration tests, or retain them exclusively in diagnostics/in-memory NO_WORK logic. Non-null snapshot means measured; null partial/failed means unknown/not supplied; zero means measured zero. Terminal-write failure leaves a safe non-durable state/diagnostic, never false completion. Start-run errors: transient retries; permanent errors map to controlled failure/blocked, not endless retry.

Tests: success, retry after partial work, permanent failure, cancellation, stale abort, old defaults, null/zero compatibility, terminal-write failure, and NO_WORK including skipped/errors.

## 16-C — Retention checkpoint state machine

Replace boolean `completed_<target>`/ignored failed-key semantics with versioned per-target records: `PENDING`, `COMPLETED(rowsPurged, auditEmitted)`, and `FAILED(controlledErrorCode, transient, attemptMetadata)`. Read legacy booleans compatibly before writing v2; never reinterpret booleans as counts.

Define resumed states, permanent-failure scheduled-run policy, exactly-once audit identity, and cleanup only when all desired states/audits are durable. No blanket preferences clear. Feed partial/failed target counts into the chosen ledger contract.

Tests: legacy keys; purge→checkpoint failure; checkpoint→audit crash; cancellation; transient/permanent failure; rerun; exactly-once audit; no payload/error-message leakage.

## 16-D — Bounded repairs

Add `nextAttemptAt` predicate to notification intake claims. Return an unexpectedly absent claimed warranty item to retryable/failed state. Add required specVersions. NO_WORK includes skipped/errors; BillReminder counts scanned work; ReceiptMatching counts only `DELIVERED` notification results and records controlled non-delivery error/suppression.

## Gates and completion

Order: 16-A → 16-B schema decision/API → 16-C → 16-D. Any Room change requires version, migration, snapshot, and migration test. Run focused worker tests when approved; strict worker/architecture review is required after each risky batch.

## Validation

- **NOT RUN (for the post-triage fixes)** — lane RP-16 is static-edits-only; no Gradle/validation-runner
  invocation from this agent. Prior validation-runner runs:
  `vr-20260920-215450-2e855315` (`targeted-unit-test`, `--tests *Worker*`): 381 passed, **10** final
  failures (triaged 2026-09-20 below); `vr-20260920-230554-58d8ed37` (`migration-tests`,
  `--tests *Migration*`): 13 tests — **6 passed, 7 failed** (the run brief reported 6 failed; the log
  shows a seventh: `ALL_MIGRATIONS includes migration 141 to 142`. Triaged 2026-09-20 below).

### Failure triage (2026-09-20, lane RP-16 specialist-coder)

- **FIXED (test corrections only; production code unchanged; no assertion weakening — each fix pins plan semantics):**
  - `WorkerRunCountersTerminalTest > dao completeTerminal maps only scalar counter arguments` —
    reflection mismatch: `partialFailureCount`/`failedTargetCount` are `Int?` per D9 null-vs-zero
    semantics and reflect as boxed `java.lang.Integer`, not primitive `int`. Expected list corrected
    to `Integer::class.java` for those two (scalar-only pin unchanged) plus a `!it.isSynthetic` bridge filter.
  - `WorkerRunCountersTerminalTest > old callers without snapshot keep legacy defaults` — test bug:
    a `WorkerRunHandle` is once-terminal (PR12B mutex state machine), so `skipped()` after a durable
    `success()` on the same handle never reaches the DAO. SKIPPED leg moved to a second run
    (`insert` returns 1L then 2L); both legacy-default rows verified against their own runId.
  - `WorkerRunCountersTerminalTest > context snapshot is immutable after mutation` — test bug: the
    captured `WorkerRunCounters` snapshot assertions (1/2/3) were already passing; the extra
    assertions wrongly required the LIVE context to stay frozen. Corrected to assert the context
    continued accumulating (101/202) — mutation affects the context, never the captured snapshot.
  - `WorkerRunLoggerTest > retry_persists_terminal_reason_code_and_classifies_diagnostic` — matcher
    position bug: `retry()` persists the safe reason in `retryReason` and leaves `statusReason` null
    (only `failure()` writes `statusReason`). Positional verify corrected to
    `isNull()` (statusReason) + `eq("WORKER_UNHANDLED_EXCEPTION")` (retryReason); errorClass
    `TimeoutCancellationException` and diagnostic `TIMEOUT` pins unchanged.
- **PRE-EXISTING, NOT lane-caused, NOT fixed (untouched by the lane diff — verified via `git diff`):**
  - `DailyBriefingWorkerTest > timeout_after_delivery_returns_retry` — one-off environmental flake in
    the real-time helper `fakeTimeoutCancellationException()` (`runBlocking { withTimeout(1) { delay(10) } }`),
    failing during test setup before any lane-touched code runs; the identical helper call in
    `timeout_after_delivery_retry_marks_artifact_delivered_once` (eager, line 696) and four lazy
    call sites PASSED in the same run. File and `DailyBriefingWorker` are untouched by the lane.
  - `WorkerRestoreBarrierIdempotencyGoldenTest > write barrier is idempotent...` — deterministic
    `MockKException: no answer found for RestoreMaintenanceMode.currentMode()`; the golden test stubs
    only `isWritesAllowed()`, while `DatabaseWriteBarrier.checkWritesAllowed` calls `currentMode()`
    (`data/backup/` and `golden/` untouched by the lane). Requires a base-side test-fixture update,
    out of lane scope.
  - `DbGuardPolicyFixtureTest` ownership entries — **the log shows FOUR such findings, not two**:
    `DataRetentionWorker.doWork` (expected `privacyAuditDao`, policy `daoAccessor: auditDao`),
    `WorkerRunLoggerImpl.start` and `Handle.terminal` (expected `backgroundJobRunDao`, policy `daoAccessor: dao`),
    and `WarrantyExpirationWorker` (expected `warrantyReminderDeliveryDao`, policy `daoAccessor: deliveryDao`).
    All four are the SAME disclosed pre-existing drift class: the test fixtures still expect legacy
    canonical DAO names while the committed v2 `config/guards/db_ownership_policy.yml` (base commit
    `55d89710`, untouched by the lane) spells source-level accessor aliases; both files are unmodified
    by this lane, so these failures exist identically at HEAD. This matches the NEEDS-HUMAN FG-06/07
    item recorded in `workflows/active/handoff-test-diagnosis-20260919.md`. Hand-editing config/guards
    is forbidden; resolution is the sanctioned policy-regeneration flow. Additionally, the lane's new
    mutation identities (`PrivacyAuditDao.countAuditEventsByKey`, `WarrantyReminderDeliveryDao.markFailedByKey`,
    updated `completeTerminal` parameterTypes) must be folded into that same regeneration at gate time.

### Migration-suite triage (2026-09-20, run `vr-20260920-230554-58d8ed37`, static fixes applied)

- **FIXED (schema-parity root cause; migration SQL unchanged — still additive, version still 149):**
  - `DatabaseMigrationProofTest > fresh_install_schema_matches_migrated_schema` — the lane had declared
    `rowsSkipped`/`errors` in the MIDDLE of `BackgroundJobRun` (after `notificationsSent`), so the
    entity-defined fresh-install CREATE TABLE (`149.json`) ordered them at positions 9–10, while
    `MIGRATION_148_149`'s `ALTER TABLE ADD COLUMN` appends them at the END (after
    `failedTargetCount`). Exact diff: fresh had `... notificationsSent, rowsSkipped DEFAULT 0,
    errors DEFAULT 0, retryReason, ... failedTargetCount`; migrated had
    `... notificationsSent, retryReason, ... failedTargetCount, rowsSkipped DEFAULT 0,
    errors DEFAULT 0`. Fix: moved the two fields (with their `@ColumnInfo(defaultValue = "0")`)
    to the END of `BackgroundJobRun` (`entity/BackgroundJobRun.kt`) and reordered `149.json`
    accordingly (fields array + `createSql`); all constructor/`copy` call sites use named
    arguments (verified by grep), so no caller breaks. Column names/types/affinities/NOT NULL/
    defaults and all three indices already matched — order was the sole difference.
  - `Migration148to149Test > migration 148 to 149 adds rowsSkipped and errors columns with zero
    defaults` — the test's second seed INSERT
    (`INSERT INTO background_job_runs (id, workerName, startedAt, status) VALUES (2, ...)`) omitted
    `rowsScanned`/`rowsUpdated`/`notificationsSent`, which are `NOT NULL` with NO SQL default in
    both v148 and v149 (Kotlin `= 0` defaults are not column defaults) — SQLite correctly rejected
    it (`NOT NULL constraint failed: background_job_runs.rowsScanned`). Fix: the INSERT now supplies
    those three pre-existing NOT NULL columns (0/0/0); it still omits `rowsSkipped`/`errors`, so the
    pin under test — the new columns' `DEFAULT 0` backfill on an insert that doesn't name them — is
    unchanged. No assertion weakened.
- **PRE-EXISTING at HEAD, NOT lane-caused, NOT fixed (per lane rules; all evidence from the run log +
  `git diff HEAD`, which touches only `MIGRATION_148_149`/`DatabaseMigrations.ALL`/version 149):**
  - `MigrationRegistrationTest > ALL_MIGRATIONS includes migration 142 to 143` (assert
    "Migration 142→143 must be registered in ALL_MIGRATIONS", MigrationRegistrationTest.kt:82),
    `> ALL_MIGRATIONS includes migration 117 to 119` (asserts 117→118 AND 118→119, lines 60–67) and
    `> ALL_MIGRATIONS includes migration 141 to 142` (line 73): the test reads
    `AppDatabase.ALL_MIGRATIONS`, which is `= DatabaseMigrations.ALL` (AppDatabase.kt:8757). At HEAD
    that array was `arrayOf(MIGRATION_145_146, MIGRATION_146_147, MIGRATION_147_148)` (per the lane
    diff's removed line) — the documented v145 migration-baseline consolidation
    (`DatabaseSchemaPolicy.MIGRATION_BASELINE = 145`). The lane only APPENDED `MIGRATION_148_149`,
    so these assertions fail identically at HEAD: they pin a pre-baseline expectation that the
    117/118/141/142/143 steps no longer exist as registered migrations. The complementary chain tests
    (`production_ALL_MIGRATIONS_forms_continuous_chain`, `migration_145_to_current_chain_passes`) PASS,
    confirming the baseline policy is self-consistent. Fixing this requires a base-side test decision
    (update the stale assertions to the v145 baseline), not a lane change.
  - `MigrationRegistrationTest > migration 120 to 121 creates group_lifecycle_events table` —
    `helper.createDatabase(TEST_DB_NAME, 120)` builds from the lane-UNTOUCHED `120.json`, whose
    `expenses` table has no `effectiveAmount` column (verified: 39 columns, no `effectiveAmount`);
    the test's seed INSERT at MigrationRegistrationTest.kt:32 therefore fails with
    `table expenses has no column named effectiveAmount`. Test file and `120.json` are identical at
    HEAD (`git status`: neither modified; the lane adds only `149.json`).
  - `MigrationRegistrationTest > migration 119 to 121 creates both settlement and lifecycle tables` —
    `runMigrationsAndValidate(…, 121, …, MIGRATION_119_120, MIGRATION_120_121)` fails Room's
    structural validation of `pipeline_diagnostic_events` against lane-untouched `121.json`
    (`Migration didn't properly handle: pipeline_diagnostic_events`). Both migrations live in
    `AppDatabase.kt:7628/7653`, whose only lane diff is the version constant (148→149); `121.json`
    is unmodified. Identical behavior at HEAD.
- Re-run required: all six FIXED tests — the four worker tests (`targeted-unit-test`
  `*WorkerRunCountersTerminal*`, `*WorkerRunLogger*`) and the two migration tests
  (`migration-tests` profile, `--tests *Migration*`) — need a validation-runner pass before any
  DONE claim.
- Recommended targeted profiles (execute through `validation-runner` when approved):
  `*NotificationIdGenerator*`, `*WorkerRunLogger*`, `*WorkerRunCountersTerminal*`,
  `*WorkerExecutionGuard*`, `*Migration148to149*`, `*DatabaseMigrationProof*`,
  `*RetentionCheckpointStore*`, `*DataRetentionWorker*`, `*WorkerClaimRepairDao*`,
  `*BillReminderWorker*`, `*ReceiptMatchingWorker*`, `*WarrantyExpirationWorker*`,
  `*AnomalyAlertOrchestrator*`, plus `compile` first.
- Schema snapshot `149.json` is hand-authored; KSP regenerates the identityHash on the next build
  (same precedent as the hand-authored 142.json/143.json bundles).
- Open items for review: merge reconciliation of APP_DATABASE_SCHEMA_VERSION if parallel lanes bump
  the version; PENDING batches: none — all four batches implemented.
