# Deep Analysis — Batch 10: AI Services - Hybrid + Workers (@debugger)

## Scope
- domain/ai/service/HybridReviewExplanationService.kt
- domain/ai/service/HybridReceiptItemCategorizationService.kt
- domain/ai/service/HybridServiceDelegationModels.kt (not found in codebase)
- domain/ai/service/SmartReceiptAssistService.kt
- domain/ai/provider/internal/CloudJsonParser.kt
- domain/ai/provider/internal/CloudPiiSanitizer.kt
- domain/ai/provider/internal/CloudCorrelation.kt
- domain/ai/provider/internal/CloudRetryPolicy.kt
- data/ai/worker/DailyBriefingWorker.kt
- data/ai/worker/AiWorkerModels.kt (not found in codebase)

## Issues Found

| # | File:Line | Severity | Type | Description | Reproduction Steps | Suggested Fix |
|---|-----------|----------|------|-------------|-------------------|---------------|
| 1 | HybridReceiptAssistService.kt:24,30,45 | 🔴 High | Race Condition / State Corruption | `lastUsedImageInput` is a mutable `var` on a `@Singleton`. If two coroutines call `suggest()` concurrently, one may read `lastUsedImageInput` set by the other's call. For example: coroutine A does cloud path (sets `lastUsedImageInput = true`), coroutine B does on-device path (sets `lastUsedImageInput = false`), then coroutine A's caller reads `usedImageInput()` and gets `false` — wrong answer. | Two concurrent receipt assist requests from different receipts. | Either (a) make `usedImageInput` return value part of the `AiServiceResult` itself (no shared state), or (b) use a `ConcurrentHashMap<ReceiptAssistInput, Boolean>` keyed by input, or (c) remove this state and delegate to the underlying service's `usedImageInput()`. |
| 2 | SmartReceiptAssistService.kt:168,174,178-180 | 🟡 Medium | Logic Bug — Asymmetric fallback chain | `isOnDeviceRouteEligible(route)` returns `true` only when `route == AiRoute.ON_DEVICE`. This means when the router selects `AiRoute.CLOUD`, the retry chain attempts Cloud Vision (1) → Cloud Text (3) but **never** attempts On-Device Vision (2) or On-Device Text (4). The documented 5-step retry chain (lines 25-29) promises "2. On-Device Vision AI" and "4. On-Device Text AI" as fallbacks, but they are unreachable when the route is CLOUD. If cloud fails twice, it jumps directly to deterministic fallback. | Set route to `AiRoute.CLOUD`, give an image input, mock cloud vision and cloud text to both return low-confidence results. On-device attempts are skipped entirely; the user gets deterministic fallback instead of trying on-device. | Either relax `isOnDeviceRouteEligible()` to also return `true` for `AiRoute.CLOUD` (allowing cross-route fallback), or update the KDoc to accurately describe the actual behavior (no cross-route fallback). |
| 3 | SmartReceiptAssistService.kt:79,94,109,122 | 🟡 Medium | Swallowed Failures — Silent Fallback Without Diagnostics | When a `shouldAttempt*` check returns `false`, no attempt detail is recorded. The `attempts` list will have gaps in attempt numbers (e.g., [1, 5] skipping 2-4). Downstream consumers relying on `attemptDetails` to understand what was tried get incomplete data. | Route = ON_DEVICE. Attempt 1 (Cloud Vision) is skipped, attempt 3 (Cloud Text) is skipped. `attemptDetails` shows only attempts 2, 4, 5 with no record of why 1 and 3 were not tried. | Add a "skipped" `AttemptDetails` entry when `shouldAttempt*` returns false, with `success=false` and `errorMessage="skipped_by_route_policy"`. |
| 4 | CloudRetryPolicy.kt:40 | 🟡 Medium | Integer Overflow Risk | `BASE_RETRY_BACKOFF_MS shl (safeAttempt - 1)` uses Long shift. For `safeAttempt` values > 62 (if callers pass very large values), the shift wraps to 0 or negative. While current callers cap at `MAX_RETRY_ATTEMPTS = 3`, the function is `public` and has no upper bound validation. A future caller passing `attempt = 64` or higher would produce `0L + jitter` instead of the expected capped backoff. | Call `backoffDelayMs(attempt = 64)` — returns a near-zero delay instead of `MAX_RETRY_BACKOFF_MS`. | Add `val safeShift = (safeAttempt - 1).coerceAtMost(62)` before the shift operation, or cap the shift amount: `(safeAttempt - 1).coerceIn(0, 10)`. |
| 5 | CloudPiiSanitizer.kt:8 | 🟡 Medium | PII Leak — Weak Card Regex | `CARD_REGEX = Regex("""\b(?:\d[ -]?){13,19}\b""")` uses `\b` word boundaries. In receipt text like `"Total:4111111111111111"`, there is no word boundary before the digits (they are adjacent to `:`), so the card number **will not be matched and not redacted**. Similarly, card numbers at start/end of text or adjacent to other non-word chars may slip through. | Pass `"Total:4111111111111111 paid"` to `sanitizeText()`. The card number remains in the output unredacted. | Remove `\b` boundaries or use a lookaround: `Regex("""(?<!\d)(?:\d[ -]?){13,19}(?!\d)""")` |
| 6 | CloudPiiSanitizer.kt:9 | 🟡 Medium | PII Leak — Overly Broad Phone Regex | `PHONE_REGEX = Regex("""\+?\d[\d\s().-]{6,}\d""")` is very greedy and will match monetary amounts like `"1,234.56"` or `"12345678"` (order IDs). But more critically, it may fail to match valid short phone numbers (e.g., `"+301234"` = 7 chars including `+` is 6 between first/last digit). The regex also lacks `\b` boundaries, so it matches into surrounding text aggressively, potentially corrupting output. | Input: `"Contact: +30 210 1234567, total: 1234.5678"` — the total gets redacted as a phone number, corrupting the receipt data. | Tighten the regex with phone-specific patterns, or apply order-of-operations (run card regex first, then phone regex only on unmatched regions). |
| 7 | CloudReviewExplanationService.kt:181-215 | 🟡 Medium | PII Leakage — Unsanitized Prompt Data | `buildPrompt()` directly interpolates `input.merchant`, `input.amount`, `input.packageName`, `input.notificationTitle`, `input.notificationText` into the prompt sent to the cloud **without any PII sanitization**. The `CloudPiiSanitizer` exists but is not used here. The `notificationText` often contains personal banking details (account numbers, card last-4 digits, balances). | User has `redactBeforeCloud = true` in settings. A review explanation is generated. The merchant name, notification text (potentially containing account info), and package name are sent raw to Google's API. | Apply `CloudPiiSanitizer.sanitizeText()` to notification fields and `CloudPiiSanitizer.sanitizeMerchant()` to the merchant field, respecting the user's `settings.redactBeforeCloud` preference (which needs to be threaded through to this service, similar to how `CloudReceiptAssistService` does it). |
| 8 | DailyBriefingWorker.kt:56-61 | 🟡 Medium | Swallowed Exception — Always Returns Success | The catch block catches all `Exception` types and returns `Result.success()`. While the comment explains this is intentional (to avoid WorkManager exponential backoff), it means `CancellationException` is also caught and swallowed. In Kotlin coroutines, catching `CancellationException` prevents proper cooperative cancellation, meaning the worker cannot be cancelled by the system or user. | System cancels the worker (e.g., `cancelUniqueWork()`). The `CancellationException` is caught, logged, and `Result.success()` is returned instead of properly propagating cancellation. | Re-throw `CancellationException`: `catch (e: CancellationException) { throw e } catch (e: Exception) { ... Result.success() }`. Or use `catch (e: Exception) { if (e is CancellationException) throw e; ... }`. |
| 9 | DailyBriefingWorker.kt:46-48 | 🟢 Low | Potential Indefinite Hang | `dashboardDataProvider.getProcessedDataFlow(analyticsRepository).first()` collects the first emission from a `Flow` that uses `flatMapLatest` and multiple `combine` operators. If any upstream flow (expenses, categories, budgets, weather, etc.) **never emits** (e.g., a Room DAO query that blocks forever, or a flow that errors before emitting), `.first()` will **suspend indefinitely**. WorkManager has a 10-minute execution limit, but the worker will be stuck the entire time. The `catch { emit(default) }` clauses in `DashboardDataProvider` help, but they only catch errors — a flow that simply never emits is not caught. | One of the underlying Room DAO flows hangs (database lock contention, or the DAO method blocks). The worker hangs until WorkManager kills it at the 10-minute mark. | Add `withTimeoutOrNull(30_000)` around the `.first()` call, and handle the `null` case gracefully. |
| 10 | HybridReceiptItemCategorizationService.kt:29 | 🟢 Low | Inconsistent Fallback Handling | Uses `else -> null` for `DETERMINISTIC_FALLBACK` and `DISABLED` routes, while other hybrid services (e.g., `HybridReviewExplanationService`, `HybridCategorizationAssistService`) explicitly enumerate both branches. The `else` branch silently swallows any **new** `AiRoute` enum values added in the future — the compiler won't warn about unhandled cases. | Add a new `AiRoute` enum value (e.g., `AiRoute.EDGE`). All hybrid services with explicit `when` branches get compiler warnings. `HybridReceiptItemCategorizationService` silently returns `null`. | Replace `else -> null` with explicit branches: `AiRoute.DETERMINISTIC_FALLBACK, AiRoute.DISABLED -> null` to get exhaustive `when` checks. |
| 11 | CloudJsonParser.kt:57-62 | 🟢 Low | Potential Stack Overflow — Recursive Call | `extractFencedJsonObject` calls `extractFirstJsonObject(fencedBody)`, and `extractFirstJsonObject` calls `extractFencedJsonObject(text)` first. If the fenced content itself contains a ```json fence, this creates a recursive loop. While unlikely in practice (the inner fence would need to re-match the regex), it's a defensive coding concern. For deeply nested fenced blocks it could recurse significantly. | Input: ` ```json ```json {"a":1} ``` ``` ` — The outer fence extracts ` ```json {"a":1} ``` `, then `extractFirstJsonObject` calls `extractFencedJsonObject` on that content, which finds another fence, recursing again. | Add a `recursive = false` flag or have `extractFencedJsonObject` call a non-fenced-checking variant of the JSON extractor. |
| 12 | CloudJsonParser.kt:86 | 🟢 Low | Floating-point Comparison | `optStrictLongStrictOrNull` checks `asLong.toDouble() != asDouble` to detect fractional values. This comparison can produce false negatives for very large longs (> 2^53) where `Long → Double → Long` round-trips lose precision. A value like `9007199254740993L` would convert to `9007199254740992.0` as a Double, and `toLong()` on that gives `9007199254740992L`, so the check `9007199254740992.0 != 9007199254740993.0` would be `false` (both map to the same Double), incorrectly accepting a fractional loss. | Pass a JSON with value `9007199254740993` — parsed as Double it becomes `9007199254740992.0`, the Long check passes, but returns `9007199254740992L` instead of the original value. | For values > 2^53 or < -2^53, parse as String and use `String.toLong()` directly. Or document the precision limitation. |
| 13 | AiWorkSchedulerImpl.kt:20-31 | 🟢 Low | No Worker Constraints | The `PeriodicWorkRequestBuilder` creates a work request with no constraints (no network required, no battery check, no storage check). The daily briefing worker fetches dashboard data and potentially calls AI services, which may require network. Without constraints, the worker could run during airplane mode and waste resources. | Device in airplane mode, worker fires, all AI calls fail, worker returns `success()` anyway (issue #8). | Add `.setConstraints(Constraints.Builder().setRequiredNetworkType(NetworkType.CONNECTED).build())` to ensure network is available. |
| 14 | CloudPiiSanitizer.kt:31-35 | 🟢 Low | Inconsistent Redaction for `null` merchant | `sanitizeMerchant(null, shouldRedact = true)` returns `"merchant_<hash_of_Unknown>"` because `trimmed` becomes `"Unknown"` and then it's hashed. But `sanitizeMerchant(null, shouldRedact = false)` returns `"Unknown"`. This means the redacted version leaks the information that the merchant was null/unknown through a predictable hash, while the non-redacted version clearly shows `"Unknown"`. The hash of `"Unknown"` is deterministic and easily precomputed, defeating the purpose of redaction. | Call `sanitizeMerchant(null, true)` — returns `"merchant_b94d27b9934d3e08..."`. Any attacker who knows `"Unknown".sha256Prefix()` can identify these entries as null-merchant records. | When `raw` is null/blank and `shouldRedact` is true, return a fixed sentinel like `"merchant_unknown"` rather than hashing the fallback string. |

### Cross-Component Pipeline Issues

| # | Pipeline | Severity | Type | Description | Suggested Fix |
|---|----------|----------|------|-------------|---------------|
| 1 | `DailyBriefingWorker → DashboardDataProvider → HybridDashboardBriefingService` | 🟡 Medium | Pipeline Hang Risk | The worker calls `getProcessedDataFlow().first()` which internally uses `flatMapLatest` with nested `combine` operators over 6+ upstream flows. Any single upstream flow that never emits blocks the entire pipeline. The `catch` operators only handle errors, not "never-emit" scenarios. The worker has no timeout of its own; it relies on WorkManager's 10-minute limit. | Add `withTimeoutOrNull(60_000)` in `DailyBriefingWorker.doWork()` and return `Result.retry()` on timeout. |
| 2 | `HybridReceiptAssistService ↔ SmartReceiptAssistService` | 🟡 Medium | Dual Implementation Confusion | Both `HybridReceiptAssistService` and `SmartReceiptAssistService` implement `ReceiptAssistService`. `HybridReceiptAssistService` is a simple router; `SmartReceiptAssistService` is a complex retry chain. If DI binds the wrong one, the behavior is radically different. `HybridReceiptAssistService` has the thread-safety bug (issue #1); `SmartReceiptAssistService` doesn't. It's unclear which is the "active" implementation. | Deprecate or remove one. If `SmartReceiptAssistService` is the intended implementation, ensure DI always binds it and remove `HybridReceiptAssistService` from the DI graph. |
| 3 | `SmartReceiptAssistService → CloudRetryPolicy → CloudReceiptAssistService` | 🟢 Low | Double Retry Amplification | `SmartReceiptAssistService` has its own retry chain (up to 5 attempts: cloud vision → on-device vision → cloud text → on-device text → fallback). Each cloud attempt internally uses `CloudRetryPolicy.MAX_RETRY_ATTEMPTS = 3` retries. This means a single `suggest()` call could make up to **2 cloud × 3 retries = 6 HTTP calls** before giving up on cloud and falling back. This compounds latency under degraded network conditions. | Consider reducing inner retry count when called from the smart service, or add a total-timeout budget across all attempts. |
| 4 | `CloudPiiSanitizer → CloudReceiptAssistService/CloudReviewExplanationService` | 🟢 Low | Inconsistent Sanitization | `CloudPiiSanitizer.sanitizeText()` is shared infrastructure, but `CloudReceiptItemCategorizationService` (line 482, 527) and `CloudWarrantyExtractionService` (line 366) define their **own** private `sha256Prefix()` and sanitization logic. Different regex patterns or thresholds could lead to inconsistent PII handling across services. | Centralize all PII sanitization through `CloudPiiSanitizer` and remove duplicate implementations in individual cloud services. |

### Summary
- **Total issues: 18** (14 individual + 4 cross-component)
- **Files with issues: 8/10** (files without issues: `CloudCorrelation.kt`, interface files `ReviewExplanationService.kt`, `ReceiptItemCategorizationService.kt`, `ReceiptAssistService.kt`)
- **High severity: 1** (race condition in `HybridReceiptAssistService`)
- **Medium severity: 7** (logic errors, PII leaks, hang risks)
- **Low severity: 10** (defensive coding, consistency, precision)
- **Key concern**: The `lastUsedImageInput` race condition (#1) is the most urgent — it silently corrupts state in production under concurrent receipt processing. The PII sanitizer regex weaknesses (#5, #6) are also concerning as they could leak personal data to cloud AI services.
