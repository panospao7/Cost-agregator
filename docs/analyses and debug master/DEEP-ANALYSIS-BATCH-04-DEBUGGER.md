# Deep Analysis — Batch 04: Forecasting & Synthesis (@debugger)

## Scope
- domain/forecasting/FinancialStressForecastEngine.kt
- domain/forecasting/MonteCarloSpendingSimulator.kt
- domain/forecasting/ForecastModels.kt (not found in codebase)
- domain/forecasting/DataQualityAssessor.kt
- domain/forecasting/HistoricalSpendingDistribution.kt
- domain/logic/SynthesisEngine.kt
- domain/logic/SynthesisModels.kt (not found in codebase)
- domain/logic/RecurringExpenseEngine.kt
- domain/logic/CustomSplitParser.kt
- domain/logic/SplitCalculator.kt

## Per-File Issues

| # | File:Line | Severity | Type | Description | Reproduction Steps | Suggested Fix |
|---|-----------|----------|------|-------------|-------------------|---------------|
| 1 | HistoricalSpendingDistribution.kt:52 | CRITICAL | Locale-dependent Calendar bug | `calendar.set(Calendar.DAY_OF_WEEK, Calendar.MONDAY)` does not set `firstDayOfWeek`. In US locale, Sunday is day 1. If 18-months-ago date falls on Sunday, setting DAY_OF_WEEK=MONDAY may jump forward to next week instead of backward, corrupting lookback window. | Run on US-locale device when `now - 18 months` lands on Sunday. lookbackStart will be 6 days later than expected, data loss. | Use `TimePeriodUtils.getStartOfWeek(calendar.timeInMillis)` for both lookbackStart and currentWeekStart. |
| 2 | FinancialStressForecastEngine.kt:195-214 | CRITICAL | Missed recurring outflows | `calculateRecurringOutflows` only enters while-loop when `nextDate in startDate..endDate`. If `pattern.nextExpectedDate` is before startDate (e.g., weekly pattern with nextExpectedDate 3 days before now), loop never executes, pattern entirely skipped. Future occurrences within window missed. | Weekly pattern with nextExpectedDate = 5 days ago; call computeStressForecast(). Pattern's future occurrences within 30/60/90 day window all missed. | Pre-loop: `while (nextDate < startDate) { nextDate = advanceByFrequency(nextDate, pattern.frequency) }`. |
| 3 | SplitCalculator.kt:208-221 | CRITICAL | Potential infinite loop | In `calculateAmountsFromPercentages`, when `remainder < 0`, while-loop only decrements `remainingToRemove` when `current > 0`. If all members have `baseCents == 0`, `current` always 0, `remainingToRemove` never decreases, infinite loop. | totalAmount=0.01, percentages={A:0.001, B:0.001,...} where all get baseCents=0 but rawCents>0. | Add safety counter `if (index >= bySmallestFraction.size * 2) break`. |
| 4 | SplitCalculator.kt:229-233 | MEDIUM | Integer overflow in toCents | `toCents()` returns `Int` via `BigDecimal.toInt()`. For amounts > ~€21,474,836.47, cent value exceeds Int.MAX_VALUE, silent overflow. Group trip expenses or business expenses could hit this. | calculateEqualSplit(25_000_000.0, members) — toCents returns wrapped negative value. | Use `Long` instead of `Int` for cents throughout, or add bounds check. |
| 5 | SynthesisEngine.kt:98-101 | MEDIUM | TOCTOU — double now() call | Line 98 captures `val now = timeProvider.now()`, but line 101 calls `timeProvider.now()` again for Calendar. At midnight boundary, `now` may be in one day/month and Calendar in another. Comment says "Fix: Use single Calendar instance" but re-introduces the exact bug. | Execute at 23:59:59.999 — `now` in month N, second now() in month N+1. daysInMonth, dayOfMonth, daysRemaining from wrong month. | Change line 101 to: `Calendar.getInstance().apply { timeInMillis = now }`. |
| 6 | FinancialStressForecastEngine.kt:407 | MEDIUM | No-op math | `horizons.first().recurringObligations / 30.0 * 30.0` — divides by 30 then multiplies by 30, mathematically no-op. Comment says "Monthly rate" but normalizes nothing. Works by coincidence for 30-day horizon only. | N/A — currently benign for 30-day horizon only. | Should be `horizons.first().recurringObligations / horizons.first().daysAhead * 30.0`. |
| 7 | FinancialStressForecastEngine.kt:319-323 | LOW | Redundant isEmpty() check | `if (dailyTotals.isNotEmpty())` inside sampling loop, but `dailyTotals.isEmpty()` already handled above (lines 281, 294). else 0.0 branch is dead code. | N/A — cosmetic/dead code. | Remove inner guard. |
| 8 | FinancialStressForecastEngine.kt:356 | MEDIUM | Division by zero risk | `calculateCrunchProbability` computes `crunchCount / simulatedTotals.size`. If simulatedTotals empty (shouldn't happen but method is private, not hardened), produces NaN. NaN propagates to classifyRiskLevel, all comparisons false, falls through to CRITICAL. | Call calculateCrunchProbability with empty simulatedTotals. | Add `if (simulatedTotals.isEmpty()) return 0.0` guard. |
| 9 | FinancialStressForecastEngine.kt:312 | LOW | Fixed seed for Monte Carlo | `Random(SEED)` re-created with same seed for each of 3 horizons (30/60/90). Random samples for first 30 days of 60-day simulation are identical to 30-day simulation. Artificial correlation between horizons. | Run computeStressForecast(); compare 30-day vs 60-day simulated totals — 30-day values are subset of 60-day. | Use different seeds per horizon (e.g., `SEED + daysAhead`) or share single Random instance. |
| 10 | HistoricalSpendingDistribution.kt:117 | MEDIUM | Trimming can produce all-zero data | After trimming, if trimmed list contains only very small positive values close to zero, `ln(value)` produces very large negative numbers, destabilizing log-normal fit (mu → -∞). If trimmed contains exactly one value, variance = 0 (Bessel's correction: /(1-1) = /0). | 5 qualifying weeks: [0.001, 1.0, 2.0, 3.0, 4.0]. After 10% trim, all remain. ln(0.001) = -6.9, severely skewing mu. | Add minimum threshold (e.g., `trimmed.filter { it >= 1.0 }`) before fitting, or clamp mu to reasonable range. |
| 11 | RecurringExpenseEngine.kt:93 | LOW | Redundant isEmpty() check | `if (dates.isEmpty() || dates.last() < sixMonthsAgo)` — dates derived from sorted.map where sorted already checked for size < 3. dates always has ≥3 elements here. | N/A — cosmetic only. | Remove dates.isEmpty() guard for clarity. |
| 12 | SynthesisEngine.kt:191 | MEDIUM | Percentage weight at map level | `likelyExpensesByDay` applies `* LIKELY_EXPENSE_WEIGHT` at group (day) level via mapValues. sumOf inside mapValues already sums amounts, then multiplies sum by 0.7. Mathematically correct only if all expenses in day are LIKELY. Duplicates weighting logic with plannedOnDay calculation (line 369-377). | N/A — currently correct but fragile. | Apply weight at per-expense level inside sumOf for consistency. |
| 13 | CustomSplitParser.kt:113 | LOW | Set equality comparison | `parsed.keys != groupMemberIds` uses set equality. parsed is LinkedHashMap, .keys is LinkedHashSet. == on sets checks element equality regardless of order. LinkedHashMap unnecessary overhead since ordering never used. | N/A — correct behavior. | Use HashMap or mutableMapOf instead of linkedMapOf. |
| 14 | FinancialStressForecastEngine.kt:139 | MEDIUM | DST-unsafe horizonEnd | `now + (daysAhead * TimePeriodUtils.DAY_IN_MILLIS)` assumes exactly 86,400,000ms per day. During DST transitions, day may be 23 or 25 hours. For 90-day horizon, accumulates up to ±1 hour error. | Run during DST spring-forward with 90-day horizon; end timestamp off by an hour. | Use `TimePeriodUtils.addDays(now, daysAhead)` which uses Calendar.add() (DST-aware). |
| 15 | SplitCalculator.kt:359 | LOW | Hardcoded $ currency symbol | `formatBalance` uses `$` regardless of user's actual currency. App elsewhere uses EUR. | Non-USD user sees $ in balance display. | Accept currency parameter or use formatter. |

### Cross-Component Pipeline Issues

| # | Pipeline | Severity | Type | Description | Suggested Fix |
|---|----------|----------|------|-------------|---------------|
| 1 | FinancialStressForecastEngine → RecurringExpenseEngine.getPatterns() → calculateRecurringOutflows() | CRITICAL | Past-date patterns silently dropped | RecurringExpenseEngine predicts nextExpectedDate from last observed transaction. If user hasn't recorded recent recurring expense, nextExpectedDate may be in the past relative to now. calculateRecurringOutflows skips entire pattern, all recurring obligations vanish from stress forecast. Forecast shows artificially low risk. | In calculateRecurringOutflows, advance stale nextDate forward before checking horizon window. |
| 2 | SynthesisEngine.synthesize() → FinancialStressForecastEngine.computeStressForecast() | MEDIUM | Inconsistent recurring filtering thresholds | SynthesisEngine uses confidence ≥ 0.90 for "committed" and ≥ 0.70 for "likely". FinancialStressForecastEngine.calculateRecurringOutflows uses ≥ 0.50. Monte Carlo discretionary filter also uses ≥ 0.50. Patterns with 0.50-0.69 confidence are subtracted from stress forecast as recurring AND not counted as discretionary — they vanish entirely. | Align thresholds or document intentional difference. Patterns in 0.50-0.69 range should contribute either as recurring or discretionary, not neither. |
| 3 | MonteCarloSpendingSimulator → HistoricalSpendingDistribution | MEDIUM | Redundant computation on end-of-month | buildEndOfMonthResult calls historicalDistribution.computeDistribution() again just to get fit for confidence assessment, even though simulation produces no stochastic output. Triggers full 18-month query + week grouping + log-normal fit for no value. | Cache DistributionFit or pass as parameter, or skip confidence assessment when daysRemaining == 0. |
| 4 | SplitCalculator → CustomSplitParser | MEDIUM | SplitType.EQUAL routed to parser then rejected | parseCustomSplit calls expense.splitType.toCustomSplitMode() which maps SplitType.EQUAL → CustomSplitMode.EQUAL. Parser immediately returns Invalid("EQUAL split type does not require custom splits"). Triggers fallbackToEqualForInvalidLegacyData which logs warning and recalculates equal split. Every EQUAL expense generates spurious warning log. | Add clear comment or make methods enforce type expectations. |
| 5 | FinancialStressForecastEngine.runMonteCarloSimulation() — internal | MEDIUM | Injected dependency unused | FinancialStressForecastEngine injects monteCarloSimulator: MonteCarloSpendingSimulator (line 27) but never uses it. runMonteCarloSimulation() (line 245) implements its own bootstrap-based Monte Carlo from scratch. Two divergent Monte Carlo implementations with different methodologies (log-normal vs bootstrap). Fixes to one don't apply to other. | Either delegate to injected MonteCarloSpendingSimulator or remove injection. |

### Summary
- **Total issues: 20** (15 per-file + 5 cross-component)
- **Files with issues: 7/8** (DataQualityAssessor.kt is clean)
- **CRITICAL: 3** (#1 locale-dependent week start, #2 missed recurring outflows, #3 infinite loop in SplitCalculator)
- **MEDIUM: 9**
- **LOW: 5**

### Highest-Risk Combination
Issues #1 and #2 compound with cross-component pipeline issue #1 — recurring obligations can be simultaneously undercounted (stale nextExpectedDate skipped entirely) AND historical spending data window shifted (locale bug), producing stress forecast that dramatically underestimates financial risk. Issue #3 (infinite loop in SplitCalculator) is most severe runtime risk — can hang main thread.
