You are the @debugger agent. Analyze these UI screen files for bugs:
Files:
- ui/screens/home/HomeScreen.kt
- ui/screens/home/HomeViewModel.kt
- ui/screens/transactions/TransactionsScreen.kt
- ui/screens/transactions/TransactionsViewModel.kt
- ui/screens/addexpense/AddExpenseSheet.kt
- ui/screens/addexpense/AddExpenseViewModel.kt
Check for: Compose recomposition issues, state bugs, memory leaks, null safety, edge cases, navigation issues, performance bottlenecks.

Report findings directly in the chat as a markdown table with columns: File:Line | Severity | Type | Description | Suggested Fix
Bug Analysis Report
#	File:Line	Severity	Type	Description	Suggested Fix
1	HomeScreen.kt:791-797	🔴 Critical	Infinite Recomposition Loop	showAiSettings block: when showAiSettings is true, a LaunchedEffect(Unit) sets it to false and navigates. But because this composable block is only entered when showAiSettings == true, and the state change triggers recomposition, the LaunchedEffect(Unit) key is stable — it fires once then the block is exited. However, if onNavigateToFeature triggers a rapid recomposition that re-enters this branch before state propagates, you get a navigation double-fire. Same pattern at lines 799-805 for showCategories.	Replace with DisposableEffect or move the navigation trigger out of if (showAiSettings) block entirely. Use a one-shot event pattern: set a navigateTo state, collect it in a single LaunchedEffect, and reset after navigating.
2	HomeScreen.kt:828	🟡 Medium	Duplicate collectAsState / Unnecessary Resubscription	val totalsState by viewModel.totalsDrillDownState.collectAsState() is called inside the if (showCategoryBreakdown) block. This means a new collectAsState subscription is created every time the dialog opens/closes. Meanwhile, the same flow is already collected at line 600 inside the TotalsDashboard widget branch. Multiple subscriptions to the same StateFlow are safe but wasteful.	Hoist totalsState collection to the top of HomeScreen alongside other state collections (line 83-86), or at least outside the conditional block.
3	HomeScreen.kt:306	🟡 Medium	Variable Shadowing	val widgetId is already declared at line 253 in the outer items lambda scope. Inside DashboardWidget.BudgetBlockParty (line 306) and DashboardWidget.TopCategories (line 513), it is re-declared with the same name. While Kotlin allows this, it shadows the outer variable and could lead to confusion if the inner block is refactored. Same with widgetStyle at lines 307, 514.	Rename inner variables to e.g., localWidgetId / localWidgetStyle, or remove them since the outer widgetId and widgetStyle already have the same values.
4	HomeScreen.kt:287	🟢 Low	Potential Division by Zero	(widget.totalBudget - widget.amount) / widget.totalBudget — if widget.totalBudget is 0.0, this produces NaN. The .toFloat().coerceIn(0f, 1f) does not clamp NaN (NaN comparisons are always false). The progress indicator will receive NaN.	Add a guard: if (widget.totalBudget > 0) before the division, or use if (widget.totalBudget != 0.0) ... else 0f.
5	HomeViewModel.kt:154	🟡 Medium	SharedFlow Event Loss	MutableSharedFlow<NavigationAction>(extraBufferCapacity = 1) with extraBufferCapacity = 1 means if two navigation actions are emitted before the collector processes them, the first is dropped (default BufferOverflow.SUSPEND won't drop, but with only 1 buffer slot it suspends the emitter). Also, emit() is called from viewModelScope.launch, so it can suspend indefinitely if no collector is active — but with extraBufferCapacity = 1, only 1 action can be buffered. If the LaunchedEffect collector is temporarily inactive (e.g., during config change), events are lost.	Use Channel<NavigationAction>(Channel.BUFFERED) exposed as a Flow via receiveAsFlow() for guaranteed single-delivery of one-shot events.
6	HomeViewModel.kt:87-97	🟡 Medium	LaunchedEffect Key Stability / Collector Lifecycle	LaunchedEffect(viewModel) collects navigationActions. The key viewModel is a Hilt ViewModel and remains stable across recompositions but not across configuration changes if the composable is in a NavHost that re-creates. If the screen recomposes with the same ViewModel instance, the LaunchedEffect won't restart, which is correct. However, if the Activity is recreated but the ViewModel survives, the new LaunchedEffect starts a new collector, and any events emitted between destroy and re-create are lost (see #5).	Combine fix with #5 — use Channel-based approach.
7	TransactionsViewModel.kt:198-222	🟡 Medium	Unstable _selectedTab Read Inside combine	groupedTransactions combines transactions and _sortOrder, but inside the lambda it reads _selectedTab.value directly (line 199). This is not reactive — if selectedTab changes, this combine won't re-emit unless transactions or _sortOrder also change. The sort logic for ALL tab may be stale.	Add _selectedTab as a third input to the combine and use the provided value instead of reading .value.
8	TransactionsViewModel.kt:316-325	🟢 Low	Refresh Trigger Overflow	_refreshTrigger.value += 1 will eventually overflow Int.MAX_VALUE after ~2 billion refreshes. Extremely unlikely but theoretically unsound.	Not critical; could use modular arithmetic or reset periodically.
9	TransactionsViewModel.kt:112-115	🟡 Medium	Initial Filter Applied on Every Recomposition	In TransactionsScreen.kt:112-116, LaunchedEffect(initialFilter) fires whenever initialFilter changes. Since TransactionFilter has correlationId = System.currentTimeMillis() as a default, every new instance is unique. If a parent recomposes and passes a new TransactionFilter(...) each time (even with the same logical content), this triggers a redundant applyFilter call that resets pagination.	Use LaunchedEffect(initialFilter?.hashCode()) with a stable key, or make TransactionFilter exclude correlationId from equals/hashCode. Better: use remember { initialFilter } and only apply once via LaunchedEffect(Unit).
10	TransactionsScreen.kt:460	🟡 Medium	forEach Inside LazyColumn Instead of items	groupedTransactions.forEach { (dateString, items) -> stickyHeader { ... } items(...) } — this is a known pattern but defeats lazy composition for the group headers structure. The entire forEach runs eagerly to build the LazyListScope DSL. With many date groups, this can cause frame drops during recomposition since all group structures are evaluated upfront.	Acceptable for moderate-size lists, but for 100+ date groups consider LazyColumn with a flat list model that interleaves headers and items.
11	TransactionsScreen.kt:866	🟢 Low	Unsafe Color Parsing	Color(transaction.categoryColor.toInt()) — if categoryColor is a hex string (e.g., "#FF0000"), calling .toInt() throws NumberFormatException. The try-catch handles it, but transaction.categoryColor type isn't clear; if it's actually a Long-backed color int this is fine, but if it could be a string, toInt() is incorrect — should use android.graphics.Color.parseColor().	Verify the type. If it's a color hex string, use Color(android.graphics.Color.parseColor(transaction.categoryColor)) like done elsewhere in this codebase (HomeScreen.kt:561).
12	AddExpenseSheet.kt:786-788	🟡 Medium	DatePickerState Not Synced With External Date Changes	rememberDatePickerState(initialSelectedDateMillis = dateMs) — the initialSelectedDateMillis is only used at first composition. If dateMs changes externally (e.g., via viewModel.updateDate()), the DatePickerState won't reflect the new date because remember preserves the old state. The displayed date text updates correctly (line 790), but re-opening the picker shows the stale initial date.	Use a key(dateMs) wrapper around rememberDatePickerState or manually update datePickerState.selectedDateMillis via a LaunchedEffect(dateMs).
13	AddExpenseViewModel.kt:121	🟢 Low	Locale-Dependent Amount Formatting	String.format("%.2f", suggestion.avgAmount) uses the default locale. In locales using , as decimal separator (e.g., German), this produces "12,50" which is then set as the amount field value. Later, AmountUtils.parseAmount() may not handle comma-separated decimals, depending on implementation.	Use String.format(Locale.US, "%.2f", ...) for programmatic amount strings, or ensure AmountUtils.parseAmount handles both . and ,.
14	AddExpenseViewModel.kt:274-279	🟢 Low	Ambiguous XOR Validation for Shared Expense	if (hasSharePercentage == hasShareAmount) rejects the case where both are empty AND where both are filled. When both are empty for a shared expense, the error says "Set either share % or share amount" — but the user might legitimately want a 50/50 split without specifying. This is a UX design choice but could confuse users.	Consider defaulting to 50% if both are empty, or make the error message clearer: "Please set exactly one of share % or share amount".
15	AddExpenseViewModel.kt:38	🟡 Medium	Default date = 0L in Data Class	AddExpenseState defaults date to 0L (epoch). The actual ViewModel initializes it with timeProvider.now(), but if anyone constructs AddExpenseState() directly (e.g., in tests), the date is January 1, 1970. The future-date validation at line 239 would pass, but saving an expense dated 1970 is incorrect.	Default to System.currentTimeMillis() or make date non-optional and require it at construction.
16	TransactionsViewModel.kt:376-388	🟡 Medium	deleteExpense Calls refresh() Which Resets State	deleteExpense calls refresh() which increments _refreshTrigger, and if on ALL tab, also calls loadInitialAll(). This resets pagination (_currentPage = 0, _pagedExpenses = []) causing the list to jump to the top. For non-ALL tabs, the reactive Flow handles updates automatically, making refresh() redundant.	For ALL tab: remove the deleted item from _pagedExpenses optimistically instead of full reload. For other tabs: the Flow-based approach already handles it — remove the refresh() call and let the reactive pipeline update.
17	HomeScreen.kt:812-814	🟢 Low	State Mutation During Composition	showDebug = false is set directly in the else if branch (line 813) during composition. Setting state during composition can cause immediate recomposition.	Wrap in LaunchedEffect(Unit) { showDebug = false } or use SideEffect { showDebug = false }.
18	HomeViewModel.kt:636-657	🟢 Low	Fragile Period Key Parsing	parseYear, parseYearMonth, parseYearWeek use string splitting that can crash on malformed keys. parseYear splits on - and takes first().toInt() — if the key is empty or non-numeric, it throws NumberFormatException. The parseYearWeek has a fallback, but parseYear and parseYearMonth do not.	Wrap in try-catch or validate the key format before parsing.
19	TransactionsScreen.kt:767	🟢 Low	Sticky Header Background Doesn't Match Theme	DateHeader uses MaterialTheme.colorScheme.surface (line 767) as the solid background for sticky header visibility, but the screen's containerColor is SemanticColors.BaseNavy. This causes a visible color mismatch when the header sticks during scrolling.	Use SemanticColors.BaseNavy instead of MaterialTheme.colorScheme.surface for the background.
20	HomeViewModel.kt:141	🟢 Low	SimpleDateFormat Thread Safety	private val dateKeyFormat = SimpleDateFormat("yyyy-MM-dd", Locale.US) is stored as an instance field and used in dashboardBriefingKeyForToday(). SimpleDateFormat is not thread-safe. If called from multiple coroutines concurrently, it can produce corrupt date strings.	Use java.time.format.DateTimeFormatter (thread-safe) or create a new SimpleDateFormat instance each call.
21	AddExpenseSheet.kt:281-286	🟢 Low	Hardcoded English Content Descriptions	contentDescription = if (state.showTransactionType) "Transaction type expanded" else "Transaction type collapsed" and similar at lines 327-330 ("Notes expanded"/"Notes collapsed") — these are hardcoded English strings, not using stringResource().	Replace with stringResource(R.string.xxx) calls for proper localization.
---
Summary by Severity
Severity	Count	Key Items
🔴 Critical	1	Navigation double-fire from LaunchedEffect inside conditional block (#1)
🟡 Medium	10	SharedFlow event loss (#5), stale _selectedTab read (#7), DatePicker desync (#12), state mutation risks (#9, #16), variable shadowing (#3), duplicated collectAsState (#2), thread-unsafe SimpleDateFormat (#20), default date 0L (#15), initial filter reapplication (#9)
🟢 Low	10	Division by zero (#4), color parsing (#11), locale formatting (#13), hardcoded strings (#21), sticky header color (#19), fragile parsing (#18), state mutation during composition (#17), refresh overflow (#8), ambiguous validation (#14), eager forEach (#10)
Top 3 Recommendations (by impact):
1. Fix #5+#6: Replace MutableSharedFlow navigation events with Channel<NavigationAction>(Channel.BUFFERED).receiveAsFlow() to prevent event loss across config changes.
2. Fix #7: Add _selectedTab to the combine in groupedTransactions to prevent stale sort logic when the tab changes independently.
3. Fix #12: Sync DatePickerState with external date changes using key(dateMs) or LaunchedEffect(dateMs) to prevent stale picker state.

You are the @debugger agent. Analyze these UI screen files for bugs:
Files:
- ui/screens/analytics/AnalyticsScreen.kt
- ui/screens/analytics/AnalyticsViewModel.kt
- ui/screens/analytics/AdvancedAnalyticsScreen.kt
- ui/screens/budget/BudgetScreen.kt
- ui/screens/budget/BudgetViewModel.kt
- ui/screens/budget/BudgetForecastingScreen.kt
Check for: Compose recomposition issues, state bugs, memory leaks, null safety, edge cases, navigation issues, performance bottlenecks.
Report findings directly in the chat as a markdown table with columns: File:Line | Severity | Type | Description | Suggested Fix

Bug Analysis Report
File:Line	Severity	Type	Description	Suggested Fix
AnalyticsViewModel.kt:128-129	🔴 Critical	Memory Leak	ConcurrentHashMap caches (analyticsCache, advancedCache) grow unbounded. Each PeriodCacheKey includes dataVersion which auto-increments, so old keys are never evicted — every data change adds new entries forever.	Use an LRU cache with a fixed max size (e.g. 5-10 entries), or clear all entries when data version changes (partially done at L181-185 but only for version mismatch, not for stale period keys).
AnalyticsViewModel.kt:178-186	🟡 Medium	Race Condition	lastExpenseDataVersion and lastBudgetDataVersion are @Volatile but read-then-write in a non-atomic block inside a flow {} block. Two concurrent collectors could interleave, causing one to clear the cache while the other reads stale data.	Wrap the check-and-clear in a synchronized block, or use AtomicLong with compareAndSet.
AnalyticsViewModel.kt:142-149	🟡 Medium	State Bug / Edge Case	The .scan() + .drop(1) on expenses and budgets means the very first emission from Room is silently dropped. On a fresh install or cold start, the combine will not trigger until the second emission, causing a permanent loading screen if data doesn't change again.	Remove .drop(1) and instead initialize ExpenseFreshness.dataVersion to 0 as a sentinel, checking dataVersion > 0 before clearing cache.
BudgetScreen.kt:164-169	🟡 Medium	UI / Layout Bug	The ErrorBanner is rendered outside the Scaffold content padding — it's at the same level as the if (uiState.isLoading) block but before it, meaning it overlaps the TopAppBar and isn't scrollable within the LazyColumn.	Move ErrorBanner inside the LazyColumn as the first item {}, or render it within the padded content area.
BudgetScreen.kt:479	🟡 Medium	Recomposition / Performance	val nowMs = System.currentTimeMillis() is called directly in the composable body (not in remember). This value changes every recomposition, triggering downstream remember recalculations for elapsedPeriodMs and periodProgress on every frame.	Wrap in remember { System.currentTimeMillis() } or use a LaunchedEffect timer that updates a mutableLongStateOf once per minute.
BudgetForecastingScreen.kt:110-111	🔴 Critical	Null Safety / Crash	uiState.budget!! and uiState.forecast!! are force-unwrapped. The when block checks uiState.forecast != null but not uiState.budget. If an error is cleared and forecast is non-null while budget is still null, this crashes with NullPointerException.	Use uiState.budget?.let { budget -> uiState.forecast?.let { forecast -> ... } } or add uiState.budget != null to the condition.
BudgetForecastingScreen.kt:52-53	🟡 Medium	State Bug	BudgetForecastingViewModel.generateForecast() stores budget in _uiState only on success (L73). refreshForecast() (L93-96) reads _uiState.value.budget, which is null until the first successful load. So if the first load fails and user taps "Refresh", nothing happens silently.	Store budget in the state eagerly at the start of generateForecast() (e.g. _uiState.value = _uiState.value.copy(budget = budget, isLoading = true)).
BudgetForecastingScreen.kt:602-605	🟠 Low	Performance	NumberFormat.getCurrencyInstance(Locale.getDefault()) is created inside every RecommendationCard composable for each recommendation item. This is a relatively expensive allocation during scrolling.	Hoist currencyFormatter to the parent ForecastContent and pass it down, or wrap in remember.
AdvancedAnalyticsViewModel.kt:41-42	🟡 Medium	Silent Failure	On exception in loadDashboardData(), _dashboardData is set to null with no error message. The UI shows nothing (falls through dashboardData?.let) — the user sees a blank screen with no way to retry or know what happened.	Add an _error StateFlow, set it on exception, and display an error state in AdvancedAnalyticsScreen.
AdvancedAnalyticsViewModel.kt:28-30	🟠 Low	Side Effect in init	loadDashboardData() fires a coroutine in init. If the dashboard is never observed (screen never composed), this work is wasted. Also prevents testing without a coroutine dispatcher override.	Use SharingStarted.Lazily or trigger load from a LaunchedEffect in the screen composable instead.
AnalyticsScreen.kt:1038	🟠 Low	Deprecated API	TimePeriod.values() is deprecated in Kotlin 1.9+.	Replace with TimePeriod.entries.
BudgetScreen.kt:838	🟠 Low	Deprecated API	BudgetPeriod.values() is deprecated in Kotlin 1.9+.	Replace with BudgetPeriod.entries.
AnalyticsScreen.kt:393	🟠 Low	Locale Bug	String.format("%.1f", change) uses the default locale, which may produce a comma (3,5%) instead of a dot in some locales, breaking the visual +3.5% pattern.	Use String.format(Locale.US, "%.1f", change) for display consistency, or use the already-available Locale-aware formatting.
AnalyticsScreen.kt:717	🟠 Low	Locale Bug (same pattern)	String.format("%.1f", change) in TotalSpentHero — same locale issue as above.	Use explicit Locale.US or Locale.ROOT.
BudgetViewModel.kt:55	🟡 Medium	Cold Flow Re-emission	flow { emit(budgetRepository.getSuggestions()) } inside flatMapLatest creates a cold flow that emits once. On each _refreshTrigger change this recreates the flow, calling getSuggestions() — this is fine, but the suspend call happens on the default dispatcher with no error handling. An exception here kills the entire combine chain.	Wrap in try/catch or use .catch { emit(emptyList()) }.
BudgetViewModel.kt:151-161	🟡 Medium	Validation Edge Case	validateThresholds rejects notifyAtWarning == 0f (due to <= 0f) and notifyAtCritical == 1.0f (due to > 1.0f — this is allowed). But the default budget may have notifyAtWarning = 0.75f, notifyAtCritical = 0.9f. If a user creates a Budget without explicitly setting thresholds and the defaults happen to be 0, every add/update silently fails with an error set but no dialog feedback.	Show the validation error in the AddEditBudgetDialog, or set sensible defaults in the Budget entity constructor and skip validation when thresholds are at default values.
BudgetScreen.kt:847-848	🟠 Low	Hardcoded Strings	"Period mode", "Rolling", "Calendar" are hardcoded English strings in AddEditBudgetDialog instead of using stringResource().	Extract to strings.xml and use stringResource().
AnalyticsScreen.kt:577	🟡 Medium	Division by Zero	val maxSpend = analysis.dayOfWeekStats.values.maxOfOrNull { it.totalSpent } ?: 1.0 — if all totalSpent are 0.0, maxOfOrNull returns 0.0 (not null), causing heightRatio = 0.0 / 0.0 = NaN → NaN.coerceAtLeast(0.05f) = NaN. The Box height becomes NaN.	Use ?.takeIf { it > 0 } ?: 1.0 instead of ?: 1.0.
AnalyticsViewModel.kt:307	🟠 Low	Edge Case	For TimePeriod.TODAY, chartDays = 1, which feeds into buildDailyTotals. A single-day chart with one data point may render incorrectly in Vico (some chart libs need ≥2 points). The workaround at L827 adds a padding 0f but only if values.size < 2 — this works but creates a phantom day.	Consider showing a single stat card instead of a chart for TODAY period.
BudgetForecastingScreen.kt:171	🟠 Low	Recomposition	val context = androidx.compose.ui.platform.LocalContext.current is used inside RiskLevelCard to call context.getString(). This defeats Compose's localization recomposition — if locale changes while the composable is live, the destructured strings won't update.	Use stringResource() instead of context.getString().
AnalyticsViewModel.kt:605	🟠 Low	Edge Case / Index OOB	monthNames[month] where month comes from TimePeriodUtils.getMonth(). If getMonth() returns a 1-based index (1-12) instead of 0-based (0-11), this causes ArrayIndexOutOfBoundsException.	Verify getMonth() returns 0-based; if not, use monthNames[month - 1] with bounds check.
Summary by Severity
Severity	Count
🔴 Critical (crash / data loss)	2
🟡 Medium (incorrect behavior)	8
🟠 Low (code quality / edge)	10
Top 3 Issues Requiring Immediate Attention
1. BudgetForecastingScreen.kt:110-111 — Force-unwrap !! on uiState.budget will crash if budget is null while forecast is non-null. This is reachable because generateForecast only sets budget on success.
2. AnalyticsViewModel.kt:142-149 — The .drop(1) on the scanned expense/budget flows causes the combine to stall forever on cold start if no second emission arrives, leaving the user on a permanent loading spinner.
3. AnalyticsScreen.kt:577 — Division by actual zero (not null) in SpendingPatternsCard when all day-of-week totals are zero produces NaN chart heights, which can cause layout crashes or invisible UI.