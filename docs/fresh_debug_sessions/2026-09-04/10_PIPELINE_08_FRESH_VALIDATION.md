# PIPELINE_08 — Fresh Validation (2026-09-04)

- Validated at: d1fa9c68 (branch `atomicity-pr21-enforcement-final`). Static validation only — no compile/test run.
- Source doc: docs/analyses and debug master/PIPELINE_8_CONSOLIDATED_ISSUES.md
- Cross-checks: PIPELINE_ISSUES_MASTER_TRACKER.md (P8 section), FIXED_CLAIMS_VALIDATION_AUDIT_v4.md, docs/architecture/{CODEBASE_SEGMENTS,LEGAL_PATHS,dao-map,ENGINE_INTERACTION_MAP}.md

## 1. Pipeline theme

Privacy/AI/redaction: cloud-AI gating (PrivacySettings x AiSettings reconciliation), PII sanitization before cloud dispatch, write-time raw-storage modes, retention purging, audit-log hygiene, and privacy-denied UX. At HEAD, **18 of 20 issues are fixed in code** — the P8 fix wave (2e6b5f14 "complete U-PR5 + Pipeline 8", ede53b72 "P8-P1-07/10/11", P8-PR1/PR2/PR3 series) closed essentially the whole registry, but **no tracker reflects this**: all of NEW-P8-001..008 and most P8-P1-0x are still marked OPEN/TODO. Only P8-P1-09 (gate ordering in notification capture) and NEW-P8-007 (null-vs-empty sanitizer variant with zero production callers) remain genuinely incomplete.

## 2. Verdict summary

| Verdict | Count |
|---|---:|
| Verified fixed | 18 |
| Verified open | 0 |
| Verified partial | 2 |
| Regressed | 0 |
| Tracker drift (doc/tracker says open, code says fixed) | 16 |
| New issues found this session | 3 |
| Not verifiable statically | 0 |

## 3. Issue-by-issue validation

| ID | Title (short) | Old status | Fresh status | Class | Evidence (file:line) | Notes |
|----|---------------|------------|--------------|-------|----------------------|-------|
| P8-P1-01 | Privacy setting changes don't stop active workers | FIXED | **VERIFIED_FIXED** | PIPELINE | `app/src/main/java/com/yourname/expensetracker/data/privacy/PrivacySettingsRepositoryImpl.kt:150-175` (`applyPrivacyChange` via `PrivacyRuntimeWorkerPolicy` + `WorkerRegistry`) | `data_retention` is cancel-exempt by policy — cleanup worker keeps running (matches AGENTS rule that cleanup must not be gated). |
| P8-P1-02 | `PrivacySettings`/`AiSettings` can disagree | TODO ONLY | **VERIFIED_FIXED** (drift) | UNIVERSAL (U-PRIVACY-02) | `app/src/main/java/com/yourname/expensetracker/domain/privacy/CloudAiPrivacyGate.kt:15-73` (delegates to `EffectiveCloudAiPolicyResolver`); `EffectiveCloudAiPolicy.kt:52-90` (blocks when EITHER system disables; composite flags for receipt-image/bank-statement) | All 8 `Cloud*Service` providers call `privacyGate.check` (1-2 each). |
| P8-P1-03 | Audit logging noisy / not semantic | TODO ONLY | **VERIFIED_FIXED** (drift) | PIPELINE | `app/src/main/java/com/yourname/expensetracker/data/privacy/PrivacyAuditLoggerImpl.kt:26-61` — single final-decision log with typed `ALLOWED/DENIED/NOT_APPLICABLE/DENIED_FAIL_CLOSED` + `capabilityDescription()` (:68-96) | CompositePrivacyGate logs once with the final decision (`CompositePrivacyGate.kt:62`). Tagged P8-PR2. |
| P8-P1-04 | Audit context stores caller-provided sensitive data | TODO ONLY | **VERIFIED_FIXED** (drift) | PIPELINE (privacy) | `PrivacyAuditLoggerImpl.kt:19-24` (fixed allowlist of 14 keys), `:98-106` (`sanitizeContext` drops unknown keys, 200-char cap) | Arbitrary caller context maps can no longer reach the DB. |
| P8-P1-05 | Raw data stored first, purged later | PARTIAL | **VERIFIED_FIXED** (drift; caveat) | UNIVERSAL (U-PRIVACY-01) | `app/src/main/java/com/yourname/expensetracker/service/NotificationCaptureService.kt:519` (gate) + `:555-563, :670-676, :710-728` (mode applied at capture); `app/src/main/java/com/yourname/expensetracker/domain/notification/capture/NotificationIntakeCoordinator.kt:43,75-81` (DO_NOT_STORE rejects, isRaw flag); `ReceiptRepository.kt:122-127` (OCR sanitize before insert); `ReceiptLifecycleCoordinator.kt:621,664,804` (email) | Write-time `RawStorageMode` enforcement on all main capture/OCR/email paths; retention purge remains as defense-in-depth. Static proof of every historical path impossible — tracker flip should note the caveat. |
| P8-P1-06 | Retention worker scope incomplete | TODO ONLY | **VERIFIED_FIXED** (drift) | UNIVERSAL (U-PRIVACY-03) | `app/src/main/java/com/yourname/expensetracker/di/RetentionModule.kt:33-228` — 10 targets: raw_notifications, scanned_receipts.rawOcrText, ai_artifacts, **ai_chat_messages** (:103), email_receipt_sources, notification_intake, pipeline_diagnostic_events, pending_reviews.notificationText, background_job_runs.errorMessage, bank_statement_import_items.merchant | Chat-message gap closed. Purges are batched SQL updates/deletes (bounded 100-row loops) — avoids materializing payloads. Residual DOC-DRIFT: stale header at `DataRetentionWorker.kt:1-2` still claims "chat messages" is a gap (NEW-P8-2026-003). `ServiceDiagnostics` (in-memory debug state) is the only remaining unregistered surface. |
| P8-P1-07 | Bank-statement cloud path sends raw prompt | TODO ONLY | **VERIFIED_FIXED** (drift) | PIPELINE (privacy) | `app/src/main/java/com/yourname/expensetracker/data/ai/provider/CloudReceiptAssistService.kt:268-295` — `privacyGate.check(CLOUD_AI_BANK_STATEMENT)` + `cloudPayloadPolicy.prepareBankStatementValidation(prompt)` before send | Commit ede53b72. Self-defending service + policy-level redaction. |
| P8-P1-08 | Redaction not a purpose-aware payload contract | TODO ONLY | **VERIFIED_FIXED** (drift) | PIPELINE | `app/src/main/java/com/yourname/expensetracker/domain/privacy/PreparedCloudPayload.kt`, `CloudPayloadPolicy.kt`, `data/privacy/DefaultCloudPayloadPolicy.kt` | All 8 cloud providers consume the policy: CloudCategorizationAssist/CloudDedupeJudge/CloudReceiptItemCategorization/CloudReviewExplanation/CloudWarrantyExtraction/CloudReceiptAssist via `cloudPayloadPolicy.prepare*`; CloudDashboardBriefingService.kt:275 (`prepareText`); CloudQueryInterpretationService.kt:103 (builds `PreparedCloudPayload`). Matches CODEBASE_SEGMENTS claim. |
| P8-P1-09 | Notification privacy gate too late; runtime state not cached | TODO ONLY | **VERIFIED_PARTIAL** | PIPELINE (privacy) | `NotificationCaptureService.kt:519-546` — gate runs before intake insert; `else` branch fails CLOSED for NotApplicable (:536-547); extras JSON mode-applied at :555-563 | Persistence is now gate-gated and fail-closed, and every notification re-checks the gate (settings take effect on next capture). But text is still *extracted in memory* before the gate (inherent to NotificationListenerService), and the listener service is not stopped on setting change — compensated per-capture. Tracker/doc semantics ("gate too late") no longer hold for storage; marking partial to keep the in-memory-extraction caveat visible. |
| P8-P1-10 | Geocoding/location gate coverage not statically guaranteed | TODO ONLY | **VERIFIED_FIXED** (drift) | PIPELINE (privacy) | `NominatimGeocodingService.kt:90,119,137`; `GeoapifyGeocodingService.kt:54,72`; `CompositeGeocodingService.kt:71,126`; `domain/location/LocationResolver.kt:85` (DEVICE_GPS), `:265` (OVERPASS_API), `:335-337` (EXTERNAL_GEOCODING) | Every external geocoder is self-defending (checks its own gate before HTTP). Commit ede53b72. |
| P8-P1-11 | Raw backup/export remains reachable | TODO ONLY | **VERIFIED_FIXED** (drift) | PIPELINE (privacy) | `DatabaseBackupRepositoryImpl.kt:361-364` (`if (!BuildConfig.DEBUG) throw UnsupportedOperationException`), `:367-377` (`RAW_DATABASE_EXPORT` privacy gate) | Fail-closed in release; debug path double-gated + wrapped in BACKUP_EXPORTING. |
| P8-P1-12 | Denied privacy states not consistently visible | TODO ONLY | **VERIFIED_FIXED** (drift) | PIPELINE | `app/src/main/java/com/yourname/expensetracker/ui/components/PrivacyBlockedCard.kt` (typed reason card); `PrivacyDecision.toPrivacyBlocked(...)` used at `CloudReceiptAssistService.kt:284-286`; sealed `PrivacyBlocked` family per CODEBASE_SEGMENTS.md:558-560 | Unified typed denial model + reusable Compose card. Spot-checked; not exhaustively UI-audited. |
| NEW-P8-001 | `updateSettings()` TOCTOU race | OPEN | **VERIFIED_FIXED** (drift) | UNIVERSAL (TOCTOU) | `PrivacySettingsRepositoryImpl.kt:58-61` (`settingsMutex`), `:112-118` (`withLock`), `:120` (re-reads current inside `edit {}` via `prefs.toLoadState().settings()` → FAIL_CLOSED_DEFAULTS on corruption) | Mutex + in-transaction re-read + fail-closed base. Tagged "P8-PR1 (NEW-P8-001)". Resolves audit-v4 open flag. |
| NEW-P8-002 | DataRetentionWorker loop no checkpoint | OPEN | **VERIFIED_FIXED** (drift) | UNIVERSAL (worker idempotency/checkpoint) | `DataRetentionWorker.kt:69-75` (resume init), `:99-115` (deterministic order + skip completed), `:152-155, :261-266` (per-target commit), `:180-181, :297-299` (clear at end) | Tagged "P8-PR1 (NEW-P8-002)". Crash-resume works; WorkManager-retry path has a residual inefficiency (NEW-P8-2026-001). |
| NEW-P8-003 | `MERCHANT_LINE_REGEX` over-matches | OPEN | **VERIFIED_FIXED** (drift; fixed by removal) | PIPELINE | `app/src/main/java/com/yourname/expensetracker/data/ai/provider/internal/CloudPiiSanitizer.kt:38-43` — `sanitizeMerchant` no longer extracts lines via regex; whole trimmed merchant is SHA-256-hashed (`merchant_<hash>`) when redaction is on | The problematic regex no longer exists anywhere in the file. |
| NEW-P8-004 | CloudPiiSanitizer missing patterns | OPEN | **VERIFIED_FIXED** (drift) | PIPELINE | `CloudPiiSanitizer.kt:14-29` — SSN, NI, SIN, TFN, PASSPORT regexes; applied at :31-45 | Tagged "P8-PR3 (NEW-P8-004)". Note: TFN/SIN patterns are broad (any 3-3-3 digit group) — over-redaction only, fail-safe direction. |
| NEW-P8-005 | `requireAllowed()` ignores capability | OPEN | **VERIFIED_FIXED** (drift; audit caveat resolved) | UNIVERSAL (fail-closed) | `EffectiveCloudAiPolicy.kt:24-44` — checks the capability argument, throws `SecurityException` for blocked **and unrecognized** capabilities; **production caller now exists**: `app/src/main/java/com/yourname/expensetracker/domain/receipt/lifecycle/ReceiptLifecycleCoordinator.kt:295-296` (`ocrPolicy.requireAllowed(CLOUD_AI_RECEIPT_OCR)` on the receipt intake path, U-PR5) | Audit v4's two caveats (capability ignored, test-only) are both resolved at HEAD. |
| NEW-P8-006 | DataRetentionWorker swallows purge failures | OPEN | **VERIFIED_FIXED** (drift; residual noted) | UNIVERSAL (worker diagnostics/retry) | `DataRetentionWorker.kt:130-149` (per-target catch; **CancellationException rethrown** :135), `:158-177` (structured diagnostic with `DiagnosticReasonCode` constants + errorClass only), `:231-235` (transient → `RetryableWorkerException`) | Failures are no longer silent. Residual: permanent (non-transient) failures still end the run as SUCCESS — deliberate partial-failure policy, diagnostics recorded; acceptable but should be documented. |
| NEW-P8-007 | `sanitizeRawOcr` conflates null with empty | OPEN | **VERIFIED_PARTIAL** | PIPELINE | `app/src/main/java/com/yourname/expensetracker/domain/privacy/RawContentSanitizer.kt:33-37` — `sanitizeRawOcrNullable` preserves null vs "" and is test-covered (`app/src/test/.../P8PrivacyFixesTest.kt`); **zero production callers** | All 5 production call sites still use the conflating variant: `ReceiptRepository.kt:126`, `BankStatementLifecycleProcessor.kt:286`, `ReceiptLifecycleCoordinator.kt:621,664,804`. Fix-the-API-without-migrating-callers — exactly the audit-v4 recurring failure mode. |
| NEW-P8-008 | `detectRedactedFields` misses truncation | OPEN | **VERIFIED_FIXED** (drift) | PIPELINE | `app/src/main/java/com/yourname/expensetracker/data/privacy/DefaultCloudPayloadRedactor.kt:87-114` (full + truncated markers incl. new PII fields), `:116+` (`truncationMatches`) | Tagged "P8-PR3 (NEW-P8-008)". |

### 3.1 Tracker/doc drift found this session

1. **MASTER TRACKER STALE — 16 issues listed OPEN/TODO are fixed at HEAD:** P8-P1-02/03/04/05/06/07/08/10/11/12 (all "📝 TODO ONLY" or PARTIAL) and NEW-P8-001/002/003/004/005/006/008 (all "🔴 OPEN"). Only P8-P1-01 (FIXED) is tracker-correct. Resolves the audit-v4 flag on NEW-P8-005: fix confirmed real AND its "no production caller" caveat has since been resolved (`ReceiptLifecycleCoordinator.kt:295-296`).
2. **Source doc internal staleness:** PIPELINE_8_CONSOLIDATED_ISSUES.md "Last validated 2026-05-31" predates the P8 fix wave (2e6b5f14, ede53b72, P8-PR1/2/3) and the P8-PR1 comment tags embedded in the code.
3. **DOC-DRIFT inside code (NEW-P8-2026-003):** `DataRetentionWorker.kt:1-2` header claims remaining retention gaps are "chat messages (AiChatMessageDao), debug diagnostics (ServiceDiagnostics)" — but the `ai_chat_messages` target exists at `RetentionModule.kt:103`. Only the ServiceDiagnostics half of the comment remains true.

## 4. New issues & regressions found this session

| ID | Sev | Evidence | Description | Suspected origin |
|----|-----|----------|-------------|------------------|
| NEW-P8-2026-001 | MINOR | `DataRetentionWorker.kt:180-181` (`clearCheckpoint(prefs)`) executes **before** the transient-failure retry decision at `:231-235` (`throw RetryableWorkerException`) | When a purge target fails transiently, the checkpoint is cleared before the run throws the retryable exception, so the WorkManager retry starts from scratch — the NEW-P8-002 checkpoint only protects against process death mid-loop, not WorkManager retries. Correctness is preserved (purges are idempotent: only unpurged rows selected), so this is wasted-work/diagnostics-noise, not data risk. | P8-PR1 checkpoint implementation ordering. |
| NEW-P8-2026-002 | MINOR (privacy) | `NotificationCaptureService.kt:557-559` — under `STORE_RAW`, exception path writes `"{\"error\": \"${e.message}\"}"` into `extrasJson` which is **persisted** via `intakeCoordinator.capture(...)` | Arbitrary `e.message` (from `buildExtrasJson` failure — can embed provider/format detail) persisted into the notification intake DB, violating "never persist arbitrary e.message". Gated behind user-opted STORE_RAW and non-notification content, hence MINOR, but it is the one raw-message persist path left in the capture flow. | Pre-existing; not covered by the PII burn-down (which targeted log suppressions). |
| NEW-P8-2026-003 | MINOR (doc drift) | `DataRetentionWorker.kt:1-2` vs `RetentionModule.kt:103` | Stale file header lists `ai_chat_messages` as a retention gap although the target exists and runs. Misleads auditors into re-flagging P8-P1-06. | Header written before the ai_chat_messages target was added; never updated. |

No regressions found from the inspected rework waves touching P8 files: T2A/T2B/T3A/T3B time migration (explicit clocks, no lost defaults; DAO wall-clock defaults removed compile-safely in 94fb5d86), PR22-small ca760783 (runCatching→try/catch with CancellationException rethrow in NotificationProcessingPipeline/ReceiptLinkService — clean), 92a6ebf7 (DebugDataStorage moved to src/debug with release stub — privacy improvement), e7f3496e/b2025407 (allowlist burn-down). Zero `System.currentTimeMillis` remains in any P8-area file.

## 5. Universal candidates

- **U-PRIVACY-02 (authoritative cloud gate):** now genuinely wired — `CloudAiPrivacyGate` → `EffectiveCloudAiPolicyResolver` composes both settings stores, `requireAllowed` is capability-aware with a production caller. Covered; keep `EffectiveCloudAiPolicy.requireAllowed` as the model for future capabilities (its unrecognized-capability `throw` is the fail-closed pattern to copy).
- **U-PRIVACY-01 (RawStorageMode write-time consistency):** covered on capture/intake/OCR/email paths at HEAD; only residual is the null-vs-empty conflation at 5 call sites (NEW-P8-007 partial) — migrating those to `sanitizeRawOcrNullable` would close the universal item fully.
- **U-PRIVACY-03 (retention/redaction scope):** 10 registered targets cover everything the issue listed (incl. chats and email). Residual: `ServiceDiagnostics` (in-memory debug surface) unregistered — verify it never persists; then the universal item can close.
- **U-PR1 (CancellationException):** good discipline in `DataRetentionWorker` (:135, :174 rethrow), notification pipeline, and P8 services after ca760783. No P8-file violations found in the audited set.
- **U-PR2-family (TOCTOU):** `updateSettings` mutex pattern (mutex + re-read-inside-edit + fail-closed base) is the reference implementation for other settings-like RMW paths.
- **U-PR7 (TimeProvider):** fully clean across P8 files (audit timestamps use injected `timeProvider`).

## 6. Recommended next actions (ordered, concrete, with file paths)

1. **Tracker correction (docs only):** flip the 16 drifted items to FIXED in `docs/analyses and debug master/PIPELINE_ISSUES_MASTER_TRACKER.md` citing this doc; keep P8-P1-09 and NEW-P8-007 as PARTIAL.
2. **NEW-P8-007 (highest-value real code work):** migrate the 5 production call sites to `RawContentSanitizer.sanitizeRawOcrNullable` — `ReceiptRepository.kt:126`, `BankStatementLifecycleProcessor.kt:286`, `ReceiptLifecycleCoordinator.kt:621,664,804` — so the null-vs-empty distinction actually reaches the DB; then deprecate the conflating overload.
3. **NEW-P8-2026-001:** in `DataRetentionWorker.kt`, move `clearCheckpoint(prefs)` after the failure classification (or skip clearing when `anyFailure`), so transient-retry resumes instead of re-running all targets.
4. **NEW-P8-2026-002:** replace `"{\"error\": \"${e.message}\"}"` at `NotificationCaptureService.kt:558` with a controlled constant, e.g. `"{\"error\": \"EXTRAS_SERIALIZE_FAILED\"}"`.
5. **NEW-P8-2026-003:** update the stale header at `DataRetentionWorker.kt:1-2` to list only `ServiceDiagnostics` as the remaining unregistered surface (or register a target if it persists anything).
6. **NEW-P8-006 documentation:** record the deliberate policy "permanent per-target purge failures → SUCCESS + structured diagnostic" in the worker KDoc so future reviewers don't re-file it as a bug.
7. **Static guarantee follow-up:** add an architecture-guard test enumerating external HTTP egress points (cloud AI, geocoding, Overpass) and asserting each touches `privacyGate.check`, converting P8-P1-10's "not statically guaranteed" into a guarded invariant.
