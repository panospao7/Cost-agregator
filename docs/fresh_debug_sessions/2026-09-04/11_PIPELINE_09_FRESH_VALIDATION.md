# PIPELINE_09 — Fresh Validation (2026-09-04)

- Validated at: `d1fa9c68` (branch `atomicity-pr21-enforcement-final`). Static validation only — no compile/test run.
- Source doc: `docs/analyses and debug master/PIPELINE_9_CONSOLIDATED_ISSUES.md`
- Cross-checked against: `PIPELINE_ISSUES_MASTER_TRACKER.md` (P9 section, 🟢 COMPLETE, only NEW-P9-008 PARTIAL) and `FIXED_CLAIMS_VALIDATION_AUDIT_v4.md` (addendum 1: NEW-P9-001 flagged ACTUALLY_PARTIAL at `661e78a1`).
- Architecture law checked against: `docs/architecture/LEGAL_PATHS.md` (Workers/Background Jobs, Diagnostics), `docs/architecture/CODEBASE_SEGMENTS.md` (Segment 12), `docs/architecture/ENGINE_INTERACTION_MAP.md` (WorkerExecutionGuard impact matrix), `docs/architecture/dao-map.md` (BackgroundJobRunDao).

## 1. Pipeline theme

Workers / background jobs: the `WorkerExecutionGuard` contract (barrier-first execution, durable `BackgroundJobRun` run logging, retry-vs-failure classification, lease/drain during restore, notification-permission and privacy gates), the `WorkerSpec`/`WorkerSpecScheduler` scheduling layer, and the 7 guarded workers (BillReminder, WarrantyExpiration, DataRetention, DailyBriefing, ReceiptMatching, LocationBackfill, MerchantKeyBackfill) plus the bespoke NotificationIntakeWorker. All P9 issues live in Segment 12 (`domain/workers/`, `service/*`, `data/ai/worker/`, `data/privacy/`, `data/location/`, `worker/`).

## 2. Verdict summary

| Verdict | Count |
|---|---:|
| Verified fixed | 26 |
| Verified open | 0 |
| Verified partial | 0 |
| Regressed | 0 |
| Tracker drift | 1 (NEW-P9-008: doc says PARTIAL, code is FIXED — under-claim) |
| New issues found | 4 (all MINOR) |
| Not verifiable statically | 0 |

The doc claims "23 FIXED, 1 PARTIAL, 🟢 COMPLETE". At HEAD the claim is **conservative-correct**: all 26 issues are fixed in source, including NEW-P9-008 which the doc still marks PARTIAL. No regressions found in any guarded worker contract area (barrier-first, CE rethrow, retry contract, idempotency, diagnostics sanitization).

## 3. Issue-by-issue validation

| ID | Title (short) | Old status | Fresh status | Class | Evidence (file:line) | Notes |
|----|---------------|-----------|--------------|-------|----------------------|-------|
| P9-P1-01 | BackgroundJobRun unused by workers | FIXED | VERIFIED_FIXED | PIPELINE | `domain/workers/WorkerRunLogger.kt:52-57,92-116` (`WorkerRunLoggerImpl` inserts `BackgroundJobRun` via `BackgroundJobRunDao`); consumed by guard `startRunSafely` (`WorkerExecutionGuard.kt:559-581`) | Terminal writes via `dao.completeTerminal` (`WorkerRunLogger.kt:196-215`) |
| P9-P1-02 | No shared WorkerExecutionGuard | FIXED | VERIFIED_FIXED | PIPELINE | All 7 workers call `runGuardedWithContext`: `service/reminder/BillReminderWorker.kt:46`, `service/warranty/WarrantyExpirationWorker.kt:72`, `data/privacy/DataRetentionWorker.kt:77`, `data/ai/worker/DailyBriefingWorker.kt:71`, `service/receiptmatching/ReceiptMatchingWorker.kt:41`, `data/location/LocationBackfillWorker.kt:52`, `data/location/MerchantKeyBackfillWorker.kt:49`; + NotificationIntakeWorker.kt:55 | Matches LEGAL_PATHS "EVERY worker → WorkerExecutionGuard" |
| P9-P1-03 | Restore cancellation not a running-worker barrier | FIXED | VERIFIED_FIXED | PIPELINE | `domain/workers/WorkerLeaseRegistry.kt` (acquire/requestStopAll/awaitNoActiveWorkers/isStopRequested); `WorkerLeaseRegistryImpl.kt` (drain impl, `requestStopAndAwaitDrain`); guard acquires lease (`WorkerExecutionGuard.kt:136-140,306-328`) and stops at checkpoints (`:250-274`); `RestoreMaintenanceMode.kt:150` resets stop flag on exit | Lease is drain/registry, not mutual exclusion — documented in LEGAL_PATHS |
| P9-P1-04 | Daily briefing chain breaks on early exits | FIXED | VERIFIED_FIXED | PIPELINE | `DailyBriefingWorker.kt:128-174`: `shouldRescheduleNextMidnight` reschedules on Success and all Skipped except `DISABLED_BY_SPEC_REASON` (`:188`) | Matches LEGAL_PATHS scheduling law |
| P9-P1-05 | Bill reminder disabled by static spec | FIXED | VERIFIED_FIXED | PIPELINE | `domain/workers/WorkerSpec.kt:68-77`: `bill_reminder_periodic` `enabled = true`, `version = 2` | |
| P9-P1-06 | Bill reminders not exactly-once | FIXED | VERIFIED_FIXED | PIPELINE | `BillReminderWorker.kt:82` `claimReminderDelivery` before dispatch; post-claim revalidation `:91-99`; `markReminderSent` only from CLAIMED `:107`; failure → `markReminderFailed` `:155` | Claim → notify → mark; permission-revoked delivery cancelled (`:133-137`) — see NEW-P9-2026-003 |
| P9-P1-07 | runOnce bypasses unique scheduling | FIXED | VERIFIED_FIXED | PIPELINE | `ReceiptMatchingWorker.kt:296-310` (`runOnce` unique name + `ExistingWorkPolicy.KEEP`); overlap guard = atomic per-receipt claim `requireUnmatchedClaim = true` (`:84-97`); `ReceiptAlreadyClaimedException` treated as benign `:146-152` | Matches LEGAL_PATHS AUTO-MATCH concurrency invariant |
| P9-P1-08 | Receipt matching outcomes not durable | FIXED | VERIFIED_FIXED | PIPELINE | `ReceiptMatchingWorker.kt:69-71` MATCH_SKIPPED_DOCUMENT_TYPE, `:77-79` MATCH_ATTEMPTED, `:181-183` MATCH_NOT_FOUND, `:158-165` AUTO_MATCH_LINK_FAILED (structured code `linkFailureCode` `:230-237`), notification-suppressed events `:114-143` | Via `ReceiptMatchLifecycleService` (Segment 38 owner) |
| P9-P1-09 | Warranty sent-state outside DB | FIXED | VERIFIED_FIXED | PIPELINE | `WarrantyExpirationWorker.kt:171-209`: `insertOrIgnore` → `claim` (SCHEDULED/FAILED only) → `markSentFromClaimed`; stale-claim recovery `:91-94,219` | Claim-before-notify protocol as documented |
| P9-P1-10 | Pause/resume registry hardcoded/asymmetric | FIXED | VERIFIED_FIXED | PIPELINE | `domain/workers/WorkerRegistry.kt:75-85` single registry; explicit `oneShotPolicy` on one-shot specs (`WorkerSpec.kt:94,118`); `PrivacyRuntimeWorkerPolicy` names validated against `WorkerSpec.DEFAULTS` (`:79-86`) | |
| P9-P1-11 | Privacy changes don't cancel workers | FIXED | VERIFIED_FIXED | PIPELINE | `domain/workers/PrivacyRuntimeWorkerPolicy.kt:57-103` (cancel/reschedule sets; `data_retention` cancel-exempt `:73`); wired in `data/privacy/PrivacySettingsRepositoryImpl.kt:161-172` | Exemption of cleanup worker matches privacy law (cleanup must keep running) |
| P9-NEW-03 | BackgroundJobRun rows recorded zero counts | FIXED | VERIFIED_FIXED | PIPELINE | Counters threaded through ctx in every worker (e.g. `BillReminderWorker.kt:84,97,109`; `ReceiptMatchingWorker.kt:62,100,108`; `WarrantyExpirationWorker.kt:86,98,112`) and persisted on success by guard (`WorkerExecutionGuard.kt:360-371`) | |
| NEW-P9-001 | TimeoutCancellationException misclassified | FIXED (audit: PARTIAL — dead path) | VERIFIED_FIXED | UNIVERSAL (CE classification) | `WorkerExecutionGuard.kt:187-198` (**runGuarded**) AND `:379-390` (**runGuardedWithContext**) both branch on `TimeoutCancellationException` BEFORE the generic CE check, honoring `timeoutPolicy` (RETRY default, `:57`) | **Audit gap CLOSED at HEAD** by worker-architecture PR12H-1 (`7076d730`); the production path (`runGuardedWithContext`) now has the same branch. No longer partial |
| NEW-P9-002 | BillReminderWorker bypasses guard for settings | FIXED | VERIFIED_FIXED | PIPELINE | `BillReminderWorker.kt:57-68`: settings/quiet-hours check is the first statements INSIDE `runGuardedWithContext` block (comment cites NEW-P9-002) | |
| NEW-P9-003 | WorkerRunContext counters not thread-safe | FIXED | VERIFIED_FIXED | PIPELINE | `domain/workers/WorkerRunContext.kt:15-31`: all 5 counters are `AtomicInteger` with `addAndGet` | |
| NEW-P9-004 | WarrantyExpirationWorker uses runGuarded (no context) | FIXED | VERIFIED_FIXED | PIPELINE | `WarrantyExpirationWorker.kt:72` uses `runGuardedWithContext`; ctx counters incremented `:86,98,112,119,132` | |
| NEW-P9-005 | WarrantyExpirationWorker uses System.currentTimeMillis | FIXED | VERIFIED_FIXED | UNIVERSAL (U-PR7) | `WarrantyExpirationWorker.kt:85,88,92-93` use injected `timeProvider.now()`; no `System.currentTimeMillis` anywhere in `domain/workers/`, `service/*`, `worker/`, `domain/bank/` (grep clean) | |
| NEW-P9-006 | WorkerSpecScheduler uses deprecated REPLACE | FIXED | VERIFIED_FIXED | PIPELINE | `WorkerSpecScheduler.kt:96` uses `ExistingPeriodicWorkPolicy.UPDATE` on version bump; one-shot version bump uses `ExistingWorkPolicy.REPLACE` (`:141,253`) which is NOT the deprecated API | Doc note "specs may still use REPLACE as design choice" is accurate |
| NEW-P9-007 | Version write not atomic with enqueue | FIXED | VERIFIED_FIXED | PIPELINE | `WorkerSpecScheduler.kt:114-117,155-157,291-293`: version persisted AFTER successful enqueue, inside the try block; crash between them safely re-enqueues | |
| NEW-P9-008 | NotificationIntakeWorker not in guard/registry | PARTIAL | VERIFIED_FIXED (tracker drift) | PIPELINE | `worker/NotificationIntakeWorker.kt:55` full `runGuardedWithContext` for the main flow and `:397-417` a second guarded run for privacy cleanup; DB ops all inside guard with checkpoints (`:76,86,99,111,117,125,…`) | **Tracker drift:** tracker + consolidated doc still say PARTIAL ("checkpoint only"). Worker-architecture PR6C/PR12D/PR12H-2/PR12I-3 (`e47cc327`,`46ff0fcd`,`8269f957`,`a9d552f8`) migrated it. Doc/tracker need flip to FIXED |
| NEW-P9-009 | LocationBackfillWorker isStopped exits SUCCESS | FIXED | VERIFIED_FIXED | PIPELINE | `LocationBackfillWorker.kt:165-167`: `if (isStopped) throw RetryableWorkerException(...)` after loop (P9-PR2 comment) | |
| NEW-P9-010 | MerchantKeyBackfillWorker same isStopped issue | FIXED | VERIFIED_FIXED | PIPELINE | `MerchantKeyBackfillWorker.kt:119-121`: same `RetryableWorkerException` on `isStopped` | |
| NEW-P9-011 | scheduleAtMidnight near-zero delay | FIXED | VERIFIED_FIXED | PIPELINE | `WorkerSpecScheduler.kt:273-277`: `val delayMs = maxOf(rawDelayMs, 60_000L)` | |
| NEW-P9-012 | DailyBriefing reschedule failure swallowed | FIXED | VERIFIED_FIXED | UNIVERSAL (U-PR6) | `DailyBriefingWorker.kt:136-138` logs `result.error` on failed reschedule; `WorkerSpecScheduler.scheduleAtMidnight` also emits a durable schedule diagnostic on failure (`:302-323`) | |
| NEW-P9-013 | Guard read-only path no exception handling | FIXED | VERIFIED_FIXED | PIPELINE | `WorkerExecutionGuard.kt:109-119` (runGuarded) and `:288-298` (runGuardedWithContext): try/catch around `readBarrier.checkReadAllowed` with `CancellationSafe.rethrowIfCancellation` | Note: the read-only *block* itself (`:143-146,311-315`) is still uncaught by design (no run logged) — see §4 observation |
| NEW-P9-014 | No battery constraint for merchant_key_backfill | FIXED | VERIFIED_FIXED | PIPELINE | `WorkerSpec.kt:119-124`: `setRequiresBatteryNotLow(true)` with P9 (NEW-P9-014) comment | |
| NEW-P9-015 | WorkerRunLogger.Handle not idempotent | FIXED | VERIFIED_FIXED | PIPELINE | `WorkerRunLogger.kt:136-146` Mutex + `completed` flag; `terminal()` marks completed only after `affected == 1` or DB-confirmed terminal (`:223-235`); all 6 terminal methods funnel through `terminal()` | Supersedes the AtomicBoolean fix from 5ed47933 (PR12B) |

### 3.1 Tracker/doc drift found this session

1. **NEW-P9-008 under-claimed (doc + tracker stale):** both `PIPELINE_9_CONSOLIDATED_ISSUES.md` (:39, :66, :80) and `PIPELINE_ISSUES_MASTER_TRACKER.md` (:302) mark NotificationIntakeWorker as ⚠ PARTIAL "checkpoint only". At HEAD (`d1fa9c68`) the worker runs the full `runGuardedWithContext` lifecycle including a second guarded run for privacy cleanup (`NotificationIntakeWorker.kt:55,397-417`). Status should be flipped to ✅ FIXED, making P9 fully 27/27 fixed.
2. **NEW-P9-001 audit flag resolved:** FIXED_CLAIMS_VALIDATION_AUDIT_v4 addendum 1 (:138, :147) flagged the timeout-retry branch as present only in the unused `runGuarded`. The WORKER_ARCHITECTURE plan landed since: PR12H-1 (`7076d730`, "Guard timeout + checkpoint block semantics") added the branch to `runGuardedWithContext` too (`WorkerExecutionGuard.kt:379-390`). The flag is closed at HEAD; audit doc itself needs no edit (it was accurate at `661e78a1`) but a fresh note is warranted.
3. **Minor doc-internal inconsistency (cosmetic):** `PIPELINE_9_CONSOLIDATED_ISSUES.md:24` says "All workers migrated to runGuardedWithContext with counts" while NEW-P9-008 in the same file says intake does not use the full lifecycle — internally contradictory since the PR12-series landed.

## 4. New issues & regressions found this session

| ID | Sev | Evidence | Description | Suspected origin |
|----|-----|----------|-------------|------------------|
| NEW-P9-2026-001 | MINOR | `DailyBriefingWorker.kt:71-82` (`requiresNotificationPermission = true`) + `WorkerExecutionGuard.kt:356-358` (whole block skipped, run logged SKIPPED) | Daily briefing **artifact generation** is gated on notification permission, not just notification posting. When notifications are denied, no briefing artifact is generated at all, although the artifact has in-app value. AGENTS.md law: "Notification permission should gate notification posting only. Do not globally block unrelated core work." Contrast: `ReceiptMatchingWorker.kt:44` correctly sets `requiresNotificationPermission = false` and checks locally before posting (`:101`). Midnight chain survives (skip → reschedule), so impact is bounded to artifact staleness | Pre-existing (worker-architecture PR12C wired the policy); universal-pattern candidate |
| NEW-P9-2026-002 | MINOR | `WorkerExecutionGuard.kt:574-579` in `startRunSafely`: both `if (classifyTransient(e))` and `else` return `StartRunResult.Retry` | A **permanent** failure to start a run (e.g. non-transient crash in `WorkerRunLogger.start`) is classified as Retry, identical to transient — cannot be distinguished and cannot FAIL. Bounded in practice by WorkManager's attempt budget, but the else-branch is dead logic and the retry-vs-failure contract is blurred for the start path | Pre-existing |
| NEW-P9-2026-003 | MINOR | `BillReminderWorker.kt:131-137`: on `NotificationSendResult.Failed("permission_denied")` (SecurityException caught in `sendNotification` `:251-253`) the worker **cancels** the claimed delivery (`cancelClaimedReminderDelivery(reason = "notification_permission_revoked")`) | If permission is revoked between the guard pre-check (`WorkerExecutionGuard.kt:174-176`) and the notify call, the bill reminder is **permanently lost** (CANCELLED deliveries never re-dispatch). Leaving it CLAIMED (stale-claim recovery resets to SCHEDULED after 1h) would retry once permission is restored. Trade-off was chosen in P4, but it silently drops a due reminder | Pre-existing (P4 design); edge case |
| NEW-P9-2026-004 | MINOR | `DailyBriefingWorker.kt:113-119`: `ctx.addNotificationsSent()` unconditionally after `deliverProactiveBriefingNotificationUseCase` returns, although the use case "may still internally no-op on settings/dedupe" (acknowledged in the comment) | `BackgroundJobRun.notificationsSent` can over-count (metrics incremented without an actual successful post). Violates "increment notification metrics only after a real successful post" in spirit; the code comment documents the limitation and defers the return-value change | Pre-existing (P9-S4 counts-only slice) |

**Non-issue observations (recorded, not tracked as defects):**
- Guard read-only backup path (`allowDuringBackupExport = true` + `requiresDatabaseWrite = false`, `WorkerExecutionGuard.kt:102-146,285-315`) currently has **zero** users — every worker passes `allowDuringBackupExport = false`. Dead-but-tested path; its block() is intentionally not wrapped in the exception classifier (no run row exists to close).
- `WorkerLeaseRegistryImpl.WorkerLeaseImpl.checkpoint()` throws a raw `CancellationException` on stop (`WorkerLeaseRegistryImpl.kt`, lease checkpoint), but no caller uses it — workers use `ctx.checkpoint` → `WorkerExecutionGuard.checkpoint` which throws `WorkerCheckpointBlockedException` (`WorkerExecutionGuard.kt:250-274`) so the guard can apply `blockedPolicy`. Safe.
- `WorkerCheckpointBlockedException.message` embeds `e.message` of the barrier exception (`WorkerExecutionGuard.kt:267-271`) — in-memory only; only the controlled `reasonCode` is persisted (`WorkerRunLogger.kt:76`), so no durable privacy exposure.
- T3A time migration (`214bbb44`) touched `WorkerSpecScheduler` additively (TimeProvider param for midnight math); PR18–PR24 range (`bffc4299..65c265fb`) touched nothing under `domain/workers/` or `domain/bank/`; PII burn-down commits left the guard path clean (`WorkerRunLogger.kt:269-284` sanitizes `error?.message` via `EventMetadataSanitizer`, persists only `errorClass.simpleName`; `WorkerReasonCodes.sanitizeReasonCode` bounds reason fields to `[A-Z0-9_]{1,80}`).

## 5. Universal candidates

- **Permission gating unrelated core work** — NEW-P9-2026-001 (DailyBriefingWorker artifact generation gated on notification permission). This matches the known universal category. The U-PR fixes covered receipt matching / DB repair (ReceiptMatchingWorker correctly un-gated at `:44`), but the briefing worker still gates its core work. Not covered by any U-PR fix; recommend a targeted follow-up.
- **CancellationException swallowed in broad catches** — recurring P9/P10 pattern historically; at HEAD every broad catch in the 7 workers + intake + guard + BankApiIntegration (`:360-365`) + BankStatementLifecycleProcessor (`:141-142,157-158,278-279,687-688,837-843`) rethrows CE. **Covered by U-PR1; verified clean.**
- **System.currentTimeMillis instead of TimeProvider** — zero occurrences in all P9 key files. **Covered by U-PR7; verified clean.**
- **Raw payload/exception-message logging** — worker diagnostics persist sanitized reason codes, exception class simpleName only, and sanitized messages (`WorkerRunLogger.kt:269-284`; `WorkerReasonCodes.kt`; `OperationRunRecorder.kt:238-239`). **Covered by PR12I/J/K burn-down; verified clean.**
- **Fix-in-dead-path (audit roll-up failure mode #1)** — historically NEW-P9-001; closed at HEAD (both guard paths). No new instances in P9.

## 6. Recommended next actions (ordered, concrete, with file paths)

1. **Flip NEW-P9-008 to FIXED** in `docs/analyses and debug master/PIPELINE_9_CONSOLIDATED_ISSUES.md` (rows :39, :66, :80) and `docs/analyses and debug master/PIPELINE_ISSUES_MASTER_TRACKER.md:302` — evidence: `app/src/main/java/com/yourname/expensetracker/worker/NotificationIntakeWorker.kt:55,397-417`. P9 becomes 100% fixed.
2. **Fix NEW-P9-2026-001**: in `app/src/main/java/com/yourname/expensetracker/data/ai/worker/DailyBriefingWorker.kt:75` set `requiresNotificationPermission = false` and check permission locally only around `deliverProactiveBriefingNotificationUseCase` (mirror `ReceiptMatchingWorker.kt:101-143`).
3. **Reconsider NEW-P9-2026-003**: in `app/src/main/java/com/yourname/expensetracker/service/reminder/BillReminderWorker.kt:131-137`, on `permission_denied` prefer leaving the delivery CLAIMED (or reset to SCHEDULED) so it retries after permission is restored, instead of `cancelClaimedReminderDelivery`.
4. **Tighten NEW-P9-2026-002**: in `WorkerExecutionGuard.startRunSafely` (`app/src/main/java/com/yourname/expensetracker/domain/workers/WorkerExecutionGuard.kt:574-579`), return a distinct non-retryable outcome (e.g. `Blocked`/`Failed`) for non-transient start failures, or document why Retry-only is intentional.
5. **Close NEW-P9-2026-004** when the delivery use case grows a return value: only `ctx.addNotificationsSent()` on a confirmed delivery (`app/src/main/java/com/yourname/expensetracker/data/ai/worker/DailyBriefingWorker.kt:113-119`).
6. **Optional hygiene**: add a `WorkerSpec.DEFAULTS`/`listAllWorkerNames` note for the second guarded name `notification_intake_privacy_cleanup` (`NotificationIntakeWorker.kt:400`) so guard-verifier tooling does not flag it as unknown.
7. Human-run validation (not executed in this session, static only): `./gradlew :app:testDebugUnitTest --tests "*WorkerExecutionGuard*" --tests "*WorkerRunLogger*" --tests "*WorkerTerminalDiagnostic*" --console=plain`.
