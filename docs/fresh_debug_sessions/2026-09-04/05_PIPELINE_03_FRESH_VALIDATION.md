# PIPELINE_03 — Fresh Validation (2026-09-04)

- Validated at: d1fa9c688c0a6e5cd1a537ceff9830ad29748057 (branch `atomicity-pr21-enforcement-final`). Static validation only — no compile/test run.
- Source doc: docs/analyses and debug master/PIPELINE_3_CONSOLIDATED_ISSUES.md (last validated 2026-05-31)
- Cross-checked against: PIPELINE_ISSUES_MASTER_TRACKER.md (Pipeline 3 section, lines 118–142) and FIXED_CLAIMS_VALIDATION_AUDIT_v4.md flags.
- All paths below are relative to `app/src/main/java/com/yourname/expensetracker/` unless prefixed `app/src/test/`.

## 1. Pipeline theme

Receipt/OCR/Email ingestion: OCR capture, receipt parsing, receipt lifecycle (save/dedup/link/delete), bank-statement import, receipt↔expense matching. The theme of this validation round: every consolidated-doc "open" item has since been closed in code — the remaining risks are (a) the P3-P1-07 audit flag (build-red at ca5972bf-era HEAD) which is now resolved, (b) stale receipt snapshots read outside transactions in `ReceiptLinkService`, and (c) a dead side-effect dispatcher with stale KDoc.

## 2. Verdict summary

| Verdict | Count |
|---|---:|
| Verified fixed | 19 |
| Verified open | 0 |
| Verified partial | 0 |
| Regressed | 0 |
| Tracker drift | 9 (8 stale entries in the consolidated doc + 1 audit-flag resolution for P3-P1-07) |
| New issues found | 3 (all MINOR) |
| Not verifiable statically | 0 |

Note: the drift runs in the "consolidated doc is stale" direction — PIPELINE_3_CONSOLIDATED_ISSUES.md still lists 8 items as open/partial/todo that the tracker already flipped to FIXED and that code confirms as fixed. The master tracker's Pipeline 3 section is accurate at HEAD.

## 3. Issue-by-issue validation

| ID | Title (short) | Old status | Fresh status | Class | Evidence (file:line) | Notes |
|----|---------------|-----------|--------------|-------|----------------------|-------|
| P3-P0-01 | Scanned receipts saved with `createdAt = 0` | FIXED | VERIFIED_FIXED | PIPELINE | domain/receipt/lifecycle/ReceiptTimestampPolicy.kt:23–27 (`forInsert` repairs `createdAt=0`); ReceiptLifecycleCoordinator.kt:510–513 (`forInsert` at save) | Guard applied on both insert and update paths (`forUpdate` repairs too). |
| P3-P1-01 | Receipt save/update/event not atomic | FIXED | VERIFIED_FIXED | PIPELINE | ReceiptLifecycleCoordinator.kt:510–556 (insert + RECEIPT_SAVED event + PendingReview inside single `transactionRunner.runInTransaction`) | Atomicity holds including the MIT-041 PendingReview-in-transaction addition (line 535). |
| P3-P1-02 | `ReceiptLinkService` lacks restore guard | FIXED | VERIFIED_FIXED | UNIVERSAL (restore/write barrier) | ReceiptLinkService.kt:160–165 (link), 372–378 (unlink) — `writeBarrier.checkWritesAllowed` with CE rethrow | Barrier precedes any read/write. |
| P3-P1-03 | Matching result computed but not persisted | TODO ONLY → FIXED | VERIFIED_FIXED | PIPELINE | ReceiptMatchLifecycleService.kt:159–206 (MATCH_ATTEMPTED/MATCH_NOT_FOUND/MATCH_SKIPPED_DOCUMENT_TYPE); ReceiptMatchingWorker.kt:181; ReceiptSideEffectPlanner.kt:404–413 (NoMatch → durable MATCH_NOT_FOUND) | All three match outcomes persisted. Matches LEGAL_PATHS.md auto-match contract. |
| P3-P1-04 | Receipt-created expense + link not atomic | PARTIAL → FIXED (tracker) | VERIFIED_FIXED (tracker drift: consolidated doc still says PARTIAL) | PIPELINE | ReceiptLifecycleCoordinator.kt:1326–1368 (`createExpenseAndLinkReceipt` — barrier + `transactionRunner.runInTransaction`); ReceiptRepository.kt:386–393, 541, 731, 769, 848–858 (legacy paths `@Deprecated(ERROR)`) | Legacy convenience paths are compile-time errors; coordinator is single owner. |
| P3-P1-05 | Direct repository methods bypass lifecycle | PARTIAL → FIXED (tracker) | VERIFIED_FIXED (tracker drift) | PIPELINE | ReceiptRepository.kt:413 ("No production callers remain"), 506/526/533 (WARNING deprecations routed to coordinator/ReceiptRecordWriter) | Remaining direct-DAO surfaces are deprecated and documented for backfill/debug only. |
| P3-P1-06 | `ScannedReceiptDao.insert()` IGNORE conflict not checked | TODO ONLY → FIXED | VERIFIED_FIXED | PIPELINE | data/repository/ReceiptInsertResolver.kt:31–49 (`insertOrResolve` treats `id <= 0` as conflict; resolves via imageHash → sourceFingerprint → textFingerprint → semanticFingerprint); typed `ConflictUnresolved` result surfaced | Callers branch on the sealed result; no caller proceeds with id=0. |
| P3-P1-07 | Currency fallback hardcoded EUR in OCR parse | TODO ONLY → FIXED, but audit v4 flagged ACTUALLY_OPEN | VERIFIED_FIXED at HEAD d1fa9c68 (with caveats) | UNIVERSAL (silent EUR fallback) — special verdict, see §3.1 | Compile error fixed in commit 25c636e1 (added `?: "EUR"` at domain/usecase/receipt/ProcessReceiptUseCase.kt:26); `ReceiptParser.parse(rawText: String, homeCurrency: String = "EUR")` at domain/receipt/ReceiptParser.kt:209 now receives non-null (ProcessReceiptUseCase.kt:30,41). Production path: ReceiptRepository.kt:427–432 `homeCurrency()` resolves settings with `"XXX"` last resort; ReceiptLifecycleCoordinator.kt:130 `FALLBACK_CURRENCY = "XXX"` | bb2a6f18 did NOT fix this (its stat touches no receipt file) — the fix predates it in 25c636e1. Residual caveats: (a) use case has no production callers (only `app/src/test/.../ProcessReceiptUseCaseHomeCurrencyTest.kt`); (b) line 26 `?: "EUR"` + AppConfigCurrencyProvider EUR last-resort (see NEW-P3-2026-003). |
| P3-P1-08 | Parse failures classified as `OCR_COMPLETED` | TODO ONLY → FIXED | VERIFIED_FIXED | PIPELINE | data/repository/ReceiptRepository.kt:254 (`processingStatus = PARSE_FAILED` on parse-throw path), 260 (coordinator owns event in same transaction) | Durable event accompanies the status. |
| P3-P1-09 | Batch receipt import no longer creates pending reviews | TODO ONLY → FIXED (tracker) | VERIFIED_FIXED (tracker drift) | PIPELINE | ReceiptRepository.kt:572–606 (batch routes each item through `processReceiptInput` with `createReview = true`, CE rethrow at 609); ReceiptLifecycleCoordinator.kt:135–142 + 536–556 (PendingReview when `createReview` or confidence < 0.75 threshold) | Threshold constant `BATCH_REVIEW_CONFIDENCE_THRESHOLD = 0.75` documented in coordinator. |
| P3-P1-10 | Bank statement lifecycle dedupe weaker than legacy | TODO ONLY → FIXED (tracker) | VERIFIED_FIXED (tracker drift) | PIPELINE | BankStatementLifecycleProcessor.kt:528–545 (expense-level dedup: `existsByMerchantKeyInRangeCurrencyAware` OR `existsByMerchantInRangeCurrencyAware`), 592–600 (pending-review-level dedup), 641–660 (review + import item inserted atomically) | Currency-aware, type-aware, tolerance window; matches LEGAL_PATHS three-layer dedup. |
| NEW-P3-001 | CancellationException swallowed in `ReceiptSideEffectDispatcher` | FIXED (U-PR1) | VERIFIED_FIXED | UNIVERSAL (CE swallowed) | ReceiptSideEffectDispatcher.kt:46–48 (`if (e is kotlinx.coroutines.CancellationException) throw e` before Timber.e) | U-PR1 pattern present. |
| NEW-P3-002 | CancellationException swallowed in `BankStatementLifecycleProcessor` per-item | FIXED (U-PR1) | VERIFIED_FIXED | UNIVERSAL (CE swallowed) | BankStatementLifecycleProcessor.kt:140–142, 157–158, 278–279, 687–688, 837–838, 908–909, 929–930 — every catch rethrows CE | No remaining broad catch without CE rethrow in this file. |
| NEW-P3-003 | CancellationException swallowed in `ReceiptLinkService.unlinkReceiptFromExpense` | FIXED (U-PR1) | VERIFIED_FIXED | UNIVERSAL (CE swallowed) | ReceiptLinkService.kt:462–464 (outer catch), 317–321 (RCP-30 propagation), 162–163/375–376 (barrier guards) | Also hardened by ca760783 (raw `runCatching` → try/catch with CE rethrow). |
| NEW-P3-004 | Double `attachReceipt` call in `BankStatementLifecycleProcessor` | FIXED (P3-PR3) | VERIFIED_FIXED | PIPELINE | BankStatementLifecycleProcessor.kt:322–332 — attachReceipt calls sit in mutually exclusive `when (val write = receiptRecordWriter.insertOrResolve(...))` branches (Inserted vs Duplicate) | No double-attach path exists at HEAD. |
| NEW-P3-005 | Race in post-OCR duplicate path | OPEN → FIXED (tracker, P3-PR3) | VERIFIED_FIXED (tracker drift) | PIPELINE (race — universal candidate) | ReceiptLifecycleCoordinator.kt:429–475 — for `receipt.id > 0` the dedup check + `getById` + update + DUPLICATE_DETECTED event are all inside one `transactionRunner.runInTransaction` (line 431); draft path (id ≤ 0) documented as intentionally read-only-outside (477–500) | Comment block at 429–433 explicitly cites NEW-P3-005. |
| NEW-P3-006 | Privacy leak — merchant/category logged in production | OPEN → FIXED (tracker, P3-PR3) | VERIFIED_FIXED (tracker drift) | UNIVERSAL (privacy logging) | ReceiptLifecycleCoordinator.kt:1002 (`validation failed: [REDACTED]`), 1010 (`insert conflict: [REDACTED]`), 1242 (`assetPath=[REDACTED]`), 1262 (`assetPath=[REDACTED]`); BankStatementLifecycleProcessor.kt:584/633 (`SKIP: ... [REDACTED]`, merchant sanitized at 585/637 via `sanitizeMerchant`); constant reason strings introduced in d40c230b | No merchant/category/URI content in production Timber calls found in this segment. |
| NEW-P3-007 | `deleteReceipt` writes event for non-existent receipt | OPEN → FIXED (tracker, P3-PR3) | VERIFIED_FIXED (tracker drift) | PIPELINE | ReceiptLifecycleCoordinator.kt:1200–1209 — `requireNotNull(scannedReceiptDao.getById(receiptId))` inside the delete transaction aborts before event/asset deletion | Exception (not silent success) for missing receipt — fail-closed. |
| NEW-P3-008 | `homeCurrency()` inside `withContext` may cause thread starvation | OPEN → FIXED (tracker, P3-PR3) | VERIFIED_FIXED (tracker drift) | UNIVERSAL (unbounded settings-flow read) | domain/currency/CurrencySettingsRepository.kt:66–86 — `resolveHomeCurrency()` bounds the flow read with `withTimeoutOrNull(5_000L)` and returns typed `HomeCurrencyResolution`; ReceiptLifecycleCoordinator.kt:814–818 resolves BEFORE opening the transaction (P11-CURRENT-020) with `"XXX"` last resort; domain/receipt/BankStatementParser.kt:113 (`withTimeoutOrNull(3_000L)`) | Both coordinator and parser bounded; no settings Flow awaited inside DB transactions. |

### 3.1 Tracker/doc drift found this session

1. **P3-P1-07 — audit flag resolution (the headline item).** FIXED_CLAIMS_VALIDATION_AUDIT_v4.md flagged P3-P1-07 as ACTUALLY_OPEN: `getHomeCurrency(): String?` passed into non-null `ReceiptParser.parse(homeCurrency: String)` at ProcessReceiptUseCase.kt:29/40 with no fallback (hard compile error introduced by ca5972bf), plus hardcoded EUR and dead code. At HEAD d1fa9c68:
   - The nullability compile error is FIXED — commit 25c636e1 ("docs: sweep-update…", 2026-06-01, which also carried this 5-line code repair) added `?: "EUR"` (ProcessReceiptUseCase.kt:26). Statically the types now line up: `String?` → coerced non-null `String` → matches `parse(rawText: String, homeCurrency: String)` (ReceiptParser.kt:209).
   - bb2a6f18 ("repair pre-existing compile/test/gate blockers", the commit the audit asked us to check) did NOT touch ProcessReceiptUseCase.kt — `git show bb2a6f18 --stat` lists 19 files, none receipt-related. It repaired other blockers (SystemMonotonicTimeProvider, MIGRATION_145_146, test compile errors). So the P3-P1-07 compile blocker was already gone before bb2a6f18.
   - Hardcoded EUR is reduced but not extinct: the production OCR path never uses EUR fallback (ReceiptRepository.homeCurrency() → "XXX", ReceiptRepository.kt:427–432; coordinator FALLBACK_CURRENCY "XXX", line 130). The dead use case still has a `?: "EUR"` (line 26) and AppConfigCurrencyProvider still last-resorts to `AppConfig.DEFAULT_CURRENCY = "EUR"` (AppConfig.kt:9, AppConfigCurrencyProvider.kt:17–20). See NEW-P3-2026-003.
   - Dead code: ProcessReceiptUseCase still has zero production call sites (repo-wide grep: only its own file + one unit test). The "fix in the dead path" failure mode from the audit therefore still applies to this file, but the user-visible behavior is now correct because the production path (ReceiptRepository + coordinator) resolves currency properly.
   - **Verdict: P3-P1-07 = VERIFIED_FIXED at HEAD d1fa9c68** (compile fixed at 25c636e1; EUR fallback removed from the production path), with MINOR residuals tracked as NEW-P3-2026-003. Tracker status FIXED is accurate.
2. **Consolidated doc staleness (8 items).** PIPELINE_3_CONSOLIDATED_ISSUES.md still shows P3-P1-04/PARTIAL, P3-P1-05/PARTIAL, P3-P1-09/TODO, P3-P1-10/TODO, NEW-P3-005..008/OPEN. All eight are fixed in code (evidence above) and already flipped to FIXED in PIPELINE_ISSUES_MASTER_TRACKER.md:128–142. The consolidated doc needs a status sweep; the tracker does not.
3. **Attribution drift (audit note).** Audit v4 correctly observed that P3-P1-03/06/08 fixes pre-date ca5972bf; confirmed — the parity work lives in earlier pipeline commits (e.g., eb0b6a6e "Pipeline 3: finalize all remaining gaps"), not in ca5972bf.
4. **KDoc drift in ReceiptRepository.processBatch** (line 578) claims "Post-save side effects via ReceiptSideEffectDispatcher" — see NEW-P3-2026-002; the dispatcher has no call sites.

## 4. New issues & regressions found this session

| ID | Sev | Evidence | Description | Suspected origin |
|----|-----|----------|-------------|------------------|
| NEW-P3-2026-001 | MINOR | ReceiptLinkService.kt:168–177 (receipt+expense loaded outside transaction) vs 252–260 (full-entity `scannedReceiptDao.update(receipt.copy(...))` inside transaction); same pattern 382 vs 411–421 in unlinkReceiptFromExpense | Stale-snapshot TOCTOU: the receipt is read before `transactionRunner.runInTransaction` and the whole entity is rewritten from that stale copy inside the transaction. A concurrent mutation (e.g., auto-match status change, updatedAt bump) between read and commit is silently overwritten (lost update). The `requireUnmatchedClaim` CAS path (242–250) is safe; only the non-claim DIRECT_SAVE/REVIEW_APPROVAL/unlink paths are affected. AUTO_MATCH overlap is guarded by `claimForAutoMatch` (ReceiptLinkService.kt:242–250; LEGAL_PATHS.md:85–88). | Pre-existing (structure unchanged by PR20–22 work) |
| NEW-P3-2026-002 | MINOR | ReceiptSideEffectDispatcher.kt:22–51 (compat wrapper, hardcodes `RawStorageMode.STORE_RAW` + `ephemeralRawOcrText = null` at line 41–44); zero call sites of `dispatchAfterSave` (repo grep); injected but unused at ReceiptLifecycleCoordinator.kt:111; stale KDoc ReceiptRepository.kt:578 | Dead side-effect dispatcher + doc drift. The planner (ReceiptSideEffectPlanner.kt:74–82) treats null ephemeral text as "skip raw-dependent side effects" (P3-EB0-06), so if anything ever calls the dispatcher, warranty extraction would silently no-op. Camera-path side effects actually run later via the review-approval path (data/repository/ReviewQueueRepository.kt uses PostCommitActionRunner). Same "fix lands in the dead path" pattern the audit flagged for P3-P1-07. Recommend deleting the wrapper or wiring it, and fixing the KDoc. | Pre-existing (planner/dispatcher refactor leftover) |
| NEW-P3-2026-003 | MINOR | ProcessReceiptUseCase.kt:26 (`?: "EUR"`); AppConfigCurrencyProvider.kt:17–20 + AppConfig.kt:9 (`DEFAULT_CURRENCY = "EUR"`); vs ReceiptRepository.kt:427–432 and ReceiptLifecycleCoordinator.kt:130 (`"XXX"` ISO-unknown) | Inconsistent last-resort currency policy inside the same segment: receipt persistence paths use ISO-4217 "XXX" (never silently EUR) while the (dead) use case and the AppConfig-based provider silently fall back to EUR. If the use case is ever wired in, a settings-read failure would silently book OCR totals in EUR. Align all last-resorts on explicit `HomeCurrencyResolution`/`"XXX"` semantics. | Pre-existing; universal "silent EUR fallback" category residual |

No regressions found in this segment from the T1–T4C time migration (see §5), the PR18–24 cancellation rework, or the PII burn-down commits (e7f3496e, 92a6ebf7, b2025407, ca760783, f652218f, d40c230b) — the latter strictly improved this segment (RCP-30 CE rethrow in ca760783; NaN/Infinity ledger no longer persists raw amounts, f652218f).

## 5. Universal candidates

| Universal category | Pipeline-3 instances | Covered by U-PR fixes? |
|---|---|---|
| CancellationException swallowed in broad catches | NEW-P3-001, 002, 003 | YES — U-PR1 guards verified at all three files (ReceiptSideEffectDispatcher.kt:47, ReceiptLinkService.kt:463, BankStatementLifecycleProcessor 7 sites); shared helper `CancellationSafe` (domain/util/CancellationSafe.kt:15) + runCatching ban (PR12b d9cac7f4, PR22-small ca760783) hold here. |
| Mixed-currency money math / silent EUR fallback | P3-P1-07; NEW-P3-2026-003 | PARTIAL — production paths fixed ("XXX" policy); the dead use case + AppConfigCurrencyProvider still carry EUR fallbacks. Not covered by any tracked universal item; recommend folding into U-MONEY follow-up. |
| Raw payload/exception-message logging | NEW-P3-006; d40c230b reason-constant conversions | YES — [REDACTED] placeholders + sanitized failure strings verified; `safeFailureReason` in ReceiptRepository.kt:434–447 redacts URIs. |
| TOCTOU read-outside-transaction | NEW-P3-2026-001 (link/unlink stale snapshot) | NO — no universal item tracks full-entity stale-snapshot rewrites; recommend a universal rule "load inside transaction or use conditional UPDATE" (the recurring pipeline already does this better — see 06 doc). |
| Maintenance/restore barrier mishandling | P3-P1-02, P3-P1-05, BankStatementLifecycleProcessor barrier at 140/197/320 + per-tx re-checks (565/601/646/690) | YES — barrier-before-read ordering verified at every write entry point. |
| Time migration blast radius | Recurring/receipt pipeline uses `TimePeriodUtils.getStartOfDay/getEndOfDay` (RecurringLifecycleCoordinator.kt:292–293 etc.) whose `getEndOfDay` semantics changed from inclusive 23:59:59.999 to exclusive next-midnight in T4C (0c68f5c6, via 8e40187f) | SAFE HERE — the recurring DAO range query is half-open (`dueDate >= :start AND dueDate < :end`, RecurringOccurrenceDao.kt:26–27) and bank-statement dedup already used `windowEndExclusive` (BankStatementLifecycleProcessor.kt:524). No pipeline-3 file relied on the old inclusive end. addDays/addMonths remain Calendar-based DST-safe (TimePeriodUtils.kt:1344–1360). |
| Fail-open privacy | BankStatementLifecycleProcessor writes own events outside ReceiptLifecycleCoordinator (comment at 311–318) | ACCEPTED DEVIATION — LEGAL_PATHS.md "Bank Statement Mutations" sanctions the processor as the legal entry point; barrier + events present. |

## 6. Recommended next actions (ordered, concrete)

1. **Fix NEW-P3-2026-001** — in `ReceiptLinkService.linkReceiptToExpense`/`unlinkReceiptFromExpense`, move the `scannedReceiptDao.getById` load inside `transactionRunner.runInTransaction` (ReceiptLinkService.kt:168/382) or switch the legacy-field update to a targeted conditional UPDATE (as `claimForAutoMatch` already does) so concurrent mutations are not clobbered by a stale full-entity copy.
2. **Close NEW-P3-2026-002** — delete the unused `ReceiptSideEffectDispatcher` (ReceiptSideEffectDispatcher.kt) and its unused injection at ReceiptLifecycleCoordinator.kt:111, or wire it to the planner with real ephemeral text; update the stale KDoc at ReceiptRepository.kt:578 in the same diff.
3. **Align currency last-resort policy (NEW-P3-2026-003)** — change ProcessReceiptUseCase.kt:26 fallback and AppConfigCurrencyProvider (data/currency/AppConfigCurrencyProvider.kt) to the "XXX"/`HomeCurrencyResolution.Failed` pattern used by ReceiptRepository.kt:427–432; decide whether to keep the dead use case at all (delete or wire it).
4. **Sweep the consolidated doc** — update docs/analyses and debug master/PIPELINE_3_CONSOLIDATED_ISSUES.md statuses for P3-P1-04/05/09/10 and NEW-P3-005..008 to FIXED (tracker at PIPELINE_ISSUES_MASTER_TRACKER.md:128–142 is already correct), and record the P3-P1-07 resolution (25c636e1) against audit v4.
5. **Human-run validation** (per repo convention, not run here): `./gradlew :app:testDebugUnitTest --tests "*ProcessReceiptUseCaseHomeCurrencyTest*" --tests "*ReceiptInsertResolver*" --tests "*ReceiptMatching*" --console=plain`.
