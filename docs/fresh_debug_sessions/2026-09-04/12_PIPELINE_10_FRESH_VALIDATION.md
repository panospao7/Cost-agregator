# PIPELINE_10 — Fresh Validation (2026-09-04)

- Validated at: `d1fa9c68` (branch `atomicity-pr21-enforcement-final`). Static validation only — no compile/test run.
- Source doc: `docs/analyses and debug master/PIPELINE_10_CONSOLIDATED_ISSUES.md`
- Cross-checked against: `PIPELINE_ISSUES_MASTER_TRACKER.md` (P10 section: P10-P1-01..09 "design TODOs") and `FIXED_CLAIMS_VALIDATION_AUDIT_v4.md` addendum 2 (NEW-P10-002 flagged ACTUALLY_PARTIAL — `DecryptResult.KeyInvalidated` never consumed; NEW-P10-001 flagged as introducing a test-compile regression).
- Architecture law checked against: `docs/architecture/LEGAL_PATHS.md` (Expense Mutations, Privacy/Cloud AI, Backup/Restore, Bank Statement Mutations), `docs/architecture/CODEBASE_SEGMENTS.md` (Segments 14, 28, 38), `docs/architecture/dao-map.md` (Bank Domain), `docs/architecture/ENGINE_INTERACTION_MAP.md`.

## 1. Pipeline theme

Bank integration: the debug-stub `BankApiIntegration` (`requireStubMode` release guard, connection persistence, demo sync), token security (`BankTokenCipher` AES/GCM keystore, key-invalidation surfacing), the bank-connections UI (`BankConnectionsViewModel` + `BankConnectionLifecycleCoordinator`), low-confidence review routing, bank-provenance metadata on imported expenses, restore/write-barrier coverage, and bank-statement import dedupe shared with expense dedupe. Largely Segment 14 (`domain/bank/`, `ui/screens/bank/`) with Segment 28 security (`data/security/BankTokenCipher.kt`) and the statement processor (Segment 4 lifecycle).

## 2. Verdict summary

| Verdict | Count |
|---|---:|
| Verified fixed | 10 |
| Verified open | 1 (P10-P1-02 OAuth/PKCE) |
| Verified partial | 4 (P10-P1-03, P10-P1-07, P10-P1-09, NEW-P10-002) |
| Regressed | 0 in tracked issues; 1 new regression (NEW-P10-2026-001) |
| Tracker drift | 7 (6 under-claims: P10-P0-02, P1-01, P1-04, P1-05, P1-06, P1-08 fixed in code; 1 over-claim: NEW-P10-002) |
| New issues found | 4 (2 MAJOR, 2 MINOR) |
| Not verifiable statically | 0 |

The source doc ("Last validated 2026-05-31", 14 open items) is heavily stale: commit `2e9ed3fe` ("fix(p10,p11): complete P10+P11 — 11 issues FIXED", 2026-06-01) and `d40c230b` ("PR A", 2026-07-10) fixed or improved 10 of the 15 tracked issues. The tracker claim that "P10-P1-01..09 are design TODOs" is itself drift — only P10-P1-02 remains genuinely open; P1-03/07/09 and NEW-P10-002 are partial; the rest are fixed in code.

## 3. Issue-by-issue validation

| ID | Title (short) | Old status | Fresh status | Class | Evidence (file:line) | Notes |
|----|---------------|-----------|--------------|-------|----------------------|-------|
| P10-P0-01 | Bank API demo-only stub | FIXED | VERIFIED_FIXED | PIPELINE | `domain/bank/BankApiIntegration.kt:597-602`: `requireStubMode()` = `BuildConfig.DEBUG` check + `require(BankApiConfig.isStubMode)`; `@StubForDemo` on every stub entry point (`:117,131,158,417,557`) | Double guard intact; release-safe |
| P10-P0-02 | Bank connection UI ViewModel no-op | TODO ONLY | VERIFIED_FIXED (tracker drift) | PIPELINE | `ui/screens/bank/BankConnectionsViewModel.kt:19-21` injects `BankConnectionLifecycleCoordinator`; `init:29-39` observes connections; `syncConnection:41-56`, `disconnect:58-73`; screen wires `onSync`/`disconnect` (`BankConnectionsScreen.kt:84,137`) | Fixed by `2e9ed3fe` (DAO+API), re-parented to the coordinator by `d40c230b` (A2, DAO removed from UI layer — legal path). `refresh()` has a regression — NEW-P10-2026-001 |
| P10-P1-01 | completeConnection doesn't persist entity | TODO ONLY | VERIFIED_FIXED (tracker drift) | PIPELINE | `BankApiIntegration.kt:151-155`: `createdAt = timeProvider.now()`; `bankConnectionDao.insert(connection)`; returns `connection.copy(id = id)` | Write-barrier checked at entry `:137` |
| P10-P1-02 | No OAuth state/PKCE/callback validation | TODO ONLY | VERIFIED_OPEN | PIPELINE | `BankApiIntegration.kt:118-128`: `initiateConnection` returns hardcoded `https://oauth.<bank>.example.com/auth?client_id=demo&response_type=code` — no `state`, no PKCE verifier, no durable session; `completeConnection(bankId, authCode)` `:131-156` accepts any authCode without validation; grep finds no OAuth session/state storage anywhere | Commit `2e9ed3fe` message explicitly "Skipped: P10-P1-02 (OAuth/PKCE)". Low real-world impact while demo-stub, but must close before any real provider |
| P10-P1-03 | Sync has no durable run ledger or checkpoint | TODO ONLY | VERIFIED_PARTIAL (tracker drift) | PIPELINE | Ledger NOW EXISTS: `BankApiIntegration.kt:167-392` wraps sync in `operationRunRecorder.runOperation("BANK_SYNC")` with durable stage events (SYNC_STARTED `:168`, WRITE_BARRIER `:175-181`, TOKEN_REFRESH/REAUTH `:185-216`, PAGE_FETCHED `:220`, per-transaction events `:289-377`) and counters (`run.increment`) | Cursor/checkpoint still absent: `since` param (`:159-162`) is never persisted; `connection.lastSync` is never written (see NEW-P10-2026-002). Half of the issue remains |
| P10-P1-04 | No low-confidence review route | TODO ONLY | VERIFIED_FIXED (tracker drift) | PIPELINE | `BankApiIntegration.kt:88-89` threshold `BANK_REVIEW_CONFIDENCE_THRESHOLD = 0.75f`; `:232-298` routes low-confidence transactions to `PendingReview` (insert in `database.withTransaction` `:265-287`) with pending-duplicate pre-check `:244-254`; mock confidence spread `:574-578` | |
| P10-P1-05 | Bank metadata not preserved on expenses | TODO ONLY | VERIFIED_FIXED (tracker drift) | PIPELINE | `domain/transaction/CreateExpenseRequest.kt:51-55,111-119` defines `bankSyncRunId`, `bankConnectionId`, `accountId`, `bankProviderTransactionIdHash`, `bankAccountIdHash`; populated in `mapTransactionToExpense` (`BankApiIntegration.kt:522-526`) with HMAC-hashed identity (`:507-508`) | |
| P10-P1-06 | Token refresh doesn't persist new tokens | TODO ONLY | VERIFIED_FIXED (tracker drift) | PIPELINE | `BankApiIntegration.kt:418-436`: refresh generates new stub tokens, encrypts, and persists via `bankConnectionDao.updateToken(...)` (`:428-434`) | |
| P10-P1-07 | No restore/write barrier around bank writes | PARTIAL | VERIFIED_PARTIAL | UNIVERSAL (maintenance/restore barrier) | Barriers: `initiateConnection:120`, `completeConnection:137`, `syncTransactions:172` (denial → durable CANCELLED run `:175-181`), per-review in-txn re-check `:266`; coordinator disconnect checks barrier (`BankConnectionLifecycleCoordinator.kt:60`) | Residual gaps: `refreshToken → bankConnectionDao.updateToken` (`:428-434`) runs with no barrier re-check (TOCTOU between the `:172` entry check and the write during a long sync); `bankConnectionDao.insert/updateToken` are not barrier-wrapped inside the DAO. Doc's "barrier exists but gaps remain" still accurate |
| P10-P1-08 | Statement import dedupe weaker than expense dedupe | PARTIAL | VERIFIED_FIXED (tracker drift) | PIPELINE | Shared policy: `domain/intelligence/DuplicateDetectionPolicy.kt` consumed by both `BankApiIntegration.kt:235-241` and `BankStatementLifecycleProcessor.kt:~520-530` (window/tolerance/currency/type-aware checks of BOTH existing expenses and pending reviews — P3-P1-11 note in code); provider-identity dedupe `STRICT_EXTERNAL_ID` + idempotencyKey for bank API imports (`BankApiIntegration.kt:527-534`); pre-OCR SHA-256 dedup in processor (`BankStatementLifecycleProcessor.kt:131-160`) | Shared with expense dedupe now (ExpenseDao/PendingReviewDao use the same policy per dao-map/engine map) |
| P10-P1-09 | Bank import one-by-one, no sync tx semantics | TODO ONLY | VERIFIED_PARTIAL | PIPELINE | Still per-item: each expense via `coordinator.createExpenseStandaloneV2` (`:303`, legal coordinator path) with per-item atomicity + correlationId propagation (`:302`); PendingReview batch wrapped per-review in `withTransaction` (`:265-287`); durable per-item run events exist | No outer sync transaction, no import-row state for the high-confidence path (commit `2e9ed3fe` "Skipped: P10-P1-09 (sync tx)"). Per-item atomicity is defensible design; issue is effectively a design decision now — tracker wording should say PARTIAL, not TODO |
| NEW-P10-001 | isStubMode mutable global | FIXED | VERIFIED_FIXED | PIPELINE | `domain/bank/BankApiConfig.kt:10-11`: `val isStubMode: Boolean = BuildConfig.DEBUG` (immutable, KDoc cites NEW-P10-001) | **Audit-flagged test regression is repaired:** no `isStubMode =` assignments remain in tests; both files now carry explanatory comments (`app/src/test/.../domain/bank/BankApiIntegrationTest.kt:80`, `app/src/test/.../domain/privacy/PR5PrivacyContractTest.kt:102`) |
| NEW-P10-002 | BankTokenCipher swallows KeyPermanentlyInvalidatedException | FIXED (audit: PARTIAL — never consumed) | VERIFIED_PARTIAL (tracker over-claims FIXED) | UNIVERSAL (fix-in-dead-path) | Cipher: `data/security/BankTokenCipher.kt:37-41` sealed `DecryptResult` incl. `KeyInvalidated`; `decryptWithResult:57-82` typed. **Consumption now exists:** `BankApiIntegration.kt:421-447` — `refreshToken` uses `decryptWithResult`; `KeyInvalidated → RefreshOutcome.ReauthRequired`; sync records a DISTINCT durable `TOKEN_REAUTH_REQUIRED` operation event (`:196-205`) and `failedFinal("REAUTH_REQUIRED...")` + SyncResult error (`:202-204`) | **Still partial:** no re-auth prompt. `BankConnectionLifecycleCoordinator.syncConnection` (`:43-45`) ignores the returned `SyncResult` entirely and returns `ConnectionSyncResult.Success` whenever no exception is thrown — so `success=false`/REAUTH_REQUIRED never reaches the UI; grep shows no other consumer of `ReauthRequired`/`REAUTH_REQUIRED` outside `BankApiIntegration.kt`. The user-visible "silent auth failure" is fixed at the ledger layer, still invisible at the UI layer (see NEW-P10-2026-003) |
| NEW-P10-003 | Statement processor per-item swallows CancellationException | FIXED | VERIFIED_FIXED | UNIVERSAL (U-PR1) | `domain/receipt/lifecycle/BankStatementLifecycleProcessor.kt:141-142,157-158,278-279,687-688,837-843,908-909,929-930`: every broad catch rethrows CE; `CancellationSafe.runCatchingCancellable` used for best-effort reads (`:505`) | |
| NEW-P10-004 | generateMockTransactions non-reproducible | FIXED | VERIFIED_FIXED | PIPELINE | `BankApiIntegration.kt:565-566`: `Random(bankId.hashCode().toLong() + (since ?: 0L))` seeded (P10-PR1 comment) | Stale contradicting comment at `:71-72` ("intentionally non-deterministic") — see NEW-P10-2026-004 |

### 3.1 Tracker/doc drift found this session

1. **Six under-claims (doc + master tracker stale):** `PIPELINE_10_CONSOLIDATED_ISSUES.md` and the P10 section of `PIPELINE_ISSUES_MASTER_TRACKER.md:311-327` still list P10-P0-02, P10-P1-01, P10-P1-04, P10-P1-05, P10-P1-06 as 📝 TODO ONLY and P10-P1-08 as ⚠ PARTIAL. All five TODOs + the dedupe partial are FIXED in source (fixed by `2e9ed3fe`, P10-P0-02 re-parented to the coordinator by `d40c230b`). Evidence per row above.
2. **One over-claim:** tracker says NEW-P10-002 "User now prompted to re-authenticate (P10-PR1)" ✅ FIXED. No re-auth prompt exists anywhere; the durable signal is not surfaced past the coordinator. Fresh verdict VERIFIED_PARTIAL (matches audit addendum 2's ACTUALLY_PARTIAL, now with better wiring than at `d935ee5c`).
3. **P10-P1-02/03/09 wording:** `2e9ed3fe` explicitly skipped 02; 03 and 09 were improved later (OperationRun ledger exists at HEAD). Tracker should read: 02 OPEN, 03 PARTIAL, 09 PARTIAL/design-decision.
4. **In-code doc drift:** stale comments contradicting behavior — `BankApiIntegration.kt:71-72` ("intentionally non-deterministic" vs seeded RNG at `:566`) and `:601` ("set BankApiConfig.isStubMode = true" — impossible since `:10` made it a `val`). Both were already flagged in audit addendum 2 (B3) and remain.

## 4. New issues & regressions found this session

| ID | Sev | Evidence | Description | Suspected origin |
|----|-----|----------|-------------|------------------|
| NEW-P10-2026-001 | MAJOR (latent — UI does not currently call `refresh()`) | `ui/screens/bank/BankConnectionsViewModel.kt:75-87`: `refresh()` launches `coordinator.observeConnections().collect { … }` — an **infinite** collect inside `try`, so `finally { _isLoading.value = false }` (`:83-85`) can never run, and each `refresh()` call leaks one more permanent collector writing to `_connections`. History: `65a59828` fixed dead refresh() with `.first()`; `5ed47933` replaced it with a SharedFlow `refreshTrigger` + `flatMapLatest` (correct reactive refresh); **`d40c230b` ("PR A — restore truthful CI", A2 coordinator refactor) dropped the trigger and reintroduced an infinite collect** — an edge case lost in the coordinator refactor | After any `refresh()` invocation the loading spinner would spin forever and collectors accumulate (each emitting into `_connections`). Currently unreachable from `BankConnectionsScreen.kt` (only `syncConnection`/`disconnect` are wired, `:84,137`), which is why severity is MAJOR-not-CRITICAL; it becomes user-visible the moment refresh is wired (e.g. pull-to-refresh) | Regression from the PR-A lifecycle refactor (`d40c230b`); cancellation-rework era refactor |
| NEW-P10-2026-002 | MAJOR | `data/database/dao/BankConnectionDao.kt:42-43` defines `updateSyncStatus(id, timestamp, status)` — **zero callers** (grep across `app/src/main/java`). `BankApiIntegration.syncTransactions` never updates `connection.lastSync`/`lastSyncStatus` on success/failure; `shouldSync()` (`BankApiIntegration.kt:461-474`) returns true whenever `lastSync == null` (`:465`) | Sync-frequency gating is dead: every autoSync connection is perpetually "due" (HOURLY/DAILY/WEEKLY all ignored), and sync outcome is never persisted on the connection. Duplicate protection (STRICT_EXTERNAL_ID) prevents data duplication, but the sync scheduler contract is non-functional and repeated full-window syncs are wasted work | Pre-existing design gap (tied to P10-P1-03 cursor/checkpoint half); surfaced this session |
| NEW-P10-2026-003 | MINOR | `domain/bank/BankConnectionLifecycleCoordinator.kt:43-45`: `bankApiIntegration.syncTransactions(connection)` result discarded → `ConnectionSyncResult.Success` even when `SyncResult.success == false` (incl. `REAUTH_REQUIRED`, validation failures) | The REAUTH_REQUIRED durable signal (NEW-P10-002) and per-sync errors are invisible above the coordinator; the UI cannot show re-auth or failure states. Complements NEW-P10-002's PARTIAL verdict — the fix needs one more hop: map `SyncResult.errors` (e.g. prefix `REAUTH_REQUIRED`) to a dedicated `ConnectionSyncResult` variant | Pre-existing (coordinator authored `d40c230b` after the REAUTH signal existed); universal "fix in the dead path" family |
| NEW-P10-2026-004 | MINOR | `BankApiIntegration.kt:71-72` and `:601` (stale comments described in §3.1 item 4) | Misleading in-code documentation contradicts actual behavior (deterministic RNG; immutable `isStubMode`). Cosmetic but has already misled one audit round | Pre-existing (audit addendum 2 flagged at `d935ee5c`; never cleaned) |

**Non-issue observations (recorded, not tracked as defects):**
- Money math: `generateMockTransactions` hardcodes `currency = "EUR"` (`:586`) and Double amounts — acceptable inside a demo stub; real providers must go through MoneyAggregate/normalization law before leaving stub mode.
- Privacy: `mapTransactionToExpense` sanitizes description/reference via `RawContentSanitizer.sanitizeBankDescription` per `rawBankStatementStorageMode` (`:491-500`); provider IDs persisted only as HMAC hashes (`putHashed`, `:260,293,310`); `OperationRunRecorder` sanitizes exception messages (`OperationRunRecorder.kt:238-239`). No raw bank payload or SQL/exception text persisted — clean.
- Fail-closed: unknown `rawStorageMode` fails closed to metadata-only in the intake path; statement processing checks `AiSettings.AI_BANK_STATEMENT` privacy gate per LEGAL_PATHS.
- T1–T4C time migration (`da2b8565..96c6b27d`) touched no bank files; PR18–PR24 (`bffc4299..65c265fb`) touched no `domain/bank/` or `domain/workers/` files; no cancellation regressions found in the bank path (`syncTransactions:365` rethrows CE; coordinator rethrows CE at `:46-47,51-52,65-66`; ViewModel rethrows CE at `:50-51,67-68`).

## 5. Universal candidates

- **Fix-in-dead-path / half-wired fix** (audit roll-up failure mode #1) — NEW-P10-002 (signal recorded in the ledger but not surfaced to the UI) and NEW-P10-2026-003 are the live instances in P10. The audit's rule "validation MUST trace to the production caller" was applied here: the production UI caller still cannot see the fix. Not yet covered by any U-PR.
- **Maintenance/restore barrier mishandling** — P10-P1-07 residual (updateToken path + TOCTOU). Matches the universal category; U-BARRIER-01/U-PR4 covered backup/export paths but not this bank DAO path.
- **CancellationException swallowed in broad catches** — verified clean at HEAD in all bank files (U-PR1 covers).
- **Side-effect double dispatch / dedup** — mitigated by STRICT_EXTERNAL_ID idempotency (`:527-534`) and pending-duplicate checks (`:244-254`); the double-dispatch risk now hinges on NEW-P10-2026-002 (no lastSync gate) — after a `shouldSync()` fix, dedup remains the load-bearing safety, which is the correct layering.
- **Permission gating unrelated core work** — not present in P10 (no worker in this pipeline gates non-notification work on notification permission).

## 6. Recommended next actions (ordered, concrete, with file paths)

1. **Repair NEW-P10-2026-001 (regression):** restore a bounded refresh trigger in `app/src/main/java/com/yourname/expensetracker/ui/screens/bank/BankConnectionsViewModel.kt:75-87` — either re-introduce the `refreshTrigger` SharedFlow + `flatMapLatest` from `5ed47933`, or make `refresh()` a one-shot (`observeConnections().first()`), so `finally` can run. Add a test asserting `isLoading` returns to false.
2. **Persist sync completion (NEW-P10-2026-002):** call `bankConnectionDao.updateSyncStatus(...)` at the end of `BankApiIntegration.syncTransactions` (after the run ledger finalizes, `:383-392`) with success/failure status; this also gives P10-P1-03 its missing cursor (`lastSync` → next `since`). Zero-caller DAO method: `app/src/main/java/com/yourname/expensetracker/data/database/dao/BankConnectionDao.kt:42-43`.
3. **Finish NEW-P10-002 (re-auth surfacing):** in `app/src/main/java/com/yourname/expensetracker/domain/bank/BankConnectionLifecycleCoordinator.kt:43-45`, inspect the `SyncResult` and map `REAUTH_REQUIRED` errors to a new `ConnectionSyncResult.ReauthRequired` variant; consume it in `BankConnectionsViewModel.syncConnection:45-49` to surface a re-auth state. This closes the audit's "no re-auth prompt" finding.
4. **Close P10-P1-07 residuals:** re-check the write barrier immediately before `bankConnectionDao.updateToken` in `BankApiIntegration.refreshToken` (`:425-434`) (or route the update through a barrier-checked helper), and add a barrier re-check next to `:266` for the token-write path.
5. **Tracker refresh:** update `docs/analyses and debug master/PIPELINE_ISSUES_MASTER_TRACKER.md:311-327` and `docs/analyses and debug master/PIPELINE_10_CONSOLIDATED_ISSUES.md` to the fresh statuses (10 fixed / 4 partial / 1 open), citing `2e9ed3fe` + `d40c230b`.
6. **In-code comment hygiene (NEW-P10-2026-004):** fix `BankApiIntegration.kt:71-72` and `:601` to describe actual behavior (seeded RNG; `isStubMode` immutable).
7. **P10-P1-02 (OAuth/PKCE)** remains a genuine design TODO before any real provider lands — no code change recommended while demo-stub, but the tracker should stop implying the feature is near-complete.
8. Human-run validation (not executed in this session, static only): `./gradlew :app:testDebugUnitTest --tests "*BankApiIntegration*" --tests "*BankConnectionsViewModel*" --console=plain`.
