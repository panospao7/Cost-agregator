"""DB access guard discovery helpers.

Fail-closed Room mutator inventory discovery and SQL classification.
All modules are imported as ``scripts.db_guard.<module>`` so relative
imports resolve inside the package; no sys.path manipulation is needed.
"""
