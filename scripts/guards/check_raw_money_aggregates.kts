#!/usr/bin/env kotlin

// TODO: Implement scan for raw Double financial aggregates.
// Patterns to flag: sumOf { it.amount }, sumOf { it.effectiveAmount }
// Allowlist: MoneyAggregateBuilder, CurrencyConverter, tests

println("Raw money aggregate guard: not yet implemented. Run manually against source.")
